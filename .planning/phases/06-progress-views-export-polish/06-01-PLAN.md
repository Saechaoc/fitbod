---
phase: 06
plan: 01
type: execute
wave: 1
slug: one-rep-max-kernel
depends_on: []
files_modified:
  - fitbod/Progress/OneRepMax.swift
  - fitbodTests/Phase6/OneRepMaxTests.swift
autonomous: true
requirements: ["PROG-02"]
must_haves:
  truths:
    - "OneRepMax.estimate(weight:reps:) returns nil for reps > 10 (CONTEXT D-04 suppression)"
    - "OneRepMax.estimate returns identity (weight) when reps == 1 (RESEARCH Pitfall 4 — Epley over-estimates here)"
    - "OneRepMax.estimate returns nil for non-positive weight (RESEARCH Pitfall 5 — bodyweight-assist suppression)"
    - "OneRepMax.estimate returns Brzycki for reps 2..6 and Epley for reps 7..10 (CONTEXT D-04)"
  artifacts:
    - path: "fitbod/Progress/OneRepMax.swift"
      provides: "public enum OneRepMax { static func estimate(weight: Double, reps: Int) -> Double? }"
    - path: "fitbodTests/Phase6/OneRepMaxTests.swift"
      provides: "Swift Testing @Suite covering identity / Brzycki / Epley / suppression / nil-on-non-positive"
  key_links:
    - from: "fitbod/Progress/OneRepMax.swift"
      to: "Foundation"
      via: "import Foundation only — no SwiftData dependency"
      pattern: "import Foundation\\s*\\npublic enum OneRepMax"
---

<objective>
Ship the canonical e1RM estimator kernel — pure `Foundation`-only value-type function used by ExerciseProgressView (chart series), PRDetector (e1RM PR bucket), and ExercisePRsView. Locks the math for PROG-02 per CONTEXT D-04 and dodges RESEARCH Pitfalls 4 (Epley over-estimate at reps == 1) and 5 (negative weight → nonsense e1RM).

Purpose: Every Phase 6 surface that compares lifters across rep ranges does so by routing through this one function. Centralizing it now prevents drift in Wave 2 chart code.

Output: `fitbod/Progress/OneRepMax.swift` (one public enum, one static function, ~30 LOC) + `fitbodTests/Phase6/OneRepMaxTests.swift` (Swift Testing @Suite with 6 parameterized @Test funcs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-progress-views-export-polish/06-CONTEXT.md
@.planning/phases/06-progress-views-export-polish/06-RESEARCH.md
@.planning/phases/06-progress-views-export-polish/06-PATTERNS.md
@fitbod/Routines/PrescriptionDefaults.swift
@fitbodTests/PrescriptionDefaultsTests.swift

<interfaces>
File-header pattern, public-enum-namespace pattern, and Swift Testing @Suite shape are mirrored from `PrescriptionDefaults.swift` + `PrescriptionDefaultsTests.swift` verbatim. See 06-PATTERNS.md §"Progress/OneRepMax.swift" lines 122–181.

Target signature (per RESEARCH §"OneRepMax (verified math)" lines 549–565):
```
public enum OneRepMax {
    public static func estimate(weight: Double, reps: Int) -> Double?
}
```

Switch branches:
- reps == 1 → return weight (identity)
- 2...6 → weight * 36.0 / (37.0 - Double(reps))     // Brzycki
- 7...10 → weight * (1.0 + Double(reps) / 30.0)      // Epley
- default → nil
Guard: `reps > 0 && weight > 0` else nil.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OneRepMax.swift pure-function kernel</name>
  <files>fitbod/Progress/OneRepMax.swift</files>
  <read_first>
    - fitbod/Routines/PrescriptionDefaults.swift (file-header doc-block format + public enum + static func namespace pattern — copy shape verbatim)
    - .planning/phases/06-progress-views-export-polish/06-PATTERNS.md §"Progress/OneRepMax.swift" lines 122–181 (header doc + body)
    - .planning/phases/06-progress-views-export-polish/06-RESEARCH.md §"OneRepMax (verified math)" lines 549–565 (exact body)
    - .planning/phases/06-progress-views-export-polish/06-CONTEXT.md §"Area 2 — e1RM calculation and series" D-04, D-05, D-06 (formula authority)
  </read_first>
  <behavior>
    - identity: estimate(weight: 100, reps: 1) == 100 (per RESEARCH Pitfall 4)
    - Brzycki: estimate(weight: 100, reps: 5) == 100 * 36 / 32 == 112.5
    - Epley: estimate(weight: 100, reps: 8) == 100 * (1 + 8/30) ≈ 126.67
    - suppression: estimate(weight: 100, reps: 11) == nil (per D-04)
    - non-positive weight: estimate(weight: -10, reps: 5) == nil (per Pitfall 5 — bodyweight-assist)
    - non-positive reps: estimate(weight: 100, reps: 0) == nil
  </behavior>
  <action>Create `fitbod/Progress/` directory and add `OneRepMax.swift` containing a single `public enum OneRepMax` with one `public static func estimate(weight: Double, reps: Int) -> Double?`. Body per RESEARCH §"OneRepMax (verified math)" lines 549–565 (guard `reps > 0 && weight > 0`, switch on reps with identity case 1, Brzycki 2...6, Epley 7...10, default nil). File-header doc-block follows the verbatim shape from `PrescriptionDefaults.swift` lines 1–24 — first line `//  OneRepMax.swift`, second line `//  fitbod`, then `Wave-1 plan 06-01` reference, callers list (ExerciseProgressView / PRDetector / ExercisePRsView), formula table per CONTEXT D-04, and pitfall anchors (Pitfall 4 / Pitfall 5). Single `import Foundation` — no SwiftData. Public visibility on enum and method.</action>
  <verify>
    <automated>find fitbod/Progress -name 'OneRepMax.swift' | xargs xcrun swiftc -parse</automated>
  </verify>
  <done>File exists at `fitbod/Progress/OneRepMax.swift` with public enum + public static func + the four-branch switch; parse-clean; only imports `Foundation`.</done>
</task>

<task type="auto">
  <name>Task 2: Create OneRepMaxTests.swift with parameterized boundary coverage</name>
  <files>fitbodTests/Phase6/OneRepMaxTests.swift</files>
  <read_first>
    - fitbodTests/PrescriptionDefaultsTests.swift (file-header + @MainActor @Suite + @Test + parameterized arguments pattern — verbatim)
    - .planning/phases/06-progress-views-export-polish/06-PATTERNS.md §"fitbodTests/Phase6/OneRepMaxTests.swift" lines 632–682 (suite shape)
    - .planning/phases/06-progress-views-export-polish/06-RESEARCH.md §"Validation Architecture" lines 770–807 (Wave 0 gap entry)
    - fitbod/Progress/OneRepMax.swift (Task 1 output — read AFTER Task 1 completes)
  </read_first>
  <behavior>
    - identityAtOneRep — reps == 1 returns weight verbatim
    - brzyckiRange — reps 2..6 use Brzycki within 0.1 kg tolerance (parameterized @Test arguments)
    - epleyRange — reps 7..10 use Epley within 0.1 kg tolerance (parameterized)
    - suppressesHighRep — reps 11..20 return nil (parameterized)
    - nilOnNonPositiveInputs — weight ≤ 0 or reps ≤ 0 returns nil
    - handlesNegativeWeight — Pitfall 5 bodyweight-assist returns nil
  </behavior>
  <action>Create `fitbodTests/Phase6/` directory (new sub-folder for Phase 6 tests). Add `OneRepMaxTests.swift` containing `@MainActor @Suite("OneRepMax (PROG-02 + CONTEXT D-04)") struct OneRepMaxTests`. File-header doc-block follows `PrescriptionDefaultsTests.swift` lines 1–18 shape — name, target, six numbered @Test summaries. Six @Test functions matching the behavior block. Brzycki + Epley + suppression branches use `arguments: [(Double, Int, Double?)]` parameterized form per `PrescriptionDefaultsTests` line 36–58 idiom. Use `try #require(...)` to unwrap optional then `#expect(abs(actual - expected) < 0.1)` for floating-point tolerance. `import Foundation` + `import Testing` + `@testable import fitbod`.</action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:fitbodTests/OneRepMaxTests -quiet</automated>
  </verify>
  <done>Tests file exists at `fitbodTests/Phase6/OneRepMaxTests.swift`; all six @Test funcs PASS via xcodebuild; suite name `OneRepMax (PROG-02 + CONTEXT D-04)`.</done>
</task>

</tasks>

<verification>
- `find fitbod fitbodTests -name '*.swift' | xargs xcrun swiftc -parse` exits 0 (parse-clean — Phase 2 convention)
- `xcodebuild test -scheme fitbod -only-testing:fitbodTests/OneRepMaxTests` exits 0
</verification>

<success_criteria>
- `OneRepMax.estimate` exists as a public static func returning `Double?`
- All four formula branches (identity/Brzycki/Epley/nil) verified by parameterized tests
- Edge cases (reps == 0, weight ≤ 0, reps > 10) all return nil and are tested
- Zero SwiftData / SwiftUI imports in the kernel file
</success_criteria>

<output>
Create `.planning/phases/06-progress-views-export-polish/06-01-SUMMARY.md` documenting:
- Exact `OneRepMax.estimate` signature and behavior table
- Test names + count
- Commit SHAs (one per task: feat + test)
</output>
