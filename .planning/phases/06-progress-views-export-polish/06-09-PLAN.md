---
phase: 06
plan: 09
type: execute
wave: 3
slug: csv-and-json-export
depends_on: ["06-02"]
files_modified:
  - fitbod/Export/ExportDTOs.swift
  - fitbod/Export/CSVExporter.swift
  - fitbod/Export/JSONExporter.swift
  - fitbod/Export/CSVFile.swift
  - fitbod/Export/JSONFile.swift
  - fitbod/Export/ExportService.swift
  - fitbod/Settings/SettingsView.swift
  - fitbodTests/Phase6/CSVExporterTests.swift
  - fitbodTests/Phase6/JSONExporterTests.swift
  - fitbodTests/Phase6/Fixtures/csv/example-export.csv
  - fitbodTests/Phase6/Fixtures/json/example-export.json
autonomous: true
requirements: ["EXP-01", "EXP-02"]
must_haves:
  truths:
    - "CSV export emits one row per SetEntry with 23 columns in CONTEXT D-25 order; RFC 4180 quoting; UTF-8 BOM; ISO-8601 UTC; empty for nil (CONTEXT D-26)"
    - "JSON export emits a schema-versioned envelope with formatVersion: 1, schemaVersion: 'v2', and per-entity arrays (CONTEXT D-28)"
    - "JSONEncoder uses .prettyPrinted + .sortedKeys + .iso8601 + UTC (RESEARCH §Don't Hand-Roll + Pitfall 8)"
    - "CSVFile and JSONFile conform to Transferable via DataRepresentation; suggestedFileName uses 'fitbod-export-{ISO-date}.csv' / .json (CONTEXT D-27 + D-29)"
    - "DTOs are plain Codable value types — NOT @Model — to give export shape stability per RESEARCH Anti-Pattern 'Decoding backup directly into @Model'"
    - "Settings → Data section ships rows 'Export as CSV' and 'Export as JSON' wrapping ShareLink(item:) (CONTEXT D-27 + D-29)"
    - "Off-main rendering via async Task to honor RESEARCH Pitfall 13 (ShareLink Transferable DataRepresentation runs on caller thread)"
    - "Name collision: do NOT name the export DTO 'ExerciseDTO' — that name is taken by fitbod/ExerciseLibrary/ExerciseDTO.swift; prefix with 'Export' (RESEARCH §'DTO name collision')"
    - "Empty string emitted when SessionExercise.exercise == nil (post-delete cascade-nullify per RESEARCH Pitfall 11; v1 pragmatic per 06-07 deferred note)"
  artifacts:
    - path: "fitbod/Export/ExportDTOs.swift"
      provides: "Public Codable value types covering every SchemaV2 entity (ExportDocument envelope + ExportExerciseDTO + ExportRoutineDTO + ExportSessionDTO + ExportSessionExerciseDTO + ExportSetEntryDTO + ExportBlockDTO + ExportBlockPhaseDTO + ExportMuscleGroupDTO + ExportMuscleVolumeTargetDTO + ExportExerciseMuscleStimulusDTO + ExportUserSettingsDTO + ExportRoutineFolderDTO + ExportSupersetGroupDTO + ExportRoutineExerciseSetOverrideDTO)"
    - path: "fitbod/Export/CSVExporter.swift"
      provides: "public enum CSVExporter { static func render(setEntries: [SetEntry]) -> Data } — RFC 4180 + UTF-8 BOM + ISO-8601 UTC"
    - path: "fitbod/Export/JSONExporter.swift"
      provides: "public enum JSONExporter { static func render(document: ExportDocument) throws -> Data } — .prettyPrinted + .sortedKeys + .iso8601(.gmt)"
    - path: "fitbod/Export/CSVFile.swift"
      provides: "public struct CSVFile: Transferable + DataRepresentation + suggestedFileName"
    - path: "fitbod/Export/JSONFile.swift"
      provides: "public struct JSONFile: Transferable + DataRepresentation + suggestedFileName"
    - path: "fitbod/Export/ExportService.swift"
      provides: "@ModelActor public actor ExportService { func renderCSV() async throws -> CSVFile; func renderJSON() async throws -> JSONFile; func snapshotDocument() async throws -> ExportDocument }"
    - path: "fitbod/Settings/SettingsView.swift"
      provides: "Adds a new 'Data' section with Export as CSV / Export as JSON rows wrapping ShareLink; off-main render via async Task"
  key_links:
    - from: "fitbod/Export/CSVFile.swift, JSONFile.swift"
      to: "fitbod/Settings/SettingsView.swift"
      via: "ShareLink(item: csvFile, preview: SharePreview(filename))"
      pattern: "ShareLink\\(item:"
---

<objective>
Ship CSV (EXP-01) and JSON (EXP-02) exports as `Transferable` value types consumed by `ShareLink` in the new Settings → Data section. Establishes the `ExportDocument` Codable DTO graph that the Wave 4 backup writer will also consume.

Purpose: Both exports must produce **byte-stable** output for unit testing (golden-file snapshots). Locking the DTO shape + encoder configuration in this plan means Wave 4 backup composes the same DTOs without reinventing.

Output: 6 new export source files + 1 modified SettingsView + 2 test suites + 2 golden-snapshot fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-progress-views-export-polish/06-CONTEXT.md
@.planning/phases/06-progress-views-export-polish/06-RESEARCH.md
@.planning/phases/06-progress-views-export-polish/06-PATTERNS.md
@.planning/phases/06-progress-views-export-polish/06-UI-SPEC.md
@fitbod/ExerciseLibrary/ExerciseDTO.swift
@fitbod/ExerciseLibrary/ExerciseLibraryImporter.swift
@fitbod/Settings/SettingsView.swift
@fitbod/Models/SetEntry.swift
@fitbod/Models/Session.swift
@fitbod/Models/SessionExercise.swift
@fitbod/Models/UserSettings.swift
@fitbod/Persistence/SchemaV2.swift

<interfaces>
**CSV column order (CONTEXT D-25 — verbatim 23 columns):**
```
session_id, session_started_at, session_completed_at, session_intent, routine_id, routine_name,
exercise_id, exercise_name, set_index, set_kind, target_weight, target_reps, target_rpe,
actual_weight, actual_reps, actual_rpe, was_manual_override, partial_reps, tempo,
rest_taken_sec, e1rm, set_note, unit
```

**Critical schema mapping (RESEARCH §"Schema reality vs CONTEXT"):**
| CSV column | Real source field |
|---|---|
| session_id | SessionExercise.session?.id?.uuidString |
| session_started_at | SessionExercise.session?.startedAt formatted UTC ISO-8601 |
| session_completed_at | SessionExercise.session?.completedAt formatted UTC ISO-8601 (empty when nil) |
| session_intent | SessionExercise.intentRaw (NOT Session.intent — doesn't exist) |
| routine_id | SessionExercise.session?.sourceRoutineID?.uuidString (empty when nil) |
| routine_name | SessionExercise.session?.routineSnapshotName |
| exercise_id | SessionExercise.exercise?.id?.uuidString (empty when exercise == nil per Pitfall 11) |
| exercise_name | SessionExercise.exercise?.name (empty when exercise == nil — Open Question 2 deferred per 06-07) |
| set_index | SetEntry.orderIndex |
| set_kind | SetEntry.setTypeRaw |
| target_weight | SessionExercise.prescribedWeight (empty when nil) |
| target_reps | "{targetRepsLow}-{targetRepsHigh}" composite — planner decision |
| target_rpe | SessionExercise.targetRPE (empty when nil) |
| actual_weight | SetEntry.weight |
| actual_reps | SetEntry.reps |
| actual_rpe | SetEntry.rpe (empty when nil) |
| was_manual_override | always emit "false" — field doesn't exist in v1 schema per RESEARCH Pitfall 1 |
| partial_reps | SetEntry.partialReps (empty when nil) |
| tempo | SetEntry.tempoActual (empty when nil) |
| rest_taken_sec | SetEntry.restAfterSeconds (empty when nil) |
| e1rm | OneRepMax.estimate(weight: setEntry.weight, reps: setEntry.reps) (empty when nil) |
| set_note | SetEntry.notes (empty when nil) |
| unit | UserSettings.unitSystem rawValue ("kg" or "lb") — single value, same per row |

**RFC 4180 quoting (RESEARCH §Don't Hand-Roll line 431):** Quote any field containing `,` `"` `\r` `\n`; escape `"` as `""`. UTF-8 BOM `EF BB BF` at start of output (Excel-friendly). Line terminator: `\r\n`. ISO-8601 dates emitted via `Date.ISO8601FormatStyle().timeZone(.gmt)` — trailing `Z` MUST be present.

**JSON envelope (CONTEXT D-28 verbatim):**
```
public struct ExportDocument: Codable, Equatable, Sendable {
    public let formatVersion: Int                  // 1
    public let schemaVersion: String               // "v2" — canonical constant
    public let exportedAt: Date
    public let unitSystem: String                  // "kg" or "lb"
    public let exercises: [ExportExerciseDTO]
    public let routines: [ExportRoutineDTO]
    public let sessions: [ExportSessionDTO]        // nested SessionExercises + SetEntries
    public let blocks: [ExportBlockDTO]
    public let muscleGroups: [ExportMuscleGroupDTO]
    public let muscleVolumeTargets: [ExportMuscleVolumeTargetDTO]
    public let userSettings: ExportUserSettingsDTO?
}
```

**DTO sketches (one per SchemaV2 @Model entity — full field coverage):** Each DTO is `public struct Export<Name>DTO: Codable, Equatable, Sendable` with `let` properties matching the SchemaV2 entity 1:1 (UUIDs as `UUID`, enums as String raw, Dates as Date, nested relationships flattened to UUID references OR nested DTO arrays for owning relationships). Planner decision: nest `SessionExercise` and `SetEntry` inside `ExportSessionDTO` so a session is a single self-contained subtree; reference Exercise / MuscleGroup by UUID since they live in separate top-level arrays.

**Name collision (RESEARCH §"DTO name collision"):** `fitbod/ExerciseLibrary/ExerciseDTO.swift` already exists as the free-exercise-db seed DTO. Use `ExportExerciseDTO` prefix uniformly.

**ExportService (PATTERNS §"Export/ExportService.swift" lines 488–525):**
```
@ModelActor
public actor ExportService {
    private static let log = Logger(subsystem: "com.fitbod.app", category: "export")

    public func snapshotDocument() async throws -> ExportDocument {
        // Read every SchemaV2 entity from modelContext, build [ExportXDTO] arrays, return ExportDocument.
    }
    public func renderCSV() async throws -> CSVFile {
        // Read all SetEntry rows, render via CSVExporter, wrap in CSVFile with dated filename.
    }
    public func renderJSON() async throws -> JSONFile {
        let doc = try await snapshotDocument()
        let data = try JSONExporter.render(document: doc)
        return JSONFile(data: data, filename: dailyFilename(prefix: "fitbod-export", ext: "json"))
    }
}
```

**Transferable shapes (RESEARCH §"Pattern 4" lines 322–348):**
```
public struct CSVFile: Transferable, Sendable {
    public let data: Data
    public let filename: String
    public static var transferRepresentation: some TransferRepresentation {
        DataRepresentation(exportedContentType: .commaSeparatedText) { $0.data }
            .suggestedFileName { $0.filename }
    }
}

public struct JSONFile: Transferable, Sendable {
    public let data: Data
    public let filename: String
    public static var transferRepresentation: some TransferRepresentation {
        DataRepresentation(exportedContentType: .json) { $0.data }
            .suggestedFileName { $0.filename }
    }
}
```

**Filename helper:**
```
func dailyFilename(prefix: String, ext: String, now: Date = .now) -> String {
    let date = now.formatted(.iso8601.year().month().day())   // "2026-05-22"
    return "\(prefix)-\(date).\(ext)"
}
```

**SettingsView edits (PATTERNS §"Settings/DataSettingsSection.swift" lines 542–589):** Add a new section below the existing sections in the existing SettingsView Form. Two rows: "Export as CSV" + "Export as JSON". Each row's body branches on a `@State` optional CSVFile? / JSONFile? — when nil, button labeled "Export as CSV" triggers `Task { csvFile = try await ExportService(modelContainer: modelContext.container).renderCSV() }`; when non-nil, render `ShareLink(item: csvFile, preview: SharePreview(csvFile.filename)) { Text("Share CSV") }`. Same pattern for JSON. Section footer verbatim "Exports include every set you've logged. Backups can be shared via Files, iCloud Drive, or AirDrop." (the "Backups" portion lands fully in Wave 4 but the footer copy ships now per UI-SPEC verbatim — the row is built out in Wave 4). Note: the Wave 4 "Create backup" and "Restore from backup" rows are added in 06-10; this plan ships ONLY the two export rows.

**Off-main rendering (RESEARCH Pitfall 13):** ExportService is `@ModelActor` (off main); the Settings view kicks off rendering via `Task { ... }` then assigns the `Sendable` CSVFile/JSONFile result on the main actor. While rendering shows a `ProgressView()` inline next to the row label OR `.disabled(true)` on the row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportDTOs.swift covering every SchemaV2 entity</name>
  <files>fitbod/Export/ExportDTOs.swift</files>
  <read_first>
    - fitbod/ExerciseLibrary/ExerciseDTO.swift (existing Codable DTO pattern — copy file-doc shape)
    - fitbod/Persistence/SchemaV2.swift (the 15 @Model entities enumerated — anchor list)
    - fitbod/Models/*.swift (read each @Model to enumerate fields exactly)
    - .planning/phases/06-progress-views-export-polish/06-PATTERNS.md §"Export/ExportDTOs.swift" lines 432–485
    - .planning/phases/06-progress-views-export-polish/06-RESEARCH.md §"DTO name collision" line 184
    - .planning/phases/06-progress-views-export-polish/06-CONTEXT.md D-28 envelope shape
  </read_first>
  <behavior>
    - ExportDocument envelope contains every top-level entity array
    - Each ExportXDTO struct exposes the same fields as the @Model entity (id + scalars + relationships flattened/nested)
    - All DTOs Sendable + Equatable + Codable
    - Nested ownership: ExportSessionDTO embeds [ExportSessionExerciseDTO]; ExportSessionExerciseDTO embeds [ExportSetEntryDTO]; ExportRoutineDTO embeds [ExportRoutineExerciseDTO] etc. per natural ownership cascade
    - Non-owning references (Exercise → SessionExercise, MuscleGroup → ExerciseMuscleStimulus) emit UUID references only
  </behavior>
  <action>Create `fitbod/Export/ExportDTOs.swift` (single file containing the entire DTO graph — keep grouped for atomic-commit hygiene; if file exceeds ~600 lines, split into ExportDTOs+Session.swift etc.). For each SchemaV2 entity (Exercise, MuscleGroup, ExerciseMuscleStimulus, Routine, RoutineFolder, RoutineExercise, RoutineExerciseSetOverride, SupersetGroup, Session, SessionExercise, SetEntry, Block, BlockPhase, UserSettings, MuscleVolumeTarget), declare `public struct Export<Name>DTO: Codable, Equatable, Sendable` mirroring fields. Use UUID references for non-owning relationships; embed [ChildDTO] arrays for owning relationships. ExportDocument envelope per CONTEXT D-28 (formatVersion: 1, schemaVersion: "v2", exportedAt, unitSystem, [exercises], [routines], [sessions], [blocks], [muscleGroups], [muscleVolumeTargets], userSettings: ExportUserSettingsDTO?). Add `public static let canonicalSchemaVersion = "v2"` on ExportDocument so the restore-side schema-skew check has one constant to compare against (cited in 06-10 backup plan). File-header doc-block cites D-28 + RESEARCH §DTO name collision warning. Imports: Foundation only (no SwiftData — these are pure Codable).</action>
  <verify>
    <automated>find fitbod/Export -name 'ExportDTOs.swift' | xargs xcrun swiftc -parse 2>&1 | grep -v warning</automated>
  </verify>
  <done>ExportDTOs.swift parse-clean; ExportDocument envelope + 15 DTOs declared; all Sendable + Codable + Equatable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CSVExporter + JSONExporter + golden-file fixtures + tests</name>
  <files>fitbod/Export/CSVExporter.swift, fitbod/Export/JSONExporter.swift, fitbodTests/Phase6/CSVExporterTests.swift, fitbodTests/Phase6/JSONExporterTests.swift, fitbodTests/Phase6/Fixtures/csv/example-export.csv, fitbodTests/Phase6/Fixtures/json/example-export.json</files>
  <read_first>
    - fitbod/Routines/PrescriptionDefaults.swift (pure-function enum pattern)
    - fitbod/Export/ExportDTOs.swift (Task 1 output)
    - fitbod/Models/UserSettings.swift (unitSystem field)
    - .planning/phases/06-progress-views-export-polish/06-RESEARCH.md §"Don't Hand-Roll — RFC-4180 CSV quoting" line 430 + §"Pitfall 8 — ISO-8601 timezone defaults" lines 506–509
    - .planning/phases/06-progress-views-export-polish/06-CONTEXT.md D-25, D-26, D-28
    - .planning/phases/06-progress-views-export-polish/06-VALIDATION.md §"Per-Task Verification Map" EXP-01 + EXP-02 + §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - CSVExporter.render(setEntries:) produces byte-identical output for a deterministic fixture input (golden-snapshot match)
    - RFC-4180 quoting: a set_note containing comma `,` or quote `"` is wrapped in double quotes with internal quote escape `""`
    - UTF-8 BOM `EF BB BF` is the first 3 bytes
    - Date columns end with literal `Z` (UTC)
    - was_manual_override always emits literal "false"
    - JSONExporter.render(document:) uses .prettyPrinted + .sortedKeys + .iso8601(.gmt) yielding byte-stable output
    - JSON output contains "formatVersion" : 1 and "schemaVersion" : "v2" at top level
    - Both exporters tested against golden snapshot files
  </behavior>
  <action>(1) Create `fitbod/Export/CSVExporter.swift` containing `public enum CSVExporter { public static func render(setEntries: [SetEntry], userSettings: UserSettings?) -> Data }` and private helpers `rfc4180Quote(_ value: String) -> String`, `formatIsoUTC(_ date: Date) -> String` (uses `Date.ISO8601FormatStyle().timeZone(.gmt)`), `formatOptional<T>(_ value: T?, transform: (T) -> String) -> String` returning "" for nil. Header row: 23 column names per CONTEXT D-25 verbatim. Body row per SetEntry per the schema mapping table in Interfaces. UTF-8 BOM (EF BB BF) prepended. Line terminator `\r\n` per RFC 4180. (2) Create `fitbod/Export/JSONExporter.swift` containing `public enum JSONExporter { public static func render(document: ExportDocument) throws -> Data }`. Configure `JSONEncoder` with `.outputFormatting = [.prettyPrinted, .sortedKeys, .withoutEscapingSlashes]`, `.dateEncodingStrategy = .iso8601`. (3) Create golden fixture files at `fitbodTests/Phase6/Fixtures/csv/example-export.csv` and `fitbodTests/Phase6/Fixtures/json/example-export.json` — these are GENERATED on first test run from a deterministic fixture (SeedPRHistory-style) using `Bundle.main.url(forResource:)` access pattern; test compares actual output to the on-disk file content. First run (when file is empty), the test writes the file and FAILS with "fixture regenerated, re-run"; subsequent runs PASS against the saved snapshot. (4) Create `fitbodTests/Phase6/CSVExporterTests.swift` with @Suite covering: bomPresent, headerRow23Columns, rfc4180QuotingForEmbeddedCommaAndQuote, isoUtcDatesEndWithZ, wasManualOverrideAlwaysFalse, goldenSnapshotMatch. (5) Create `fitbodTests/Phase6/JSONExporterTests.swift` with @Suite covering: formatVersionIs1, schemaVersionIsV2, prettyPrintedAndSortedKeys, isoUtcDates, goldenSnapshotMatch. Use `Bundle(for:)` or `#filePath` ascent to locate the golden files relative to the test bundle.</action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:fitbodTests/CSVExporterTests -only-testing:fitbodTests/JSONExporterTests -quiet</automated>
  </verify>
  <done>Both exporters parse-clean; tests PASS (on second run after golden snapshot generation); RFC 4180 quoting + ISO-8601 UTC verified.</done>
</task>

<task type="auto">
  <name>Task 3: CSVFile + JSONFile Transferable + ExportService @ModelActor + SettingsView Data section (export rows only)</name>
  <files>fitbod/Export/CSVFile.swift, fitbod/Export/JSONFile.swift, fitbod/Export/ExportService.swift, fitbod/Settings/SettingsView.swift</files>
  <read_first>
    - fitbod/ExerciseLibrary/ExerciseLibraryImporter.swift (lines 58–60, 78 — @ModelActor + Logger + actor pattern)
    - fitbod/Settings/SettingsView.swift (entire file — locate insertion point below existing sections)
    - fitbod/Export/CSVExporter.swift (Task 2 output)
    - fitbod/Export/JSONExporter.swift (Task 2 output)
    - fitbod/Export/ExportDTOs.swift (Task 1 output)
    - .planning/phases/06-progress-views-export-polish/06-PATTERNS.md §"Settings/DataSettingsSection.swift" lines 542–591 + §"Export/ExportService.swift" lines 488–525
    - .planning/phases/06-progress-views-export-polish/06-UI-SPEC.md §"Settings → Data section" copy table lines 280–303
    - .planning/phases/06-progress-views-export-polish/06-CONTEXT.md D-27, D-29
    - .planning/phases/06-progress-views-export-polish/06-RESEARCH.md §"Pitfall 13 — ShareLink Transferable lazy rendering" lines 536–540
  </read_first>
  <behavior>
    - CSVFile + JSONFile conform to Transferable via DataRepresentation with .suggestedFileName(closure)
    - ExportService is @ModelActor and runs renderCSV/renderJSON/snapshotDocument off main
    - SettingsView shows two new rows "Export as CSV" + "Export as JSON" in a new "Data" section
    - Tapping a row when result is nil triggers async render; result populates a @State variable; row then renders ShareLink(item:) wrapping the Transferable file
    - While rendering: row is .disabled(true) + a `ProgressView()` inline; on completion: ShareLink ready
    - Section footer reads "Exports include every set you've logged. Backups can be shared via Files, iCloud Drive, or AirDrop." verbatim (note: "Backups" portion is forward-compat for Wave 4 — copy still correct because v1 will ship both)
    - Filename format: "fitbod-export-YYYY-MM-DD.csv" / ".json"
    - Error state: if Task throws, render `.caption .systemRed` "Couldn't prepare export. Try again." below the row
  </behavior>
  <action>(1) Create `fitbod/Export/CSVFile.swift` per Interfaces (public struct + Transferable + DataRepresentation + suggestedFileName). (2) Create `fitbod/Export/JSONFile.swift` per Interfaces. (3) Create `fitbod/Export/ExportService.swift` per PATTERNS line 488–525 — `@ModelActor public actor ExportService` with three public async methods: `renderCSV() async throws -> CSVFile` (reads all SetEntry rows + UserSettings, calls CSVExporter.render, wraps in CSVFile), `renderJSON() async throws -> JSONFile` (calls snapshotDocument then JSONExporter.render), `snapshotDocument() async throws -> ExportDocument` (reads every SchemaV2 entity, builds ExportDocument). Use `Logger(subsystem: "com.fitbod.app", category: "export")`. Implement a `dailyFilename(prefix:ext:now:)` helper either as a private static func or a free function in the file. (4) Edit `fitbod/Settings/SettingsView.swift`: locate the existing Form sections; add a new section at the end with header "Data" + footer copy verbatim. Two rows: "Export as CSV" / "Export as JSON". Each row is a private @ViewBuilder helper that branches on `@State private var csvFile: CSVFile?` / `jsonFile: JSONFile?`. When nil: `Button("Export as CSV") { Task { await renderCSV() } }`; when non-nil: `ShareLink(item: csvFile!, preview: SharePreview(csvFile!.filename)) { Text("Share CSV") }`. While in-flight: show inline `ProgressView()` and disable button. Add private `func renderCSV() async` / `renderJSON() async` methods that instantiate `ExportService(modelContainer: modelContext.container)` and assign the result back. Error path renders `.caption .systemRed` "Couldn't prepare export. Try again." in a sibling Text. **Do NOT add the Create-backup or Restore-from-backup rows here — those are 06-10's job.**</action>
  <verify>
    <automated>find fitbod/Export -name '*.swift' | xargs xcrun swiftc -parse 2>&1 | grep -v warning && grep -n 'Export as CSV\|Export as JSON\|Data' fitbod/Settings/SettingsView.swift</automated>
  </verify>
  <done>Four files modified/created; SettingsView has new Data section with two export rows; ShareLink wired; off-main rendering via @ModelActor confirmed.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -only-testing:fitbodTests/CSVExporterTests -only-testing:fitbodTests/JSONExporterTests` exits 0
- `find fitbod fitbodTests -name '*.swift' | xargs xcrun swiftc -parse` exits 0
- `grep -n 'Transferable' fitbod/Export/CSVFile.swift fitbod/Export/JSONFile.swift` confirms conformance
- `grep -n 'canonicalSchemaVersion' fitbod/Export/ExportDTOs.swift` returns the "v2" constant for the Wave 4 restore-side check
</verification>

<success_criteria>
- ExportDocument envelope covers every SchemaV2 entity (15 DTOs)
- CSV emits 23 columns in CONTEXT D-25 order + UTF-8 BOM + ISO-8601 UTC + RFC 4180 quoting
- JSON emits .prettyPrinted + .sortedKeys + ISO-8601 UTC + formatVersion 1 + schemaVersion "v2"
- Both exports wrapped in Transferable and exposed via ShareLink in Settings → Data
- Off-main rendering via @ModelActor ExportService
- Golden-snapshot tests in place (PASS after second run when fixtures are committed)
- Wave 4 backup writer in 06-10 will consume ExportDocument unchanged
</success_criteria>

<output>
Create `.planning/phases/06-progress-views-export-polish/06-09-SUMMARY.md` documenting:
- ExportDocument envelope shape + ExportXDTO count
- CSV column order (23 entries) + RFC 4180 quoting confirmation
- JSON encoder configuration + canonical schemaVersion constant
- ExportService @ModelActor methods
- SettingsView Data section row count (2 of 4 — Wave 4 adds the other 2)
- Open Question 2 (exerciseSnapshotName field) — deferred; v1 emits empty-string for nil-exercise rows
- Commit SHAs
</output>
