---
phase: 02
plan: 02-03
wave: 2
slug: rest-timer-overlay-and-engine-integration
complexity: S
requirements: ["SESS-04"]
covers_pitfalls: ["RESEARCH §6 Pattern 2 (TimelineView 1s tick for Date-derived UI)", "UI-SPEC § Reduced motion (progress arc respects accessibilityReduceMotion)"]
depends_on: ["02-01", "02-02"]
files_modified:
  - fitbod/Sessions/RestTimer/RestTimerEngine.swift                # MODIFY — compose RestTimerActivityController; expose activityController init param
  - fitbod/Sessions/RestTimer/RestTimerOverlay.swift               # NEW — collapsed 64pt pill + expandable .medium-detent sheet
  - fitbod/Sessions/RestTimer/RestTimerProgressRing.swift          # NEW — circular accent progress arc
  - fitbodTests/RestTimerOverlayCopyTests.swift                     # NEW — verbatim UI-SPEC strings + accent surfaces
created: 2026-05-11
---

# Plan 02-03 — Rest Timer Overlay (In-App UI) and Engine Integration

> **Wave 2 / Sequence 3.** Wires `RestTimerActivityController` from plan `02-02` into `RestTimerEngine` from plan `02-01` as a composed dependency. Adds the in-app `RestTimerOverlay` (the 64pt collapsed pill above the tab bar + the `.medium`-detent expanded sheet with ±15s / Skip controls + exercise-name subtitle + circular progress ring). All copy strings are UI-SPEC verbatim. Closes the user-visible in-app overlay half of SESS-04.

## Goal

The rest timer is now a single observable unit: `RestTimerEngine` owns `startedAt` / `targetSeconds` / `currentExerciseName`, schedules the lock-screen notification (plan `02-01`'s `RestTimerNotificationScheduler`), AND drives the Live Activity (plan `02-02`'s `RestTimerActivityController`). The `RestTimerOverlay` consumes `@Bindable engine` and renders via `TimelineView(.periodic(from: startedAt, by: 1))` so the UI re-renders once per second while running — and the recompute reads from `Date.now`, so no drift.

The collapsed-pill ↔ expanded-sheet pattern matches UI-SPEC § Spacing exceptions ("Rest-timer overlay height when collapsed is 64pt") and § Live Activity ("Apple Music's now-playing pill is the visual reference"). The expanded `.medium` detent contains the ±15s and Skip controls; the progress arc tints accent (UI-SPEC accent surface #11 + #17).

## Requirements Covered

- **SESS-04** (rest timer overlay in-app; Live Activity + lock-screen + Dynamic Island integration) — the user-visible overlay half. Together with plans `02-01` + `02-02`, this closes the requirement.

## Files to Create / Modify

### Modify — `fitbod/Sessions/RestTimer/RestTimerEngine.swift`

Compose the activity controller as an injected dependency. The engine now owns three orthogonal concerns: notification scheduler, activity controller, and the Date-based countdown state.

Diff against the version landed by plan `02-01`:

- Add `private let activityController: RestTimerActivityControlling` field.
- Extend `init(...)` to take an additional `activityController: RestTimerActivityControlling = NoopActivityController()` parameter (Noop default so existing plan-`02-01` tests don't break).
- In `start(...)`: after `scheduler.schedule(...)`, call `activityController.start(startedAt: self.startedAt!, targetSeconds: self.targetSeconds, exerciseName: exerciseName)`.
- In `adjust(...)`: after the scheduler reschedule, call `activityController.update(startedAt: startedAt, targetSeconds: targetSeconds)`.
- In `stop()`: after `scheduler.cancel(...)`, call `activityController.end()`.
- Add a `public static func makeProduction() -> RestTimerEngine` factory that wires the live `LiveNotificationScheduler` + `RestTimerActivityController` (used by `SessionLoggerView` in plan `04-01`).

The diff is additive — existing `RestTimerEngineTests` from plan `02-01` continue to pass because the `NoopActivityController` default keeps the controller side a no-op in those tests.

### Create — `fitbod/Sessions/RestTimer/RestTimerOverlay.swift`

UI-SPEC § Rest timer (`RestTimerOverlay` + `RestTimerLiveActivity`) and § Spacing → Rest-timer overlay 64pt collapsed pill.

```
import SwiftUI

/// The in-app overlay rendered above the SessionLoggerView's exercise list.
/// Collapsed = 64pt pill. Tap to expand to a .medium-detent sheet with
/// ±15s / Skip controls. Pure SwiftUI; reads from a `RestTimerEngine`
/// `@Bindable` injection. The engine is the single source of truth; the
/// overlay does no Date math itself (TimelineView ticks the recompute).
public struct RestTimerOverlay: View {
    @Bindable public var engine: RestTimerEngine
    @State private var presentingExpanded = false
    @Environment(\.accessibilityReduceMotion) private var reduceMotion

    public init(engine: RestTimerEngine) {
        self.engine = engine
    }

    public var body: some View {
        if engine.isRunning {
            collapsedPill
                .frame(height: 64)                                           // UI-SPEC Spacing exception
                .background(.regularMaterial)                                // glass effect
                .clipShape(.capsule)
                .padding(.horizontal, 16)                                    // UI-SPEC lg inset
                .padding(.bottom, 8)
                .onTapGesture {
                    presentingExpanded = true
                }
                .accessibilityElement(children: .combine)
                .accessibilityLabel("Rest timer: \(remainingAccessibility)")
                .accessibilityHint("Tap to expand controls")
                .accessibilityAdjustableAction { direction in                // UI-SPEC accessibility § overlay collapsed
                    switch direction {
                    case .increment: engine.adjust(deltaSeconds: 15)
                    case .decrement: engine.adjust(deltaSeconds: -15)
                    @unknown default: break
                    }
                }
                .sheet(isPresented: $presentingExpanded) {
                    expandedSheet
                        .presentationDetents([.medium])                      // UI-SPEC exception
                }
        } else {
            EmptyView()
        }
    }

    // MARK: - Collapsed pill

    private var collapsedPill: some View {
        TimelineView(.periodic(from: engine.startedAt ?? .now, by: 1)) { _ in
            HStack(spacing: 8) {                                             // UI-SPEC sm
                Text(formatRemaining(engine.remaining))                       // UI-SPEC verbatim: "2:14 · Rest"
                    .font(.title2)
                    .fontWeight(.semibold)
                    .monospacedDigit()
                Text("· Rest")                                               // UI-SPEC verbatim
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            .padding(.horizontal, 16)
        }
    }

    // MARK: - Expanded sheet

    private var expandedSheet: some View {
        NavigationStack {
            VStack(spacing: 24) {                                            // UI-SPEC xl section spacing
                Text("Rest Timer")                                           // UI-SPEC verbatim header
                    .font(.headline)
                Text(engine.currentExerciseName)                             // UI-SPEC verbatim subtitle (snapshotted exercise name)
                    .font(.body)
                    .foregroundStyle(.secondary)

                TimelineView(.periodic(from: engine.startedAt ?? .now, by: 1)) { _ in
                    ZStack {
                        RestTimerProgressRing(
                            remaining: engine.remaining,
                            target: Double(engine.targetSeconds),
                            reduceMotion: reduceMotion
                        )
                        Text(formatRemaining(engine.remaining))               // UI-SPEC verbatim countdown
                            .font(.title2)
                            .fontWeight(.semibold)
                            .monospacedDigit()
                    }
                    .frame(width: 160, height: 160)
                }

                HStack(spacing: 16) {                                        // UI-SPEC lg
                    Button {
                        engine.adjust(deltaSeconds: -15)
                    } label: {
                        Text("−15s")                                         // UI-SPEC verbatim
                            .frame(minWidth: 60, minHeight: 44)              // UI-SPEC HIG 44pt
                    }
                    .buttonStyle(.bordered)
                    .accessibilityLabel("Subtract 15 seconds")               // UI-SPEC accessibility

                    Button {
                        engine.adjust(deltaSeconds: 15)
                    } label: {
                        Text("+15s")                                         // UI-SPEC verbatim
                            .frame(minWidth: 60, minHeight: 44)
                    }
                    .buttonStyle(.bordered)
                    .accessibilityLabel("Add 15 seconds")
                }

                Button("Skip") {                                             // UI-SPEC verbatim "Skip" text button
                    engine.stop()
                    presentingExpanded = false
                }
                .buttonStyle(.plain)
                .foregroundStyle(.secondary)                                 // UI-SPEC: Skip is secondary
                .accessibilityLabel("Skip remaining rest")

                if engine.targetSeconds > 0 {
                    Text("Prescribed: \(engine.targetSeconds)s")             // UI-SPEC verbatim footer
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            }
            .padding(24)
        }
    }

    // MARK: - Helpers

    private func formatRemaining(_ seconds: TimeInterval) -> String {
        let s = max(0, Int(seconds))
        return String(format: "%d:%02d", s / 60, s % 60)
    }

    private var remainingAccessibility: String {
        let s = max(0, Int(engine.remaining))
        if s >= 60 { return "\(s / 60) minutes \(s % 60) seconds" }
        return "\(s) seconds"
    }
}
```

### Create — `fitbod/Sessions/RestTimer/RestTimerProgressRing.swift`

```
import SwiftUI

/// Circular accent-color progress ring used in the expanded overlay
/// sheet. Respects `accessibilityReduceMotion` (UI-SPEC accessibility
/// contract: when on, the arc snaps to discrete states instead of
/// sweeping).
public struct RestTimerProgressRing: View {
    public let remaining: TimeInterval
    public let target: Double
    public let reduceMotion: Bool

    public init(remaining: TimeInterval, target: Double, reduceMotion: Bool) {
        self.remaining = remaining
        self.target = target
        self.reduceMotion = reduceMotion
    }

    public var body: some View {
        let total = max(1, target)
        let elapsed = max(0, total - remaining)
        let progress = min(1, elapsed / total)

        ZStack {
            Circle()
                .stroke(Color(.systemGray5), lineWidth: 8)
            Circle()
                .trim(from: 0, to: progress)
                .stroke(
                    Color.accentColor,
                    style: StrokeStyle(lineWidth: 8, lineCap: .round)
                )
                .rotationEffect(.degrees(-90))
                .animation(reduceMotion ? nil : .linear(duration: 1), value: progress)
        }
    }
}
```

### Create — `fitbodTests/RestTimerOverlayCopyTests.swift`

Verifies UI-SPEC verbatim copywriting. The overlay is a SwiftUI View — runtime visual tests live on the user's device. The unit tests anchor copy strings against the source.

```
import Foundation
import Testing
@testable import fitbod

@Suite("RestTimerOverlayCopy")
struct RestTimerOverlayCopyTests {

    @Test("verbatimCopyAnchors — UI-SPEC strings present in RestTimerOverlay source")
    func verbatimCopyAnchors() throws {
        // Read the on-disk source. (Build-time grep is more typical;
        // this test executes a runtime fixture against the source file
        // for parse-clean strictness.)
        let url = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent()
            .deletingLastPathComponent()
            .appendingPathComponent("fitbod/Sessions/RestTimer/RestTimerOverlay.swift")
        let src = try String(contentsOf: url, encoding: .utf8)

        // UI-SPEC § Rest timer (verbatim):
        #expect(src.contains("\"· Rest\""))               // collapsed pill suffix
        #expect(src.contains("\"Rest Timer\""))           // expanded header
        #expect(src.contains("\"−15s\""))                  // expanded "−15s" button
        #expect(src.contains("\"+15s\""))                  // expanded "+15s" button
        #expect(src.contains("\"Skip\""))                  // skip text button
        #expect(src.contains("\"Prescribed: \\(engine.targetSeconds)s\""))   // footer

        // UI-SPEC accessibility verbatim labels:
        #expect(src.contains("\"Add 15 seconds\""))
        #expect(src.contains("\"Subtract 15 seconds\""))
        #expect(src.contains("\"Skip remaining rest\""))
        #expect(src.contains("\"Tap to expand controls\""))
    }

    @Test("reduceMotionWiredThroughEnvironment")
    func reduceMotionWiredThroughEnvironment() throws {
        // The progress ring takes `reduceMotion: Bool` and gates the
        // animation. Read the source and grep for the wire-up.
        let url = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent()
            .deletingLastPathComponent()
            .appendingPathComponent("fitbod/Sessions/RestTimer/RestTimerOverlay.swift")
        let src = try String(contentsOf: url, encoding: .utf8)

        #expect(src.contains("@Environment(\\.accessibilityReduceMotion)"))
        #expect(src.contains("reduceMotion: reduceMotion"))
    }

    @Test("timelineViewTickEverySecond")
    func timelineViewTickEverySecond() throws {
        let url = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent()
            .deletingLastPathComponent()
            .appendingPathComponent("fitbod/Sessions/RestTimer/RestTimerOverlay.swift")
        let src = try String(contentsOf: url, encoding: .utf8)

        // The collapsed pill AND the expanded body both wrap in
        // TimelineView(.periodic(from:, by: 1)) so the Date.now-derived
        // countdown re-renders once per second.
        #expect(src.contains("TimelineView(.periodic(from:"))
        #expect(src.contains("by: 1)"))
    }
}
```

## Acceptance Criteria

1. `fitbod/Sessions/RestTimer/RestTimerEngine.swift` (modified) takes a `RestTimerActivityControlling` parameter in `init(...)` with a `NoopActivityController()` default. Verified by `grep -nE 'activityController: RestTimerActivityControlling|NoopActivityController\(\)' fitbod/Sessions/RestTimer/RestTimerEngine.swift` returns ≥2 matches.
2. `RestTimerEngine.makeProduction()` factory exists and wires `LiveNotificationScheduler()` + `RestTimerActivityController()`. Verified by `grep -nE 'public static func makeProduction\(\)|LiveNotificationScheduler\(\)|RestTimerActivityController\(\)' fitbod/Sessions/RestTimer/RestTimerEngine.swift` returns ≥3 matches.
3. `RestTimerEngine.start(...)` calls `activityController.start(...)` after `scheduler.schedule(...)`. `RestTimerEngine.adjust(...)` calls `activityController.update(...)`. `RestTimerEngine.stop()` calls `activityController.end()`. Verified by `grep -nE 'activityController\.start|activityController\.update|activityController\.end' fitbod/Sessions/RestTimer/RestTimerEngine.swift` returns 3 matches.
4. `fitbod/Sessions/RestTimer/RestTimerOverlay.swift` exists with `public struct RestTimerOverlay: View` consuming `@Bindable public var engine: RestTimerEngine`. Verified by `grep -nE 'public struct RestTimerOverlay: View|@Bindable public var engine: RestTimerEngine' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns 2 matches.
5. The collapsed-pill height is 64pt (UI-SPEC Spacing exception). Verified by `grep -n '.frame(height: 64)' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns 1 match.
6. The expanded sheet uses `.presentationDetents([.medium])`. Verified by `grep -n '.presentationDetents(\[.medium\])' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns 1 match.
7. UI-SPEC verbatim copy strings are present: "· Rest", "Rest Timer", "−15s", "+15s", "Skip", "Prescribed: \(engine.targetSeconds)s". Verified by the `verbatimCopyAnchors` test (passes when the test compiles + the assertions succeed).
8. Accessibility labels are present: "Add 15 seconds", "Subtract 15 seconds", "Skip remaining rest", "Tap to expand controls". Verified by `grep -E '"Add 15 seconds"|"Subtract 15 seconds"|"Skip remaining rest"|"Tap to expand controls"' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns ≥4 matches.
9. The `accessibilityAdjustableAction` is wired so VoiceOver swipe-up/swipe-down adjusts ±15s. Verified by `grep -n 'accessibilityAdjustableAction' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns 1 match.
10. `TimelineView(.periodic(from:, by: 1))` is used for the countdown render. Verified by `grep -nE 'TimelineView\(\.periodic\(from:' fitbod/Sessions/RestTimer/RestTimerOverlay.swift` returns ≥2 matches (one per collapsed pill, one per expanded sheet body).
11. `fitbod/Sessions/RestTimer/RestTimerProgressRing.swift` exists with `accessibilityReduceMotion` honored. Verified by `grep -nE 'reduceMotion ? nil : \.linear\(duration: 1\)' fitbod/Sessions/RestTimer/RestTimerProgressRing.swift` returns 1 match.
12. The progress ring uses `Color.accentColor` (UI-SPEC accent surface #11). Verified by `grep -n 'Color.accentColor' fitbod/Sessions/RestTimer/RestTimerProgressRing.swift` returns ≥1 match.
13. `fitbodTests/RestTimerOverlayCopyTests.swift` has exactly 3 `@Test` functions. Verified by `grep -c '@Test' fitbodTests/RestTimerOverlayCopyTests.swift` returns 3.
14. Parse-clean: `find fitbod fitbodTests -name '*.swift' -type f | xargs xcrun swiftc -parse` exits 0.

## Tests

| Test | Suite | Asserts |
|------|-------|---------|
| `verbatimCopyAnchors` | `RestTimerOverlayCopy` | UI-SPEC § Rest timer copy strings are present in the source verbatim |
| `reduceMotionWiredThroughEnvironment` | `RestTimerOverlayCopy` | `@Environment(\.accessibilityReduceMotion)` is consumed and forwarded to the progress ring |
| `timelineViewTickEverySecond` | `RestTimerOverlayCopy` | Both render paths use `TimelineView(.periodic(by: 1))` (the canonical Date-derived UI re-render pattern) |

The existing `RestTimerEngineTests` suite (plan `02-01`) continues to pass because the `NoopActivityController` default preserves the engine's prior behavior in tests.

## Dependencies

- **Hard:** plan `02-01` (engine), plan `02-02` (`RestTimerActivityControlling` + `NoopActivityController`).
- **Consumed by:** plan `04-01` (`SessionLoggerView` instantiates `RestTimerEngine.makeProduction()` and renders `RestTimerOverlay(engine: engine)` above the exercise list).

## Wave

**Wave 2** — sequenced after plans `02-01` and `02-02`. Closes the rest-timer wave; plans `03-xx` (routines) can begin without waiting for this plan since they don't consume the engine surface.

## Complexity

**S (small).** The wiring is mostly composition (~50 LOC of engine diff) + a clean overlay view (~150 LOC) + a small progress-ring view (~30 LOC). The test suite covers copy and motion-respect. The visual+functional validation happens on-device.

## Anti-Patterns to Avoid

- Do NOT replace the `NoopActivityController` default in `RestTimerEngine.init(...)`. The default keeps tests fast and keeps the engine unit-testable without ActivityKit side effects. The production call site uses `RestTimerEngine.makeProduction()`.
- Do NOT render the collapsed pill as a system `.toolbarItem` or a `.safeAreaInset`. The overlay is a free-floating SwiftUI view above the tab bar, positioned by the consuming `SessionLoggerView` in plan `04-01`. Pinning to a system slot would conflict with the existing tab bar.
- Do NOT use `Timer.publish` to drive the countdown re-render. `TimelineView(.periodic(from:, by: 1))` is the SwiftUI-native primitive; it auto-pauses when the view isn't on-screen.
- Do NOT pin `.presentationDetents([.large, .medium])` — UI-SPEC § Rest timer overlay specifies `.medium` ONLY (half-screen). The user shouldn't be able to drag the sheet to full-screen height.
- Do NOT use `Color.accentColor` inside the "Skip" button. UI-SPEC § Accent reserved for — "Skip rest" is explicitly NOT an accent surface. It's `.secondaryLabel` on system fill.
- Do NOT add a haptic on the collapsed-pill tap. UI-SPEC accessibility § Haptics: the haptic-bearing events are set-row commit + rest-timer reaches-zero. The pill tap is a UI navigation action, not a value commit.
- Do NOT instantiate a second `RestTimerEngine` instance for the overlay's preview. Use `RestTimerEngine(scheduler: StubScheduler(), now: { fixedDate })` to drive a controllable preview state.
- Do NOT call `engine.start(...)` from inside the overlay's body. The overlay is read-only against the engine state; the caller (`SessionLoggerView`'s set-row completion handler) starts the timer.
