---
phase: 04-periodization-blocks
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-05"]
files_modified:
  - fitbod/Prescription/ProgressionStrategy.swift
  - fitbod/Prescription/BlockPeriodizedStrategy.swift
  - fitbod/Prescription/HybridStrategy.swift
  - fitbod/Prescription/ProgressionStrategyFactory.swift
  - fitbod/Prescription/PrescriptionExplanation.swift
  - fitbodTests/BlockPeriodizedStrategyTests.swift
  - fitbodTests/HybridStrategyTests.swift
autonomous: true
requirements: [PRES-05, PRES-06]

must_haves:
  truths:
    - "ProgressionStrategy.prescribe(...) signature is extended with `block: Block? = nil, today: Date = Date.now` default-valued params; Phase 3's RPEAutoregStrategy and DoubleProgressionStrategy continue to compile unchanged (they ignore the new params)"
    - "BlockPeriodizedStrategy: ProgressionStrategy resolves the current phase via PeriodizationEngine.phase(for: block, on: today); for non-deload phases applies intensity multiplier to baseline (lastSessionWeight ?? 0 if no history) and rounds via PlateCalculator (if available — falls back to round-to-smallestIncrement otherwise); for deload weeks HOLDS the baseline weight unchanged (D-12)"
    - "BlockPeriodizedStrategy emits a PrescriptionExplanation with formulaName 'Block periodized' and computedLine 'Phase {kind}: baseline {N} kg × ×{intensityMultiplier} → {raw} kg' (deload variant: '{kind} phase: weight held at baseline {N} kg')"
    - "HybridStrategy = min(blockTarget, rpeTarget); when block has no context or RPE has no calibration, falls back to whichever branch is non-zero; emits formulaName 'Hybrid (block + RPE)' with computedLine showing both branches + which was chosen"
    - "ProgressionStrategyFactory.make(for: .block) returns BlockPeriodizedStrategy() (was DoubleProgressionStrategy stub in Phase 3); .make(for: .hybrid) returns HybridStrategy()"
    - "PrescriptionExplanation gains two optional sentences for 'Phase context' ({kind} phase — Week {N} of {M}) and 'Deload note' (working sets cut / weight held)"
    - "BlockPeriodizedStrategyTests + HybridStrategyTests turn GREEN; property invariant `chosen <= blockTarget && chosen <= rpeTarget` pins the Hybrid contract"
  artifacts:
    - path: "fitbod/Prescription/ProgressionStrategy.swift"
      provides: "Protocol extended with `block: Block? = nil, today: Date = Date.now` default-valued params; existing PrescriptionExplanation + CalibrationStatus + HistoryPoint preserved"
      contains: "block: Block? = nil"
    - path: "fitbod/Prescription/BlockPeriodizedStrategy.swift"
      provides: "struct BlockPeriodizedStrategy: ProgressionStrategy; pure-function; multiplies baseline by phase.intensityMultiplier; deload week holds baseline weight unchanged per D-12; defensive when block / phase / baseline is nil"
      contains: "struct BlockPeriodizedStrategy"
    - path: "fitbod/Prescription/HybridStrategy.swift"
      provides: "struct HybridStrategy: ProgressionStrategy; composes BlockPeriodizedStrategy + RPEAutoregStrategy; chosen = min(blockTarget, rpeTarget); fallback when either branch returns 0"
      contains: "struct HybridStrategy"
    - path: "fitbod/Prescription/ProgressionStrategyFactory.swift"
      provides: "Updated to route .block -> BlockPeriodizedStrategy() and .hybrid -> HybridStrategy() (was DoubleProgressionStrategy stub in Phase 3 plan 03-05)"
      contains: "case .block: return BlockPeriodizedStrategy()"
    - path: "fitbod/Prescription/PrescriptionExplanation.swift"
      provides: "Optional phaseContextLine + optional deloadNoteLine fields (default nil); WhyThisWeightSheet renders them when non-nil"
      contains: "var phaseContextLine: String?"
  key_links:
    - from: "fitbod/Prescription/BlockPeriodizedStrategy.swift"
      to: "fitbod/Prescription/PeriodizationEngine.swift"
      via: "PeriodizationEngine.phase(for: block, on: today) resolves the current phase to pull multipliers from"
      pattern: "PeriodizationEngine.phase"
    - from: "fitbod/Prescription/HybridStrategy.swift"
      to: "fitbod/Prescription/BlockPeriodizedStrategy.swift"
      via: "Constructs BlockPeriodizedStrategy() + RPEAutoregStrategy() inline; calls each prescribe(); min() the weights"
      pattern: "min(blockResult.weight, rpeResult.weight)"
    - from: "fitbod/Prescription/ProgressionStrategyFactory.swift"
      to: "fitbod/Prescription/BlockPeriodizedStrategy.swift"
      via: ".make(for: .block) returns BlockPeriodizedStrategy()"
      pattern: "BlockPeriodizedStrategy()"
---

<objective>
Ship the two new progression strategies. After this plan, when the user runs a session for a routine that is linked to an active block, the session prescription uses block-aware math (intensity multiplier × baseline, deload-week weight-holding behavior) and the Hybrid strategy combines block ceiling with RPE-driven autoregulation. Closes PRES-05 (BlockPeriodizedStrategy resolves weight from active block phase's intensity curve) and PRES-06 (HybridStrategy combines block phase context with RPE-driven daily adjustment).

Purpose: Phase 3 ships the protocol + first two strategies; Phase 4 fills the remaining two. The protocol extension (default-valued `block: Block? = nil, today: Date = Date.now`) preserves Phase 3 call-sites while letting block-aware strategies consume context via DI. The Hybrid `min(...)` rule encodes BLOCK-08 spirit: block is the ceiling, RPE pulls down on bad days but never above the block schedule.

Output: Two new strategy structs, three modified Phase 3 files (protocol / factory / explanation), two test suites flipped GREEN.

**PREREQUISITE NOTE:** Phase 3 plan 03-05 ships the `ProgressionStrategy` protocol + `RPEAutoregStrategy` + `DoubleProgressionStrategy` + `ProgressionStrategyFactory` + `PrescriptionExplanation` + `CalibrationStatus`. As of plan-write time, `fitbod/Prescription/` does not exist on disk. If Phase 3 has not yet shipped 03-05 when this plan runs, the executor either (a) BLOCKS this plan until Phase 3 ships 03-05, OR (b) inlines a minimal Phase 3 protocol surface inside this plan's new files using the canonical references at `.planning/phases/03-smart-prescription-warm-ups/03-05-PLAN.md` lines 99-102, 138-149, 156-161. Option (a) is strongly preferred — Phase 3 plans exist and should be executed first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/04-periodization-blocks/04-CONTEXT.md
@.planning/phases/04-periodization-blocks/04-RESEARCH.md
@.planning/phases/04-periodization-blocks/04-UI-SPEC.md
@.planning/phases/04-periodization-blocks/04-PATTERNS.md
@.planning/phases/04-periodization-blocks/04-01-PLAN.md

@.planning/phases/03-smart-prescription-warm-ups/03-05-PLAN.md
@.planning/phases/03-smart-prescription-warm-ups/03-CONTEXT.md

@fitbod/Models/Block.swift
@fitbod/Models/BlockPhase.swift
@fitbod/Models/Enums/BlockPhaseKind.swift
@fitbod/Models/Enums/ProgressionKind.swift
@fitbod/Sessions/PreviousMatchingIntent.swift

<interfaces>
<!-- Phase 3 types this plan depends on (per 03-05-PLAN.md if Phase 3 has shipped, OR inlined per the prerequisite note). -->

ProgressionStrategy protocol shape (from 03-05-PLAN.md line 100):
```
public protocol ProgressionStrategy: Sendable {
    func prescribe(
        history: [HistoryPoint],
        targetRepsLow: Int, targetRepsHigh: Int,
        targetRPE: Double?,
        smallestIncrement: Double,
        plates: [(weight: Double, countPerSide: Int)],
        barWeight: Double,
        minCalibrationSets: Int,
        lastSessionWeight: Double?,
        lastSessionReps: Int?,
        lastSessionRPE: Double?,
        lastSessionDate: Date?
        // Phase 4 ADDS: block: Block? = nil, today: Date = Date.now
    ) -> (weight: Double, explanation: PrescriptionExplanation)
}
```

PrescriptionExplanation value type (from 03-05-PLAN.md):
```
public struct PrescriptionExplanation: Sendable {
    public var lastSessionLine: String?
    public var formulaName: String
    public var computedLine: String?
    public var roundedWeight: Double
    public var roundedLine: String?
    public var status: CalibrationStatus
    public var bumpOccurred: Bool
    public var range: ClosedRange<Double>?
    // Phase 4 ADDS: var phaseContextLine: String?, var deloadNoteLine: String?
}
```

HistoryPoint value type (from 03-05-PLAN.md):
```
public struct HistoryPoint: Sendable {
    public let date: Date
    public let weight: Double
    public let reps: Int
    public let rpe: Double?
}
```

CalibrationStatus enum (from 03-05-PLAN.md):
```
public enum CalibrationStatus: Sendable, Equatable {
    case notApplicable
    case calibrating(Int, Int)  // (current, required)
    case calibrated
}
```

ProgressionStrategyFactory (from 03-05-PLAN.md — Phase 3 stubs):
```
public enum ProgressionStrategyFactory {
    public static func make(for kind: ProgressionKind) -> any ProgressionStrategy {
        switch kind {
        case .rpe: return RPEAutoregStrategy()
        case .double: return DoubleProgressionStrategy()
        case .block: return DoubleProgressionStrategy()  // <- Phase 4 swaps to BlockPeriodizedStrategy()
        case .hybrid: return DoubleProgressionStrategy()  // <- Phase 4 swaps to HybridStrategy()
        }
    }
}
```

PlateCalculator (from 03-05-PLAN.md — Phase 3 ships):
```
public enum PlateCalculator {
    public static func roundDown(target: Double, barWeight: Double, plates: [(weight: Double, countPerSide: Int)]) -> Double
}
```

From plan 04-01 (shipped):
- `enum PeriodizationEngine` — `.phase(for: Block, on: Date) -> BlockPhase?`, `.weekIndex(for: Block, on: Date) -> Int?`, `.weekContext(for:weekIndex:) -> MesocycleWeekContext?`.

From fitbod/Models/Block.swift / BlockPhase.swift / BlockPhaseKind.swift: see plan 04-01 interfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ProgressionStrategy protocol + PrescriptionExplanation; verify Phase 3 strategies unaffected</name>
  <read_first>
    fitbod/Prescription/ProgressionStrategy.swift (EXISTS only if Phase 3 plan 03-05 shipped — if not, see prerequisite note in objective),
    fitbod/Prescription/RPEAutoregStrategy.swift (existing — must continue compiling unchanged),
    fitbod/Prescription/DoubleProgressionStrategy.swift (existing — must continue compiling unchanged),
    .planning/phases/03-smart-prescription-warm-ups/03-05-PLAN.md (canonical protocol shape — lines 99-102, 138-149),
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 819-829 — extension decision: extend in place with default-valued params),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 1458-1481 protocol extension pattern)
  </read_first>
  <files>fitbod/Prescription/ProgressionStrategy.swift, fitbod/Prescription/PrescriptionExplanation.swift</files>
  <action>
    Verify `fitbod/Prescription/` exists. If not, refuse this task and surface a blocker — Phase 3 plan 03-05 is the prerequisite. If Phase 3 shipped a partial subset (e.g., the protocol but not all 4 strategies), proceed with the protocol extension and rely on Phase 3's existing test green-state to confirm no Phase 3 strategy regression.
    
    (1) Modify fitbod/Prescription/ProgressionStrategy.swift:
      - Add `import SwiftData` to the imports (because the new `block: Block?` param is a SwiftData @Model type).
      - Locate the `func prescribe(...)` protocol requirement signature; append two default-valued params at the END of the param list:
        ```
        block: Block? = nil,
        today: Date = Date.now
        ```
      - The protocol requirement now reads:
        ```
        public protocol ProgressionStrategy: Sendable {
            func prescribe(
                history: [HistoryPoint],
                targetRepsLow: Int, targetRepsHigh: Int,
                targetRPE: Double?,
                smallestIncrement: Double,
                plates: [(weight: Double, countPerSide: Int)],
                barWeight: Double,
                minCalibrationSets: Int,
                lastSessionWeight: Double?,
                lastSessionReps: Int?,
                lastSessionRPE: Double?,
                lastSessionDate: Date?,
                lastSessionRepsArray: [Int]?,
                block: Block?,
                today: Date
            ) -> (weight: Double, explanation: PrescriptionExplanation)
        }
        ```
        (Match Phase 3's exact parameter ordering — verify against `03-05-PLAN.md` line 100 BEFORE editing.)
      
      - For protocol requirements in Swift, default values on parameters are not honored at the protocol level — the conforming type's implementation must declare the defaults. So this protocol extension achieves the goal by: (a) requiring conforming types to add the new params (they can default to nil/.now in their concrete impl), and (b) updating Phase 3's `RPEAutoregStrategy` + `DoubleProgressionStrategy` to add the two trailing params (with `= nil` and `= Date.now` defaults) so callers don't have to pass them.
      
      - Modify fitbod/Prescription/RPEAutoregStrategy.swift and fitbod/Prescription/DoubleProgressionStrategy.swift to add the same two trailing params to their `func prescribe(...)` implementation signatures. Default-valued at the call site (`block: Block? = nil, today: Date = Date.now`). Inside the function body, IGNORE the new params (they have no effect on RPE-autoreg or double-progression math). Add a single-line comment `// Phase 4 protocol extension — RPE-autoreg ignores block context (no block awareness in this strategy).` for code reviewer clarity.
      
      - Run Phase 3 test suites (RPEAutoregStrategyTests, DoubleProgressionStrategyTests, PrescriptionExplanationTests) to confirm zero regression — every call-site in Phase 3 tests still works because both new params default at the impl level.
    
    (2) Modify fitbod/Prescription/PrescriptionExplanation.swift (split out of ProgressionStrategy.swift if Phase 3 colocates them; in that case, modify ProgressionStrategy.swift's PrescriptionExplanation struct in-place):
      - Add two optional fields with default nil:
        ```
        public var phaseContextLine: String? = nil  // e.g. "intensification phase — Week 5 of 8"
        public var deloadNoteLine: String? = nil    // e.g. "Working sets cut: 3 → 2. Weight held at phase intensity."
        ```
      - Update the memberwise public init (if present) to accept the new optional params with default nil. NB: Swift auto-synthesizes memberwise init for structs — if Phase 3 declared it explicitly, extend it; if it relied on synthesized init, the new fields auto-flow into the synthesized init.
      
      - This is a purely additive change: Phase 3's WhyThisWeightSheet (plan 03-06) renders PrescriptionExplanation via a List of String? rows already; the new phaseContextLine + deloadNoteLine fields render naturally when non-nil. NO modification to WhyThisWeightSheet code is required IF the sheet's List body iterates over a `[String?]` and skips nil — verify by reading 03-06-PLAN.md and the WhyThisWeightSheet source if it exists.
      
      - If WhyThisWeightSheet.swift exists and renders explanation rows via an explicit ordered list of properties (not a flexible iteration), modify the rendering body to add two new conditional rows immediately after the existing "Last session" row and before the "Formula" row:
        - phaseContextLine row label "Phase context", value `phaseContextLine` (only when non-nil).
        - deloadNoteLine row label "Deload note", value `deloadNoteLine` (only when non-nil).
        Per UI-SPEC line 300-301.
        If WhyThisWeightSheet does not yet exist, defer the rendering update to plan 04-08 (which surfaces BlockReviewView and can hook into the same explanation rendering surface).
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/RPEAutoregStrategy -only-testing fitbodTests/DoubleProgressionStrategy -only-testing fitbodTests/PrescriptionExplanation 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    ProgressionStrategy protocol has two new trailing params; RPEAutoregStrategy + DoubleProgressionStrategy still compile and pass all Phase 3 tests; PrescriptionExplanation has two new optional fields; WhyThisWeightSheet (if present) renders them conditionally.
  </done>
</task>

<task type="auto">
  <name>Task 2: BlockPeriodizedStrategy + GREEN BlockPeriodizedStrategyTests</name>
  <read_first>
    fitbod/Prescription/ProgressionStrategy.swift (just modified — Task 1),
    fitbod/Prescription/PrescriptionExplanation.swift (just modified — Task 1),
    fitbod/Prescription/RPEAutoregStrategy.swift (analog for Sendable struct conforming to protocol),
    fitbod/Prescription/PlateCalculator.swift (Phase 3 — for rounding; if Phase 3 has not shipped it, use a simple `let rounded = (target / smallestIncrement).rounded(.down) * smallestIncrement` fallback),
    fitbod/Prescription/PeriodizationEngine.swift (from plan 04-01 — phase resolution API),
    fitbod/Models/Block.swift, fitbod/Models/BlockPhase.swift, fitbod/Models/Enums/BlockPhaseKind.swift,
    .planning/phases/04-periodization-blocks/04-CONTEXT.md (D-11 multiplicative against routine baseline, D-12 deload halves sets / keeps weight, D-17 no-history baseline),
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 228-257 BlockPeriodizedStrategy pseudocode, lines 322-359 deload mechanics, lines 800-816 Open Question 1 baseline resolution),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 99-164 BlockPeriodizedStrategy pattern)
  </read_first>
  <files>fitbod/Prescription/BlockPeriodizedStrategy.swift, fitbodTests/BlockPeriodizedStrategyTests.swift</files>
  <action>
    (1) Create fitbod/Prescription/BlockPeriodizedStrategy.swift as `public struct BlockPeriodizedStrategy: ProgressionStrategy`:
      - `public init() {}`
      - `public func prescribe(history:targetRepsLow:targetRepsHigh:targetRPE:smallestIncrement:plates:barWeight:minCalibrationSets:lastSessionWeight:lastSessionReps:lastSessionRPE:lastSessionDate:lastSessionRepsArray:block:today:) -> (weight: Double, explanation: PrescriptionExplanation)` with the full ProgressionStrategy param list (15 params); both `block` and `today` carry default values (`= nil` and `= Date.now`).
      
      Algorithm body (per 04-RESEARCH §BlockPeriodizedStrategy + RESOLUTION per Open Question 1):
      
      ```
      // Resolution per Open Question #1: use lastSessionWeight as baseline (Phase 2's PreviousMatchingIntent already supplies this via the caller).
      // No RoutineExercise.prescribedWeight field is added — keep Phase 4 schema scope at one optional Date? field.
      let baseline = lastSessionWeight ?? 0
      
      // Defensive: no block context → degraded explanation, return baseline as-is.
      guard let block,
            let phase = PeriodizationEngine.phase(for: block, on: today) else {
          return (
              weight: baseline,
              explanation: PrescriptionExplanation(
                  lastSessionLine: nil,
                  formulaName: "Block periodized",
                  computedLine: "No block context — using baseline.",
                  roundedWeight: baseline,
                  roundedLine: nil,
                  status: .notApplicable,
                  bumpOccurred: false,
                  range: nil,
                  phaseContextLine: nil,
                  deloadNoteLine: nil
              )
          )
      }
      
      // Defensive: no baseline → return 0 with a "log a baseline session first" explanation.
      if baseline <= 0 {
          return (
              weight: 0,
              explanation: PrescriptionExplanation(
                  lastSessionLine: nil,
                  formulaName: "Block periodized",
                  computedLine: "No prior data — log a baseline session first.",
                  roundedWeight: 0,
                  roundedLine: nil,
                  status: .notApplicable,
                  bumpOccurred: false,
                  range: nil,
                  phaseContextLine: phaseContextLine(for: block, on: today),
                  deloadNoteLine: nil
              )
          )
      }
      
      // D-12 deload-week special case: HOLD baseline weight unchanged (do NOT apply intensityMultiplier).
      // The deload volume cut (set count halving) is enforced separately by SessionFactory.start in plan 04-07.
      if phase.kind == .deload {
          return (
              weight: baseline,
              explanation: PrescriptionExplanation(
                  lastSessionLine: "Last: \(baseline) kg × \(lastSessionReps ?? 0)",
                  formulaName: "Block periodized",
                  computedLine: "Deload week: weight held at \(baseline) kg",
                  roundedWeight: baseline,
                  roundedLine: nil,
                  status: .notApplicable,
                  bumpOccurred: false,
                  range: nil,
                  phaseContextLine: phaseContextLine(for: block, on: today),
                  deloadNoteLine: "Working sets cut by ~50%. Weight held at phase intensity."
              )
          )
      }
      
      // Non-deload phases: apply intensity multiplier multiplicatively.
      let rawTarget = baseline * phase.intensityMultiplier
      let rounded = roundToPlates(target: rawTarget, barWeight: barWeight, plates: plates, smallestIncrement: smallestIncrement)
      
      return (
          weight: rounded,
          explanation: PrescriptionExplanation(
              lastSessionLine: "Last: \(baseline) kg × \(lastSessionReps ?? 0)",
              formulaName: "Block periodized",
              computedLine: "Phase \(phase.kind.rawValue): baseline \(baseline) kg × ×\(formatMultiplier(phase.intensityMultiplier)) → \(rawTarget) kg",
              roundedWeight: rounded,
              roundedLine: "→ \(rounded) kg (plate-rounded)",
              status: .notApplicable,
              bumpOccurred: false,
              range: nil,
              phaseContextLine: phaseContextLine(for: block, on: today),
              deloadNoteLine: nil
          )
      )
      ```
      
      Helpers (private file-scope):
      - `private func phaseContextLine(for block: Block, on date: Date) -> String?`: resolve current phase + current week index; if both present return "\(phase.kind.rawValue) phase — Week \(weekIndex + 1) of \(totalWeeks)"; else return nil. Use PeriodizationEngine.phase + .weekIndex.
      - `private func roundToPlates(target: Double, barWeight: Double, plates: [(weight: Double, countPerSide: Int)], smallestIncrement: Double) -> Double`: if Phase 3 PlateCalculator is available, call `PlateCalculator.roundDown(target:barWeight:plates:)`; otherwise fall back to `(target / smallestIncrement).rounded(.down) * smallestIncrement` (Phase 3 ships PlateCalculator per 03-05-PLAN.md — this fallback is defensive for any execution-order gap).
      - `private func formatMultiplier(_ m: Double) -> String`: returns String(format: "%.2f", m) — two-decimal display so 0.88 doesn't round to 0.9.
      
      Imports: `import Foundation`, `import SwiftData` (Block is @Model).
    
    (2) Flip RED placeholder in fitbodTests/BlockPeriodizedStrategyTests.swift to GREEN. Required @Test functions (`@MainActor @Suite("BlockPeriodizedStrategy", .serialized)` over in-memory ModelContainer):
      - `accumulationPhaseAppliesIntensityMultiplier`: build block with [.accumulation 3, .intensification 2]; baseline 100 kg; today = block.startDate (week 0 — accumulation, intensity 0.75); call `BlockPeriodizedStrategy().prescribe(... lastSessionWeight: 100, lastSessionReps: 5, block: block, today: today, ...)`; assert weight == 75 (within smallestIncrement rounding tolerance ±2.5kg).
      - `intensificationPhaseAppliesHigherIntensity`: same block; today = week 4 (intensification, intensity 0.88); baseline 100 kg; assert weight == 88 (within rounding).
      - `realizationPhaseAppliesHigherIntensity`: build 4-phase generic block; today = realization week; baseline 100 kg; assert weight == 97 (within rounding).
      - `deloadWeekHoldsBaselineWeight`: build block with deload phase; today = deload week; baseline 100 kg; assert returned weight == 100 (D-12 — NOT 100 * 0.75); assert explanation.computedLine starts with "Deload week:"; assert deloadNoteLine is non-nil.
      - `noBlockReturnsBaselineWithDegradedExplanation`: pass `block: nil`; baseline 100; assert weight == 100; assert formulaName == "Block periodized" + computedLine == "No block context — using baseline."
      - `noBaselineReturnsZeroWithLogPrompt`: baseline = nil; block present; assert weight == 0; assert computedLine == "No prior data — log a baseline session first."
      - `pastEndOfBlockTreatedAsNoBlockContext`: today = block.endDate + 1 day (past end — engine returns nil); assert weight == baseline (degraded path); explanation.computedLine == "No block context — using baseline."
      - `phaseContextLineSetWhenBlockPresent`: any non-degraded path; assert phaseContextLine contains both the phase kind raw value AND "Week" AND "of".
      - `multiplierRoundedDownToPlates`: baseline 100 kg, intensity 0.88 → raw 88 kg; with plates [(45, 2), (25, 2), (10, 2), (5, 2), (2.5, 2)] and barWeight 20: assert PlateCalculator-rounded result (Phase 3 ships the calculator; the test computes expected via the same algorithm).
    
    The test fixture uses the same in-memory ModelContainer + Schema(SchemaV4.models) pattern as plan 04-01's PeriodizationEngineTests. Use the same `makeBlock(phases:)` helper to construct synthetic Blocks.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/BlockPeriodizedStrategy 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    BlockPeriodizedStrategy.swift compiles; conforms to ProgressionStrategy; handles all 6 edge cases (accumulation / intensification / realization / deload / no-block / no-baseline); BlockPeriodizedStrategyTests has 9+ @Test functions all GREEN; D-12 deload weight-holding behavior pinned by `deloadWeekHoldsBaselineWeight`.
  </done>
</task>

<task type="auto">
  <name>Task 3: HybridStrategy + ProgressionStrategyFactory swap + GREEN HybridStrategyTests</name>
  <read_first>
    fitbod/Prescription/BlockPeriodizedStrategy.swift (just shipped — Task 2),
    fitbod/Prescription/RPEAutoregStrategy.swift (Phase 3 — the other branch of the composition),
    fitbod/Prescription/ProgressionStrategyFactory.swift (Phase 3 — current routes .block/.hybrid to DoubleProgressionStrategy stub),
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 261-289 HybridStrategy pseudocode, lines 953-1008 full HybridStrategy code reference),
    .planning/phases/04-periodization-blocks/04-CONTEXT.md (D-16 min(blockTarget, rpeTarget) decision),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 168-189 HybridStrategy pattern)
  </read_first>
  <files>fitbod/Prescription/HybridStrategy.swift, fitbod/Prescription/ProgressionStrategyFactory.swift, fitbodTests/HybridStrategyTests.swift</files>
  <action>
    (1) Create fitbod/Prescription/HybridStrategy.swift as `public struct HybridStrategy: ProgressionStrategy`:
      - `public init() {}`
      - `public func prescribe(...)` with the full ProgressionStrategy 15-param list; both `block` and `today` default-valued.
      
      Algorithm body:
      ```
      // Compute both branches with the full param payload.
      let blockResult = BlockPeriodizedStrategy().prescribe(
          history: history, targetRepsLow: targetRepsLow, targetRepsHigh: targetRepsHigh,
          targetRPE: targetRPE, smallestIncrement: smallestIncrement, plates: plates,
          barWeight: barWeight, minCalibrationSets: minCalibrationSets,
          lastSessionWeight: lastSessionWeight, lastSessionReps: lastSessionReps,
          lastSessionRPE: lastSessionRPE, lastSessionDate: lastSessionDate,
          lastSessionRepsArray: lastSessionRepsArray, block: block, today: today
      )
      let rpeResult = RPEAutoregStrategy().prescribe(
          history: history, targetRepsLow: targetRepsLow, targetRepsHigh: targetRepsHigh,
          targetRPE: targetRPE, smallestIncrement: smallestIncrement, plates: plates,
          barWeight: barWeight, minCalibrationSets: minCalibrationSets,
          lastSessionWeight: lastSessionWeight, lastSessionReps: lastSessionReps,
          lastSessionRPE: lastSessionRPE, lastSessionDate: lastSessionDate,
          lastSessionRepsArray: lastSessionRepsArray, block: nil, today: today  // RPE branch deliberately gets no block context
      )
      
      // Composition logic: min when both branches are non-zero; else fall back to whichever is non-zero; else 0.
      let chosen: Double
      let source: String
      if blockResult.weight > 0 && rpeResult.weight > 0 {
          chosen = min(blockResult.weight, rpeResult.weight)
          source = (chosen == blockResult.weight) ? "block ceiling" : "rpe-driven"
      } else if blockResult.weight > 0 {
          chosen = blockResult.weight
          source = "block ceiling (rpe-no-history)"
      } else if rpeResult.weight > 0 {
          chosen = rpeResult.weight
          source = "rpe-driven (no-block-context)"
      } else {
          chosen = 0
          source = "no data"
      }
      
      return (
          weight: chosen,
          explanation: PrescriptionExplanation(
              lastSessionLine: rpeResult.explanation.lastSessionLine,
              formulaName: "Hybrid (block + RPE)",
              computedLine: "Block ceiling: \(blockResult.weight) kg · RPE target: \(rpeResult.weight) kg · Using: \(source) → \(chosen) kg",
              roundedWeight: chosen,
              roundedLine: "→ \(chosen) kg (chose lower of block / RPE)",
              status: rpeResult.explanation.status,  // forward RPE calibration status
              bumpOccurred: false,
              range: nil,
              phaseContextLine: blockResult.explanation.phaseContextLine,
              deloadNoteLine: blockResult.explanation.deloadNoteLine
          )
      )
      ```
      
      Imports: `import Foundation`, `import SwiftData`.
    
    (2) Modify fitbod/Prescription/ProgressionStrategyFactory.swift:
      - Locate `public static func make(for kind: ProgressionKind) -> any ProgressionStrategy`.
      - Replace `case .block: return DoubleProgressionStrategy()` with `case .block: return BlockPeriodizedStrategy()`.
      - Replace `case .hybrid: return DoubleProgressionStrategy()` with `case .hybrid: return HybridStrategy()`.
      - File header comment: append "Phase 4: .block and .hybrid now route to BlockPeriodizedStrategy and HybridStrategy respectively (Phase 3 routed both to DoubleProgressionStrategy as a stub)."
    
    (3) Flip RED placeholder in fitbodTests/HybridStrategyTests.swift to GREEN. Required @Test functions (`@MainActor @Suite("HybridStrategy", .serialized)`):
      - `hybridChoosesLowerOfBlockAndRPE`: build block where block target == 100 kg (accumulation, baseline 100, multiplier 0.75 → 75; OR pass synthetic baseline 133 + 0.75 → 100); build RPE branch where rpeTarget == 110 (higher); call HybridStrategy.prescribe; assert returned weight == 100; assert explanation.computedLine contains "block ceiling" or "Using: block".
      - `hybridChoosesRPEWhenLowerOnFatigue`: same block but rpeTarget == 90 (RPE pulls down on a bad day); assert returned weight == 90; computedLine contains "rpe-driven" or "Using: rpe".
      - `blockNeverExceededByRPE` (property-based, 10 random inputs): for randomly-generated (blockTarget, rpeTarget) pairs both > 0: assert `chosen <= blockResult.weight && chosen <= rpeResult.weight`. This pins BLOCK-08 spirit: Hybrid never pulls above the block ceiling.
      - `hybridFallsBackToBlockWhenRPEHasNoHistory`: block target 100; baseline = nil for RPE (no history) — RPE returns 0; assert hybrid returns block target.
      - `hybridFallsBackToRPEWhenNoBlock`: block: nil → BlockPeriodized returns baseline (per Task 2 defensive path); RPE returns rpe target; assert hybrid uses min of (baseline, rpeTarget) — i.e., the RPE branch dominates when block context is absent.
      - `hybridReturnsZeroWhenBothBranchesEmpty`: block: nil + baseline nil → both branches 0; assert hybrid returns 0; computedLine contains "no data".
      - `hybridForwardsCalibrationStatus`: RPE branch returns `.calibrating(5, 10)`; assert hybrid's explanation.status == `.calibrating(5, 10)`.
      - `hybridDeloadNotePassesThrough`: block on deload week (BlockPeriodized sets deloadNoteLine); assert HybridStrategy's explanation.deloadNoteLine equals BlockPeriodized's.
      - `factoryReturnsHybridForHybridKind`: assert `ProgressionStrategyFactory.make(for: .hybrid) is HybridStrategy`.
      - `factoryReturnsBlockPeriodizedForBlockKind`: assert `ProgressionStrategyFactory.make(for: .block) is BlockPeriodizedStrategy`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/HybridStrategy 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    HybridStrategy.swift compiles + conforms to protocol; Factory routes .block and .hybrid to the new strategies; HybridStrategyTests has 10+ @Test functions all GREEN; property invariant `chosen <= blockTarget && chosen <= rpeTarget` pinned across 10 random inputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy → Block (read-only) | BlockPeriodizedStrategy reads `block.phases` (cascade-loaded) to compute multipliers. No write. Cannot mutate the block schedule (BLOCK-08 spirit). |
| Strategy → SessionFactory (returns Double) | Both strategies return only `(weight: Double, explanation: PrescriptionExplanation)` — no Block-mutating return type per type-level contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | A malicious / buggy strategy could write to Block via the param ref | mitigate | Block is passed `Block?` — a class reference. While Swift doesn't prevent the strategy from mutating the @Model's properties, the type-level contract is "strategies are pure functions returning Double + explanation." Code-review enforces it; FOUND-07 + the Sendable protocol constrain mutable stored state. The actual single-writer of `Block.isActive` and `Block.reviewedAt` is `BlockDraft.save` (plan 04-02) + `BlockReviewView.acknowledgeAndDismiss` (plan 04-08) + `BlockCard.endBlock` (plan 04-04). |
| T-04-06-02 | Information Disclosure | PrescriptionExplanation.computedLine carries baseline weight + multipliers | accept | The "Why this weight?" sheet (Phase 3) intentionally surfaces this information to the user. Not sensitive. |
| T-04-06-03 | Denial of Service | A block with thousands of phases would slow PeriodizationEngine.phase iteration | mitigate | Stepper range 1-12 weeks per phase + UI affordance limits phases. Even a 100-phase block iterates in microseconds — engine is O(n) where n = phase count. |
| T-04-06-04 | Repudiation | Strategy outputs are not audit-logged | accept | Single-user app. PrescriptionExplanation IS the audit trail surfaced via WhyThisWeightSheet. |
| T-04-06-05 | Elevation of Privilege | HybridStrategy's `min(...)` rule prevents RPE from pulling weight ABOVE the block ceiling — BLOCK-08 enforcement | mitigate | Pinned by `blockNeverExceededByRPE` property-based test. The composition is intentionally one-way (RPE pulls down, never up). |
</threat_model>

<verification>
- `xcodebuild test -only-testing fitbodTests/BlockPeriodizedStrategy -only-testing fitbodTests/HybridStrategy` exits 0.
- Phase 3 strategy tests (RPEAutoregStrategy / DoubleProgressionStrategy / PrescriptionExplanation) still GREEN — the protocol extension with default-valued params is backward-compatible.
- Plan 04-01..05 suites all still GREEN.
- The factory routes `.block` and `.hybrid` to the new strategies — verifiable via factory tests.
- ProgressionStrategy protocol surface compiles with the new trailing params + Phase 3 strategies update their impl signatures to match.
</verification>

<success_criteria>
- PRES-05: BlockPeriodizedStrategy resolves weight from the active block phase's intensity curve (verified across accumulation/intensification/realization).
- PRES-06: HybridStrategy combines block phase context with RPE-driven daily adjustment via min().
- D-12 deload weight-holding behavior is pinned by `deloadWeekHoldsBaselineWeight`.
- BLOCK-08 spirit (block is the ceiling, RPE pulls down only) is pinned by `blockNeverExceededByRPE` property test.
- ProgressionStrategyFactory swaps the Phase 3 stubs for the real strategies — verified by factory tests.
- Phase 3 strategy tests have ZERO regression — verified by re-running Phase 3 suite.
- BlockPeriodizedStrategyTests + HybridStrategyTests both turn GREEN (RED placeholders from plan 04-01 flipped).
</success_criteria>

<output>
After completion, create `.planning/phases/04-periodization-blocks/04-06-SUMMARY.md`.
</output>
