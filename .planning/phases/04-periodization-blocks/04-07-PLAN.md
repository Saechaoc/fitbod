---
phase: 04-periodization-blocks
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-05"]
files_modified:
  - fitbod/Sessions/SessionFactory.swift
  - fitbodTests/SessionBlockSnapshotTests.swift
  - fitbodTests/DeloadVolumeApplicationTests.swift
autonomous: true
requirements: [BLOCK-04]

must_haves:
  truths:
    - "SessionFactory.start(routine:on:context:) snapshots routine.block onto Session.block (D-20) — verified by reading SessionFactory.swift line 101 which already contains `session.block = routine.block` (Phase 2 wired the field; Phase 4 pins the snapshot contract by test and adds the deload set-count cut)"
    - "After session start, detaching routine.block (setting routine.block = nil) does NOT mutate session.block — the historical session retains its original block context per the snapshot pattern (PITFALLS #1 extension)"
    - "When PeriodizationEngine.phase(for: routine.block, on: date).kind == .deload, SessionFactory.start creates `max(1, Int(floor(Double(re.targetSets) * phase.volumeMultiplier)))` SetEntry rows per RoutineExercise instead of `re.targetSets` rows — D-12 deload halving with Pitfall 7 clamp"
    - "On non-deload phases (accumulation / intensification / realization) AND when routine.block is nil, the set count is unchanged from `re.targetSets`"
    - "SessionBlockSnapshotTests + DeloadVolumeApplicationTests turn GREEN; both contracts pinned via in-memory ModelContainer fixtures"
  artifacts:
    - path: "fitbod/Sessions/SessionFactory.swift"
      provides: "Extended `start(routine:on:context:)` body — after the existing previousHint computation, resolves deloadVolumeMultiplier via PeriodizationEngine.phase(for: routine.block, on: date); replaces the `for setIndex in 0..<re.targetSets` loop bound with `for setIndex in 0..<scaledSetCount`; the existing `session.block = routine.block` snapshot line is preserved (no edit)"
      contains: "PeriodizationEngine.phase(for: routine.block, on: date)"
  key_links:
    - from: "fitbod/Sessions/SessionFactory.swift"
      to: "fitbod/Prescription/PeriodizationEngine.swift"
      via: "PeriodizationEngine.phase(for:on:) resolves whether the current week is a deload week; .volumeMultiplier reads the cut factor"
      pattern: "PeriodizationEngine.phase(for: routine.block"
    - from: "fitbodTests/SessionBlockSnapshotTests.swift"
      to: "fitbod/Models/Session.swift"
      via: "Reads session.block after start and after routine.block mutation to assert snapshot survives"
      pattern: "session.block"
---

<objective>
Wire the deload set-count cut into SessionFactory and pin the existing Session.block snapshot via tests. After this plan, starting a session on a deload week creates half (clamped ≥1) the working SetEntry rows the user would otherwise see — the deload volume cut becomes a real, user-visible behavior. The Session.block snapshot (already wired Phase 2) gets a dedicated test suite that pins the snapshot-survives-template-mutation contract.

Purpose: Plan 04-06's BlockPeriodizedStrategy holds the deload-week WEIGHT unchanged per D-12 (split design — strategy returns weight only). The SET COUNT cut is a SessionFactory concern because only SessionFactory creates the planned SetEntry rows. This plan closes BLOCK-04 (scheduled deload weeks auto-reduce prescribed volume ~50% per the deload phase definition).

Output: One modified `SessionFactory.start` (single function — the change is a few lines: read phase, compute scaledSetCount, replace loop bound), two test suites flipped GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/04-periodization-blocks/04-CONTEXT.md
@.planning/phases/04-periodization-blocks/04-RESEARCH.md
@.planning/phases/04-periodization-blocks/04-PATTERNS.md
@.planning/phases/04-periodization-blocks/04-01-PLAN.md
@.planning/phases/04-periodization-blocks/04-06-PLAN.md

@fitbod/Sessions/SessionFactory.swift
@fitbod/Models/Routine.swift
@fitbod/Models/RoutineExercise.swift
@fitbod/Models/Session.swift
@fitbod/Models/SessionExercise.swift
@fitbod/Models/SetEntry.swift

<interfaces>
<!-- Existing SessionFactory shape (Phase 2 + Phase 3 plans) -->

From fitbod/Sessions/SessionFactory.swift (Phase 2 — current state per STATE.md plan 02-01-01 complete):
- `public enum SessionFactory` namespace.
- `public static func start(routine: Routine, on date: Date = .now, context: ModelContext) throws -> Session`
- The function:
  1. Validates one-active-session invariant.
  2. Validates routine has at least one exercise.
  3. Creates Session, sets startedAt = date, sourceRoutineID = routine.id, routineSnapshotName = routine.name.
  4. Sets `session.block = routine.block` (line 101 — D-20 snapshot already wired).
  5. For each RoutineExercise (sorted by orderIndex), creates a SessionExercise, copies prescription fields.
  6. **Pre-populates SetEntry rows: `for setIndex in 0..<re.targetSets { ... }`** (lines 152-165 — THIS IS THE LOOP THIS PLAN MODIFIES).
  7. Single `try context.save()`.

Phase 3 plan 03-08 may extend the inner per-RoutineExercise block to call ProgressionStrategyFactory + WarmupRamp. This plan's change is orthogonal — it modifies the SET COUNT loop bound only, regardless of warmup additions Phase 3 makes.

From plan 04-01 (shipped):
- `PeriodizationEngine.phase(for: Block, on: Date) -> BlockPhase?` — returns the current phase given a date inside the block.

From RoutineExercise.swift:
- `re.targetSets: Int` is the field this plan scales.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify SessionFactory.start to apply deload volumeMultiplier to set count</name>
  <read_first>
    fitbod/Sessions/SessionFactory.swift (full current state — read the pre-populate loop lines 152-165),
    fitbod/Prescription/PeriodizationEngine.swift (from plan 04-01),
    fitbod/Models/BlockPhase.swift,
    fitbod/Models/Enums/BlockPhaseKind.swift,
    .planning/phases/04-periodization-blocks/04-CONTEXT.md (D-12 halve sets / keep weight),
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 322-359 deload mechanics, line 1103 Pitfall 7 floor clamp),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 1329-1371 SessionFactory modification pattern)
  </read_first>
  <files>fitbod/Sessions/SessionFactory.swift</files>
  <action>
    Read the current SessionFactory.start body. Locate the inner `for re in sortedExercises { ... }` loop that creates SessionExercise + the inner `for setIndex in 0..<re.targetSets { ... }` loop that creates SetEntry rows.
    
    Add a SINGLE new local computation immediately BEFORE the inner SetEntry-creation loop (and AFTER any Phase 3 warmup-generation code that may be present):
    
    ```swift
    // BLOCK-04 / D-12: when the routine is in an active block and the current
    // calendar week resolves to a deload phase, cut the working SetEntry count
    // by the phase's volumeMultiplier (clamped to >= 1 per RESEARCH §Pitfall 7).
    let scaledSetCount: Int
    if let block = routine.block,
       let phase = PeriodizationEngine.phase(for: block, on: date),
       phase.kind == .deload {
        scaledSetCount = max(1, Int(floor(Double(re.targetSets) * phase.volumeMultiplier)))
    } else {
        scaledSetCount = re.targetSets
    }
    ```
    
    Then replace the loop bound:
    ```swift
    for setIndex in 0..<scaledSetCount {
        // ... existing SetEntry creation body, unchanged ...
    }
    ```
    
    Key invariants:
    - The clamp `max(1, ...)` is load-bearing — `targetSets = 1` with `volumeMultiplier = 0.5` → `floor(0.5) = 0` without the clamp; the user expects at least 1 SetEntry per exercise on deload.
    - On non-deload phases (accumulation / intensification / realization), the loop bound stays `re.targetSets` — unchanged from Phase 2 behavior.
    - When `routine.block == nil`, the loop bound stays `re.targetSets`.
    - When the date is BEFORE the block's startDate OR AFTER the block's endDate, `PeriodizationEngine.phase` returns nil (or the first phase for before-start) — the `phase.kind == .deload` check handles both cases correctly (returns false; loop bound stays unchanged).
    - The `session.block = routine.block` line earlier in the function (line ~101) is NOT touched — it was already wired Phase 2 per D-20. Pin it via tests in Task 2.
    
    No new imports beyond what Phase 2 already declares (Foundation + SwiftData). If `PeriodizationEngine` is in a different module (it's not — both files live under the `fitbod` target), no import needed.
    
    Preserve every other existing behavior in SessionFactory.start: warmup generation (if Phase 3 plan 03-08 has shipped), previousHint computation, prescribedWeight assignment, weight hint, isComplete sentinel, single-transaction save.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/SessionFactory 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    SessionFactory.start compiles with the new scaledSetCount block; Phase 2 SessionFactoryTests still GREEN (they don't use a block — fall through to the `else { scaledSetCount = re.targetSets }` branch); Phase 3 SessionFactoryPhase3Tests (if shipped) still GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: SessionBlockSnapshotTests + DeloadVolumeApplicationTests turn GREEN</name>
  <read_first>
    fitbod/Sessions/SessionFactory.swift (just modified — Task 1),
    fitbod/Models/Session.swift, fitbod/Models/SessionExercise.swift, fitbod/Models/SetEntry.swift,
    fitbod/Models/Block.swift,
    fitbodTests/SessionFactoryTests.swift (Phase 2 — fixture pattern),
    fitbodTests/PeriodizationEngineTests.swift (from plan 04-01 — makeBlock helper),
    fitbodTests/SchemaV2MigrationTests.swift (in-memory fixture template)
  </read_first>
  <files>fitbodTests/SessionBlockSnapshotTests.swift, fitbodTests/DeloadVolumeApplicationTests.swift</files>
  <action>
    (1) Flip RED placeholder in fitbodTests/SessionBlockSnapshotTests.swift to GREEN. `@MainActor @Suite("SessionBlockSnapshot", .serialized)` over in-memory ModelContainer with `Schema(SchemaV4.models)` + FitbodSchemaMigrationPlan.
    
    Helper for both suites:
    ```swift
    func makeBlock(in ctx: ModelContext, phases: [(kind: BlockPhaseKind, weeks: Int, vol: Double, int: Double)], startDate: Date = .now) -> Block {
        let block = Block(); block.name = "Test"; block.startDate = startDate; block.isActive = true
        ctx.insert(block)
        for (idx, p) in phases.enumerated() {
            let phase = BlockPhase()
            phase.orderIndex = idx; phase.nameRaw = p.kind.rawValue; phase.weeks = p.weeks
            phase.volumeMultiplier = p.vol; phase.intensityMultiplier = p.int
            phase.block = block
            ctx.insert(phase)
        }
        return block
    }
    func makeRoutineWithOneExercise(in ctx: ModelContext, block: Block?, targetSets: Int = 3) -> Routine {
        // ... insert Routine + 1 Exercise + 1 RoutineExercise linking to the exercise ...
        // (use Exercise.previewSample if available, else inline a minimal Exercise instance)
    }
    ```
    
    Required @Test functions:
    - `sessionBlockSnapshotsRoutineBlock`: build block; build routine with `routine.block = block`; call `SessionFactory.start(routine: routine, on: block.startDate, context: ctx)`; assert returned session.block?.id == block.id.
    - `detachingRoutineBlockAfterStartDoesNotMutateSessionBlock`: same setup; after `try ctx.save()`, mutate `routine.block = nil`, save again; refetch the session; assert session.block?.id is still == block.id (snapshot pattern PITFALLS #1 — Session retains its original context even after the routine's template state changes).
    - `sessionBlockIsNilWhenRoutineHasNoBlock`: build routine with no block; start session; assert session.block == nil.
    - `editingBlockNameAfterSnapshotPropagatesViaReference`: this is a deliberate negative — Session.block is a RELATIONSHIP, not a value snapshot of name/etc. Mutate block.name = "Renamed"; refetch session; assert session.block?.name == "Renamed". This confirms that the snapshot is the RELATIONSHIP (which block) not the field VALUES (which would require a separate sessionBlockSnapshotName field — out of scope for Phase 4 per D-20 ("snapshot routine.block onto Session.block" means the reference, not all the block's fields)).
    - `deletingBlockAfterSnapshotNullifiesSessionBlock`: delete block; refetch session; assert session.block == nil (SwiftData inverse default). This pins the Block→Session inverse semantics (nullify).
    
    (2) Flip RED placeholder in fitbodTests/DeloadVolumeApplicationTests.swift to GREEN. Same fixture pattern. Required @Test functions:
    - `deloadWeekHalvesWorkingSetCount`: build block [(accumulation, 1, 1.0, 0.75), (deload, 1, 0.5, 0.75)] starting 7 days ago; routine with targetSets=4; call `SessionFactory.start(routine, on: .now, context: ctx)` — today resolves to the deload week (day 8); assert `session.exercises.first?.setEntries.filter { $0.isWarmup == false }.count == 2` (floor(4 * 0.5) = 2; warmups excluded if Phase 3 plan 03-08 added them).
    - `deloadWeekClampsToOneSetWhenTargetSetsIsOne`: same block setup; routine with targetSets = 1; on deload week, assert exactly 1 working SetEntry was created (Pitfall 7 clamp — floor(0.5) = 0 → clamped to 1).
    - `accumulationWeekKeepsTargetSetCount`: block [(accumulation, 4, 1.0, 0.75)]; today = block.startDate (accumulation week); routine targetSets = 3; assert 3 working SetEntries created.
    - `intensificationWeekKeepsTargetSetCount`: block [(accumulation, 1, 1.0, 0.75), (intensification, 1, 0.85, 0.88)] starting 7 days ago; today resolves to intensification week (day 8); routine targetSets = 3; assert 3 working SetEntries (D-12 only halves on DELOAD, not on intensification even though intensification's volumeMultiplier is 0.85 < 1.0 — the set-count cut applies ONLY when phase.kind == .deload per the Task 1 conditional).
    - `realizationWeekKeepsTargetSetCount`: similar — assert 3 sets despite realization volumeMultiplier = 0.6.
    - `noBlockKeepsTargetSetCount`: routine with no block; assert targetSets == created count.
    - `pastEndOfBlockKeepsTargetSetCount`: block ends → PeriodizationEngine.phase returns nil; assert set count unchanged.
    - `multipleExercisesInDeloadAllHalved`: routine with 3 exercises (targetSets 4, 5, 6); on deload week, assert created SetEntry counts are (2, 2, 3) — floor(4*0.5), floor(5*0.5), floor(6*0.5).
    
    Use `PeriodizationEngine.weekIndex` in the test setup to confirm `today` actually falls in the deload week before asserting on the resulting set count — defensive: if you compute dates wrong, the test passes for the wrong reason.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/SessionBlockSnapshot -only-testing fitbodTests/DeloadVolumeApplication 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    SessionBlockSnapshotTests has 5 GREEN @Test functions; DeloadVolumeApplicationTests has 8 GREEN @Test functions; Pitfall 7 clamp + D-12 halve-on-deload-only contract pinned; Phase 2 SessionFactoryTests + Phase 3 SessionFactoryPhase3Tests (if present) still GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionFactory.start reads routine.block | Read-only — no mutation. The PeriodizationEngine.phase call is pure. |
| SessionFactory.start writes scaled SetEntry count | Write happens inside the existing `try context.save()` transaction (Phase 2). No new transactional surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | A bug in PeriodizationEngine.phase returning the wrong phase would silently create the wrong number of SetEntry rows | mitigate | Plan 04-01's PeriodizationEngineTests pin the phase-resolution math. DeloadVolumeApplicationTests pins the END-TO-END contract from "block has deload phase + today falls in week N" → "scaledSetCount == expected halved count." |
| T-04-07-02 | Information Disclosure | session.block reveals which block the session belongs to | accept | Single-user app; the user owns all sessions and all blocks. |
| T-04-07-03 | Denial of Service | An adversarial block with `volumeMultiplier = 0.001` and `targetSets = 1` would `floor(0.001) = 0` → clamp to 1 | mitigate | Clamp is enforced; tested by `deloadWeekClampsToOneSetWhenTargetSetsIsOne`. |
| T-04-07-04 | Repudiation | Session.block reference may be nullified by Block deletion (SwiftData inverse default) | accept | This is the documented snapshot pattern behavior (PITFALLS #1 + plan 04-03 delete-cascade test). The Session retains its sourceRoutineID and routineSnapshotName from Phase 2 — the block context is the only thing that becomes nil on block deletion, and that's intentional. |
| T-04-07-05 | Elevation of Privilege | None — SessionFactory writes only to its own Session graph | accept | No cross-tenant escalation in a single-user app. |
</threat_model>

<verification>
- `xcodebuild test -only-testing fitbodTests/SessionBlockSnapshot -only-testing fitbodTests/DeloadVolumeApplication` exits 0.
- Phase 2 SessionFactoryTests still GREEN.
- Phase 3 SessionFactoryPhase3Tests (if shipped per plan 03-08) still GREEN — the scaledSetCount change is orthogonal to Phase 3's warmup-generation insertion.
- Plans 04-01..06 suites still GREEN.
- Manual sim run (developer verification): create a 2-week block with [(accumulation, 1, 1.0, 0.75), (deload, 1, 0.5, 0.75)] starting today; link a routine with targetSets=4; start a session immediately — observe 4 working SetEntry rows (week 0, accumulation). Advance system clock by 8 days (or modify the test to inject `today: Date`); start a new session — observe 2 working SetEntry rows (week 1, deload, floor(4*0.5)=2).
</verification>

<success_criteria>
- BLOCK-04: scheduled deload weeks auto-reduce prescribed volume (~50%) per the deload phase's volumeMultiplier; clamped ≥1.
- D-20 snapshot pattern survives template detachment — pinned by `detachingRoutineBlockAfterStartDoesNotMutateSessionBlock`.
- Pitfall 7 floor-div clamp prevents zero-set sessions on deload weeks.
- Non-deload phases (accumulation / intensification / realization) keep the full targetSets count — the volume cut is DELOAD-EXCLUSIVE per D-12.
- SessionBlockSnapshotTests + DeloadVolumeApplicationTests turn GREEN (RED placeholders from plan 04-01 flipped).
- Phase 2 + Phase 3 SessionFactory tests have ZERO regression.
</success_criteria>

<output>
After completion, create `.planning/phases/04-periodization-blocks/04-07-SUMMARY.md`.
</output>
