---
phase: 04-periodization-blocks
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - fitbod/Models/Block.swift
  - fitbod/Persistence/SchemaV4.swift
  - fitbod/Persistence/FitbodSchemaMigrationPlan.swift
  - fitbod/Prescription/PeriodizationEngine.swift
  - fitbod/Prescription/FatigueAdvisory.swift
  - fitbod/Prescription/StubFatigueAdvisory.swift
  - fitbod/Periodization/BlockPhaseColors.swift
  - fitbod/Periodization/BlockTemplates.swift
  - fitbod/Periodization/MesocycleWeekContext.swift
  - fitbodTests/SchemaV4MigrationTests.swift
  - fitbodTests/PeriodizationEngineTests.swift
  - fitbodTests/BlockPeriodizedStrategyTests.swift
  - fitbodTests/HybridStrategyTests.swift
  - fitbodTests/BlockDraftSaveTests.swift
  - fitbodTests/SingleActiveBlockInvariantTests.swift
  - fitbodTests/SessionBlockSnapshotTests.swift
  - fitbodTests/DeloadVolumeApplicationTests.swift
  - fitbodTests/BlockReviewMathTests.swift
  - fitbodTests/FatigueAdvisoryCanonicalityTests.swift
  - fitbodTests/BlockTemplatesTests.swift
  - fitbodTests/BlockBuilderViewCopyTests.swift
autonomous: true
requirements: [BLOCK-08]

must_haves:
  truths:
    - "SchemaV4 ships as the additive successor to SchemaV3 — adds only Block.reviewedAt: Date? = nil (FOUND-02 safe; lightweight migration eligible)"
    - "FitbodSchemaMigrationPlan registers SchemaV4 and migrateV3toV4 as MigrationStage.lightweight; v2->v3 stage from Phase 3 plan 03-01 preserved"
    - "PeriodizationEngine is a pure-function namespace enum living in fitbod/Prescription/ with no SwiftData ModelContext touch; exposes phase(for:on:), weekIndex(for:on:), weekContext(for:weekIndex:), recommendedNextKind(after:)"
    - "PeriodizationEngine.phase(for: Block, on: Date) walks BlockPhase rows by orderIndex ascending; returns first phase when date < startDate; returns nil when date past total block days"
    - "PeriodizationEngine.weekIndex returns 0-based index from floor((date - startDate) / 86400 / 7); returns nil when date is before startDate or at/past total weeks"
    - "PeriodizationEngine.recommendedNextKind deterministically maps .accumulation->.intensification, .intensification->.realization, .realization->.deload, .deload->.accumulation (D-23)"
    - "FatigueAdvisory protocol is Sendable; required surface is shouldSuggest(context:) -> Bool and suggestion(context:) -> FatigueSuggestion; the protocol cannot return any Block-mutating type (BLOCK-08 type-level enforcement, D-25)"
    - "FatigueSuggestion is a Sendable value struct with only `reason: String`; no method references Block in its definition"
    - "StubFatigueAdvisory: FatigueAdvisory ships with shouldSuggest returning false and suggestion returning FatigueSuggestion(reason: '') — Phase 5 swaps for real signal without touching UI"
    - "BlockTemplates static catalog exposes .generic / .hypertrophy / .powerliftingPeak / .blank constants matching D-03 phase structures with D-10 RP-default multipliers"
    - "BlockTemplates.template(for: BlockPhaseKind) returns the correct stock template for BlockReviewView CTA seeding per D-23 mapping"
    - "BlockPhaseColors namespace enum returns Color via color(for:) and tint(for:) for the four BlockPhaseKind cases with the locked UI-SPEC hex values"
    - "MesocycleWeekContext is a Sendable value struct carrying (phase: BlockPhase, weekStartDate: Date, weekEndDate: Date, daysRemaining: Int, isCurrentWeek: Bool, isDeloadWeek: Bool)"
    - "All 12 Wave-0 test files exist as @Suite skeletons compiling against the in-memory ModelContainer + Schema(SchemaV4.models) + FitbodSchemaMigrationPlan fixture; PeriodizationEngineTests / BlockTemplatesTests / FatigueAdvisoryCanonicalityTests / SchemaV4MigrationTests turn GREEN this plan; the remaining 8 stay RED (Issue.record placeholders) until later plans wire production code"
  artifacts:
    - path: "fitbod/Models/Block.swift"
      provides: "Block @Model entity with new reviewedAt: Date? = nil field appended after isActive; existing fields and relationships untouched; #Index<Block>([\\.isActive], [\\.startDate]) added at top of class body"
      contains: "var reviewedAt: Date?"
    - path: "fitbod/Persistence/SchemaV4.swift"
      provides: "VersionedSchema enum SchemaV4 with versionIdentifier Schema.Version(4, 0, 0) and models = SchemaV3.models (Block@V4 with reviewedAt is the only delta — entity types unchanged in count)"
      contains: "enum SchemaV4: VersionedSchema"
    - path: "fitbod/Persistence/FitbodSchemaMigrationPlan.swift"
      provides: "schemas list extended to [SchemaV1, SchemaV2, SchemaV3, SchemaV4]; stages list extended to include migrateV3toV4 (MigrationStage.lightweight); existing migrateV1toV2 + migrateV2toV3 (plan 03-01) preserved"
      contains: "migrateV3toV4"
    - path: "fitbod/Prescription/PeriodizationEngine.swift"
      provides: "Public enum PeriodizationEngine with four static funcs (phase / weekIndex / weekContext / recommendedNextKind); pure-function namespace; imports Foundation + SwiftData (Block is @Model)"
      contains: "enum PeriodizationEngine"
    - path: "fitbod/Prescription/FatigueAdvisory.swift"
      provides: "public protocol FatigueAdvisory: Sendable + public struct FatigueSuggestion: Sendable { public let reason: String }; D-25 type-level canonicality contract"
      contains: "protocol FatigueAdvisory"
    - path: "fitbod/Prescription/StubFatigueAdvisory.swift"
      provides: "Phase 4 default impl returning false / empty suggestion; used as DI default in ConsiderDeloadBanner"
      contains: "struct StubFatigueAdvisory: FatigueAdvisory"
    - path: "fitbod/Periodization/BlockPhaseColors.swift"
      provides: "BlockPhaseColors namespace enum with color(for:) / tint(for:) / phaseLabel(_:) helpers; inline hex values matching UI-SPEC D-07 locked palette"
      contains: "enum BlockPhaseColors"
    - path: "fitbod/Periodization/BlockTemplates.swift"
      provides: "BlockTemplates static catalog + BlockTemplate value struct + template(for: BlockPhaseKind) selector"
      contains: "enum BlockTemplates"
    - path: "fitbod/Periodization/MesocycleWeekContext.swift"
      provides: "MesocycleWeekContext: Sendable value struct (phase / weekStartDate / weekEndDate / daysRemaining / isCurrentWeek / isDeloadWeek)"
      contains: "struct MesocycleWeekContext"
  key_links:
    - from: "fitbod/Prescription/PeriodizationEngine.swift"
      to: "fitbod/Models/Block.swift"
      via: "phase(for: Block, on: Date) reads Block.phases (cascade-loaded BlockPhase rows)"
      pattern: "block.phases"
    - from: "fitbod/Persistence/SchemaV4.swift"
      to: "fitbod/Persistence/SchemaV3.swift"
      via: "Composition — SchemaV4.models == SchemaV3.models; the additive delta is on the Block @Model only, not the entity catalog"
      pattern: "SchemaV3.models"
    - from: "fitbod/Periodization/BlockTemplates.swift"
      to: "fitbod/Models/Enums/BlockPhaseKind.swift"
      via: "BlockPhase drafts use BlockPhaseKind raw values for kind; template(for:) switch on BlockPhaseKind"
      pattern: "BlockPhaseKind"
    - from: "fitbod/Prescription/StubFatigueAdvisory.swift"
      to: "fitbod/Prescription/FatigueAdvisory.swift"
      via: "Conformance — StubFatigueAdvisory: FatigueAdvisory"
      pattern: "FatigueAdvisory"
---

<objective>
Lay the Wave-0 foundation for Phase 4: schema bump (SchemaV4 with `Block.reviewedAt`), the pure-function `PeriodizationEngine` namespace, the `FatigueAdvisory` protocol that enforces BLOCK-08 at the type level (D-25), the four phase color helpers, the three stock block templates with D-10 RP multipliers, and the `MesocycleWeekContext` value type. Scaffold all 12 Wave-0 test suites — four turn GREEN immediately (PeriodizationEngineTests, BlockTemplatesTests, FatigueAdvisoryCanonicalityTests, SchemaV4MigrationTests), the other eight stay RED with `Issue.record` placeholders that later plans flip.

Purpose: every downstream Phase 4 plan composes against these primitives. The engine, the templates, the colors, and the advisory protocol have no UI / SwiftData-write dependencies, so they can land first and be unit-tested in isolation per FOUND-07. The schema bump unblocks `BlockDraft.save` (plan 04-02) and `BlockReviewView.acknowledgeAndDismiss` (plan 04-08).

Output: SchemaV4 + migration plan, six new production files in `fitbod/Prescription/` and `fitbod/Periodization/`, one modified `Block.swift` (one new field + `#Index`), 12 new test files; four GREEN, eight RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-periodization-blocks/04-CONTEXT.md
@.planning/phases/04-periodization-blocks/04-RESEARCH.md
@.planning/phases/04-periodization-blocks/04-PATTERNS.md
@.planning/phases/04-periodization-blocks/04-UI-SPEC.md
@.planning/phases/04-periodization-blocks/04-VALIDATION.md

@fitbod/Models/Block.swift
@fitbod/Models/BlockPhase.swift
@fitbod/Models/Enums/BlockPhaseKind.swift
@fitbod/Persistence/SchemaV1.swift
@fitbod/Persistence/SchemaV2.swift
@fitbod/Persistence/SchemaV3.swift
@fitbod/Persistence/FitbodSchemaMigrationPlan.swift
@fitbod/Sessions/PreviousMatchingIntent.swift
@fitbodTests/SchemaV2MigrationTests.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->
<!-- Executor uses these directly — no codebase exploration required. -->

From fitbod/Models/Block.swift (current shape; plan adds `reviewedAt`):
```
@Model
public final class Block {
    @Attribute(.unique) public var id: UUID
    public var name: String
    public var startDate: Date
    public var endDate: Date?
    public var notes: String?
    public var isActive: Bool
    @Relationship(deleteRule: .cascade, inverse: \BlockPhase.block)
    public var phases: [BlockPhase]?
    @Relationship(inverse: \Routine.block)
    public var routines: [Routine]?
    @Relationship(inverse: \Session.block)
    public var sessions: [Session]?
    public init() {}
}
```

From fitbod/Models/BlockPhase.swift:
```
public final class BlockPhase {
    public var id: UUID
    public var block: Block?
    public var orderIndex: Int
    public var nameRaw: String        // BlockPhaseKind.rawValue
    public var weeks: Int             // count of 7-day chunks in this phase
    public var volumeMultiplier: Double
    public var intensityMultiplier: Double
    public var notes: String?
}
```
Computed accessor: `BlockPhase.kind: BlockPhaseKind { BlockPhaseKind(rawValue: nameRaw) ?? .accumulation }`.

From fitbod/Models/Enums/BlockPhaseKind.swift:
```
public enum BlockPhaseKind: String, CaseIterable, Sendable {
    case accumulation
    case intensification
    case realization
    case deload
}
```

From fitbod/Persistence/SchemaV3.swift (sealed — committed 096256e):
```
public enum SchemaV3: VersionedSchema {
    public static let versionIdentifier = Schema.Version(3, 0, 0)
    public static var models: [any PersistentModel.Type] {
        SchemaV2.models + [PlateInventory.self]
    }
}
```

From fitbod/Persistence/FitbodSchemaMigrationPlan.swift (existing shape — extended this plan):
```
public enum FitbodSchemaMigrationPlan: SchemaMigrationPlan {
    public static var schemas: [any VersionedSchema.Type] { [SchemaV1.self, SchemaV2.self, SchemaV3.self] }
    public static var stages: [MigrationStage] { [migrateV1toV2, migrateV2toV3] }
    public static let migrateV1toV2 = MigrationStage.lightweight(fromVersion: SchemaV1.self, toVersion: SchemaV2.self)
    public static let migrateV2toV3 = MigrationStage.lightweight(fromVersion: SchemaV2.self, toVersion: SchemaV3.self)
}
```

From fitbod/Sessions/PreviousMatchingIntent.swift (Sendable value-type carrier template):
```
public struct PreviousMatchingIntentHit: Sendable {
    public let weight: Double
    public let reps: Int
    public let rpe: Double?
    public let sessionStartedAt: Date
    public init(weight: Double, reps: Int, rpe: Double?, sessionStartedAt: Date) { ... }
}
public enum PreviousMatchingIntent {
    public static func fetchTopWorkingSet(exerciseID: UUID?, intentRaw: String, context: ModelContext) -> PreviousMatchingIntentHit? { ... }
}
```

From fitbodTests/SchemaV2MigrationTests.swift (in-memory fixture template — copy verbatim, change SchemaV2 → SchemaV4):
```
@MainActor
@Suite("SchemaV2Migration", .serialized)
struct SchemaV2MigrationTests {
    func makeContext() -> ModelContext {
        let schema = Schema(SchemaV2.models)
        let config = ModelConfiguration(isStoredInMemoryOnly: true)
        let container = try! ModelContainer(for: schema, migrationPlan: FitbodSchemaMigrationPlan.self, configurations: [config])
        return ModelContext(container)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchemaV4 + Block.reviewedAt + migration stage + GREEN SchemaV4MigrationTests</name>
  <read_first>
    fitbod/Models/Block.swift,
    fitbod/Persistence/SchemaV3.swift,
    fitbod/Persistence/SchemaV2.swift,
    fitbod/Persistence/FitbodSchemaMigrationPlan.swift,
    fitbodTests/SchemaV2MigrationTests.swift,
    .planning/phases/04-periodization-blocks/04-CONTEXT.md (D-26),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 1080-1131 SchemaV4 + migration plan section)
  </read_first>
  <files>fitbod/Models/Block.swift, fitbod/Persistence/SchemaV4.swift, fitbod/Persistence/FitbodSchemaMigrationPlan.swift, fitbodTests/SchemaV4MigrationTests.swift</files>
  <action>
    Per D-26 reconciled (SchemaV3 sealed in commit 096256e), introduce SchemaV4 as the additive successor.
    
    (1) Modify fitbod/Models/Block.swift: add `public var reviewedAt: Date? = nil` immediately after `public var isActive: Bool = false`. Keep nil-default to satisfy FOUND-02 (cheap iCloud insurance) and lightweight-migration eligibility. Also add `#Index<Block>([\.isActive], [\.startDate])` at the top of the class body (immediately after `@Model public final class Block {`) — mirrors `Session.swift` line 29 pattern; the active-block @Query on Today and the isActive-desc-startDate-desc sort on RoutinesListView are both hot paths.
    
    (2) Create fitbod/Persistence/SchemaV4.swift mirroring SchemaV3.swift verbatim: `public enum SchemaV4: VersionedSchema { public static let versionIdentifier = Schema.Version(4, 0, 0); public static var models: [any PersistentModel.Type] { SchemaV3.models } }`. The entity-type catalog is identical to V3 (no new entities — only the additive Block field). File header comment: "SchemaV4 — Phase 4 additive delta. Only Block.reviewedAt: Date? is new; entity catalog is unchanged from V3. Lightweight migration eligible per FOUND-02 + Apple's MigrationStage.lightweight contract."
    
    (3) Modify fitbod/Persistence/FitbodSchemaMigrationPlan.swift: extend `schemas` to `[SchemaV1.self, SchemaV2.self, SchemaV3.self, SchemaV4.self]`; extend `stages` to `[migrateV1toV2, migrateV2toV3, migrateV3toV4]`; declare `public static let migrateV3toV4 = MigrationStage.lightweight(fromVersion: SchemaV3.self, toVersion: SchemaV4.self)`. Preserve existing migrateV1toV2 + migrateV2toV3 (the latter ships in Phase 3 plan 03-01 — assert it already exists; if not, abort this task and surface a blocker — Phase 3 plan 03-01 is a prerequisite per STATE.md showing it complete at commit 096256e).
    
    (4) Create fitbodTests/SchemaV4MigrationTests.swift as a `@MainActor @Suite("SchemaV4Migration", .serialized)` struct mirroring SchemaV2MigrationTests fixture. Required @Test functions (all GREEN this plan):
      - `migrationPlanRegistersV4`: assert `FitbodSchemaMigrationPlan.schemas.last == SchemaV4.self` (string-compare ObjectIdentifier or use `String(describing:)`).
      - `migrationPlanIncludesV3toV4Stage`: assert `FitbodSchemaMigrationPlan.stages.count == 3` and the last stage's `fromVersion`/`toVersion` reference V3/V4.
      - `schemaV4ModelsMatchesV3Plus`: assert `SchemaV4.models.count == SchemaV3.models.count` (Block stays the same entity type; only one field added — count unchanged).
      - `blockReviewedAtDefaultsNil`: create an in-memory ModelContainer over `Schema(SchemaV4.models)`, insert a fresh Block, save, refetch, assert `block.reviewedAt == nil`.
      - `blockReviewedAtRoundTrip`: insert Block, set `reviewedAt = .now`, save, refetch (new ModelContext for cold round-trip), assert `reviewedAt` is non-nil and within 1 second of the set value.
    
    Files for IDs in the test fixture: use `@Attribute(.unique) UUID` round-trips like SchemaV2MigrationTests; no need to seed any other entities.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/SchemaV4Migration 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    SchemaV4 file exists and compiles; Block.swift has reviewedAt field and #Index declaration; FitbodSchemaMigrationPlan lists 4 schemas and 3 stages; all 5 @Test functions in SchemaV4MigrationTests pass; existing SchemaV1Tests / SchemaV2MigrationTests still green (no regression).
  </done>
</task>

<task type="auto">
  <name>Task 2: PeriodizationEngine + MesocycleWeekContext + GREEN PeriodizationEngineTests</name>
  <read_first>
    fitbod/Sessions/PreviousMatchingIntent.swift (analog for namespace enum + Sendable value carrier),
    fitbod/Models/Block.swift,
    fitbod/Models/BlockPhase.swift,
    fitbod/Models/Enums/BlockPhaseKind.swift,
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 188-226 algorithm pseudocode, lines 866-905 reference implementation),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 56-95 PeriodizationEngine pattern, lines 1690-1693 MesocycleWeekContext no-analog notes)
  </read_first>
  <files>fitbod/Prescription/PeriodizationEngine.swift, fitbod/Periodization/MesocycleWeekContext.swift, fitbodTests/PeriodizationEngineTests.swift</files>
  <action>
    (1) Create the `fitbod/Prescription/` directory (the directory itself does not yet exist on disk — Phase 3 has not executed plan 03-05). NB: if Phase 3 has shipped its Prescription/ files in the meantime, simply add the new files alongside them without touching their contents.
    
    (2) Create fitbod/Prescription/PeriodizationEngine.swift as a `public enum PeriodizationEngine` namespace (no cases — caseless enum, no init). Imports: `import Foundation` and `import SwiftData` (because Block is @Model). Four static funcs, all pure (no ModelContext reads or writes, no side effects):
      - `public static func phase(for block: Block, on date: Date) -> BlockPhase?`: sort `block.phases ?? []` by orderIndex ascending; return nil for empty; compute `daysSinceStart = Int(floor(date.timeIntervalSince(block.startDate) / 86400))`; if daysSinceStart < 0 return phases.first; walk phases accumulating `cumulativeDays += phase.weeks * 7`, return current phase when `daysSinceStart < cumulativeDays + phase.weeks * 7`; return nil if past end.
      - `public static func weekIndex(for block: Block, on date: Date) -> Int?`: compute `daysSinceStart` same way; `totalDays = (block.phases ?? []).reduce(0) { $0 + ($1.weeks * 7) }`; return nil if `daysSinceStart < 0 || daysSinceStart >= totalDays`; else return `daysSinceStart / 7`.
      - `public static func weekContext(for block: Block, weekIndex: Int) -> MesocycleWeekContext?`: compute `weekStartDate = block.startDate.addingTimeInterval(Double(weekIndex * 7) * 86400)`; `weekEndDate = weekStartDate.addingTimeInterval(7 * 86400 - 1)`; resolve phase via `PeriodizationEngine.phase(for: block, on: weekStartDate)`; if nil return nil; compute `currentWeekIndex = PeriodizationEngine.weekIndex(for: block, on: .now)`; `isCurrentWeek = (currentWeekIndex == weekIndex)`; `daysRemaining`: only meaningful for current week — `((weekIndex + 1) * 7) - Int(floor(Date.now.timeIntervalSince(block.startDate) / 86400))` clamped to 0...7; for non-current weeks return 0; `isDeloadWeek = (phase.kind == .deload)`. Return MesocycleWeekContext with all six fields populated.
      - `public static func recommendedNextKind(after kind: BlockPhaseKind) -> BlockPhaseKind`: switch (D-23 map) — `.accumulation -> .intensification`, `.intensification -> .realization`, `.realization -> .deload`, `.deload -> .accumulation`. No default case (exhaustive on CaseIterable).
    
    (3) Create fitbod/Periodization/MesocycleWeekContext.swift as a `public struct MesocycleWeekContext: Sendable` value type. All stored properties `public let`: `phase: BlockPhase` (referenced via the @Model handle — safe because BlockPhase types are exposed via SwiftData's Sendable conformance for @Model classes accessed via ModelContext), `weekStartDate: Date`, `weekEndDate: Date`, `daysRemaining: Int`, `isCurrentWeek: Bool`, `isDeloadWeek: Bool`. Memberwise `public init(...)`. Import `Foundation` + `SwiftData`. Per PATTERNS line 1693 — MesocycleWeekContext is intentionally larger than PreviousMatchingIntentHit but shares the same Sendable / all-let / memberwise-init shape.
    
    (4) Create fitbodTests/PeriodizationEngineTests.swift as `@MainActor @Suite("PeriodizationEngine", .serialized)` struct. PURE-FUNCTION tests — do NOT require ModelContainer for the engine math; the in-memory container is only needed to construct Block + BlockPhase instances for predicate-free use (the engine never queries; it accepts the @Model handles directly). Required @Test functions (all GREEN this plan):
      - `phaseOneInAccumulationOnDayZero`: build a 3-phase block (3 accum / 2 intens / 1 deload); assert `phase(for: block, on: block.startDate)?.kind == .accumulation`.
      - `phaseTwoInIntensificationOnDay21`: same block; `phase(for: block, on: block.startDate.addingTimeInterval(21 * 86400))?.kind == .intensification`.
      - `phaseDeloadOnDay35`: same block; `phase(for: block, on: block.startDate.addingTimeInterval(35 * 86400))?.kind == .deload`.
      - `phasePastEndReturnsNil`: same block; `phase(for: block, on: block.startDate.addingTimeInterval(42 * 86400))` is nil.
      - `phaseBeforeStartReturnsFirstPhase`: assert `phase(for: block, on: block.startDate.addingTimeInterval(-86400))?.kind == .accumulation`.
      - `weekIndexOnDayZero`: assert `weekIndex(for: block, on: block.startDate) == 0`.
      - `weekIndexOnDay14`: assert `weekIndex(for: block, on: block.startDate.addingTimeInterval(14 * 86400)) == 2`.
      - `weekIndexBeforeStartReturnsNil` and `weekIndexAtTotalDaysReturnsNil`: assert both edge cases nil.
      - `recommendedNextDeterministicMap` (parameterized): for each of the four BlockPhaseKind inputs, assert the D-23 next-kind output.
      - `phaseEmptyPhasesReturnsNil`: block with empty phases array; assert phase returns nil regardless of date.
    
    Use an in-memory ModelContainer over `Schema(SchemaV4.models)` solely as the env to instantiate Block + BlockPhase (since they are @Model); do NOT use the context to query. Helper: `func makeBlock(phases: [(kind: BlockPhaseKind, weeks: Int)]) -> Block` builds Block + cascade-inserts BlockPhase rows with the right `orderIndex` / `nameRaw` / `weeks`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/PeriodizationEngine 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    PeriodizationEngine.swift exists with 4 static funcs; MesocycleWeekContext.swift exists with 6-field Sendable struct; PeriodizationEngineTests has 11+ @Test functions all GREEN; no production code references either type yet (downstream plans wire).
  </done>
</task>

<task type="auto">
  <name>Task 3: FatigueAdvisory + StubFatigueAdvisory + BlockPhaseColors + BlockTemplates + GREEN FatigueAdvisoryCanonicalityTests + BlockTemplatesTests + RED skeletons for 8 remaining suites</name>
  <read_first>
    fitbod/Models/Enums/BlockPhaseKind.swift,
    fitbod/Sessions/RestTimer/RestTimerEngine.swift (NoopActivityController DI default pattern, lines 110-118),
    fitbod/Routines/PrescriptionDefaults.swift (static factory pattern),
    .planning/phases/04-periodization-blocks/04-CONTEXT.md (D-03 templates, D-07 colors, D-10 multipliers, D-24/D-25 FatigueAdvisory contract),
    .planning/phases/04-periodization-blocks/04-UI-SPEC.md (lines 89-103 phase palette + lines 479-495 BlockPhaseColors reference implementation),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 193-240 FatigueAdvisory + Stub patterns, lines 549-622 BlockTemplates + BlockPhaseColors patterns),
    .planning/phases/04-periodization-blocks/04-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <files>fitbod/Prescription/FatigueAdvisory.swift, fitbod/Prescription/StubFatigueAdvisory.swift, fitbod/Periodization/BlockPhaseColors.swift, fitbod/Periodization/BlockTemplates.swift, fitbodTests/FatigueAdvisoryCanonicalityTests.swift, fitbodTests/BlockTemplatesTests.swift, fitbodTests/BlockPeriodizedStrategyTests.swift, fitbodTests/HybridStrategyTests.swift, fitbodTests/BlockDraftSaveTests.swift, fitbodTests/SingleActiveBlockInvariantTests.swift, fitbodTests/SessionBlockSnapshotTests.swift, fitbodTests/DeloadVolumeApplicationTests.swift, fitbodTests/BlockReviewMathTests.swift, fitbodTests/BlockBuilderViewCopyTests.swift</files>
  <action>
    (1) Create fitbod/Prescription/FatigueAdvisory.swift: declare `public protocol FatigueAdvisory: Sendable` with two requirements — `func shouldSuggest(context: SessionContext) -> Bool` and `func suggestion(context: SessionContext) -> FatigueSuggestion`. Below the protocol, declare `public struct FatigueSuggestion: Sendable { public let reason: String; public init(reason: String) { self.reason = reason } }`. Also declare a placeholder `public struct SessionContext: Sendable { public init() {} }` — Phase 5 will flesh out its members (volume / RPE creep / e1RM drop history); Phase 4 only needs the type to exist as the function input. Type-level canonicality contract per D-25: neither `Bool` nor `FatigueSuggestion` has any reference to `Block` in its type — the protocol cannot return a Block-mutating value. Add file header comment quoting D-25 verbatim and BLOCK-08 spirit.
    
    (2) Create fitbod/Prescription/StubFatigueAdvisory.swift: `public struct StubFatigueAdvisory: FatigueAdvisory { public init() {}; public func shouldSuggest(context: SessionContext) -> Bool { false }; public func suggestion(context: SessionContext) -> FatigueSuggestion { FatigueSuggestion(reason: "") } }`. Mirrors NoopActivityController shape from RestTimerEngine.swift.
    
    (3) Create fitbod/Periodization/BlockPhaseColors.swift: `public enum BlockPhaseColors` with three static funcs.
      - `public static func color(for kind: BlockPhaseKind) -> Color`: switch on kind — `.accumulation` returns `Color.accentColor`; `.intensification` returns `Color(red: 0.96, green: 0.62, blue: 0.04)` (#F59E0B); `.realization` returns `Color(red: 0.92, green: 0.34, blue: 0.05)` (#EA580C); `.deload` returns `Color(red: 0.58, green: 0.64, blue: 0.72)` (#94A3B8). Inline hex values per UI-SPEC D-07; no Assets.xcassets entries.
      - `public static func tint(for kind: BlockPhaseKind) -> Color`: returns `color(for: kind).opacity(0.15)`.
      - `public static func phaseLabel(_ kind: BlockPhaseKind) -> String`: returns `kind.rawValue.capitalized` (BlockPhaseKind raw values are lowercase; UI-SPEC requires title-case display).
      Imports: `import SwiftUI`.
    
    (4) Create fitbod/Periodization/BlockTemplates.swift with two types in the same file:
      - `public struct BlockTemplate: Sendable { public let name: String; public let phases: [(kind: BlockPhaseKind, weeks: Int, volumeMultiplier: Double, intensityMultiplier: Double)]; public init(name: String, phases: [(kind: BlockPhaseKind, weeks: Int, volumeMultiplier: Double, intensityMultiplier: Double)]) { ... } }` — value carrier of the template payload. (NB: keep the tuple representation here; plan 04-02 introduces `BlockPhaseDraft` which carries the same fields as @Observable; `BlockBuilderView` maps `BlockTemplate.phases` → `[BlockPhaseDraft]` at seed time.)
      - `public enum BlockTemplates` with four static lets and one selector func:
        - `public static let generic = BlockTemplate(name: "Generic Strength Meso", phases: [(.accumulation, 4, 1.0, 0.75), (.intensification, 2, 0.85, 0.88), (.realization, 1, 0.6, 0.97), (.deload, 1, 0.5, 0.75)])` — total 8 weeks, D-10 multipliers verbatim, D-03 structure.
        - `public static let hypertrophy = BlockTemplate(name: "Hypertrophy Meso", phases: [(.accumulation, 5, 1.0, 0.75), (.intensification, 2, 0.85, 0.88), (.deload, 1, 0.5, 0.75)])` — total 8 weeks.
        - `public static let powerliftingPeak = BlockTemplate(name: "Powerlifting Peak", phases: [(.accumulation, 3, 1.0, 0.75), (.intensification, 3, 0.85, 0.88), (.realization, 1, 0.6, 0.97), (.deload, 1, 0.5, 0.75)])` — total 8 weeks.
        - `public static let blank = BlockTemplate(name: "Blank Block", phases: [])`.
        - `public static func template(for kind: BlockPhaseKind) -> BlockTemplate`: switch — `.accumulation -> .generic`, `.intensification -> .hypertrophy`, `.realization -> .powerliftingPeak`, `.deload -> .generic` (start a new meso after deload).
      Imports: `import Foundation`.
    
    (5) Create fitbodTests/FatigueAdvisoryCanonicalityTests.swift as `@Suite("FatigueAdvisoryCanonicality")` struct (no @MainActor needed — pure type inspection). GREEN tests:
      - `protocolReturnsOnlyBoolAndFatigueSuggestion`: instantiate `StubFatigueAdvisory()`, call shouldSuggest and assert returned type is `Bool`; call suggestion and assert returned type is `FatigueSuggestion`. The compiler enforces the contract; this test pins it.
      - `fatigueSuggestionHasNoBlockField`: use Mirror reflection on a `FatigueSuggestion(reason: "test")` and assert no child label or value type references `Block`. Loop `Mirror(reflecting: suggestion).children` and #expect every child's `value` is NOT a `Block` and NOT a `BlockPhase`.
      - `stubReturnsFalseAndEmptyReason`: `#expect(StubFatigueAdvisory().shouldSuggest(context: SessionContext()) == false)`; `#expect(StubFatigueAdvisory().suggestion(context: SessionContext()).reason == "")`.
    
    (6) Create fitbodTests/BlockTemplatesTests.swift as `@Suite("BlockTemplates")` struct. GREEN tests:
      - `genericMatchesD03Structure`: assert `BlockTemplates.generic.phases.count == 4`; assert sequence of kinds is `[.accumulation, .intensification, .realization, .deload]`; assert weeks are `[4, 2, 1, 1]`; assert intensityMultiplier for accumulation is 0.75 ± 0.0001 and for deload is 0.75; total weeks == 8.
      - `hypertrophyMatchesD03Structure`: 3 phases, kinds `[.accumulation, .intensification, .deload]`, weeks `[5, 2, 1]`, total 8.
      - `powerliftingPeakMatchesD03Structure`: 4 phases, kinds `[.accumulation, .intensification, .realization, .deload]`, weeks `[3, 3, 1, 1]`, total 8.
      - `blankHasNoPhases`: assert `.blank.phases.isEmpty` and `.blank.name == "Blank Block"`.
      - `templateForKindMappingDeterministic` (parameterized over the four kinds): assert each `BlockTemplates.template(for: kind)` returns an expected non-empty template (e.g., `.accumulation -> .generic`).
      - `allTemplateNamesVerbatim`: assert names equal UI-SPEC verbatim — "Generic Strength Meso", "Hypertrophy Meso", "Powerlifting Peak", "Blank Block".
      - `d10MultipliersVerbatim` (parameterized over all phases of all three non-blank templates): for each phase, assert the (kind, volumeMultiplier, intensityMultiplier) triple matches the locked D-10 table — accumulation (1.0, 0.75), intensification (0.85, 0.88), realization (0.6, 0.97), deload (0.5, 0.75).
    
    (7) Create the 8 remaining Wave-0 test files as RED skeletons (each is a `@Suite("X")` struct with at least one `@Test func placeholder()` that calls `Issue.record("Wave 0 scaffold — see plan 04-XX")`). These RED files will be flipped GREEN by the listed downstream plans:
      - `fitbodTests/BlockPeriodizedStrategyTests.swift` (flipped by plan 04-06)
      - `fitbodTests/HybridStrategyTests.swift` (flipped by plan 04-06)
      - `fitbodTests/BlockDraftSaveTests.swift` (flipped by plan 04-02)
      - `fitbodTests/SingleActiveBlockInvariantTests.swift` (flipped by plan 04-02)
      - `fitbodTests/SessionBlockSnapshotTests.swift` (flipped by plan 04-07)
      - `fitbodTests/DeloadVolumeApplicationTests.swift` (flipped by plan 04-07)
      - `fitbodTests/BlockReviewMathTests.swift` (flipped by plan 04-08)
      - `fitbodTests/BlockBuilderViewCopyTests.swift` (flipped by plan 04-02)
    
    Each RED skeleton must compile (no syntax errors) — the placeholder @Test calls Issue.record with a non-empty string per Swift Testing API; that records a failure without crashing. The intent is to make missing tests visible in the test report rather than silently absent.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/FatigueAdvisoryCanonicality -only-testing fitbodTests/BlockTemplates 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    FatigueAdvisory.swift + StubFatigueAdvisory.swift + BlockPhaseColors.swift + BlockTemplates.swift exist and compile against Swift 6 strict concurrency; FatigueAdvisoryCanonicalityTests + BlockTemplatesTests pass; the 8 RED skeletons compile but record Issue.record placeholders; full `xcodebuild test` exit code is non-zero (expected — 8 RED suites), but the 4 Wave-0 GREEN suites (SchemaV4Migration / PeriodizationEngine / FatigueAdvisoryCanonicality / BlockTemplates) all pass when run individually.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local user → SwiftData | Single-user offline app (PROJECT.md). The only data crossing into persistence is what the user enters (block name, weeks, multipliers, reviewedAt timestamp). No network, no auth, no multi-tenant. ASVS L1 surface area is minimal. |
| Code → Code | All Phase 4 types live in `fitbod/Prescription/` (pure functions) and `fitbod/Periodization/` (UI later) — single-target iOS app, no XPC, no inter-process trust hop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `Block.reviewedAt` field added in SchemaV4 — a malicious actor with physical-device access could mutate to bypass the BlockReviewView trigger | accept | Single-user local app; physical-device access defeats every persistence layer including iOS data-protection. SwiftData is the project standard. No additional crypto justified per ASVS L1. |
| T-04-01-02 | Information Disclosure | `FatigueSuggestion.reason` field carries free-form text that may eventually surface fatigue / RPE patterns | mitigate | The Phase 4 Stub returns empty string; no real data flows yet. Phase 5 fills the reason — that's where review escalates. Phase 4 contract documents no PII / no sensitive data in the value type. |
| T-04-01-03 | Elevation of Privilege | `FatigueAdvisory` protocol returns `FatigueSuggestion` not `DeloadMutation` — protects against advisory code mutating the block schedule | mitigate | Type-level enforcement (D-25). `FatigueAdvisoryCanonicalityTests.fatigueSuggestionHasNoBlockField` reflection test pins the contract. The compiler refuses an impl that returns a Block-mutating value. |
| T-04-01-04 | Denial of Service | Malformed SchemaV4 migration could brick the database on first launch | mitigate | Lightweight migration per Apple's contract (additive, default-valued field per FOUND-02). SchemaV4MigrationTests asserts the migration plan shape + the round-trip on an in-memory container. Production lightweight migrations are reversible by reinstalling the app (personal dev install). |
| T-04-01-05 | Repudiation | None — single-user app, no audit trail required | accept | No multi-user accountability concerns. |
</threat_model>

<verification>
- Full `xcodebuild test` shows 4 NEW GREEN suites (SchemaV4Migration / PeriodizationEngine / FatigueAdvisoryCanonicality / BlockTemplates).
- 8 RED suites compile and record `Issue.record` placeholders (visible in test report; flipped GREEN by plans 04-02 / 04-06 / 04-07 / 04-08).
- No regression in Phase 1 / Phase 2 / Phase 3 test suites (SchemaV1Tests, SchemaV2MigrationTests, all existing fitbodTests pass).
- `swift-format` (or `xcrun swift-format`) idempotent on every new file.
- No new third-party SPM dependency introduced (PROJECT.md constraint).
- Block model retains every existing field and relationship — only `reviewedAt` and `#Index` added.
</verification>

<success_criteria>
- SchemaV4 + lightweight V3→V4 migration ship; `Block.reviewedAt: Date?` round-trips through an in-memory ModelContainer.
- `PeriodizationEngine` resolves phases and week indexes for a synthetic block with deterministic, edge-case-correct output (day 0, day 14, day 35, before-start, past-end).
- `recommendedNextKind` map matches D-23 verbatim across all 4 inputs.
- `FatigueAdvisory` protocol's return surface is exactly `Bool` + `FatigueSuggestion`; reflection test pins canonicality contract.
- All three stock templates (`generic`, `hypertrophy`, `powerliftingPeak`) carry D-10 RP-default multipliers verbatim with the D-03 phase structure (8 weeks each).
- `BlockPhaseColors` returns the four UI-SPEC-locked hex values + 15% tints.
- All 12 Wave-0 test files exist on disk; 4 GREEN, 8 RED with `Issue.record` placeholders.
- BLOCK-08 type-level canonicality contract is satisfied (downstream plans cannot accidentally mutate Block from advisory code).
</success_criteria>

<output>
After completion, create `.planning/phases/04-periodization-blocks/04-01-SUMMARY.md`.
</output>
