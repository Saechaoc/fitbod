---
phase: 04-periodization-blocks
plan: 08
type: execute
wave: 4
depends_on: ["04-04a", "04-04b", "04-06"]
files_modified:
  - fitbod/Periodization/BlockReviewView.swift
  - fitbod/Periodization/E1RMHelper.swift
  - fitbod/App/RootView.swift
  - fitbodTests/BlockReviewMathTests.swift
  - fitbodTests/BlockReviewTriggerTests.swift
autonomous: true
prerequisite_phase_plans: ["03-05"]
prerequisite_gate: "Plan 04-08's BlockReviewView e1RM deltas (Section 2) prefer Phase 3 plan 03-05's e1RM-bearing types (richer helper: Brzycki for reps <=6, Epley for 6-10, suppress >10). If 03-05 has not shipped the helper, this plan falls back to its own inline E1RMHelper.epley (Task 1). The dependency is a soft preference, not a hard gate: the plan can ship without 03-05's helper using the fallback, but the fallback Section 2 numbers will not be intent-rep-range-aware."
requirements: [BLOCK-07, BLOCK-08]

must_haves:
  truths:
    - "BlockReviewView is presented as .sheet(item: pendingBlockReview) with .large detent when TodayView's .task body finds an active block where endDate < .now AND reviewedAt == nil"
    - "The sheet renders 4 sections in order: Total volume (computed inline via @Query<SetEntry> scoped to block date range), e1RM deltas (per-exercise start-of-block vs end-of-block top working set via E1RMHelper.epley), PRs hit (placeholder copy 'Coming in Phase 6'), Recommended next phase (per PeriodizationEngine.recommendedNextKind static rule)"
    - "Tapping 'Done' in the sheet toolbar acknowledges the review in a single transaction: block.reviewedAt = .now AND block.isActive = false; subsequent Today opens do NOT re-surface the sheet for the same block"
    - "Tapping the accent 'Start [Recommended] Block' CTA presents BlockBuilderView seeded with BlockTemplates.template(for: recommendedNextKind) — does NOT auto-activate the new block (user must explicitly toggle Active block in builder)"
    - "Tapping 'Not now' dismisses the sheet without seeding a new block; the user can manually create one later from Routines tab"
    - "Acknowledgment is the SINGLE legitimate writer (alongside BlockDraft.save + BlockCard.endBlock) of Block.isActive transition to false; FatigueAdvisory cannot reach this code path (BLOCK-08 type-level enforcement)"
    - "BlockReviewMathTests + BlockReviewTriggerTests turn GREEN; volume sum, e1RM delta sign, 4-section render invariant, end-of-block trigger predicate are pinned"
  artifacts:
    - path: "fitbod/Periodization/BlockReviewView.swift"
      provides: ".sheet content rendering 4 sections; @Query<SetEntry> with #Predicate using local-let date bounds (RESEARCH §6 Pitfall 2); per-exercise e1RM delta via E1RMHelper; acknowledgeAndDismiss() wraps the dual write in ctx.transaction"
      contains: "struct BlockReviewView"
    - path: "fitbod/Periodization/E1RMHelper.swift"
      provides: "public enum E1RMHelper with static func epley(weight: Double, reps: Int) -> Double (Epley formula weight * (1 + reps / 30.0) for reps in 1...10, returns weight unchanged for reps == 0 or > 10); used by BlockReviewView Section 2 delta math"
      contains: "enum E1RMHelper"
    - path: "fitbod/App/RootView.swift"
      provides: "TodayView extended with .task { check end-of-block trigger predicate } + .sheet(item: $pendingBlockReview); the existing Phase 4 banner stack (plans 04-04a + 04-04b) is preserved unchanged"
      contains: "pendingBlockReview"
  key_links:
    - from: "fitbod/Periodization/BlockReviewView.swift"
      to: "fitbod/Prescription/PeriodizationEngine.swift"
      via: "recommendedNextKind(after: lastPhase.kind) drives the CTA copy + BlockTemplates seed kind"
      pattern: "PeriodizationEngine.recommendedNextKind"
    - from: "fitbod/App/RootView.swift"
      to: "fitbod/Periodization/BlockReviewView.swift"
      via: ".sheet(item: $pendingBlockReview) presents BlockReviewView(block:); .presentationDetents([.large]) for D-21 / UI-SPEC sheet shape"
      pattern: "BlockReviewView(block:"
    - from: "fitbod/Periodization/BlockReviewView.swift"
      to: "fitbod/Periodization/E1RMHelper.swift"
      via: "E1RMHelper.epley(weight: top.weight, reps: top.reps) computes start/end e1RM per exercise"
      pattern: "E1RMHelper.epley"
---

<objective>
**PREREQUISITE (cross-phase, soft preference):** Plan 04-08 prefers Phase 3 plan 03-05's e1RM helper (Brzycki/Epley rep-range-aware per PROG-02) for Section 2's delta math. If 03-05 has not shipped the helper, Task 1 of this plan provides an inline `E1RMHelper.epley` fallback that is sufficient for Phase 4 close — the fallback's numbers will not be intent-rep-range-aware, but BLOCK-07's contract ("end-of-block produces a phase-end review (total volume, e1RM deltas, PRs hit, recommended next phase)") is still satisfied. PROG-02's rep-range awareness lands in Phase 6 regardless. The `prerequisite_phase_plans: ["03-05"]` frontmatter field documents the soft dependency so `/gsd:execute-phase` can surface it.

Ship the end-of-block review surface. After this plan, when a user's active block crosses its endDate, the next Today-tab open presents a 4-section review sheet (total volume, e1RM deltas, PRs hit placeholder, recommended next phase CTA). Tapping Done acknowledges (sets reviewedAt + deactivates the block transactionally) and prevents re-surfacing. Closes BLOCK-07 (end-of-block phase-end review) and pins BLOCK-08 (acknowledgment is the only legitimate writer of `Block.isActive = false` via review acknowledgment — alongside BlockDraft.save and BlockCard.endBlock).

Purpose: BlockReviewView is the final user-visible Phase 4 surface and the only consumer of `Block.reviewedAt` (introduced in SchemaV4 in plan 04-01). The sheet's recommended-next-phase CTA closes the periodization loop — finishing a block hands the user a one-tap path to start the next one. Section 3 (PRs hit) ships a placeholder for Phase 6; sections 1, 2, and 4 ship real computation.

Output: One new sheet view (`BlockReviewView`), one new pure-function helper (`E1RMHelper.epley` — Epley formula fallback per RESEARCH if Phase 3 has not shipped a richer helper), TodayView trigger wiring, two test suites flipped GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/04-periodization-blocks/04-CONTEXT.md
@.planning/phases/04-periodization-blocks/04-RESEARCH.md
@.planning/phases/04-periodization-blocks/04-UI-SPEC.md
@.planning/phases/04-periodization-blocks/04-PATTERNS.md
@.planning/phases/04-periodization-blocks/04-01-PLAN.md
@.planning/phases/04-periodization-blocks/04-04a-PLAN.md
@.planning/phases/04-periodization-blocks/04-04b-PLAN.md
@.planning/phases/04-periodization-blocks/04-06-PLAN.md

@fitbod/App/RootView.swift
@fitbod/Models/Block.swift
@fitbod/Models/BlockPhase.swift
@fitbod/Models/Session.swift
@fitbod/Models/SessionExercise.swift
@fitbod/Models/SetEntry.swift
@fitbod/Sessions/PerSetNoteSheet.swift
@fitbod/Settings/SettingsView.swift

<interfaces>
<!-- From plan 04-01 (shipped): -->
- `enum PeriodizationEngine` with `.recommendedNextKind(after: BlockPhaseKind) -> BlockPhaseKind`.
- `enum BlockTemplates` with `.template(for: BlockPhaseKind) -> BlockTemplate`.
- `enum BlockPhaseColors` with `.phaseLabel(_: BlockPhaseKind) -> String`.

<!-- From plan 04-02 (shipped): -->
- `BlockBuilderView(draft: BlockDraft, editing: Block?)` presentable via sheet.
- `BlockDraft(template: BlockTemplate)`.

<!-- From fitbod/Sessions/PerSetNoteSheet.swift (sheet shell analog): -->
```
public struct PerSetNoteSheet: View {
    @Environment(\.dismiss) private var dismiss
    public var body: some View {
        NavigationStack {
            Form { Section { ... } }
                .navigationTitle("...")
                .toolbar { ToolbarItem(placement: .topBarTrailing) { Button("Done") { dismiss() } } }
        }
    }
}
```

<!-- From fitbod/Settings/SettingsView.swift (Form + @Query template): -->
```
public struct SettingsView: View {
    @Query private var settingsList: [UserSettings]
    public var body: some View {
        NavigationStack {
            Form {
                if let settings = settingsList.first { ... }
            }
            .navigationTitle("Settings")
        }
    }
}
```

<!-- From Phase 2 PreviousMatchingIntent.swift (local-let workaround template — applied to date predicates in this plan): -->
```
let targetID = exerciseID
let targetIntent = intentRaw
var descriptor = FetchDescriptor<SessionExercise>(predicate: #Predicate { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E1RMHelper (Epley formula) + GREEN E1RM math tests</name>
  <read_first>
    fitbod/Models/SetEntry.swift,
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (line 557 — Epley fallback formula),
    .planning/phases/03-smart-prescription-warm-ups/03-05-PLAN.md (check if Phase 3 ships an e1RM helper — if yes, this task only adds Phase 4's tests; if no, this task ships the minimal Epley fallback)
  </read_first>
  <files>fitbod/Periodization/E1RMHelper.swift</files>
  <action>
    Check whether Phase 3 has shipped an e1RM helper at `fitbod/Prescription/` (look for files matching `*E1RM*`, `*OneRepMax*`, `*Epley*`, `*Brzycki*`). If yes, use that helper directly in plan 04-08 Task 2 and SKIP this file creation (note as deferred in the plan SUMMARY). If no, create the minimal Phase 4 fallback:
    
    Create fitbod/Periodization/E1RMHelper.swift as `public enum E1RMHelper`:
    ```
    public enum E1RMHelper {
        /// Epley estimated 1RM formula: weight * (1 + reps / 30).
        /// Returns the original weight unchanged for reps == 1 (no estimation needed)
        /// or out-of-range reps (0 or > 10 — Epley loses accuracy above 10 reps).
        public static func epley(weight: Double, reps: Int) -> Double {
            guard reps >= 1 && reps <= 10 else { return weight }
            return weight * (1.0 + Double(reps) / 30.0)
        }
    }
    ```
    Pure function. No tests in this task — the function is exercised end-to-end by `BlockReviewMathTests.e1rmDeltaPositiveForImprovedTopSet` in Task 3.
    
    NB: per RESEARCH line 557 — if Phase 3 ships a richer helper (Brzycki for reps ≤6, Epley for 6-10, suppress >10 from PR detection), prefer Phase 3's helper. The Phase 4 helper is a fallback so this plan does not block on Phase 3 schedule.
  </action>
  <verify>
    <automated>xcodebuild build -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | grep -E 'BUILD SUCCEEDED|error:'</automated>
  </verify>
  <done>
    E1RMHelper.swift exists or is intentionally skipped because Phase 3 has shipped a richer helper; in either case the BlockReviewView in Task 2 can call `E1RMHelper.epley` (or the Phase 3 equivalent).
  </done>
</task>

<task type="auto">
  <name>Task 2: BlockReviewView sheet with 4 sections + acknowledge transaction + TodayView trigger</name>
  <read_first>
    fitbod/Sessions/PerSetNoteSheet.swift (sheet shell pattern),
    fitbod/Settings/SettingsView.swift (Form + @Query pattern),
    fitbod/App/RootView.swift (TodayView body — modified in plans 04-04a + 04-04b),
    fitbod/Models/Session.swift, fitbod/Models/SessionExercise.swift, fitbod/Models/SetEntry.swift,
    fitbod/Periodization/E1RMHelper.swift (just shipped — Task 1, or Phase 3 equivalent),
    fitbod/Prescription/PeriodizationEngine.swift (from plan 04-01),
    fitbod/Periodization/BlockTemplates.swift (from plan 04-01),
    fitbod/Periodization/BlockPhaseColors.swift (from plan 04-01),
    fitbod/Periodization/BlockBuilderView.swift (from plan 04-02 — for the Start [Recommended] CTA target),
    fitbod/Sessions/PreviousMatchingIntent.swift (local-let workaround),
    .planning/phases/04-periodization-blocks/04-UI-SPEC.md (lines 262-291 BlockReviewView copy + sections),
    .planning/phases/04-periodization-blocks/04-RESEARCH.md (lines 492-580 phase-end review section, lines 1039-1054 Pitfall 2 local-let),
    .planning/phases/04-periodization-blocks/04-PATTERNS.md (lines 816-923 BlockReviewView pattern, lines 1440-1454 TodayView trigger pattern)
  </read_first>
  <files>fitbod/Periodization/BlockReviewView.swift, fitbod/App/RootView.swift</files>
  <action>
    (1) Create fitbod/Periodization/BlockReviewView.swift as `public struct BlockReviewView: View`:
      - `@Environment(\\.modelContext) private var ctx`
      - `@Environment(\\.dismiss) private var dismiss`
      - `public let block: Block`
      - `@State private var pendingNextBlockBuilder: BlockTemplate? = nil` (for the Start [Recommended] CTA).
      - `@State private var acknowledgeError: String? = nil` (defensive).
      
      Computed: pre-resolve the date bounds for @Query into local lets at the top of the body (per RESEARCH §6 Pitfall 2 local-let workaround):
      ```
      let blockStart = block.startDate
      let blockEnd = block.endDate ?? Date.now
      ```
      
      Body: NavigationStack wrapping Form with 4 sections:
      
      ```
      NavigationStack {
          Form {
              totalVolumeSection(blockStart: blockStart, blockEnd: blockEnd)
              e1RMDeltasSection(blockStart: blockStart, blockEnd: blockEnd)
              prsHitSectionPlaceholder()
              recommendedNextSection()
              if let acknowledgeError {
                  Text(acknowledgeError).foregroundStyle(.red).font(.caption)
              }
          }
          .navigationTitle("\(block.name) — review")
          .toolbar {
              ToolbarItem(placement: .topBarTrailing) {
                  Button("Done") { acknowledgeAndDismiss() }
              }
          }
      }
      .sheet(item: $pendingNextBlockBuilder) { template in
          BlockBuilderView(draft: BlockDraft(template: template))
      }
      ```
      
      Section 1 — totalVolumeSection: use a local `@Query` via FetchDescriptor in a private helper (or @Query inline if @Query supports the predicate variant with captured local-lets — if not, use a one-shot context.fetch in onAppear and cache in @State):
      ```
      @ViewBuilder
      private func totalVolumeSection(blockStart: Date, blockEnd: Date) -> some View {
          Section("Total volume") {
              let setsInBlock = fetchSetsInBlock(start: blockStart, end: blockEnd)
              let totalKg = setsInBlock.reduce(0.0) { $0 + ($1.weight * Double($1.reps)) }
              let sessionsCount = Set(setsInBlock.compactMap { $0.sessionExercise?.session?.id }).count
              if setsInBlock.isEmpty {
                  Text("No logged sets in this block.").foregroundStyle(.secondary)
              } else {
                  Text("\(formattedKg(totalKg)) kg total").font(.headline)
                  Text("\(setsInBlock.count) working sets across \(sessionsCount) sessions")
                      .font(.caption).foregroundStyle(.secondary)
              }
          }
      }
      ```
      Where `fetchSetsInBlock` is a private func using FetchDescriptor + local-let workaround:
      ```
      private func fetchSetsInBlock(start: Date, end: Date) -> [SetEntry] {
          let startBound = start  // RESEARCH §6 Pitfall 2 — capture into locals before #Predicate
          let endBound = end
          let descriptor = FetchDescriptor<SetEntry>(predicate: #Predicate<SetEntry> { entry in
              entry.isComplete == true
                  && entry.isWarmup == false
                  && entry.completedAt != nil
                  && entry.completedAt! >= startBound
                  && entry.completedAt! <= endBound
          })
          return (try? ctx.fetch(descriptor)) ?? []
      }
      ```
      
      Section 2 — e1RMDeltasSection: compute per-exercise start-of-block vs end-of-block top working set:
      ```
      @ViewBuilder
      private func e1RMDeltasSection(blockStart: Date, blockEnd: Date) -> some View {
          Section("e1RM deltas") {
              let deltas = computeE1RMDeltas(start: blockStart, end: blockEnd)
              if deltas.isEmpty {
                  Text("Not enough sessions logged to compute deltas.")
                      .font(.caption).foregroundStyle(.secondary)
              } else {
                  ForEach(deltas, id: \.exerciseName) { d in
                      HStack {
                          Text(d.exerciseName)
                          Spacer()
                          Text(formattedDelta(d.delta))
                              .foregroundStyle(d.delta > 0 ? .green : (d.delta < 0 ? .red : .primary))
                      }
                  }
              }
          }
      }
      ```
      With helper struct `private struct E1RMDelta { let exerciseName: String; let delta: Double }` and helper:
      ```
      private func computeE1RMDeltas(start: Date, end: Date) -> [E1RMDelta] {
          // Fetch all SessionExercises whose Session.startedAt is in [start, end] and isComplete-set count >= 1.
          // Group by exercise.id; for each group with >= 2 sessions, take top working set from first + last session.
          // Compute Epley delta. Return sorted by abs(delta) descending.
          // ... full impl per RESEARCH lines 538-553 ...
      }
      ```
      Apply RESEARCH §6 Pitfall 2 local-let workaround inside this function for the date-bound predicate.
      
      Section 3 — prsHitSectionPlaceholder: per UI-SPEC line 277 — "Coming in Phase 6. Full PR detection ships with the progress views update.":
      ```
      private func prsHitSectionPlaceholder() -> some View {
          Section("PRs hit") {
              Text("Coming in Phase 6. Full PR detection ships with the progress views update.")
                  .font(.caption).foregroundStyle(.secondary)
          }
      }
      ```
      
      Section 4 — recommendedNextSection: resolve the just-finished phase (last phase in block.phases sorted by orderIndex desc):
      ```
      private func recommendedNextSection() -> some View {
          Section("Recommended next phase") {
              let lastPhase = (block.phases ?? []).sorted { $0.orderIndex > $1.orderIndex }.first
              let recommendedKind = lastPhase.flatMap { PeriodizationEngine.recommendedNextKind(after: $0.kind) } ?? .accumulation
              let labelKind = BlockPhaseColors.phaseLabel(recommendedKind).lowercased()
              Text("Start \(labelKind) next.")
              Button {
                  pendingNextBlockBuilder = BlockTemplates.template(for: recommendedKind)
              } label: {
                  Text("Start \(labelKind.capitalized) Block")
                      .frame(maxWidth: .infinity)
              }
              .buttonStyle(.borderedProminent)
              .tint(Color.accentColor)
              Button("Not now") { dismiss() }
                  .foregroundStyle(.secondary)
          }
      }
      ```
      Make `BlockTemplate` conform to `Identifiable` for the .sheet(item:) binding (add `extension BlockTemplate: Identifiable { public var id: String { name } }` if it does not already conform — verify against plan 04-01's BlockTemplate shape).
      
      Acknowledge action:
      ```
      private func acknowledgeAndDismiss() {
          do {
              try ctx.transaction {
                  block.reviewedAt = Date.now
                  block.isActive = false
                  try ctx.save()
              }
              dismiss()
          } catch {
              acknowledgeError = "Couldn't save review. Try again."
          }
      }
      ```
      This is the SINGLE legitimate writer of `block.isActive = false` from review acknowledgment (alongside `BlockDraft.save` from plan 04-02 and `BlockCard.endBlock` from plan 04-04a). FatigueAdvisory cannot reach this code path — the type-level enforcement from plan 04-01 holds.
      
      Helpers:
      - `private func formattedKg(_ value: Double) -> String { value.formatted(.number.precision(.fractionLength(0)).grouping(.automatic)) }` — produces "12,840".
      - `private func formattedDelta(_ value: Double) -> String { let sign = value > 0 ? "+" : ""; return "\(sign)\(String(format: "%.1f", value)) kg" }` — produces "+3.2 kg" / "-1.5 kg" / "0.0 kg".
    
    (2) Modify fitbod/App/RootView.swift's TodayView struct:
      - Add `@State private var pendingBlockReview: Block? = nil` alongside the existing Phase 4 state.
      - Add a `.task` modifier after the existing NavigationStack body modifiers:
        ```
        .task {
            let descriptor = FetchDescriptor<Block>(predicate: #Predicate<Block> { block in
                block.isActive == true
                    && block.endDate != nil
                    && block.endDate! < Date.now
                    && block.reviewedAt == nil
            })
            pendingBlockReview = (try? ctx.fetch(descriptor))?.first
        }
        ```
      - Add a `.sheet(item: $pendingBlockReview)` modifier:
        ```
        .sheet(item: $pendingBlockReview) { block in
            BlockReviewView(block: block)
                .presentationDetents([.large])
        }
        ```
      - Make Block conform to `Identifiable` if it does not (Block already has `@Attribute(.unique) var id: UUID` which makes it implicitly Identifiable via SwiftData @Model — verify; if not, add `extension Block: Identifiable {}`).
      - Preserve every existing plan-04-04a + plan-04-04b banner-stack behavior (DeloadWeekBanner / ConsiderDeloadBanner / BlockCard / StartBlockCTA / ResumeWorkoutBanner) — this plan ONLY adds the .task and .sheet(item: pendingBlockReview) modifiers.
  </action>
  <verify>
    <automated>xcodebuild build -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' 2>&amp;1 | grep -E 'BUILD SUCCEEDED|error:'</automated>
  </verify>
  <done>
    BlockReviewView.swift compiles; RootView.TodayView extended with .task + .sheet(item:) for the review trigger; the 4 sections render in order; acknowledge action wraps dual write in ctx.transaction; UI-SPEC verbatim copy for placeholder + CTA + dismiss button; Phase 4 plans 04-04a + 04-04b banner stack preserved unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: BlockReviewMathTests + BlockReviewTriggerTests turn GREEN</name>
  <read_first>
    fitbod/Periodization/BlockReviewView.swift (just shipped — Task 2),
    fitbod/Periodization/E1RMHelper.swift (just shipped — Task 1),
    fitbod/App/RootView.swift (just modified — Task 2),
    fitbod/Models/Session.swift, fitbod/Models/SessionExercise.swift, fitbod/Models/SetEntry.swift,
    fitbodTests/SchemaV2MigrationTests.swift (fixture template),
    fitbodTests/SessionFactoryTests.swift (Phase 2 — Session/SetEntry insertion helper pattern)
  </read_first>
  <files>fitbodTests/BlockReviewMathTests.swift, fitbodTests/BlockReviewTriggerTests.swift</files>
  <action>
    (1) Flip RED placeholder in fitbodTests/BlockReviewMathTests.swift to GREEN. `@MainActor @Suite("BlockReviewMath", .serialized)` over in-memory ModelContainer with `Schema(SchemaV4.models)` + FitbodSchemaMigrationPlan. Required @Test functions:
      
      Most BlockReviewView math lives in private file-scope helpers; refactor any private helpers (computeE1RMDeltas, fetchSetsInBlock, formattedKg, formattedDelta) to `internal` so the test target can call them directly. Alternative: extract the computation into a separate value type `BlockReviewStatistics` with public static funcs and have BlockReviewView call into it — pick the simpler approach.
      
      - `totalVolumeSumsOverBlockRange`: insert Block with startDate -14d, endDate -1d, isActive=true; insert 3 Sessions inside the range with 2 SetEntries each (weight 100, reps 5 → 500 each, total 3000); insert 1 Session OUTSIDE the range (before startDate) with 2 SetEntries (should be excluded); call the totalVolume helper; assert == 3000 kg.
      - `totalVolumeExcludesWarmupAndIncompleteSets`: insert sets marked isWarmup=true (excluded) AND isComplete=false (excluded); assert they don't contribute to the sum.
      - `e1rmDeltaPositiveForImprovedTopSet`: insert 2 Sessions with the same Exercise; first session top set (100 kg × 5); second session top set (105 kg × 5); compute e1RM via E1RMHelper.epley — start = 100 * (1 + 5/30) ≈ 116.67, end = 105 * (1 + 5/30) ≈ 122.5; assert delta == end - start ≈ +5.83 kg (within 0.1).
      - `e1rmDeltaNegativeForRegressedTopSet`: opposite direction; assert delta < 0.
      - `e1rmDeltaIsZeroForUnchangedTopSet`: same weight + reps on both sessions; assert delta == 0.
      - `emptyDeltasWhenLessThan2SessionsPerExercise`: insert only 1 Session per Exercise; assert computeE1RMDeltas returns an empty array (UI surfaces "Not enough sessions" copy).
      - `recommendedNextKindForBlockEndingInDeload`: block.phases = [(accumulation, ...), (deload, ...)]; call PeriodizationEngine.recommendedNextKind(after: .deload); assert returned kind == .accumulation (per D-23).
      - `recommendedNextKindForBlockEndingInRealization`: block.phases = [(realization, ...)]; assert recommended == .deload.
      - `acknowledgeAndDismissSetsReviewedAtAndDeactivates`: insert active Block with endDate in the past + reviewedAt nil; instantiate BlockReviewView (via #Preview-style construction); simulate "Done" tap by calling the internal helper that performs the transaction; refetch the block; assert reviewedAt is non-nil AND isActive == false.
      - `acknowledgeTransactionRollsBackOnFailure`: defensive test — inject a context-mutation failure (e.g., delete the block before save throws); assert reviewedAt and isActive remain at their pre-acknowledge state. NB: deterministic provocation of SwiftData save throw is hard; if too brittle, swap for a static-assertion test that the transaction wrapper IS present in source (regex match `try ctx.transaction { ... block.reviewedAt = ... block.isActive = false`).
    
    (2) Flip RED placeholder for the end-of-block trigger predicate. Create `fitbodTests/BlockReviewTriggerTests.swift` as `@MainActor @Suite("BlockReviewTrigger", .serialized)`. Required @Test functions:
      - `triggerPredicateMatchesEndedActiveUnreviewedBlock`: insert Block with isActive=true, endDate = Date.now.addingTimeInterval(-86400) (yesterday), reviewedAt = nil; FetchDescriptor with the exact predicate (capture local-let date bounds if needed); fetch; assert exactly 1 row returned.
      - `triggerExcludesActiveButUnendedBlock`: same Block but endDate = Date.now.addingTimeInterval(86400 * 7) (future); assert 0 rows.
      - `triggerExcludesEndedButInactiveBlock`: endDate in past, isActive=false; assert 0 rows.
      - `triggerExcludesEndedActiveButReviewedBlock`: endDate in past, isActive=true, reviewedAt = Date.now.addingTimeInterval(-86400); assert 0 rows.
      - `triggerExcludesBlockWithNilEndDate`: endDate = nil; assert 0 rows.
      - `triggerReturnsAtMostOneByInvariant`: insert 2 blocks both matching the predicate (which would violate D-05 single-active invariant but defensive: ensure the predicate returns 1 OR 2 — UI takes `.first` per UI-SPEC line 421). Assert fetch returns 1 or 2 rows; assert UI's `.first` is non-nil.
      - `acknowledgmentRemovesBlockFromTriggerSet`: matching Block → fetch returns 1 → mutate `reviewedAt = Date.now`, `isActive = false`, save → re-fetch with same predicate → assert 0 rows. Pins the "subsequent opens don't re-surface" contract per D-21.
      - `placeholderCopyVerbatim`: source-anchor test on BlockReviewView.swift; assert it contains `"Coming in Phase 6. Full PR detection ships with the progress views update."` AND `"Not enough sessions logged to compute deltas."` AND `"No logged sets in this block."`.
      - `recommendedCTACopyFormat`: source-anchor — assert BlockReviewView source contains `"Start \(labelKind.capitalized) Block"` format string OR equivalent (the `Start [Recommended] Block` per UI-SPEC line 280).
      - `doneToolbarCopyVerbatim`: source-anchor — assert source contains `Button("Done")` literal.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/BlockReviewMath -only-testing fitbodTests/BlockReviewTrigger 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    Both suites pass; total volume sum + e1RM delta sign + 4-section render invariant + end-of-block trigger predicate are all pinned; D-21 trigger semantics (matched-once-then-acknowledged) verified by `acknowledgmentRemovesBlockFromTriggerSet`; UI-SPEC verbatim copy anchored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TodayView .task → SwiftData fetch | Read-only end-of-block trigger predicate; runs on every Today open. |
| BlockReviewView .acknowledgeAndDismiss → dual write | `block.reviewedAt = .now` + `block.isActive = false` inside `ctx.transaction` — the third legitimate writer of Block.isActive (alongside BlockDraft.save and BlockCard.endBlock). FatigueAdvisory cannot reach this surface (BLOCK-08 type-level enforcement from plan 04-01). |
| Recommended-next CTA → BlockBuilderView seed | Presents builder with `BlockDraft(template: ...)`; user must explicitly toggle Active block in builder. No auto-activation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Tampering | Acknowledge transaction could write reviewedAt without deactivating | mitigate | Both writes wrapped in single `ctx.transaction`; rollback on any throw. Pinned by `acknowledgeAndDismissSetsReviewedAtAndDeactivates`. |
| T-04-08-02 | Tampering | Recommended-next CTA could auto-activate the new block | mitigate | UI-SPEC line 575 explicit: "does NOT auto-activate the new block — user must explicitly toggle 'Active block' in builder." Verified by code review of `pendingNextBlockBuilder = BlockTemplates.template(for: ...)`: the BlockDraft default has `isActive = false`. |
| T-04-08-03 | Information Disclosure | Total volume sum + per-exercise e1RM deltas surfaced | accept | This is the entire point of the review; user-facing data they own. |
| T-04-08-04 | Denial of Service | A block spanning years with thousands of sessions makes the volume / e1RM query slow | mitigate | The `@Query` predicate scopes to `[blockStart, blockEnd]` — even a years-long block reads at most ~1000 sessions. The local-let workaround (Pitfall 2) keeps predicate translation correct. For pathological cases, the .sheet(.large) presents a progressView via standard SwiftUI loading affordance. Plan 04-08 does not pre-optimize; profile in Phase 5/6 if needed. |
| T-04-08-05 | Repudiation | Block acknowledgment timestamps (reviewedAt) are local Date.now | accept | Single-user app; the timestamp IS the audit. |
| T-04-08-06 | Elevation of Privilege | None — review write is initiated by explicit user tap on "Done" | accept | Standard UX. |
</threat_model>

<verification>
- `xcodebuild build -scheme fitbod` succeeds.
- `xcodebuild test -only-testing fitbodTests/BlockReviewMath -only-testing fitbodTests/BlockReviewTrigger` exits 0.
- Plans 04-01..07 all still GREEN.
- Manual sim run: create a Block with endDate in the past + isActive=true + reviewedAt=nil (manually via a test fixture or by setting startDate -14d and a 1-week phase); open Today; assert BlockReviewView appears as a .large sheet with 4 sections; tap "Start [Recommended] Block" → assert BlockBuilderView appears with the recommended template seeded; tap "Cancel" in builder; back at Today, sheet still present; tap "Done"; sheet dismisses; reopen Today; sheet does NOT re-appear (reviewedAt was set; isActive was cleared).
- Full Phase 4 test suite (plans 04-01..08) all GREEN.
</verification>

<success_criteria>
- BLOCK-07: end-of-block review surfaces a 4-section sheet (total volume + e1RM deltas + PRs placeholder + recommended next) on the next Today open after block.endDate < .now.
- BLOCK-08: review acknowledgment is the third (and final) legitimate writer of `Block.isActive = false` — pinned by source-anchor; FatigueAdvisory cannot reach this path per the type-level enforcement from plan 04-01.
- D-21 trigger semantics: matched-once-then-acknowledged — `acknowledgmentRemovesBlockFromTriggerSet` pins this.
- D-22 4-section scaffold: total volume + e1RM deltas computed inline; PRs hit placeholder copy; recommended next phase via static rule.
- D-23 next-kind map: verified by `recommendedNextKindForBlockEndingInDeload` and `recommendedNextKindForBlockEndingInRealization`.
- BlockReviewMathTests + BlockReviewTriggerTests turn GREEN (RED placeholder from plan 04-01 flipped).
- Phase 4 phase requirements (BLOCK-01..08 + PRES-05 + PRES-06) are all closed across plans 04-01..08.
</success_criteria>

<output>
After completion, create `.planning/phases/04-periodization-blocks/04-08-SUMMARY.md`.
</output>
