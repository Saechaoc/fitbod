---
phase: 03-smart-prescription-warm-ups
plan: 07
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - fitbod/Routines/WarmupConfigSheet.swift
  - fitbod/Routines/PrescriptionEditorRow.swift
  - fitbod/Routines/RoutineExerciseCard.swift
  - fitbod/ExerciseLibrary/ExerciseDetailView.swift
autonomous: true
requirements: [WARM-03, SET-02, SET-04]

must_haves:
  truths:
    - "WarmupConfigSheet is a .medium-detent sheet with 'Auto warm-up' Toggle + 'Skip warm-ups this session only' Toggle + Save/Cancel toolbar items per UI-SPEC § WarmupConfigSheet verbatim"
    - "RoutineExercise long-press context menu gains an 'Edit warm-up…' action that presents WarmupConfigSheet"
    - "PrescriptionEditorRow's 'Auto warm-up' toggle is no longer disabled (the Phase 2 'Phase 3' footnote is removed); toggle now writes to RoutineExercise.warmupOverride"
    - "ExerciseDetailView gains a 'Prescription Settings' section at the bottom of the List with three rows: smallest increment, bar weight override, weight unit picker (with UI-SPEC verbatim labels + footers)"
    - "ExerciseDetailView 'Prescription Settings' fields are editable for custom exercises AND read-only-displayable for built-in exercises (built-in still show the inherited defaults)"
  artifacts:
    - path: "fitbod/Routines/WarmupConfigSheet.swift"
      provides: ".medium-detent sheet editing @Binding<WarmupConfig?>"
      contains: "struct WarmupConfigSheet: View"
    - path: "fitbod/ExerciseLibrary/ExerciseDetailView.swift"
      provides: "New 'Prescription Settings' section at the bottom of the existing List"
      contains: "Prescription Settings"
  key_links:
    - from: "fitbod/Routines/RoutineExerciseCard.swift"
      to: "fitbod/Routines/WarmupConfigSheet.swift"
      via: ".contextMenu Button('Edit warm-up…') { presentWarmupSheet = true }"
      pattern: "WarmupConfigSheet"
    - from: "fitbod/ExerciseLibrary/ExerciseDetailView.swift"
      to: "fitbod/Models/Exercise.swift"
      via: "@Bindable var ex = exercise → $ex.smallestIncrement / $ex.barWeightOverride / $ex.unitOverrideRaw"
      pattern: "@Bindable"
---

<objective>
Land the routine-builder + exercise-detail surfaces that activate the Phase 3 prescription-customization fields. WarmupConfigSheet ships standalone and is invoked from RoutineExerciseCard's long-press menu. PrescriptionEditorRow's previously-disabled "Auto warm-up" toggle becomes functional. ExerciseDetailView gains a new "Prescription Settings" section at the bottom of its List exposing smallest increment, bar weight override, and unit override per-exercise.

Purpose: Closes WARM-03 (user-overridable warm-up per exercise), SET-02 (per-exercise weight unit override), and the per-exercise half of SET-04 (smallest increment). Runs in Wave 2 alongside plan 03-04 (Settings + Plate Inventory) because it touches DIFFERENT files (Routines/ + ExerciseLibrary/ vs Settings/) — zero conflict. Both are unblocked by plan 03-01's additive fields.

Output: 1 new file (WarmupConfigSheet) + 3 existing-file modifications under fitbod/Routines/ and fitbod/ExerciseLibrary/. UI-SPEC § WarmupConfigSheet and § Per-exercise fields in ExerciseDetailView copy verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md
@.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
@fitbod/Routines/PrescriptionEditorRow.swift
@fitbod/Routines/RoutineExerciseCard.swift
@fitbod/Routines/RoutineBuilderView.swift
@fitbod/ExerciseLibrary/ExerciseDetailView.swift
@fitbod/Sessions/DecimalRPEPickerSheet.swift
@fitbod/Models/WarmupConfig.swift
@fitbod/Models/RoutineExercise.swift
@fitbod/Models/Exercise.swift
@fitbod/Models/Enums/WeightUnit.swift
@fitbod/Persistence/PreviewModelContainer.swift

<interfaces>
WarmupConfig type (shipped in plan 03-01):
- public struct WarmupConfig: Codable, Sendable, Equatable
- enabled: Bool, skipNextSession: Bool

RoutineExercise.warmupOverride accessor (shipped in plan 03-01):
- get { decode from warmupOverrideData } / set { encode to warmupOverrideData }
- nil = no override (auto warm-up runs by default per WarmupRamp.shouldGenerate)

DecimalRPEPickerSheet shape (analog for WarmupConfigSheet — same .medium detent pattern but with Save + Cancel toolbar instead of Done):
- NavigationStack + Form + .navigationTitle + .navigationBarTitleDisplayMode(.inline) + .toolbar { .topBarLeading Button("Cancel") { dismiss() } + .topBarTrailing Button("Save") { /* commit + dismiss */ } }
- Caller uses .presentationDetents([.medium]).

ExerciseDetailView current shape — the existing four sections (Instructions / Muscles / Equipment / Mechanic) live in a List; the new "Prescription Settings" section is appended at the bottom of that List. Per UI-SPEC § Per-exercise fields in ExerciseDetailView, this section appears for both built-in and custom exercises — read-write for custom (editable fields), read-only-displayable for built-in (show the inherited values or "Default"). Plan resolves this by: for built-in exercises, the fields are still editable @Bindable but the user is allowed to override (Exercise model allows mutation — Phase 1 design didn't lock built-in exercises read-only). The footers per UI-SPEC label these as per-exercise overrides regardless of custom-vs-built-in.

UI-SPEC § Per-exercise fields in ExerciseDetailView copy verbatim:
- Section header: "Prescription Settings"
- "Smallest increment" label / "e.g. 2.5" placeholder / "kg" suffix / footer "Weight advances by this amount each progression step."
- "Bar weight override" label / "Leave blank to use equipment default" placeholder / "kg" suffix / footer "Use for specialty bars (safety squat, Swiss bar, women's bar)."
- "Weight unit" label / Picker options "System default" / "kg" / "lb" / footer "Overrides the global unit for this exercise only. Affects display; history is stored in a canonical unit."

UI-SPEC § WarmupConfigSheet copy verbatim:
- title "Warm-up Settings"
- toggle "Auto warm-up"
- footer when enabled: "Generates a 4-set ramp (40% × 5, 60% × 3, 75% × 2, 90% × 1) based on your plate inventory."
- footer when disabled: "No warm-up sets will be generated for this exercise."
- "Skip warm-ups this session only" toggle
- "Save" / "Cancel" toolbar
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WarmupConfigSheet (standalone, .medium detent)</name>
  <files>
    fitbod/Routines/WarmupConfigSheet.swift
  </files>
  <read_first>
    @fitbod/Sessions/DecimalRPEPickerSheet.swift,
    @fitbod/Sessions/WorkoutNotesSheet.swift,
    @fitbod/Sessions/PinnedNoteSheet.swift,
    @fitbod/Models/WarmupConfig.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - WarmupConfigSheet is `public struct WarmupConfigSheet: View` with `@Binding public var config: WarmupConfig?`, `@Environment(\\.dismiss) private var dismiss`, `@State private var draftEnabled: Bool = true`, `@State private var draftSkipNextSession: Bool = false`.
    - On appear, initialize draft state from current config (if non-nil, use its fields; if nil, default enabled=true / skipNext=false — "no override = auto-on by default").
    - Body: NavigationStack { Form { Toggle("Auto warm-up", isOn: $draftEnabled), Section { Toggle("Skip warm-ups this session only", isOn: $draftSkipNextSession) } footer: { Text(draftEnabled ? "Generates a 4-set ramp (40% × 5, 60% × 3, 75% × 2, 90% × 1) based on your plate inventory." : "No warm-up sets will be generated for this exercise.").font(.caption).foregroundStyle(.secondary) } }.navigationTitle("Warm-up Settings").navigationBarTitleDisplayMode(.inline).toolbar { ToolbarItem(placement: .topBarLeading) { Button("Cancel") { dismiss() } } ToolbarItem(placement: .topBarTrailing) { Button("Save") { commit(); dismiss() } } } }.
    - commit(): if (draftEnabled == true && draftSkipNextSession == false) → config = nil (back to "no override = default behavior") OR config = WarmupConfig(enabled: true, skipNextSession: false) — pick "set nil" to keep the model clean per RESEARCH §Pitfall 5 ("Only write warmupOverrideData when the user explicitly saves a custom config"). Otherwise config = WarmupConfig(enabled: draftEnabled, skipNextSession: draftSkipNextSession).
    - Add #Preview blocks: "nil binding (no override yet)" and "existing override (enabled=false)".
  </behavior>
  <action>
    Create fitbod/Routines/WarmupConfigSheet.swift mirroring fitbod/Sessions/DecimalRPEPickerSheet.swift NavigationStack shape but with two toolbar items (Cancel + Save) per <interfaces> shape.

    The footer copy switches via ternary on draftEnabled. The Save action computes the next config value per <behavior>'s commit() logic and assigns through the @Binding.

    #Preview blocks use @Previewable @State var localConfig: WarmupConfig? = nil (or .some(WarmupConfig(enabled: false))) and wrap in .sheet(isPresented:) for visual rendering.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct WarmupConfigSheet' fitbod/Routines/WarmupConfigSheet.swift && grep -c 'Warm-up Settings' fitbod/Routines/WarmupConfigSheet.swift && grep -c 'Auto warm-up' fitbod/Routines/WarmupConfigSheet.swift && grep -c 'Skip warm-ups this session only' fitbod/Routines/WarmupConfigSheet.swift && grep -c 'Generates a 4-set ramp' fitbod/Routines/WarmupConfigSheet.swift && grep -c '#Preview' fitbod/Routines/WarmupConfigSheet.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Every grep returns at least 1 (with #Preview returning at least 2).
    - `grep -c 'Cancel' fitbod/Routines/WarmupConfigSheet.swift` returns at least 1.
    - `grep -c '\"Save\"' fitbod/Routines/WarmupConfigSheet.swift` returns at least 1.
  </acceptance_criteria>
  <done>Sheet ships standalone with UI-SPEC verbatim copy. Caller integration in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrescriptionEditorRow + RoutineExerciseCard integration of WarmupConfigSheet</name>
  <files>
    fitbod/Routines/PrescriptionEditorRow.swift,
    fitbod/Routines/RoutineExerciseCard.swift
  </files>
  <read_first>
    @fitbod/Routines/PrescriptionEditorRow.swift,
    @fitbod/Routines/RoutineExerciseCard.swift,
    @fitbod/Routines/RoutineBuilderView.swift,
    @fitbod/Routines/WarmupConfigSheet.swift,
    @fitbod/Models/RoutineExercise.swift,
    @fitbod/Models/WarmupConfig.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - PrescriptionEditorRow's "Auto warm-up" toggle (currently disabled with a Phase 2 footnote like "Available in Phase 3") becomes functional. Removes the disabled + footnote treatment. Toggle reads/writes a Bool derived from RoutineExercise.warmupOverride (or its nil-means-on default): `isOn: Binding(get: { (re.warmupOverride?.enabled ?? true) }, set: { newValue in if newValue && (re.warmupOverride?.skipNextSession ?? false) == false { re.warmupOverride = nil } else { re.warmupOverride = WarmupConfig(enabled: newValue, skipNextSession: re.warmupOverride?.skipNextSession ?? false) } })`. Result: simple toggle becomes "auto warm-up on" (no override) or "off" (override with enabled=false).
    - RoutineExerciseCard's existing .contextMenu gains a new menu item "Edit warm-up…" (Image + text per existing menu items). On tap: sets a `@State var pendingWarmupSheet: RoutineExercise?` on the enclosing RoutineBuilderView (since RoutineExerciseCard receives a closure callback from the builder for sheet presentation — mirror the existing onSwap / onRemove / onEditPinnedNote default-closure pattern from PATTERNS.md line 502).
    - Add init param `onEditWarmup: (RoutineExercise) -> Void = { _ in }` on RoutineExerciseCard (matches plan 02-04-02 onSwap precedent — additive with default closure preserves caller backward compat).
    - RoutineBuilderView (or its inner ForEach over RoutineExercise rows) passes `onEditWarmup: { re in pendingWarmupSheet = re }` and presents `.sheet(item: $pendingWarmupSheet) { re in @Bindable var bre = re; WarmupConfigSheet(config: $bre.warmupOverride).presentationDetents([.medium]) }`.
    - All UI copy comes from UI-SPEC § WarmupConfigSheet — toggle and footer copy already verbatim in plan-03-07 Task 1. RoutineExerciseCard context-menu label uses UI-SPEC § What's Explicitly Deferred reference: "Edit warm-up…" (matches Phase 2 menu-item ellipsis convention).
  </behavior>
  <action>
    Edit fitbod/Routines/PrescriptionEditorRow.swift:
    - Locate the "Auto warm-up" toggle row. Remove the `.disabled(true)` modifier and remove the inline "Available in Phase 3" / "Phase 3 footnote" Text view if present.
    - Replace the toggle binding with the Binding(get:set:) computed expression per <behavior>.
    - Update the footer text (if any) below the toggle to UI-SPEC verbatim: "Generates a 4-set ramp (40% × 5, 60% × 3, 75% × 2, 90% × 1) based on your plate inventory." when enabled, "No warm-up sets will be generated for this exercise." when disabled. Use ternary on the resolved enabled boolean.

    Edit fitbod/Routines/RoutineExerciseCard.swift:
    - Add the new init param `public var onEditWarmup: (RoutineExercise) -> Void = { _ in }` alongside existing onSwap / onRemove / onEditPinnedNote (or wherever those parameters live).
    - In the .contextMenu block: add a new Button { onEditWarmup(routineExercise) } label: { Label("Edit warm-up…", systemImage: "flame") } — place it adjacent to the existing "Edit Prescription" item per UI-SPEC contextual grouping.
    - Preserve all existing context-menu items.

    Edit fitbod/Routines/RoutineBuilderView.swift:
    - Add `@State private var pendingWarmupSheet: RoutineExercise?` near the existing pendingSupersetAssignment state.
    - Pass `onEditWarmup: { re in pendingWarmupSheet = re }` to every RoutineExerciseCard / RoutineExerciseRow instance.
    - Add `.sheet(item: $pendingWarmupSheet) { re in @Bindable var bre = re; WarmupConfigSheet(config: $bre.warmupOverride).presentationDetents([.medium]) }` adjacent to the existing pendingSupersetAssignment sheet.

    Do NOT modify any other Phase 2 routine surface (RoutineDraft, RoutineDuplicator, etc.).
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'onEditWarmup' fitbod/Routines/RoutineExerciseCard.swift && grep -c 'WarmupConfigSheet' fitbod/Routines/RoutineBuilderView.swift && grep -c 'Edit warm-up' fitbod/Routines/RoutineExerciseCard.swift && grep -c 'Phase 3' fitbod/Routines/PrescriptionEditorRow.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - First three greps return at least 1 each.
    - The last grep (counting "Phase 3" mentions in PrescriptionEditorRow) returns 0 — the disabled-toggle Phase 3 footnote was removed.
    - `grep -c '.disabled(true)' fitbod/Routines/PrescriptionEditorRow.swift` returns 0 OR only matches disabled-toggle on OTHER rows (visually inspect output if non-zero).
    - `grep -c 'pendingWarmupSheet' fitbod/Routines/RoutineBuilderView.swift` returns at least 2 (state + sheet binding).
  </acceptance_criteria>
  <done>"Auto warm-up" toggle is fully functional in the routine builder. Long-press context-menu "Edit warm-up…" presents WarmupConfigSheet bound to the RoutineExercise's warmupOverride accessor. Phase 2 disabled-toggle footnote is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExerciseDetailView "Prescription Settings" section</name>
  <files>
    fitbod/ExerciseLibrary/ExerciseDetailView.swift
  </files>
  <read_first>
    @fitbod/ExerciseLibrary/ExerciseDetailView.swift,
    @fitbod/Models/Exercise.swift,
    @fitbod/Models/Enums/WeightUnit.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - ExerciseDetailView gains a new Section "Prescription Settings" appended to the bottom of its List, AFTER the existing four sections AND BEFORE (or alongside) the "if !exercise.isCustom { Copy as Custom CTA }" block and the Phase 2 "History" section (which contains the View All History NavigationLink). Specifically: order becomes [existing sections] → "Prescription Settings" → "Copy as Custom" (if applicable) → "History".
    - Section content (three rows):
      - LabeledContent("Smallest increment") { TextField("e.g. 2.5", value: $ex.smallestIncrement, format: .number).multilineTextAlignment(.trailing).keyboardType(.decimalPad); Text(unitLabel).foregroundStyle(.secondary) } — uses .multilineTextAlignment(.trailing) per the LabeledContent inline-form idiom.
      - LabeledContent("Bar weight override") { TextField("Leave blank to use equipment default", value: $ex.barWeightOverride, format: .number).multilineTextAlignment(.trailing).keyboardType(.decimalPad); Text(unitLabel).foregroundStyle(.secondary) }.
      - Picker("Weight unit", selection: Binding(get: { ex.unitOverride }, set: { ex.unitOverride = $0 })) { Text("System default").tag(WeightUnit?.none); Text("kg").tag(WeightUnit?.some(.kg)); Text("lb").tag(WeightUnit?.some(.lb)) }.
    - Section footers: combine all three footers into either a single Section footer with all three explainer lines (less ideal) OR three separate Section wrappers (cleaner). Choose three separate Section wrappers per UI-SPEC § Per-exercise fields verbatim copy for each footer:
      - Footer 1: "Weight advances by this amount each progression step."
      - Footer 2: "Use for specialty bars (safety squat, Swiss bar, women's bar)."
      - Footer 3: "Overrides the global unit for this exercise only. Affects display; history is stored in a canonical unit."
    - Use `@Bindable var ex = exercise` inside the body (or a small computed property) so the three @State-equivalent property writes go straight through to SwiftData.
    - Edits work for both built-in AND custom exercises (no isCustom guard around the section).
    - Unit label: derived from the currently-active WeightUnit. Use UserSettings.first?.weightUnit ?? .lb as the global fallback (matches plan 03-04's unit label pattern).
  </behavior>
  <action>
    Edit fitbod/ExerciseLibrary/ExerciseDetailView.swift:
    - Locate the existing List body. After the four existing sections (Instructions / Muscles / Equipment / Mechanic per the Phase 1 plan-03-03 SUMMARY), insert three new Section wrappers per <behavior>.
    - Add `@Bindable var ex = exercise` at the top of the body (or in a `@ViewBuilder` helper).
    - Each section contains a single row + a footer per UI-SPEC verbatim copy.
    - Picker uses a tag of `Optional<WeightUnit>` for the "System default" sentinel (.none) and .some(.kg) / .some(.lb) for the two override cases.
    - Preserve all existing sections, the "Copy as Custom" CTA, and the Phase 2 "History" NavigationLink. The new "Prescription Settings" sections are inserted BETWEEN the existing four content sections and the Copy/History block.

    Do NOT mutate any existing Phase 1/2 section content. Do NOT remove the isCustom guard around "Copy as Custom" — it stays in place.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'Prescription Settings' fitbod/ExerciseLibrary/ExerciseDetailView.swift && grep -c 'Smallest increment' fitbod/ExerciseLibrary/ExerciseDetailView.swift && grep -c 'Bar weight override' fitbod/ExerciseLibrary/ExerciseDetailView.swift && grep -c 'Weight unit' fitbod/ExerciseLibrary/ExerciseDetailView.swift && grep -c 'Weight advances by this amount each progression step.' fitbod/ExerciseLibrary/ExerciseDetailView.swift && grep -c 'Use for specialty bars' fitbod/ExerciseLibrary/ExerciseDetailView.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Every grep returns at least 1 (all UI-SPEC verbatim copy anchors present).
    - `grep -c '@Bindable' fitbod/ExerciseLibrary/ExerciseDetailView.swift` returns at least 1 (write-through binding).
    - `grep -c 'unitOverride' fitbod/ExerciseLibrary/ExerciseDetailView.swift` returns at least 1.
    - `grep -c 'smallestIncrement' fitbod/ExerciseLibrary/ExerciseDetailView.swift` returns at least 1.
    - `grep -c 'System default' fitbod/ExerciseLibrary/ExerciseDetailView.swift` returns 1 (picker sentinel label).
  </acceptance_criteria>
  <done>ExerciseDetailView shows the new "Prescription Settings" section at the bottom of the List with editable smallest-increment / bar-weight-override / unit-override rows + UI-SPEC verbatim footers.</done>
</task>

</tasks>

<verification>
- swiftc parse exits clean across all modified files.
- Routine builder context menu shows "Edit warm-up…"; tap opens WarmupConfigSheet at .medium detent.
- "Auto warm-up" toggle in PrescriptionEditorRow is no longer disabled; writes work through to RoutineExercise.warmupOverride.
- ExerciseDetailView has the new Prescription Settings section with verbatim UI-SPEC copy and editable fields.
- No regression to Phase 1/2 test suites (Phase 3 plan 03-02 GREEN suites + all Phase 1/2 GREEN suites continue to pass).
</verification>

<success_criteria>
- 1 new file (WarmupConfigSheet) + 3 modified files.
- UI-SPEC § WarmupConfigSheet and § Per-exercise fields in ExerciseDetailView copy verbatim throughout.
- WARM-03 user-overridable warm-up: closed.
- SET-02 per-exercise unit override: closed (display layer; canonical-unit storage preserved).
- SET-04 per-exercise smallest increment: closed.
- No existing Phase 2 test suite broken.
</success_criteria>

<output>
Create `.planning/phases/03-smart-prescription-warm-ups/03-07-SUMMARY.md` listing the WarmupConfigSheet file, the three edited files, and the UI-SPEC anchors. Commit per atomic-commit convention: one commit per task (sheet, builder integration, exercise detail).
</output>
