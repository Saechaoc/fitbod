---
phase: 03-smart-prescription-warm-ups
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - fitbod/Settings/PlateInventory+Defaults.swift
  - fitbod/Settings/PlateInventoryEditor.swift
  - fitbod/Settings/PlateCalculatorSheet.swift
  - fitbod/Settings/SettingsView.swift
  - fitbod/Settings/PlateInventorySeeder.swift
autonomous: true
requirements: [SET-03, SET-04, SET-07, PRES-08]

must_haves:
  truths:
    - "On first launch (UserDefaults flag plateInventorySeeded != true), seeding creates exactly 4 PlateInventory rows (.barbell, .dumbbell, .ezBar, .trapBar) with unit-system-appropriate default plates"
    - "PlateInventoryDefaults.make(for: unitSystem:) returns the canonical kg-barbell list (25×4, 20×2, 15×2, 10×2, 5×2, 2.5×2, 1.25×2) and the lb-barbell list (45×4, 35×2, 25×2, 10×2, 5×2, 2.5×2, 1.25×2)"
    - "PlateInventoryEditor is reachable from SettingsView > Smart Progression > Plate Inventory and displays all 4 EquipmentKind tabs"
    - "Each tab edits bar weight + the plate list with live write-through (no Save button)"
    - "Reset to Defaults button on each tab presents an .alert (UI-SPEC verbatim title/body/buttons) and overwrites that tab's data on confirm"
    - "Settings > Smart Progression section adds: NavigationLink to Plate Inventory, Default-weight-increment Stepper (writes UserSettings.defaultIncrementKg), Sets-before-calibrating Stepper (writes UserSettings.minCalibrationSets, range 5...30)"
    - "PlateCalculatorSheet (Try it button per tab) is reachable from PlateInventoryEditor toolbar and shows live plate-stack against editable target weight"
  artifacts:
    - path: "fitbod/Settings/PlateInventory+Defaults.swift"
      provides: "Pure factory: PlateInventoryDefaults.make(for: PlateEquipmentKind, unitSystem: WeightUnit) -> [PlateSpec]"
      contains: "enum PlateInventoryDefaults"
    - path: "fitbod/Settings/PlateInventorySeeder.swift"
      provides: "First-launch seeder gated by UserDefaults flag; idempotent"
      contains: "enum PlateInventorySeeder"
    - path: "fitbod/Settings/PlateInventoryEditor.swift"
      provides: "Tabbed @Query<PlateInventory> Form editor with reset/add/delete"
      contains: "struct PlateInventoryEditor: View"
    - path: "fitbod/Settings/PlateCalculatorSheet.swift"
      provides: ".medium-detent sheet with target weight + live plate stack"
      contains: "struct PlateCalculatorSheet: View"
  key_links:
    - from: "fitbod/Settings/SettingsView.swift"
      to: "fitbod/Settings/PlateInventoryEditor.swift"
      via: "NavigationLink(\"Plate Inventory\") { PlateInventoryEditor() }"
      pattern: "PlateInventoryEditor"
    - from: "fitbod/Settings/PlateInventorySeeder.swift"
      to: "fitbod/Settings/PlateInventory+Defaults.swift"
      via: "PlateInventoryDefaults.make(for:unitSystem:)"
      pattern: "PlateInventoryDefaults.make"
    - from: "fitbod/Settings/PlateCalculatorSheet.swift"
      to: "fitbod/Prescription/PlateCalculator.swift"
      via: "PlateCalculator.solve(target:barWeight:plates:)"
      pattern: "PlateCalculator.solve"
---

<objective>
Land the plate inventory persistence surface end-to-end: pure defaults factory, idempotent first-launch seeder, settings UI surfaces, and the Try-It plate calculator sheet. After this plan, a user can navigate Settings → Smart Progression → Plate Inventory and see four pre-seeded tabs of plates that they can edit, reset, or add to. Manual override of weight increments and calibration window also configurable from SettingsView.

Purpose: Closes SET-03 (plate inventory per equipment), SET-04 (smallest weight increment — via Settings stepper for default; per-exercise override is plan 03-07), SET-07 (RPE-calibration window stepper). Unblocks plan 03-05 (strategies need to query the seeded inventory for rounding) and plan 03-06 (PlateStackDisclosure reads from the same persisted inventory).

Output: 5 new files under fitbod/Settings/ + extension to existing SettingsView. SettingsUnitsIntegrationTests continues to pass (no regression). New PlateInventorySeederTests added inline to confirm idempotency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smart-prescription-warm-ups/03-CONTEXT.md
@.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md
@.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md
@.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
@fitbod/Settings/SettingsView.swift
@fitbod/Models/PlateInventory.swift
@fitbod/Models/PlateSpec.swift
@fitbod/Models/Enums/EquipmentKind.swift
@fitbod/Models/Enums/WeightUnit.swift
@fitbod/Models/UserSettings.swift
@fitbod/Routines/PrescriptionDefaults.swift
@fitbod/Prescription/PlateCalculator.swift
@fitbod/Sessions/DecimalRPEPickerSheet.swift
@fitbod/ExerciseLibrary/ExerciseLibraryImporter.swift

<interfaces>
SettingsView current shape (from fitbod/Settings/SettingsView.swift) — this plan extends it with a new "Smart Progression" section ABOVE the existing footer and BELOW the existing "Units" section:

- The view uses `@Query private var settingsList: [UserSettings]` and `if let settings = settingsList.first { ... }`.
- Existing helper `private func unitsSection(settings: UserSettings) -> some View { ... }`.
- Form / NavigationStack / navigationTitle "Settings" shape per PATTERNS.md "Settings/PlateInventoryEditor.swift" Settings analog block.

PreviewModelContainer.make() pattern for previews (fitbod/Persistence/PreviewModelContainer.swift) — every #Preview block in this plan uses it.

UserSettings additive fields (shipped in plan 03-01):
- `var defaultIncrementKg: Double = 2.5`
- `var minCalibrationSets: Int = 10`

The 03-CONTEXT.md "PlateInventory defaults" section (Area 4) is normative; copy the kg/lb plate lists verbatim:
- kg barbell: 25×4, 20×2, 15×2, 10×2, 5×2, 2.5×2, 1.25×2
- lb barbell: 45×4, 35×2, 25×2, 10×2, 5×2, 2.5×2, 1.25×2
- dumbbell / ezBar / trapBar: 03-CONTEXT.md is silent → planner discretion → mirror barbell list with bar weight 0 (dumbbell handle weight is plate-weight), 7 (ez-bar), and 22 (trap bar).

UI-SPEC copy is normative (§ Settings — Smart Progression, § PlateInventoryEditor, § Plate-stack inline disclosure). Every string emitted to the UI in this plan is copied verbatim from UI-SPEC.

DecimalRPEPickerSheet (fitbod/Sessions/DecimalRPEPickerSheet.swift) is the exact analog for PlateCalculatorSheet: NavigationStack + .navigationTitle + .navigationBarTitleDisplayMode(.inline) + ToolbarItem(placement: .topBarTrailing) Button("Done") { dismiss() } + .presentationDetents([.medium]) on the caller.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defaults factory + first-launch seeder (idempotent)</name>
  <files>
    fitbod/Settings/PlateInventory+Defaults.swift,
    fitbod/Settings/PlateInventorySeeder.swift,
    fitbod/fitbodApp.swift
  </files>
  <read_first>
    @fitbod/Models/PlateInventory.swift,
    @fitbod/Models/PlateSpec.swift,
    @fitbod/Models/Enums/EquipmentKind.swift,
    @fitbod/Models/Enums/WeightUnit.swift,
    @fitbod/ExerciseLibrary/ExerciseLibraryImporter.swift,
    @fitbod/Routines/PrescriptionDefaults.swift,
    @fitbod/fitbodApp.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - PlateInventoryDefaults is a namespace enum exposing `static func make(for kind: PlateEquipmentKind, unitSystem: WeightUnit) -> [PlateSpec]` and `static func barWeight(for kind: PlateEquipmentKind, unitSystem: WeightUnit) -> Double`.
    - For (.barbell, .kg): plates [(25,4), (20,2), (15,2), (10,2), (5,2), (2.5,2), (1.25,2)] verbatim per 03-CONTEXT.md; barWeight 20.0.
    - For (.barbell, .lb): plates [(45,4), (35,2), (25,2), (10,2), (5,2), (2.5,2), (1.25,2)] verbatim; barWeight 45.0.
    - For (.dumbbell, .kg): plates [(10,2), (5,2), (2.5,2), (1.25,2)]; barWeight 0.0 (handle weight is bundled into plate calc).
    - For (.dumbbell, .lb): plates [(10,2), (5,2), (2.5,2), (1.25,2)]; barWeight 0.0.
    - For (.ezBar, .kg/.lb): same plate list as barbell-kg/lb; barWeight 7.0 (kg) / 15.0 (lb).
    - For (.trapBar, .kg/.lb): same plate list as barbell-kg/lb; barWeight 22.0 (kg) / 50.0 (lb).
    - PlateInventorySeeder is a namespace enum exposing `@MainActor static func seedIfNeeded(in context: ModelContext, unitSystem: WeightUnit)` that:
      - Reads `UserDefaults.standard.bool(forKey: "plateInventorySeeded")`. If true, return early (idempotent).
      - For each `PlateEquipmentKind.allCases`, creates a PlateInventory with equipmentKind = kind, barWeight from defaults factory, availablePlates from defaults factory.
      - `context.insert(inv)` for each. `try? context.save()`.
      - Set `UserDefaults.standard.set(true, forKey: "plateInventorySeeded")`.
    - Seeder is also gated by an actual fetch — even if the flag is missing, if `@Query<PlateInventory>` returns 4+ rows, set the flag and exit. This makes seeding doubly-idempotent (cheap insurance against UserDefaults wipes).
    - fitbodApp.swift `.task { ... }` block adds a call to `PlateInventorySeeder.seedIfNeeded(in: sharedModelContainer.mainContext, unitSystem: <current UserSettings.weightUnit or .lb default>)` alongside the existing exercise seed trigger.
  </behavior>
  <action>
    Create fitbod/Settings/PlateInventory+Defaults.swift mirroring fitbod/Routines/PrescriptionDefaults.swift pure-factory shape: `import Foundation`, then `public enum PlateInventoryDefaults` with two static functions per <behavior>. Express plate lists as `private static let kgBarbellPlates: [PlateSpec] = [PlateSpec(weight: 25, countPerSide: 4), ...]` and key into them from a switch statement in the public functions. Cross-check every weight and count against 03-CONTEXT.md before saving.

    Create fitbod/Settings/PlateInventorySeeder.swift: `import Foundation` + `import SwiftData`; `@MainActor public enum PlateInventorySeeder`. Implement `seedIfNeeded(in:unitSystem:)` per the <behavior> spec. Use a small private FetchDescriptor<PlateInventory> with no predicate to count existing rows; if count >= PlateEquipmentKind.allCases.count, set the UserDefaults flag and return.

    Edit fitbod/fitbodApp.swift: locate the existing `.task { ... }` block that runs the exercise seed. Append a single line after the exercise seed completes: `PlateInventorySeeder.seedIfNeeded(in: sharedModelContainer.mainContext, unitSystem: currentWeightUnit)` where currentWeightUnit reads `UserSettings.default()`'s weightUnit OR (preferred) reads from `@Query<UserSettings>().first?.weightUnit ?? .lb` if accessible from the .task closure; simplest: pass `.lb` as a fallback default since UserSettings may not yet exist on first launch — the user can change the unit later in Settings and the seeded inventory will not auto-regenerate (this is acceptable; the Reset to Defaults button per tab handles unit-change scenarios). Document this fallback in a `// NOTE:` comment.

    Do NOT touch the existing exercise-seed path. Do NOT alter the ExerciseLibraryImporter @ModelActor.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'enum PlateInventoryDefaults' fitbod/Settings/PlateInventory+Defaults.swift && grep -c 'enum PlateInventorySeeder' fitbod/Settings/PlateInventorySeeder.swift && grep -c 'plateInventorySeeded' fitbod/Settings/PlateInventorySeeder.swift && grep -c 'PlateInventorySeeder.seedIfNeeded' fitbod/fitbodApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Each grep returns at least 1.
    - `grep -cE 'PlateSpec\(weight: 25, countPerSide: 4\)' fitbod/Settings/PlateInventory+Defaults.swift` returns at least 1 (kg-barbell 25kg×4 row present).
    - `grep -cE 'PlateSpec\(weight: 45, countPerSide: 4\)' fitbod/Settings/PlateInventory+Defaults.swift` returns at least 1 (lb-barbell 45lb×4 row present).
    - `grep -c 'allCases' fitbod/Settings/PlateInventorySeeder.swift` returns at least 1 (seeder iterates every equipment kind).
  </acceptance_criteria>
  <done>Defaults factory and seeder ship. First launch will populate exactly 4 PlateInventory rows. Idempotent on subsequent launches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlateInventoryEditor (tabbed Form) + SettingsView Smart Progression section</name>
  <files>
    fitbod/Settings/PlateInventoryEditor.swift,
    fitbod/Settings/SettingsView.swift
  </files>
  <read_first>
    @fitbod/Settings/SettingsView.swift,
    @fitbod/Routines/RoutinesListView.swift,
    @fitbod/Models/PlateInventory.swift,
    @fitbod/Models/UserSettings.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - PlateInventoryEditor is a SwiftUI View pushed onto the Settings NavigationStack (no NavigationStack of its own).
    - Layout: `Picker` (segmented control) at top selects the active PlateEquipmentKind tab (4 cases: Barbell / Dumbbells / EZ-Bar / Trap Bar — labels per UI-SPEC § PlateInventoryEditor).
    - Below the picker, a Form with two Sections:
      - Section 1 "Bar weight" with a TextField bound (write-through @Bindable) to the active PlateInventory's barWeight, .decimalPad keyboard, "kg"/"lb" suffix per UserSettings.weightUnit.
      - Section 2 "Plates per side" with a ForEach over the active inventory's availablePlates (sorted descending by weight), each row showing "{weight} {unit}" leading and a Stepper("× {count}", value:in: 0...10) trailing. swipeActions row supports Delete (system .destructive). Below the list, an "Add Plate" button presents a sheet with weight TextField + "Add" button — appends new PlateSpec to availablePlates sorted descending.
      - Bottom of Form: a Section footer containing "Reset to Defaults" text button (accent foreground per UI-SPEC item 21) and a toolbar trailing Button("Calculator") that presents PlateCalculatorSheet.
    - Reset to Defaults: tap → .alert with UI-SPEC verbatim title "Reset plate inventory?" + body "Replaces with the default {unit system} plate set for {equipment kind}." + buttons "Reset" (destructive) / "Cancel".
    - All edits write through to the SwiftData entity immediately (no Save button). Empty-state UI (when availablePlates.isEmpty): UI-SPEC verbatim heading "No plates configured" + body "Add plates to use the plate calculator." + accent "Add Plate" text button.
    - SettingsView gains a new "Smart Progression" section below "Units" with: NavigationLink("Plate Inventory") to PlateInventoryEditor (with chevron); Stepper("Default weight increment", value: $settings.defaultIncrementKg, in: 0.25...10.0, step: 0.25) with display label "{value} {unit}" and footer per UI-SPEC; Stepper("Sets before calibrating", value: $settings.minCalibrationSets, in: 5...30, step: 1) with footer per UI-SPEC.
    - All UI copy comes from UI-SPEC § PlateInventoryEditor and § Settings — Smart Progression — verbatim, including the empty-state and the alert.
  </behavior>
  <action>
    Create fitbod/Settings/PlateInventoryEditor.swift:
    - imports: SwiftUI + SwiftData.
    - `public struct PlateInventoryEditor: View` with `@Query private var inventories: [PlateInventory]`, `@Environment(\.modelContext) private var ctx`, `@Query private var settingsList: [UserSettings]`, `@State private var activeTab: PlateEquipmentKind = .barbell`, `@State private var showResetConfirm: Bool = false`, `@State private var showAddPlateSheet: Bool = false`, `@State private var showCalculatorSheet: Bool = false`, `@State private var newPlateWeightText: String = ""`.
    - Computed `private var activeInventory: PlateInventory?` returns the inventories.first(where: { $0.equipmentKind == activeTab }).
    - body: VStack with Picker("Equipment", selection: $activeTab) tagged with each PlateEquipmentKind case + .pickerStyle(.segmented); then if let inv = activeInventory { @Bindable var binv = inv; Form { barWeightSection(binv); platesSection(binv); resetSection(binv) }.navigationTitle("Plate Inventory") .toolbar { ToolbarItem(placement: .topBarTrailing) { Button("Calculator") { showCalculatorSheet = true } } } .sheet(isPresented: $showCalculatorSheet) { PlateCalculatorSheet(equipment: activeTab, inventory: inv).presentationDetents([.medium]) } .sheet(isPresented: $showAddPlateSheet) { addPlateSheet(binv) }.alert("Reset plate inventory?", isPresented: $showResetConfirm) { ... } message: { Text("Replaces with the default \\(unitLabel) plate set for \\(activeTab.displayName).") } } else { ContentUnavailableView("No plates configured", systemImage: "scalemass", description: Text("Add plates to use the plate calculator.")).overlay(alignment: .bottom) { Button("Add Plate") { showAddPlateSheet = true }.foregroundStyle(Color.accentColor) } }.
    - Helper extension on PlateEquipmentKind: `var displayName: String { switch self { .barbell: "Barbell"; .dumbbell: "Dumbbells"; .ezBar: "EZ-Bar"; .trapBar: "Trap Bar" } }` — placed file-private to the editor file.
    - barWeightSection: Section("Bar weight") { HStack { TextField("Bar weight", value: $binv.barWeight, format: .number).keyboardType(.decimalPad); Text(unitLabel).foregroundStyle(.secondary) } }.
    - platesSection: Section("Plates per side") { ForEach(binv.availablePlates.sorted(by: { $0.weight > $1.weight }), id: \\.weight) { plate in HStack { Text("\\(plate.weight, specifier: \"%g\") \\(unitLabel)"); Spacer(); Stepper("× \\(plate.count)", value: stepperBinding(for: plate, on: binv), in: 0...10) }.swipeActions(edge: .trailing, allowsFullSwipe: false) { Button("Delete", role: .destructive) { delete(plate, from: binv) } } } Button("Add Plate") { showAddPlateSheet = true } } — the stepperBinding helper transforms count writes back through binv.availablePlates.
    - resetSection: Section { Button("Reset to Defaults") { showResetConfirm = true }.foregroundStyle(Color.accentColor) } — alert confirm runs `binv.availablePlates = PlateInventoryDefaults.make(for: activeTab, unitSystem: currentUnit); binv.barWeight = PlateInventoryDefaults.barWeight(for: activeTab, unitSystem: currentUnit); binv.updatedAt = .now; try? ctx.save()`.
    - addPlateSheet: NavigationStack { Form { TextField("e.g. 25", text: $newPlateWeightText).keyboardType(.decimalPad) } .navigationTitle("Add Plate") .toolbar { ToolbarItem(placement: .topBarLeading) { Button("Cancel") { newPlateWeightText = ""; showAddPlateSheet = false } } ToolbarItem(placement: .topBarTrailing) { Button("Add") { if let w = Double(newPlateWeightText), w > 0 { binv.availablePlates.append(PlateSpec(weight: w, countPerSide: 2)); binv.availablePlates.sort(by: { $0.weight > $1.weight }); binv.updatedAt = .now; try? ctx.save(); newPlateWeightText = ""; showAddPlateSheet = false } }.disabled(Double(newPlateWeightText) == nil) } } }.

    Edit fitbod/Settings/SettingsView.swift: add a new `smartProgressionSection(settings: UserSettings) -> some View` helper that returns a Section "Smart Progression" containing:
    - `NavigationLink { PlateInventoryEditor() } label: { LabeledContent("Plate Inventory") {} }` — disclosure chevron is automatic with NavigationLink.
    - `Stepper(value: $bsettings.defaultIncrementKg, in: 0.25...10.0, step: 0.25) { LabeledContent("Default weight increment") { Text("\\(bsettings.defaultIncrementKg, specifier: \"%g\") \\(unitLabel)") } }` with footer "Used when an exercise has no specific increment set. Applied by all progression strategies when rounding." per UI-SPEC.
    - `Stepper(value: $bsettings.minCalibrationSets, in: 5...30, step: 1) { LabeledContent("Sets before calibrating") { Text("\\(bsettings.minCalibrationSets) sets") } }` with footer "RPE autoregulation uses the Tuchscherer table until this many working sets are logged per exercise." per UI-SPEC.

    Call the new helper from the existing body, INSIDE the existing `if let settings = settingsList.first { unitsSection(settings: settings); smartProgressionSection(settings: settings) }` block. Do NOT mutate the existing unitsSection.

    Place SettingsView inside NavigationStack so NavigationLink works (preserve existing wrapping).
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct PlateInventoryEditor' fitbod/Settings/PlateInventoryEditor.swift && grep -c 'Smart Progression' fitbod/Settings/SettingsView.swift && grep -c 'PlateInventoryEditor()' fitbod/Settings/SettingsView.swift && grep -c 'Plate Inventory' fitbod/Settings/PlateInventoryEditor.swift && grep -c 'Reset to Defaults' fitbod/Settings/PlateInventoryEditor.swift && grep -c 'Reset plate inventory?' fitbod/Settings/PlateInventoryEditor.swift && grep -c 'minCalibrationSets' fitbod/Settings/SettingsView.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Every grep returns at least 1.
    - `grep -cE '\.tag\(PlateEquipmentKind\.(barbell|dumbbell|ezBar|trapBar)\)' fitbod/Settings/PlateInventoryEditor.swift` returns 4 (all four tabs present).
    - `grep -c 'in: 5...30' fitbod/Settings/SettingsView.swift` returns 1 (calibration stepper range).
    - `grep -cE 'Used when an exercise has no specific increment' fitbod/Settings/SettingsView.swift` returns 1 (UI-SPEC verbatim footer).
  </acceptance_criteria>
  <done>PlateInventoryEditor is reachable from Settings, edits write through live, Reset confirmation works, Add Plate sheet works, and the Smart Progression section in SettingsView exposes the two new UserSettings steppers with UI-SPEC-verbatim copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlateCalculatorSheet (Try-It standalone calculator) + #Preview wiring</name>
  <files>
    fitbod/Settings/PlateCalculatorSheet.swift
  </files>
  <read_first>
    @fitbod/Sessions/DecimalRPEPickerSheet.swift,
    @fitbod/Models/PlateInventory.swift,
    @fitbod/Prescription/PlateCalculator.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @fitbod/Persistence/PreviewModelContainer.swift
  </read_first>
  <behavior>
    - PlateCalculatorSheet is a sheet rendered at .medium detent (caller passes .presentationDetents([.medium]) per UI-SPEC § Plate-stack inline disclosure — though the standalone Try-It calculator is technically a separate surface, it shares the same detent contract).
    - Inputs: `let equipment: PlateEquipmentKind`, `let inventory: PlateInventory`.
    - State: `@State private var targetWeightText: String = "60"`, `@State private var barWeightText: String = ""` (default-init from inventory.barWeight on appear).
    - Body: NavigationStack { Form { Section { TextField("Target weight", text: $targetWeightText).keyboardType(.decimalPad) }; Section { TextField("Bar weight", text: $barWeightText).keyboardType(.decimalPad) } header: { Text("Bar weight") } footer: { if barWeightExceedsTarget { Text("Bar weight exceeds target. Check the bar weight.").font(.caption).foregroundStyle(Color(.systemRed)) } }; Section { plateStackView } header: { Text("Plate stack") } footer: { if solvedStack == nil { Text("No combination found").foregroundStyle(.secondary) } } }.navigationTitle("Plate Calculator").navigationBarTitleDisplayMode(.inline).toolbar { ToolbarItem(placement: .topBarTrailing) { Button("Done") { dismiss() } } } }.
    - plateStackView: a horizontal HStack of Rectangle shapes per plate per side (left mirrored), bar segment in the center, plate weight text labels per UI-SPEC § Asset Contract color palette. Use the inventory.availablePlates sorted heaviest-first for the per-side composition computed from PlateCalculator.solve(...).
    - solvedStack: computed `PlateStack?` via PlateCalculator.solve(target: parsedTarget, barWeight: parsedBar, plates: inventory.availablePlates.map { (weight: $0.weight, countPerSide: $0.countPerSide) }).
    - barWeightExceedsTarget: computed Bool — true iff parsedBar > parsedTarget AND parsedTarget > 0.
    - "Open Plate Inventory" deep-link from the no-solution state is OUT OF SCOPE for this plan — UI-SPEC marks it as an empty-state action but the sheet is already presented FROM the inventory editor, so the empty-state body for this sheet uses just the "No combination found" inline footer for now (deferred polish).
    - Add a #Preview block: `#Preview("plate calculator") { @Previewable @State var present = true; let container = PreviewModelContainer.make(); let ctx = container.mainContext; let inv = PlateInventory(); inv.equipmentKind = .barbell; inv.barWeight = 20; inv.availablePlates = PlateInventoryDefaults.make(for: .barbell, unitSystem: .kg); ctx.insert(inv); try? ctx.save(); return Color.clear.sheet(isPresented: $present) { PlateCalculatorSheet(equipment: .barbell, inventory: inv).presentationDetents([.medium]) }.modelContainer(container) }`.
  </behavior>
  <action>
    Create fitbod/Settings/PlateCalculatorSheet.swift mirroring fitbod/Sessions/DecimalRPEPickerSheet.swift verbatim shape for the NavigationStack + Done toolbar:
    - imports: SwiftUI + SwiftData.
    - `public struct PlateCalculatorSheet: View` with `@Environment(\\.dismiss) private var dismiss`, `public let equipment: PlateEquipmentKind`, `public let inventory: PlateInventory`, and the @State fields per <behavior>.
    - Implement body per <behavior>. For the plateStackView, use a horizontal HStack composing per-plate Rectangles with .frame(width: weightToPxWidth(plate.weight), height: 24) and the semantic system colors per UI-SPEC § Asset Contract (Color(.systemRed).opacity(0.8) for >= 25kg, Color(.systemBlue).opacity(0.8) for 20kg, Color(.systemYellow).opacity(0.8) for 15kg, Color(.systemGreen).opacity(0.8) for 10kg, Color(.systemGray5) for 5kg, Color(.systemGray3) for 2.5kg, Color(.systemGray6) with Color(.systemGray2) border for <2.5kg microplates). Bar center: Rectangle.fill(Color(.systemGray4)).frame(width: 60, height: 6). Reduce plate stack visualization to a single horizontal line — under each plate render Text("\\(plate.weight, specifier: \"%g\")") in .caption .secondaryLabel per UI-SPEC § Color tertiary text rule. Pure SwiftUI shapes — no Canvas.

    Add the #Preview block per <behavior>.

    Do NOT add the standalone calculator as a top-level navigation destination — UI-SPEC § What's Explicitly Deferred line "PlateCalculatorSheet as a standalone reachable from anywhere" is deferred to Phase 6 polish. This sheet is only reachable from PlateInventoryEditor's toolbar trailing "Calculator" button (Task 2 wired it).
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct PlateCalculatorSheet' fitbod/Settings/PlateCalculatorSheet.swift && grep -c 'PlateCalculator.solve' fitbod/Settings/PlateCalculatorSheet.swift && grep -c 'Plate Calculator' fitbod/Settings/PlateCalculatorSheet.swift && grep -c '#Preview' fitbod/Settings/PlateCalculatorSheet.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Each grep returns at least 1.
    - `grep -cE 'Color\(\.systemRed\)|Color\(\.systemBlue\)|Color\(\.systemYellow\)|Color\(\.systemGreen\)' fitbod/Settings/PlateCalculatorSheet.swift` returns at least 4 (asset contract plate colors present).
    - `grep -c 'No combination found' fitbod/Settings/PlateCalculatorSheet.swift` returns 1 (UI-SPEC verbatim empty state).
  </acceptance_criteria>
  <done>Try-It plate calculator sheet ships; reachable from PlateInventoryEditor toolbar trailing Calculator button; renders live plate-stack visualization against editable target/bar weight inputs.</done>
</task>

</tasks>

<verification>
- New PlateInventory* tests from plan 03-02 remain GREEN.
- A clean install of the app populates 4 PlateInventory rows on first launch (manual verification via the app — the seeder ran inside fitbodApp's .task block).
- Settings → Smart Progression → Plate Inventory navigates to the editor; all 4 tabs render; Reset to Defaults alert appears with verbatim copy; Add Plate sheet appears; Calculator sheet appears.
- xcodebuild test fitbodTests still GREEN end-to-end (no regression to existing suites).
</verification>

<success_criteria>
- 5 new files under fitbod/Settings/ (defaults factory, seeder, editor, calculator sheet) + SettingsView extension + fitbodApp seeding hook.
- First-launch seeding produces 4 PlateInventory rows; idempotent on subsequent launches.
- PlateInventoryEditor is reachable, tabbed, editable, with Reset / Add / Calculator / Delete affordances.
- SettingsView exposes two new Steppers (default-increment + calibration-window) with UI-SPEC verbatim footers.
- All UI copy matches UI-SPEC § PlateInventoryEditor / § Settings — Smart Progression / § Plate-stack inline disclosure verbatim.
</success_criteria>

<output>
Create `.planning/phases/03-smart-prescription-warm-ups/03-04-SUMMARY.md` listing the 5 new files, the SettingsView additions, the seeding hook into fitbodApp, and the closed requirements (SET-03, SET-04, SET-07, PRES-08 inventory-side). Commit per atomic-commit convention: one commit for Task 1 (defaults + seeder), one for Task 2 (editor + Settings extension), one for Task 3 (calculator sheet).
</output>
