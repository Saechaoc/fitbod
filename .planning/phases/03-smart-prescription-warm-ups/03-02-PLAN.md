---
phase: 03-smart-prescription-warm-ups
plan: 02
type: tdd
wave: 1
depends_on: ["03-01"]
files_modified:
  - fitbodTests/TuchschererTableTests.swift
  - fitbodTests/RPEAutoregStrategyTests.swift
  - fitbodTests/DoubleProgressionStrategyTests.swift
  - fitbodTests/PrescriptionExplanationTests.swift
  - fitbodTests/ProgressionRoundingTests.swift
  - fitbodTests/PlateCalculatorTests.swift
  - fitbodTests/WarmupRampTests.swift
  - fitbodTests/WarmupConfigTests.swift
  - fitbodTests/SchemaV3MigrationTests.swift
  - fitbodTests/PlateInventoryTests.swift
  - fitbodTests/ManualOverrideTests.swift
  - fitbodTests/SessionFactoryPhase3Tests.swift
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03, PRES-04, PRES-07, PRES-08, PRES-09, PRES-10, WARM-01, WARM-02, WARM-03, SET-02, SET-03, SET-04, SET-07]

must_haves:
  truths:
    - "All 12 Wave-0 test suites compile and are discoverable by Swift Testing"
    - "SchemaV3MigrationTests is GREEN immediately (depends only on plan 03-01 deliverables)"
    - "PlateInventoryTests is GREEN immediately (depends only on plan 03-01 deliverables)"
    - "WarmupConfigTests is GREEN immediately (depends only on plan 03-01 deliverables — Codable round-trip on the WarmupConfig struct)"
    - "Pure-function and integration suites (TuchschererTableTests, RPEAutoregStrategyTests, DoubleProgressionStrategyTests, PrescriptionExplanationTests, ProgressionRoundingTests, PlateCalculatorTests, WarmupRampTests, ManualOverrideTests, SessionFactoryPhase3Tests) compile but expect implementations from plans 03-03 to 03-08 — RED tests use `#expect(false, ...)` or `Issue.record(...)` placeholders that downstream plans will replace with real assertions"
  artifacts:
    - path: "fitbodTests/SchemaV3MigrationTests.swift"
      provides: "Migration plan registers V3 + lightweight stage; in-memory V3 container round-trips PlateInventory + the new additive fields"
      contains: "@Suite(\"SchemaV3Migration\")"
    - path: "fitbodTests/PlateInventoryTests.swift"
      provides: "JSON [PlateSpec] round-trip via Data accessor; equipmentKind enum accessor; default fields"
      contains: "@Suite(\"PlateInventory\""
    - path: "fitbodTests/WarmupConfigTests.swift"
      provides: "WarmupConfig Codable round-trip; nil semantics on RoutineExercise.warmupOverride; skipNextSession default"
      contains: "@Suite(\"WarmupConfig\""
    - path: "fitbodTests/TuchschererTableTests.swift"
      provides: "Parameterized 90-cell snapshot + clamp-to-10 behavior (pending plan 03-03)"
      contains: "@Suite(\"TuchschererTable\""
    - path: "fitbodTests/PlateCalculatorTests.swift"
      provides: "Greedy heaviest-first plate stack; epsilon floats; no-solution case (pending plan 03-03)"
      contains: "@Suite(\"PlateCalculator\""
  key_links:
    - from: "fitbodTests/*Tests.swift"
      to: "fitbod/Persistence/SchemaV3.swift"
      via: "Schema(SchemaV3.models) + FitbodSchemaMigrationPlan.self"
      pattern: "SchemaV3"
    - from: "fitbodTests/SchemaV3MigrationTests.swift"
      to: "fitbod/Models/PlateInventory.swift"
      via: "context.insert(PlateInventory()) + fetch round-trip"
      pattern: "PlateInventory"
---

<objective>
Land the 12 test suites listed in 03-VALIDATION.md as Wave 0 deliverables. Three of them (SchemaV3MigrationTests, PlateInventoryTests, WarmupConfigTests) are GREEN immediately because they exercise only the artifacts that plan 03-01 ships. The remaining nine are compile-clean RED scaffolds — they import the symbol names that downstream plans (03-03 through 03-08) will create, and use `#expect(false, ...)` or `Issue.record(...)` placeholders inside each `@Test` body. Each placeholder cites the specific behavior it expects, sourcing from RESEARCH.md + UI-SPEC.md verbatim. Downstream plans replace the placeholders with real assertions as they ship.

Purpose: TDD per phase policy (FOUND-07). Tests exist BEFORE the implementations so every downstream plan can attach `<verify><automated>` blocks to a concrete test suite name. The 03-VALIDATION.md "Per-Task Verification Map" can be honored end-to-end without scrambling for test names mid-execution.

Output: 12 compile-clean Swift Testing suites under fitbodTests/. 3 GREEN, 9 RED. The xcodebuild test command runs and reports the expected pass/fail counts at end-of-plan: 3 suites all pass, 9 suites have at least one explicitly-failing test that documents the missing implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smart-prescription-warm-ups/03-VALIDATION.md
@.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md
@.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
@.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md
@.planning/phases/03-smart-prescription-warm-ups/03-01-PLAN.md
@fitbodTests/PreviousMatchingIntentTests.swift
@fitbodTests/SchemaV2MigrationTests.swift
@fitbodTests/SessionFactoryTests.swift
@fitbodTests/UserSettingsTests.swift
@fitbodTests/EnumPersistenceTests.swift

<interfaces>
Existing fixture pattern from fitbodTests/PreviousMatchingIntentTests.swift that every Phase 3 test suite mirrors verbatim (Phase 3 substitutes SchemaV3 in place of SchemaV2):

- File header: `import Foundation`, `import SwiftData`, `import Testing`, `@testable import fitbod`.
- Suite declaration: `@MainActor` (only when calling SwiftData / @MainActor-isolated code), `@Suite("SuiteName", .serialized)` (.serialized for anything touching shared state — UserDefaults or ModelContainer), `struct SuiteNameTests { ... }`.
- makeContext helper: `private func makeContext() throws -> ModelContext { let schema = Schema(SchemaV3.models); let config = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true); let container = try ModelContainer(for: schema, migrationPlan: FitbodSchemaMigrationPlan.self, configurations: config); return ModelContext(container) }`.
- Pure-function suites that touch no ModelContext omit @MainActor and .serialized (TuchschererTableTests, PlateCalculatorTests, WarmupRampTests).

Test name conventions used by 03-VALIDATION.md (planner must match these literally so the verify commands resolve):
- TuchschererTableTests, RPEAutoregStrategyTests, DoubleProgressionStrategyTests, PrescriptionExplanationTests, ProgressionRoundingTests, PlateCalculatorTests, WarmupRampTests, WarmupConfigTests, SchemaV3MigrationTests, PlateInventoryTests, ManualOverrideTests, SessionFactoryPhase3Tests.

SchemaV2MigrationTests structure (template for SchemaV3MigrationTests — verbatim shape):
- Test: "SchemaV2 models list contains every V1 entity plus 3 new types" → mirrored as "SchemaV3 models list equals SchemaV2.models + PlateInventory.self".
- Test: "FitbodSchemaMigrationPlan registers V1 and V2 and a single lightweight stage" → mirrored as "FitbodSchemaMigrationPlan registers V1+V2+V3 and TWO lightweight stages".
- Test: "Fresh in-memory V2 ModelContainer opens; round-trips a Routine + new entity" → mirrored as round-trip of PlateInventory.

Existing fitbodTests directory is FLAT (not nested by folder). All 12 new files go directly under fitbodTests/.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN suites — SchemaV3MigrationTests + PlateInventoryTests + WarmupConfigTests</name>
  <files>
    fitbodTests/SchemaV3MigrationTests.swift,
    fitbodTests/PlateInventoryTests.swift,
    fitbodTests/WarmupConfigTests.swift
  </files>
  <read_first>
    @fitbodTests/SchemaV2MigrationTests.swift,
    @fitbodTests/PreviousMatchingIntentTests.swift,
    @fitbodTests/UserSettingsTests.swift,
    @fitbod/Persistence/SchemaV3.swift,
    @fitbod/Persistence/FitbodSchemaMigrationPlan.swift,
    @fitbod/Models/PlateInventory.swift,
    @fitbod/Models/PlateSpec.swift,
    @fitbod/Models/WarmupConfig.swift,
    @fitbod/Models/RoutineExercise.swift
  </read_first>
  <behavior>
    SchemaV3MigrationTests (4 @Test functions):
    - schemaV3ModelsListEqualsV2PlusPlateInventory: assert `SchemaV3.models.count == SchemaV2.models.count + 1` AND `SchemaV3.models.contains(where: { $0 == PlateInventory.self })` AND for every entry in `SchemaV2.models` it is also present in `SchemaV3.models`.
    - migrationPlanWiringIsV1V2V3: assert `FitbodSchemaMigrationPlan.schemas.count == 3` AND last schema is `SchemaV3.self`. Assert `FitbodSchemaMigrationPlan.stages.count == 2`.
    - freshV3ContainerRoundTripsPlateInventory: create in-memory V3 ModelContainer; insert one PlateInventory with availablePlates = [PlateSpec(weight: 20, countPerSide: 4), PlateSpec(weight: 5, countPerSide: 2)]; save; refetch via FetchDescriptor; assert refetched.availablePlates round-trip equals input (Equatable) AND equipmentKind defaults to .barbell AND barWeight defaults to 20.0.
    - additiveFieldsRoundTrip: insert a UserSettings via UserSettings.default(); assert defaultIncrementKg == 2.5 AND minCalibrationSets == 10. Insert an Exercise; assert smallestIncrement == nil AND barWeightOverride == nil AND unitOverride == nil. Insert a SetEntry; assert wasManualOverride == false.

    PlateInventoryTests (4 @Test functions):
    - jsonRoundTripPreservesAllFields: PlateInventory(); inv.availablePlates = [PlateSpec(weight: 25, countPerSide: 4, color: "red"), PlateSpec(weight: 2.5, countPerSide: 2, color: nil)]; assert inv.availablePlates Equatable-equals input; assert inv.availablePlatesData is non-empty Data.
    - emptyPlateArraySerializesAsEmptyJSON: inv.availablePlates = []; assert get returns [] AND availablePlatesData decodes back to [].
    - equipmentKindAccessorFallbackOnBadRaw: inv.equipmentKindRaw = "nonsense_kind"; assert inv.equipmentKind == .barbell (graceful fallback).
    - equipmentKindAccessorRoundTrip: inv.equipmentKind = .ezBar; assert inv.equipmentKindRaw == "ez_bar"; assert inv.equipmentKind == .ezBar.

    WarmupConfigTests (4 @Test functions):
    - codableRoundTripPreservesEnabledAndSkipNextSession: encode WarmupConfig(enabled: false, skipNextSession: true); decode; assert equal.
    - defaultsAreEnabledTrueSkipFalse: WarmupConfig(); assert enabled == true AND skipNextSession == false.
    - routineExerciseWarmupOverrideNilByDefault: RoutineExercise(); assert re.warmupOverrideData == nil AND re.warmupOverride == nil.
    - routineExerciseWarmupOverrideSetGetRoundTrip: re.warmupOverride = WarmupConfig(enabled: false, skipNextSession: true); assert re.warmupOverrideData != nil; assert re.warmupOverride == WarmupConfig(enabled: false, skipNextSession: true).
  </behavior>
  <action>
    Create fitbodTests/SchemaV3MigrationTests.swift mirroring fitbodTests/SchemaV2MigrationTests.swift verbatim shape: same imports, same `@Suite("SchemaV3Migration")` style (NO @MainActor needed for the metadata assertions on schemas/stages count; ADD @MainActor + .serialized for the round-trip test that touches ModelContainer). Use the PreviousMatchingIntentTests makeContext helper pattern for the round-trip test. Write all 4 @Test functions implementing the behaviors in <behavior>.

    Create fitbodTests/PlateInventoryTests.swift: `@MainActor`, `@Suite("PlateInventory", .serialized)`. Use makeContext helper (Schema(SchemaV3.models)). Write all 4 @Test functions. PlateInventory and PlateSpec instances can be constructed in-memory without `context.insert(...)` for the JSON round-trip tests (the JSON accessor lives on the @Model class but doesn't require persistence to operate).

    Create fitbodTests/WarmupConfigTests.swift: `@MainActor`, `@Suite("WarmupConfig", .serialized)`. Use makeContext helper (Schema(SchemaV3.models)) for the RoutineExercise tests (the @Model class needs a context). Write all 4 @Test functions. The Codable round-trip test does NOT need a context (pure JSON encode/decode on the value type).

    Every @Test function uses `#expect(...)` only — NO Issue.record placeholders in this task, these suites are GREEN. Use `try` for context-using tests and `throws` on the function signatures.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/SchemaV3MigrationTests -only-testing fitbodTests/PlateInventoryTests -only-testing fitbodTests/WarmupConfigTests 2>&1 | tail -50 | grep -E 'Test Suite.*passed|Test Suite.*failed|error:'</automated>
  </verify>
  <acceptance_criteria>
    - All 12 @Test functions across the 3 suites PASS (4 + 4 + 4).
    - xcodebuild output contains 3 lines matching `Test Suite '.*' passed`.
    - No `error:` lines in the build/test output.
    - `grep -c '@Test' fitbodTests/SchemaV3MigrationTests.swift` returns 4.
    - `grep -c '@Test' fitbodTests/PlateInventoryTests.swift` returns 4.
    - `grep -c '@Test' fitbodTests/WarmupConfigTests.swift` returns 4.
  </acceptance_criteria>
  <done>The three suites that have everything they need from plan 03-01 are landed and GREEN. They are now permanent regression guards on SchemaV3 wiring, PlateInventory JSON round-trip, and WarmupConfig round-trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED scaffolds — pure-function suites (TuchschererTable, PlateCalculator, WarmupRamp, Calibration-via-RPEAutoreg)</name>
  <files>
    fitbodTests/TuchschererTableTests.swift,
    fitbodTests/PlateCalculatorTests.swift,
    fitbodTests/WarmupRampTests.swift,
    fitbodTests/RPEAutoregStrategyTests.swift,
    fitbodTests/DoubleProgressionStrategyTests.swift,
    fitbodTests/PrescriptionExplanationTests.swift,
    fitbodTests/ProgressionRoundingTests.swift
  </files>
  <read_first>
    @fitbodTests/PreviousMatchingIntentTests.swift,
    @fitbodTests/EnumTests.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-VALIDATION.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    Each suite below is a compile-clean RED scaffold. Every `@Test` function body issues a single `Issue.record("PENDING IMPL — replaced by plan 03-XX: <behavior expected>")` line referencing the downstream plan that will replace the placeholder. The function bodies otherwise contain only setup that compiles against types shipped by plan 03-01 (TuchschererTable, PlateCalculator, etc. do NOT exist yet — so the test bodies must NOT reference those symbols; they only state the expectation in the Issue.record string).

    TuchschererTableTests (5 @Test scaffolds — replaced by plan 03-03):
    - rpe10reps1Returns1_000 — "TuchschererTable.percent(reps:1, rpe:10.0) must equal 1.000"
    - rpe8reps5Returns0_811 — "TuchschererTable.percent(reps:5, rpe:8.0) must equal 0.811"
    - rpe6reps10Returns0_656 — "TuchschererTable.percent(reps:10, rpe:6.0) must equal 0.656"
    - clampRepsAboveTen — "reps > 10 must clamp to reps = 10 (per CONTEXT.md Area 1 lock, revised 2026-05-22): percent(reps:12, rpe:8.0) == percent(reps:10, rpe:8.0)"
    - nearestRPESnap — "RPE 8.3 must snap to RPE 8.5; percent(reps:5, rpe:8.3) == percent(reps:5, rpe:8.5)"

    PlateCalculatorTests (5 scaffolds — replaced by plan 03-03):
    - solve100kgWith20kgBar — "100kg target / 20kg bar / [25×4,20×2,15×2,10×2,5×2,2.5×2,1.25×2] returns stack {1×25,1×15} per side, totalWeight=100"
    - roundDown102_5kgWith2_5kgIncrement — "roundDown returns 100.0 if 102.5 not loadable with given inventory"
    - belowBarReturnsBar — "roundDown(target:15, bar:20, plates:...) returns 20.0"
    - epsilonFloatDriftGuard — "roundDown(target:97.4999, bar:20, plates:standard) returns same as roundDown(target:97.5, ...) (no float-drift mis-solve per RESEARCH §Pitfall 4)"
    - noSolutionReturnsNil — "solve(target:1000, bar:20, plates:[20×2]) returns nil"

    WarmupRampTests (5 scaffolds — replaced by plan 03-05):
    - barbellCompoundAtTopGenerates4Sets — "WarmupRamp.generate(top:100, bar:20, plates:standardKg) returns 4 SetEntry rows with weights ≤ 0.40*100, 0.60*100, 0.75*100, 0.90*100 and reps 5,3,2,1"
    - dumbbellHalvesTo2Sets — "dumbbell equipment returns 2 SetEntry rows (60%×3, 90%×1)"
    - lightWeightSkipsRamp — "shouldGenerate(for: sessionExercise with working weight < 1.5×barWeight) returns false"
    - bodyweightSkipsRamp — "shouldGenerate(for: sessionExercise with bodyweight equipment) returns false"
    - deloadActiveSkipsRamp — "shouldGenerate(deloadActive: true) returns false even on qualifying compound"

    RPEAutoregStrategyTests (5 scaffolds — replaced by plan 03-05):
    - calibratingBelowThresholdShowsRange — "history < minCalibrationSets returns explanation with CalibrationStatus .calibrating(n, threshold) AND PrescriptionExplanation.range non-nil"
    - calibratedAboveThresholdReturnsPointEstimate — "history >= minCalibrationSets returns CalibrationStatus .calibrated AND PrescriptionExplanation.range == nil"
    - nilRPEInHistoryIsExcluded — "history points with nil rpe are skipped (RESEARCH §Pitfall 7)"
    - emptyHistoryReturnsFirstSessionExplanation — "empty history returns lastSessionLine == nil and a non-zero startWeight from caller-supplied prior weight hint OR 0 if absent"
    - tuchschererBackCalcUsedBelowThreshold — "below threshold: prescribedWeight derived from prior actualRPE+actualReps via TuchschererTable.percent (e1RM = actualWeight / percent)"

    DoubleProgressionStrategyTests (5 scaffolds — replaced by plan 03-05):
    - bumpWhenAllSetsHitTopOfRange — "history where every working set reps == targetRepsHigh triggers bump; explanation.bumpOccurred == true; prescribedWeight == lastWeight + smallestIncrement"
    - noBumpWhenAnySetMissesTop — "history with any working set reps < targetRepsHigh holds weight; explanation.bumpOccurred == false; prescribedWeight == lastWeight"
    - noBumpWhenWarmupsExcluded — "warmup sets (isWarmup == true) must not count toward the bump trigger"
    - noBumpFirstSessionUsesPriorHint — "empty history returns lastSessionLine == nil; prescribedWeight comes from caller-supplied hint"
    - smallestIncrementHonored — "increment of 1.25 kg used when Exercise.smallestIncrement is 1.25"

    PrescriptionExplanationTests (5 scaffolds — replaced by plan 03-05):
    - constructionExposesAllFields — "PrescriptionExplanation(lastSessionLine:formulaName:computedLine:roundedWeight:roundedLine:status:bumpOccurred:range:) initializer returns a value with those fields"
    - calibrationStatusEquatable — "CalibrationStatus.calibrated == .calibrated; .calibrating(4,10) == .calibrating(4,10); .calibrating(4,10) != .calibrating(5,10)"
    - sendableSafe — "PrescriptionExplanation value crosses Sendable boundaries (Task { let ex = explanation; ... } compiles)"
    - bumpOccurredFalseByDefault — "Memberwise init with bumpOccurred default false"
    - rangeNilByDefault — "range default nil"

    ProgressionRoundingTests (5 scaffolds — replaced by plan 03-05):
    - exerciseIncrementOverridesGlobal — "Exercise.smallestIncrement = 1.25 used in preference to UserSettings.defaultIncrementKg = 2.5"
    - globalDefaultUsedWhenNil — "Exercise.smallestIncrement == nil falls back to UserSettings.defaultIncrementKg"
    - kgVsLbUnitOverride — "Per-exercise Exercise.unitOverride does NOT affect rounding math (which stays canonical kg per RESEARCH); only display layer differs"
    - microplateIncrement — "0.5 kg increment respected end-to-end"
    - roundingDownNeverExceedsTarget — "Any rounded prescribed weight is <= raw target weight for all sample inputs"
  </behavior>
  <action>
    For each of the 7 files, create a Swift Testing suite with the imports `import Foundation`, `import Testing`, `@testable import fitbod` (and `import SwiftData` only for the suites that need ModelContext — TuchschererTableTests / PlateCalculatorTests / PrescriptionExplanationTests do NOT need it).

    Suite declarations:
    - TuchschererTableTests, PlateCalculatorTests, PrescriptionExplanationTests: `@Suite("SuiteName")` (NO @MainActor, NO .serialized — pure value-type tests).
    - WarmupRampTests, RPEAutoregStrategyTests, DoubleProgressionStrategyTests, ProgressionRoundingTests: `@Suite("SuiteName")` — these are pure functions over HistoryPoint arrays per RESEARCH, so still NO ModelContext, NO @MainActor.

    Each @Test function body contains exactly one line: `Issue.record("PENDING IMPL — replaced by plan 03-XX: <verbatim expectation string from <behavior> above>")`. The string MUST be verbatim what `<behavior>` lists for that test name so downstream planners can grep for "PENDING IMPL" and find their work targets.

    Do NOT reference symbol names that plan 03-01 did not ship (TuchschererTable, PlateCalculator, WarmupRamp, RPEAutoregStrategy, DoubleProgressionStrategy, ProgressionStrategy protocol, PrescriptionExplanation, CalibrationStatus, HistoryPoint — none of these exist yet). The Issue.record string is plain text; the test body has no other code.

    Function naming: keep names short and Swift-friendly. Don't use underscores in test names except where required to express decimal numbers (e.g. `rpe10reps1Returns1_000` is fine).

    Each @Test function must have signature `() throws -> Void` (no throws needed since Issue.record doesn't throw, but the signature must permit `throws` so that plans 03-03/03-05 can replace bodies with `try`-using assertions without changing the function signature).
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/TuchschererTableTests -only-testing fitbodTests/PlateCalculatorTests -only-testing fitbodTests/WarmupRampTests -only-testing fitbodTests/RPEAutoregStrategyTests -only-testing fitbodTests/DoubleProgressionStrategyTests -only-testing fitbodTests/PrescriptionExplanationTests -only-testing fitbodTests/ProgressionRoundingTests 2>&1 | tail -80 | grep -E 'Test (Suite|Case).*(passed|failed)|error:'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 suites compile (no `error:` lines in xcodebuild output).
    - Every @Test in these 7 suites reports as FAILED (35 expected failures: 7 suites × 5 tests).
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/TuchschererTableTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/PlateCalculatorTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/WarmupRampTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/RPEAutoregStrategyTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/DoubleProgressionStrategyTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/PrescriptionExplanationTests.swift` returns 5.
    - `grep -c 'Issue.record(\"PENDING IMPL' fitbodTests/ProgressionRoundingTests.swift` returns 5.
  </acceptance_criteria>
  <done>Compile-clean RED scaffolds exist for every pure-function suite that plans 03-03 + 03-05 must turn green. Each PENDING IMPL string is a precise contract the executor of those plans replaces with real assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED scaffolds — integration suites (ManualOverrideTests + SessionFactoryPhase3Tests)</name>
  <files>
    fitbodTests/ManualOverrideTests.swift,
    fitbodTests/SessionFactoryPhase3Tests.swift
  </files>
  <read_first>
    @fitbodTests/SessionFactoryTests.swift,
    @fitbodTests/PreviousMatchingIntentTests.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md,
    @.planning/phases/03-smart-prescription-warm-ups/03-VALIDATION.md
  </read_first>
  <behavior>
    ManualOverrideTests (4 scaffolds — replaced by plan 03-08):
    - manualOverrideFlagSetWhenWeightDiverges — "SetEntry.wasManualOverride becomes true when actualWeight diverges from rounded prescribed weight by more than exercise.smallestIncrement"
    - manualOverrideFlagFalseWhenWeightMatchesPrescription — "SetEntry.wasManualOverride stays false when actualWeight equals SessionExercise.prescribedWeight"
    - nextSessionReadsActualNotPrescribed — "PRES-07: progression next session reads SetEntry.actualWeight (the override) NOT SessionExercise.prescribedWeight (the original suggestion)"
    - userSettingsMinCalibrationSetsHonored — "SET-07: changing UserSettings.minCalibrationSets from 10 to 20 keeps RPEAutoregStrategy in calibrating mode until 20 sets are logged"

    SessionFactoryPhase3Tests (5 scaffolds — replaced by plan 03-08):
    - prescribedWeightSetOnSessionExercise — "PRES-01: after SessionFactory.start(...), every SessionExercise has non-nil prescribedWeight"
    - warmupSetEntriesInsertedForFirstQualifyingCompound — "WARM-01: first SessionExercise with mechanic==.compound AND equipment==.barbell gets 4 warmup SetEntry rows (isWarmup==true) with orderIndex 0-3"
    - workingSetsShiftAfterWarmupInsertion — "Working SetEntry rows have orderIndex >= warmup count (start at 4 when 4 warmups are inserted)"
    - secondQualifyingCompoundDoesNotGetWarmup — "WARM-01: only the FIRST qualifying compound receives ramp; second one gets working sets only"
    - warmupSkippedWhenWarmupConfigDisabled — "WARM-03: RoutineExercise.warmupOverride = WarmupConfig(enabled: false) suppresses ramp on that exercise"
  </behavior>
  <action>
    Create fitbodTests/ManualOverrideTests.swift mirroring fitbodTests/SessionFactoryTests.swift fixture shape: imports Foundation + SwiftData + Testing + @testable fitbod; `@MainActor`, `@Suite("ManualOverride", .serialized)`. Use makeContext helper (Schema(SchemaV3.models) + FitbodSchemaMigrationPlan.self + isStoredInMemoryOnly: true). Each @Test signature is `() throws -> Void`. Each body is `Issue.record("PENDING IMPL — replaced by plan 03-08: <verbatim expectation>")`.

    Create fitbodTests/SessionFactoryPhase3Tests.swift mirroring fitbodTests/SessionFactoryTests.swift verbatim: same imports, same `@MainActor + .serialized + makeContext` shape, but uses Schema(SchemaV3.models) instead of V2. Each @Test signature `() throws -> Void`. Each body is `Issue.record("PENDING IMPL — replaced by plan 03-08: <verbatim expectation>")`.

    Do NOT call SessionFactory.start in this task — the Phase 3 hooks don't exist yet (plan 03-08 adds them). The Issue.record string is the only line in each test body.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing fitbodTests/ManualOverrideTests -only-testing fitbodTests/SessionFactoryPhase3Tests 2>&1 | tail -30 | grep -E 'Test (Suite|Case).*(passed|failed)|error:'</automated>
  </verify>
  <acceptance_criteria>
    - Both suites compile (no `error:` lines).
    - All 9 @Test functions report FAILED (4 + 5 = 9 expected failures).
    - `grep -c 'Issue.record(\"PENDING IMPL — replaced by plan 03-08' fitbodTests/ManualOverrideTests.swift` returns 4.
    - `grep -c 'Issue.record(\"PENDING IMPL — replaced by plan 03-08' fitbodTests/SessionFactoryPhase3Tests.swift` returns 5.
    - `grep -c '@Suite(\"ManualOverride' fitbodTests/ManualOverrideTests.swift` returns 1.
    - `grep -c '@Suite(\"SessionFactoryPhase3' fitbodTests/SessionFactoryPhase3Tests.swift` returns 1.
  </acceptance_criteria>
  <done>RED integration scaffolds exist for plan 03-08's targets. The 03-VALIDATION.md per-task verification map can resolve every test suite name end-to-end.</done>
</task>

</tasks>

<verification>
- Full Phase 3 test suite count: 12 new suites. After this plan: 3 green, 9 red (44 explicit Issue.record placeholders + 12 actual GREEN tests = 56 total Phase-3 test cases at the end of this plan).
- Existing Phase 1 + Phase 2 suites remain GREEN — this plan adds only new files, modifies no existing test.
- xcodebuild test (full suite) reports the expected mix; the existing test infrastructure runs without breakage.
</verification>

<success_criteria>
- 12 new test files exist in fitbodTests/.
- 3 GREEN suites (12 tests) pass.
- 9 RED suites (44 tests) compile and explicitly fail with PENDING IMPL Issue.record messages naming the downstream plan that will replace them.
- The exact suite names from 03-VALIDATION.md are present (xcodebuild -only-testing resolves each one).
</success_criteria>

<output>
Create `.planning/phases/03-smart-prescription-warm-ups/03-02-SUMMARY.md` listing the 12 new files, the 3 GREEN / 9 RED partition, and the PENDING IMPL count. Commit per atomic-commit convention: one commit for Task 1 (GREEN suites), one commit for Task 2 (pure-function RED scaffolds), one commit for Task 3 (integration RED scaffolds).
</output>
