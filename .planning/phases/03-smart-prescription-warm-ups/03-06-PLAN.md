---
phase: 03-smart-prescription-warm-ups
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - fitbod/Sessions/WhyThisWeightSheet.swift
  - fitbod/Sessions/PrescriptionWeightCell.swift
  - fitbod/Sessions/PlateStackDisclosure.swift
  - fitbod/Sessions/BumpBanner.swift
  - fitbod/Sessions/CalibratingBadge.swift
  - fitbod/Sessions/WarmupRampRows.swift
autonomous: true
requirements: [PRES-02, PRES-10, PRES-08, WARM-01, WARM-02]

must_haves:
  truths:
    - "WhyThisWeightSheet renders a PrescriptionExplanation as a List with the UI-SPEC verbatim rows (Last session / Formula / Computed / Rounded / Status)"
    - "WhyThisWeightSheet uses .presentationDetents([.medium]) and .navigationBarTitleDisplayMode(.inline) per UI-SPEC + DecimalRPEPickerSheet precedent"
    - "PrescriptionWeightCell renders the prescribed weight TextField + info.circle accent button (44pt hit area) + optional 'M' override badge per UI-SPEC § Prescribed weight cell"
    - "PlateStackDisclosure renders HStack of colored Rectangles per plate per side per UI-SPEC § Asset Contract color palette; 200ms ease-in-out animation guarded on @Environment(.accessibilityReduceMotion)"
    - "BumpBanner is a 44pt pill with Color(.systemGreen).opacity(0.15) background (NOT accent per UI-SPEC) + UI-SPEC verbatim copy 'Bumping to {weight} kg — you cleared the top of the range last time.'"
    - "CalibratingBadge is Color(.systemGray5)-filled capsule with .secondaryLabel text (NOT accent — transitional state per UI-SPEC item 19 exclusion)"
    - "WarmupRampRows filters and renders SetEntry rows where isWarmup == true sorted by orderIndex, with UI-SPEC verbatim 'Working sets' divider + 'Skip warm-ups' text button"
    - "All UI copy matches UI-SPEC § WhyThisWeightSheet, § Bump banner, § Warm-up ramp rows, § Plate-stack inline disclosure, § Calibrating badge VERBATIM"
  artifacts:
    - path: "fitbod/Sessions/WhyThisWeightSheet.swift"
      provides: ".medium-detent sheet rendering PrescriptionExplanation value type"
      contains: "struct WhyThisWeightSheet: View"
    - path: "fitbod/Sessions/PrescriptionWeightCell.swift"
      provides: "Compound control: weight TextField + info.circle button + M badge"
      contains: "struct PrescriptionWeightCell: View"
    - path: "fitbod/Sessions/PlateStackDisclosure.swift"
      provides: "Inline plate-stack visualization with bar segment + plate rectangles"
      contains: "struct PlateStackDisclosure: View"
    - path: "fitbod/Sessions/BumpBanner.swift"
      provides: "44pt-tall pill banner with green-tinted background and tap-anywhere-dismiss"
      contains: "struct BumpBanner: View"
    - path: "fitbod/Sessions/CalibratingBadge.swift"
      provides: "Small capsule with dot + calibrating label in .caption + .secondaryLabel"
      contains: "struct CalibratingBadge: View"
    - path: "fitbod/Sessions/WarmupRampRows.swift"
      provides: "Filtered warmup-set ForEach + Working-sets divider + Skip-warmups text button"
      contains: "struct WarmupRampRows: View"
  key_links:
    - from: "fitbod/Sessions/WhyThisWeightSheet.swift"
      to: "fitbod/Prescription/ProgressionStrategy.swift"
      via: "consumes PrescriptionExplanation value type (status: CalibrationStatus)"
      pattern: "PrescriptionExplanation"
    - from: "fitbod/Sessions/PlateStackDisclosure.swift"
      to: "fitbod/Prescription/PlateCalculator.swift"
      via: "PlateCalculator.solve(target:barWeight:plates:)"
      pattern: "PlateCalculator.solve"
---

<objective>
Build the six new SwiftUI views that the session logger needs in Phase 3. None of them mutate any existing Phase 2 file — plan 03-08 owns wiring them into SessionExerciseCard / SetRow / SessionLoggerView. This plan keeps each component standalone with a `#Preview` block so it can be visually validated independently before the integration wave.

Purpose: Atomic component shipment. By keeping the integration changes out of this plan, we sidestep file-conflict races with plan 03-07 (which modifies RoutineBuilderView / RoutineExerciseCard / PrescriptionEditorRow + ExerciseDetailView). Wave 3 runs 03-06 + 03-07 in parallel; Wave 4 (03-08) merges them.

Output: 6 new files under fitbod/Sessions/. Each compiles standalone with a #Preview. UI-SPEC copy is verbatim throughout. No existing Phase 2 file is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md
@.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
@.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md
@fitbod/Sessions/DecimalRPEPickerSheet.swift
@fitbod/Sessions/SetRow.swift
@fitbod/Sessions/ResumeWorkoutBanner.swift
@fitbod/Sessions/SetTypeChip.swift
@fitbod/Sessions/PinnedNoteCapsule.swift
@fitbod/Sessions/SessionExerciseCard.swift
@fitbod/Prescription/ProgressionStrategy.swift
@fitbod/Prescription/PlateCalculator.swift
@fitbod/Models/PlateInventory.swift
@fitbod/Models/SetEntry.swift
@fitbod/Persistence/PreviewModelContainer.swift

<interfaces>
DecimalRPEPickerSheet shape (the exact analog for WhyThisWeightSheet) from fitbod/Sessions/DecimalRPEPickerSheet.swift:
- public struct ... : View with @Binding param and @Environment(\\.dismiss).
- NavigationStack { content + .navigationTitle + .navigationBarTitleDisplayMode(.inline) + .toolbar { ToolbarItem(.topBarTrailing) Button("Done") { dismiss() } } }.
- Caller uses .sheet(isPresented:) { ... .presentationDetents([.medium]) }.

ResumeWorkoutBanner shape (the exact analog for BumpBanner) from fitbod/Sessions/ResumeWorkoutBanner.swift:
- HStack with leading icon (Color.accentColor) + VStack text + Spacer + action buttons.
- .padding(16) + .background(Color(...)) + .clipShape(RoundedRectangle(cornerRadius: 12)) + .padding(.horizontal, 16) + .padding(.vertical, 8).
- BumpBanner substitutes Color(.systemGreen).opacity(0.15) for the background (NOT accent), Color(.label) for the primary text (NOT accent), and uses a single line of copy with tap-anywhere dismiss instead of "Resume" / "Discard" buttons.

SetTypeChip shape (analog for CalibratingBadge) from fitbod/Sessions/SetTypeChip.swift:
- Small inline label with chip/capsule shape; Color(.systemGray5) fill; .caption font; .secondaryLabel text per UI-SPEC item 19 exclusion (the calibrating badge is NOT accent — transitional state).

UI-SPEC § WhyThisWeightSheet content rows (verbatim):
- "Last session" label / "{weight} × {reps} @ RPE {rpe} ({day-of-week})" value OR "No prior data — starting fresh." value.
- "Formula" label / "RPE autoregulation" OR "Double progression" value.
- "Computed" label (only for RPE autoreg) / "Target e1RM {e1rm} kg → {pct}% × {reps} → {raw} kg" value.
- "Rounded" label / "→ {rounded} kg (rounded down to {increment} kg plates)" value.
- "Status" label / "Calibrating ({n} / {threshold} sets)" OR "Calibrated" value.
- For double progression: NO "Computed" row; "Status" row replaced by "All sets hit top of range last time → bumping by {increment} kg" when bumpOccurred; "No prior data — starting at prescribed weight." when first session.

UI-SPEC § Bump banner copy verbatim:
- text: "Bumping to {weight} kg — you cleared the top of the range last time."
- a11y label: "Weight bump: bumping to {weight} kg. Tap to dismiss."
- 44pt height per UI-SPEC § Spacing exception.
- Leading icon: `arrow.up.circle` (.caption-sized, NOT accent per UI-SPEC § SF Symbols).

UI-SPEC § Calibrating badge copy verbatim:
- label: "calibrating"
- a11y label: "Calibrating: {n} of {threshold} sets logged. Weight shown as a range."

UI-SPEC § Warm-up ramp rows copy verbatim:
- "W{N}" leading set-column label
- "{pct}% × {reps}" percentage hint in Previous column (.caption .secondaryLabel)
- "Working sets" divider label between warmup and working rows (.caption .secondaryLabel, centered)
- "Skip warm-ups" text button (.secondaryLabel — NOT accent)
- "Warm up however you'd like." placeholder when working weight < 1.5×bar

UI-SPEC § Plate-stack inline disclosure heading verbatim:
- "{bar} bar + {plates} each side" — e.g. "20 kg bar + 2×20 kg, 1×5 kg each side"
- a11y label: "Bar: {bar} kg. Plates each side: {plate list, e.g. two 20 kg, one 5 kg}. Total: {total} kg."
- Error states: "No plate combination found. Adjust inventory in Settings." (.caption .systemRed)
- Below-bar: "Target is below bar weight ({bar} kg). Log as bodyweight or adjust the bar." (.caption .secondaryLabel)

Plate-stack color palette (UI-SPEC § Asset Contract):
- ≥25kg / ≥45lb: Color(.systemRed).opacity(0.8)
- 20kg / 35lb: Color(.systemBlue).opacity(0.8)
- 15kg / 25lb: Color(.systemYellow).opacity(0.8)
- 10kg: Color(.systemGreen).opacity(0.8)
- 5kg / 10lb: Color(.systemGray5)
- 2.5kg / 5lb: Color(.systemGray3)
- <2.5kg microplates: Color(.systemGray6) with Color(.systemGray2) border
- Bar segment: Color(.systemGray4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WhyThisWeightSheet + PrescriptionWeightCell</name>
  <files>
    fitbod/Sessions/WhyThisWeightSheet.swift,
    fitbod/Sessions/PrescriptionWeightCell.swift
  </files>
  <read_first>
    @fitbod/Sessions/DecimalRPEPickerSheet.swift,
    @fitbod/Sessions/SetRow.swift,
    @fitbod/Prescription/ProgressionStrategy.swift,
    @fitbod/Models/SetEntry.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @fitbod/Persistence/PreviewModelContainer.swift
  </read_first>
  <behavior>
    - WhyThisWeightSheet is `public struct WhyThisWeightSheet: View` with `public let explanation: PrescriptionExplanation`, optional `public var onUseSuggested: (() -> Void)? = nil` (called when override is active and user taps "Use Suggested Weight" — caller from plan 03-08 writes back to SetEntry.actualWeight). Uses `@Environment(\\.dismiss) private var dismiss`.
    - Body: NavigationStack { List with sections per UI-SPEC § WhyThisWeightSheet content rows. Each row is `HStack(alignment: .firstTextBaseline) { Text(label).font(.headline).foregroundStyle(.secondary); Spacer(); Text(value).font(.body) }.accessibilityElement(children: .combine).accessibilityLabel("\\(label): \\(value)")`. Conditional render of the "Computed" row only when explanation.computedLine != nil. Status row renders different content based on CalibrationStatus case. For PrescriptionExplanation.bumpOccurred == true on double progression, status row shows the bump-line copy. For first-session double progression (lastSessionLine nil && formula == "Double progression"), the status row shows "No prior data — starting at prescribed weight.". For RPE autoreg with status .calibrated, render a capsule with Color.accentColor.opacity(0.15) fill + accent text per UI-SPEC item 19. For .calibrating, render Color(.systemGray5) capsule + .secondaryLabel text (transitional). Footer Section (when onUseSuggested != nil): Button("Use Suggested Weight") { onUseSuggested?(); dismiss() }.tint(.accentColor).buttonStyle(.borderedProminent).accessibilityLabel("Use suggested weight of \\(explanation.roundedWeight, specifier: \"%g\") kg"). }.navigationTitle("Why this weight?").navigationBarTitleDisplayMode(.inline) — no .toolbar Done button since iOS sheet dismiss gesture is the affordance per UI-SPEC.
    - PrescriptionWeightCell is `public struct PrescriptionWeightCell: View` with `@Binding public var weight: Double`, `public let prescribed: Double?`, `public let explanation: PrescriptionExplanation?`, `@Binding public var wasManualOverride: Bool`, `public var isComplete: Bool = false`, `public var onTapEmptyCell: () -> Void = {}`, `@State private var weightText: String = ""`, `@State private var showWhySheet: Bool = false`.
    - PrescriptionWeightCell body: HStack(spacing: 4) { TextField("—", text: $weightText).keyboardType(.decimalPad).frame(width: 60).onChange(of: weightText) { _, new in if let d = Double(new) { weight = d; wasManualOverride = (prescribed != nil) && abs(d - (prescribed ?? d)) > 0.001 } }; if wasManualOverride { Text("M").font(.caption).foregroundStyle(.secondary) }; if explanation != nil { Button { showWhySheet = true } label: { Image(systemName: "info.circle").foregroundStyle(Color.accentColor).contentShape(Rectangle()).frame(minWidth: 44, minHeight: 44) }.buttonStyle(.plain).accessibilityLabel("Why this weight?").accessibilityHint("Shows how this weight was calculated.") } }.padding(.vertical, 12).sheet(isPresented: $showWhySheet) { if let exp = explanation { WhyThisWeightSheet(explanation: exp).presentationDetents([.medium]) } }.onAppear { if let p = prescribed, weight == 0 { weightText = String(format: "%g", p); weight = p } else if weight > 0 { weightText = String(format: "%g", weight) } }.
    - Add #Preview blocks for both components using sample PrescriptionExplanation values (calibrating + calibrated + bump-occurred + first-session variants).
  </behavior>
  <action>
    Create fitbod/Sessions/WhyThisWeightSheet.swift mirroring fitbod/Sessions/DecimalRPEPickerSheet.swift verbatim shape for NavigationStack and dismissal:
    - imports: SwiftUI + SwiftData (for PreviewModelContainer in #Preview).
    - Implement per <behavior>. Status section content depends on explanation.status case + explanation.bumpOccurred + lastSessionLine. Use Group + switch on explanation.status to branch the status row content.
    - #Preview blocks: "calibrating" (status .calibrating(4,10), range != nil, RPE autoreg formula), "calibrated" (status .calibrated, range == nil, RPE autoreg), "bump occurred" (Double progression, bumpOccurred true), "first session double progression" (lastSessionLine nil, "No prior data — starting at prescribed weight."), "override active" (passes onUseSuggested non-nil; verifies the button renders).

    Create fitbod/Sessions/PrescriptionWeightCell.swift:
    - imports: SwiftUI.
    - Implement per <behavior>. The compound control replaces the plain weight TextField in SetRow (plan 03-08 wires this in). When prescribed is nil OR explanation is nil, the info.circle button is hidden (gracefully degrades for warmup rows or sets without a prescription).
    - #Preview blocks: "with prescription" (prescribed: 102.5, explanation: sample calibrated, weight starts at 0), "with override" (prescribed: 100, weight 105, wasManualOverride true — "M" badge visible).

    Use existing fitbod/Persistence/PreviewModelContainer.swift in #Preview wrappers via `.modelContainer(PreviewModelContainer.make())` when constructing sample model state.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct WhyThisWeightSheet' fitbod/Sessions/WhyThisWeightSheet.swift && grep -c 'struct PrescriptionWeightCell' fitbod/Sessions/PrescriptionWeightCell.swift && grep -c 'Why this weight?' fitbod/Sessions/WhyThisWeightSheet.swift && grep -c 'info.circle' fitbod/Sessions/PrescriptionWeightCell.swift && grep -c 'minWidth: 44, minHeight: 44' fitbod/Sessions/PrescriptionWeightCell.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Each grep returns at least 1.
    - `grep -c '#Preview' fitbod/Sessions/WhyThisWeightSheet.swift` returns at least 3 (multiple states previewed).
    - `grep -c '#Preview' fitbod/Sessions/PrescriptionWeightCell.swift` returns at least 2.
    - `grep -c 'Use Suggested Weight' fitbod/Sessions/WhyThisWeightSheet.swift` returns 1 (UI-SPEC verbatim).
    - `grep -c '.presentationDetents(\[.medium\])' fitbod/Sessions/WhyThisWeightSheet.swift` returns at least 1 (in #Preview wrappers).
  </acceptance_criteria>
  <done>WhyThisWeightSheet and PrescriptionWeightCell ship as standalone components with verbatim UI-SPEC copy + #Preview coverage of all PrescriptionExplanation states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlateStackDisclosure + BumpBanner + CalibratingBadge</name>
  <files>
    fitbod/Sessions/PlateStackDisclosure.swift,
    fitbod/Sessions/BumpBanner.swift,
    fitbod/Sessions/CalibratingBadge.swift
  </files>
  <read_first>
    @fitbod/Sessions/ResumeWorkoutBanner.swift,
    @fitbod/Sessions/SetTypeChip.swift,
    @fitbod/Prescription/PlateCalculator.swift,
    @fitbod/Models/PlateInventory.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @fitbod/Persistence/PreviewModelContainer.swift
  </read_first>
  <behavior>
    - PlateStackDisclosure is `public struct PlateStackDisclosure: View` with `public let targetWeight: Double`, `public let barWeight: Double`, `public let plates: [PlateSpec]`. `@Environment(\\.accessibilityReduceMotion) private var reduceMotion: Bool`.
    - Body computes `solvedStack: PlateStack?` via PlateCalculator.solve(target: targetWeight, barWeight: barWeight, plates: plates.map { (weight: $0.weight, countPerSide: $0.countPerSide) }).
    - If solvedStack == nil AND targetWeight < barWeight: render Text("Target is below bar weight (\\(barWeight, specifier: \"%g\") kg). Log as bodyweight or adjust the bar.").font(.caption).foregroundStyle(.secondary).
    - If solvedStack == nil AND targetWeight >= barWeight: render Text("No plate combination found. Adjust inventory in Settings.").font(.caption).foregroundStyle(Color(.systemRed)).
    - If solvedStack != nil: VStack(spacing: 4) { headingText, HStack(spacing: 4) renders left-side plate Rectangles (mirrored from right), bar segment Rectangle, right-side plate Rectangles, each plate has Text("\\(weight, specifier: \"%g\")").font(.caption).foregroundStyle(.secondary) underneath }. Heading text per UI-SPEC verbatim: "\\(barWeight, specifier: \"%g\") bar + \\(perSideDescription) each side".
    - Plate color picker: a private static helper plateColor(weight: Double, unit: WeightUnit) -> Color that returns the system color per UI-SPEC § Asset Contract. Default to kg-tier mappings since unit isn't passed (planner discretion — pass nothing and use the kg-based thresholds; lb thresholds are nearly identical in practice for the v1 default plate sets — document this limitation).
    - Each plate rectangle: `Rectangle().fill(plateColor(weight:)).frame(width: plateWidthForWeight(weight), height: 24)`. plateWidthForWeight uses a simple linear scale: max(8, min(32, weight)) — heavier plates render wider.
    - Bar segment: `Rectangle().fill(Color(.systemGray4)).frame(width: 40, height: 6)`.
    - Animation: wrap the disclosure content in `.animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: targetWeight)` per UI-SPEC.
    - .accessibilityElement(children: .ignore).accessibilityLabel("Bar: \\(barWeight) kg. Plates each side: \\(plateListDescription). Total: \\(solvedStack?.totalWeight ?? barWeight) kg.").
    - BumpBanner is `public struct BumpBanner: View` with `@Binding public var isVisible: Bool` and `public let bumpedToWeight: Double`.
    - Body: HStack(spacing: 12) { Image(systemName: "arrow.up.circle").font(.caption).foregroundStyle(.secondary); Text("Bumping to \\(bumpedToWeight, specifier: \"%g\") kg — you cleared the top of the range last time.").font(.headline).foregroundStyle(Color(.label)); Spacer() }.padding(.horizontal, 16).padding(.vertical, 8).frame(height: 44).background(Color(.systemGreen).opacity(0.15)).clipShape(RoundedRectangle(cornerRadius: 12)).padding(.horizontal, 16).padding(.vertical, 8).accessibilityLabel("Weight bump: bumping to \\(bumpedToWeight, specifier: \"%g\") kg. Tap to dismiss.").accessibilityHint("Tap anywhere on this exercise card to dismiss.").accessibilityAddTraits(.isButton).
    - Tap-dismiss is delegated to the parent SessionExerciseCard's onTapGesture (plan 03-08 wires this). BumpBanner does NOT add its own onTapGesture.
    - CalibratingBadge is `public struct CalibratingBadge: View` with `public let current: Int`, `public let threshold: Int`.
    - Body: HStack(spacing: 4) { Circle().fill(Color(.systemGray3)).frame(width: 6, height: 6); Text("calibrating").font(.caption).foregroundStyle(.secondary) }.padding(.horizontal, 8).padding(.vertical, 4).background(Color(.systemGray5)).clipShape(Capsule()).accessibilityLabel("Calibrating: \\(current) of \\(threshold) sets logged. Weight shown as a range.").accessibilityAddTraits(.isStaticText).
    - All three components have #Preview blocks.
  </behavior>
  <action>
    Create fitbod/Sessions/PlateStackDisclosure.swift per <behavior>. The plate-color helper is a file-private function. The plateListDescription is a String computed from solvedStack.platesPerSide as e.g. "2×20 kg, 1×5 kg".

    Create fitbod/Sessions/BumpBanner.swift mirroring fitbod/Sessions/ResumeWorkoutBanner.swift bannerBody pattern (lines 82-115 per PATTERNS.md) but with the green-tint background + headline copy + no action buttons.

    Create fitbod/Sessions/CalibratingBadge.swift mirroring fitbod/Sessions/SetTypeChip.swift capsule pattern.

    Add #Preview blocks: PlateStackDisclosure ("100 kg standard", "97.5 kg microplates", "no solution found", "below bar weight"); BumpBanner ("standard 102.5 kg bump"); CalibratingBadge ("4/10", "9/10").
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct PlateStackDisclosure' fitbod/Sessions/PlateStackDisclosure.swift && grep -c 'struct BumpBanner' fitbod/Sessions/BumpBanner.swift && grep -c 'struct CalibratingBadge' fitbod/Sessions/CalibratingBadge.swift && grep -c 'Bumping to' fitbod/Sessions/BumpBanner.swift && grep -c 'Color(.systemGreen).opacity(0.15)' fitbod/Sessions/BumpBanner.swift && grep -c 'Color(.systemGray5)' fitbod/Sessions/CalibratingBadge.swift && grep -c 'calibrating' fitbod/Sessions/CalibratingBadge.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Every grep returns at least 1.
    - `grep -cE 'Color\(\.systemRed\)|Color\(\.systemBlue\)|Color\(\.systemYellow\)|Color\(\.systemGreen\)|Color\(\.systemGray4\)' fitbod/Sessions/PlateStackDisclosure.swift` returns at least 4 (asset contract palette present).
    - `grep -c 'No plate combination found. Adjust inventory in Settings.' fitbod/Sessions/PlateStackDisclosure.swift` returns 1 (UI-SPEC verbatim error copy).
    - `grep -c 'accessibilityReduceMotion' fitbod/Sessions/PlateStackDisclosure.swift` returns 1.
  </acceptance_criteria>
  <done>PlateStackDisclosure, BumpBanner, CalibratingBadge ship as standalone components with verbatim UI-SPEC copy + colors + accessibility labels + #Preview coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WarmupRampRows component</name>
  <files>
    fitbod/Sessions/WarmupRampRows.swift
  </files>
  <read_first>
    @fitbod/Sessions/SessionExerciseCard.swift,
    @fitbod/Sessions/SetRow.swift,
    @fitbod/Sessions/SetTypeChip.swift,
    @fitbod/Models/SetEntry.swift,
    @fitbod/Models/SessionExercise.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-UI-SPEC.md,
    @fitbod/Persistence/PreviewModelContainer.swift
  </read_first>
  <behavior>
    - WarmupRampRows is `public struct WarmupRampRows: View` with `public let warmupSets: [SetEntry]`, `public let onSkip: () -> Void`, `public let sessionExercise: SessionExercise` (to compute % display from prescribedWeight on the SessionExercise).
    - Body: VStack(spacing: 4) { ForEach(sortedWarmups) { warmup in HStack(spacing: 8) { Text("W\\(warmup.orderIndex + 1)").font(.caption).foregroundStyle(.secondary); SetTypeChip-style label "warmup" (use existing SetTypeChip if it accepts a setType param; otherwise inline a Color(.systemBlue) capsule per UI-SPEC § Color "warm-up" set-type chip exception); Text(percentageHint(for: warmup)).font(.caption).foregroundStyle(.secondary); Spacer(); Text("\\(warmup.weight, specifier: \"%g\")").font(.body); Text("× \\(warmup.reps)").font(.body).foregroundStyle(.secondary) }.padding(.vertical, 12).accessibilityLabel(a11yLabel(for: warmup)) }; Button("Skip warm-ups") { onSkip() }.font(.caption).foregroundStyle(.secondary).frame(maxWidth: .infinity, alignment: .trailing).padding(.trailing, 16).accessibilityHint("Marks warm-ups as done without logging them."); Divider().overlay(Text("Working sets").font(.caption).foregroundStyle(.secondary).padding(.horizontal, 8).background(Color(.systemBackground))) }.
    - sortedWarmups: `warmupSets.sorted { $0.orderIndex < $1.orderIndex }`.
    - percentageHint(for: SetEntry): returns "40% × 5" / "60% × 3" / "75% × 2" / "90% × 1" based on orderIndex (use the per-step percentage from CONTEXT.md Area 3 — the executor knows the SetEntry was generated by WarmupRamp.generate so the percentage maps deterministically to orderIndex). For dumbbells (2 rows) — orderIndex 0 = "60% × 3", orderIndex 1 = "90% × 1".
    - To detect unilateral vs barbell ramp: warmupSets.count == 2 → unilateral (use dumbbell percent mapping); count == 4 → barbell (use barbell percent mapping); other counts → fallback "{pct based on set count fraction}%" (defensive).
    - a11yLabel(for: warmup): "Warm-up set \\(warmup.orderIndex + 1): \\(warmup.weight, specifier: \"%g\") kg × \\(warmup.reps) reps at \\(pct)% of working weight."
    - Light working weight placeholder (when warmupSets.isEmpty AND sessionExercise.exercise?.equipment ∈ {.barbell, .dumbbell}): Text("Warm up however you'd like.").font(.caption).foregroundStyle(.secondary).padding(.vertical, 12).
    - #Preview blocks: "4-set barbell ramp", "2-set dumbbell ramp", "empty warmups (placeholder)".
  </behavior>
  <action>
    Create fitbod/Sessions/WarmupRampRows.swift per <behavior>. Use the SortedForEach + conditional rendering pattern from PATTERNS.md "Sessions/WarmupRampRows.swift" reference block.

    Add #Preview blocks using PreviewModelContainer.make() and an in-memory SessionExercise + SetEntry array. The previews construct SetEntry instances manually with isWarmup = true + the proper orderIndex / weight / reps values.

    Do NOT modify SessionExerciseCard or SetRow — plan 03-08 owns those edits.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -10 && grep -c 'struct WarmupRampRows' fitbod/Sessions/WarmupRampRows.swift && grep -c 'Skip warm-ups' fitbod/Sessions/WarmupRampRows.swift && grep -c 'Working sets' fitbod/Sessions/WarmupRampRows.swift && grep -c 'Warm up however you'\''d like.' fitbod/Sessions/WarmupRampRows.swift</automated>
  </verify>
  <acceptance_criteria>
    - swiftc -parse prints zero error lines.
    - Every grep returns at least 1.
    - `grep -c '#Preview' fitbod/Sessions/WarmupRampRows.swift` returns at least 2 (multiple state previews).
    - `grep -cE 'W\\\\\(' fitbod/Sessions/WarmupRampRows.swift` returns at least 1 (W{N} leading label format present).
  </acceptance_criteria>
  <done>WarmupRampRows ships standalone with the 4-set / 2-set ramp display + Skip warm-ups text button + Working-sets divider + placeholder fallback, all UI-SPEC verbatim.</done>
</task>

</tasks>

<verification>
- All 6 new files compile under Swift 6 strict concurrency.
- #Preview blocks render correctly in Xcode previews (manual visual check — UI-SPEC compliance verified through visual inspection in Wave 4 plan 03-08's checkpoint).
- No existing Phase 2 file (SessionExerciseCard, SetRow, SessionLoggerView) is modified — plan 03-08 owns those edits.
- UI-SPEC § WhyThisWeightSheet, § Bump banner, § Calibrating badge, § Warm-up ramp rows, § Plate-stack inline disclosure copy is verbatim throughout.
</verification>

<success_criteria>
- 6 new files under fitbod/Sessions/.
- All copy matches UI-SPEC verbatim.
- Asset contract colors used for plates + green-tinted bump banner.
- 44pt minimum tap targets on info.circle + tap-anywhere-dismiss on BumpBanner.
- #Preview coverage for every prescription/calibration/bump state.
- No existing Phase 2 file modified.
</success_criteria>

<output>
Create `.planning/phases/03-smart-prescription-warm-ups/03-06-SUMMARY.md` listing the 6 new components, the UI-SPEC verbatim copy anchors, the #Preview coverage, and the integration deferred to plan 03-08. Commit per atomic-commit convention: one commit per task (Task 1 sheet + cell, Task 2 disclosure + banner + badge, Task 3 warmup rows).
</output>
