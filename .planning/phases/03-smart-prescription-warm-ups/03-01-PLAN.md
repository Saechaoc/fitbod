---
phase: 03-smart-prescription-warm-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fitbod/Models/Exercise.swift
  - fitbod/Models/RoutineExercise.swift
  - fitbod/Models/SetEntry.swift
  - fitbod/Models/UserSettings.swift
  - fitbod/Models/PlateInventory.swift
  - fitbod/Models/PlateSpec.swift
  - fitbod/Models/WarmupConfig.swift
  - fitbod/Models/Enums/EquipmentKind.swift
  - fitbod/Persistence/SchemaV3.swift
  - fitbod/Persistence/FitbodSchemaMigrationPlan.swift
  - fitbod/Persistence/PreviewModelContainer.swift
  - fitbod/fitbodApp.swift
autonomous: true
requirements: [SET-02, SET-03, SET-04, SET-07, PRES-07, WARM-03]

must_haves:
  truths:
    - "Fresh in-memory ModelContainer constructed with Schema(SchemaV3.models) opens without crash"
    - "FitbodSchemaMigrationPlan.schemas == [SchemaV1, SchemaV2, SchemaV3] and stages == [migrateV1toV2, migrateV2toV3]"
    - "PlateInventory entity is insertable and its availablePlatesData Data field round-trips a [PlateSpec] via JSON"
    - "Exercise.smallestIncrement, Exercise.barWeightOverride, Exercise.unitOverride accessors get/set correctly with nil defaults"
    - "RoutineExercise.warmupOverride computed accessor returns nil when warmupOverrideData is nil and round-trips a WarmupConfig via JSON when set"
    - "SetEntry.wasManualOverride defaults to false and persists when toggled true"
    - "UserSettings.defaultIncrementKg defaults to 2.5 and UserSettings.minCalibrationSets defaults to 10"
  artifacts:
    - path: "fitbod/Persistence/SchemaV3.swift"
      provides: "SchemaV3: VersionedSchema with versionIdentifier 3.0.0 and SchemaV2.models + [PlateInventory.self]"
      contains: "public enum SchemaV3: VersionedSchema"
    - path: "fitbod/Models/PlateInventory.swift"
      provides: "@Model PlateInventory + extension computed accessors"
      contains: "@Model"
    - path: "fitbod/Models/PlateSpec.swift"
      provides: "Codable struct PlateSpec { weight, countPerSide, color }"
      contains: "struct PlateSpec"
    - path: "fitbod/Models/WarmupConfig.swift"
      provides: "Codable struct WarmupConfig { enabled, skipNextSession }"
      contains: "struct WarmupConfig"
    - path: "fitbod/Models/Enums/EquipmentKind.swift"
      provides: "Enum PlateEquipmentKind with cases barbell/dumbbell/ezBar/trapBar"
      contains: "public enum PlateEquipmentKind"
  key_links:
    - from: "fitbod/Persistence/FitbodSchemaMigrationPlan.swift"
      to: "fitbod/Persistence/SchemaV3.swift"
      via: "static let migrateV2toV3 = MigrationStage.lightweight(fromVersion: SchemaV2.self, toVersion: SchemaV3.self)"
      pattern: "migrateV2toV3"
    - from: "fitbod/fitbodApp.swift"
      to: "fitbod/Persistence/SchemaV3.swift"
      via: "Schema(SchemaV3.models)"
      pattern: "SchemaV3"
    - from: "fitbod/Persistence/PreviewModelContainer.swift"
      to: "fitbod/Persistence/SchemaV3.swift"
      via: "Schema(SchemaV3.models)"
      pattern: "SchemaV3"
---

<objective>
SchemaV3 lands as additive-only delta from SchemaV2 — one new entity (PlateInventory), two new codable structs (PlateSpec, WarmupConfig), one new enum (PlateEquipmentKind), and additive default-valued fields on four existing @Model entities. Lightweight migration stage registered. fitbodApp.swift + PreviewModelContainer.swift flipped to Schema(SchemaV3.models). All Phase 3 implementation, UI, and test plans depend on this scaffold being in place.

Purpose: Unlocks every downstream Phase 3 plan. Without PlateInventory persistence, the editor and seeding plan (03-04) cannot ship. Without the additive fields on Exercise/RoutineExercise/SetEntry/UserSettings, the strategies (03-05), session UI (03-06), and integration (03-08) have nowhere to read/write from. All field additions are consolidated into this single plan to eliminate cross-plan file conflicts on the four model files.

Output: SchemaV3 ships. Existing Phase 2 stores migrate via lightweight MigrationStage. App + previews open against V3. No business logic added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smart-prescription-warm-ups/03-CONTEXT.md
@.planning/phases/03-smart-prescription-warm-ups/03-RESEARCH.md
@.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
@fitbod/Persistence/SchemaV2.swift
@fitbod/Persistence/SchemaV1.swift
@fitbod/Persistence/FitbodSchemaMigrationPlan.swift
@fitbod/Persistence/PreviewModelContainer.swift
@fitbod/Models/UserSettings.swift
@fitbod/Models/SetEntry.swift
@fitbod/Models/Exercise.swift
@fitbod/Models/RoutineExercise.swift
@fitbod/Models/Enums/Equipment.swift
@fitbod/Models/Enums/WeightUnit.swift
@fitbod/Models/RoutineExerciseSetOverride.swift
@fitbod/fitbodApp.swift

<interfaces>
Phase 2 codable-on-@Model pattern that this plan mirrors for PlateInventory.availablePlatesData and RoutineExercise.warmupOverrideData (extracted verbatim from SetEntry.swift Phase 2 clusterSubReps shape — see PATTERNS.md lines 228-240):

- Stored field: `public var clusterSubRepsJoined: String? = nil`
- Computed accessor: `public var clusterSubReps: [Int]` with `get` decoding from joined string and `set` re-joining; empty array sets the storage to nil.

SchemaV2 current state (this plan extends, does NOT replace):
- `FitbodSchemaMigrationPlan.schemas` returns `[SchemaV1.self, SchemaV2.self]` — becomes `[SchemaV1.self, SchemaV2.self, SchemaV3.self]`
- `FitbodSchemaMigrationPlan.stages` returns `[migrateV1toV2]` — becomes `[migrateV1toV2, migrateV2toV3]`
- `migrateV1toV2 = MigrationStage.lightweight(fromVersion: SchemaV1.self, toVersion: SchemaV2.self)` — sibling `migrateV2toV3` added.

SchemaV2.models composition target (this plan composes with `SchemaV2.models + [PlateInventory.self]`):
- SchemaV2.models exports the 15 V2 entities; SchemaV3.models appends PlateInventory.self.

fitbodApp.swift + PreviewModelContainer.swift currently reference `Schema(SchemaV2.models)` — both swap to `Schema(SchemaV3.models)` in Task 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New types (PlateInventory @Model + PlateSpec + WarmupConfig + PlateEquipmentKind)</name>
  <files>
    fitbod/Models/Enums/EquipmentKind.swift,
    fitbod/Models/PlateSpec.swift,
    fitbod/Models/WarmupConfig.swift,
    fitbod/Models/PlateInventory.swift
  </files>
  <read_first>
    @fitbod/Models/Enums/Equipment.swift,
    @fitbod/Models/Enums/WeightUnit.swift,
    @fitbod/Models/UserSettings.swift,
    @fitbod/Models/SetEntry.swift,
    @fitbod/Models/RoutineExerciseSetOverride.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - PlateEquipmentKind exposes exactly 4 cases (barbell, dumbbell, ezBar, trapBar); rawValues are "barbell", "dumbbell", "ez_bar", "trap_bar"; conforms to String, CaseIterable, Sendable; no `default` case.
    - PlateSpec is Codable + Sendable + Equatable + Hashable with fields weight: Double, countPerSide: Int, color: String?; memberwise public init with color defaulted to nil.
    - WarmupConfig is Codable + Sendable + Equatable with fields enabled: Bool = true, skipNextSession: Bool = false; memberwise public init.
    - PlateInventory is an @Model final class with @Attribute(.unique) var id: UUID = UUID(), var equipmentKindRaw: String = "barbell", var barWeight: Double = 20.0, var availablePlatesData: Data = Data(), var createdAt: Date = Date.now, var updatedAt: Date = Date.now, public init() {} — all fields default-valued per FOUND-02.
    - Extension on PlateInventory exposes computed `equipmentKind: PlateEquipmentKind` (get falls back to .barbell on bad raw, set writes raw) AND computed `availablePlates: [PlateSpec]` (get returns `(try? JSONDecoder().decode([PlateSpec].self, from: availablePlatesData)) ?? []`, set writes `(try? JSONEncoder().encode(newValue)) ?? Data()`).
    - All four files compile under Swift 6 strict concurrency. No @MainActor annotations are added — value types are implicitly Sendable; the @Model class follows existing UserSettings/RoutineFolder convention with no explicit annotation.
  </behavior>
  <action>
    Create fitbod/Models/Enums/EquipmentKind.swift mirroring fitbod/Models/Enums/Equipment.swift shape: `import Foundation`, then `public enum PlateEquipmentKind: String, CaseIterable, Sendable` with four cases — `barbell`, `dumbbell`, `ezBar` with rawValue "ez_bar", `trapBar` with rawValue "trap_bar". Do NOT add a `default` static property (PATTERNS.md confirms inventory editor renders all 4 tabs unconditionally so a default sentinel is unnecessary).

    Create fitbod/Models/PlateSpec.swift: `import Foundation`, then `public struct PlateSpec: Codable, Sendable, Equatable, Hashable` with three `public let` fields weight: Double, countPerSide: Int, color: String?, plus a memberwise `public init(weight: Double, countPerSide: Int, color: String? = nil)`.

    Create fitbod/Models/WarmupConfig.swift: `import Foundation`, then `public struct WarmupConfig: Codable, Sendable, Equatable` with two `public var` fields `enabled: Bool` and `skipNextSession: Bool`, plus a memberwise `public init(enabled: Bool = true, skipNextSession: Bool = false)`. Fields are `var` (not `let`) because SessionFactory in plan 03-08 will mutate `skipNextSession` to reset it after use.

    Create fitbod/Models/PlateInventory.swift mirroring fitbod/Models/UserSettings.swift @Model class shape: imports Foundation + SwiftData; `@Model public final class PlateInventory` with the six fields listed in <behavior>; empty `public init() {}`. Append the extension exposing the two computed accessors (equipmentKind and availablePlates) per PATTERNS.md "@Model pattern" reference block.

    Do NOT add PlateInventory to SchemaV3 in this task — Task 3 wires that.
    Do NOT call these new types from anywhere yet — Task 4 wires the container.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'public enum PlateEquipmentKind' fitbod/Models/Enums/EquipmentKind.swift` returns 1
    - `grep -c 'public struct PlateSpec' fitbod/Models/PlateSpec.swift` returns 1
    - `grep -c 'public struct WarmupConfig' fitbod/Models/WarmupConfig.swift` returns 1
    - `grep -c '@Model' fitbod/Models/PlateInventory.swift` returns 1
    - `grep -c 'var availablePlates: \[PlateSpec\]' fitbod/Models/PlateInventory.swift` returns 1
    - `grep -cE 'case (barbell|dumbbell|ezBar|trapBar)' fitbod/Models/Enums/EquipmentKind.swift` returns 4
    - The swiftc -parse command above prints zero lines (no error: lines anywhere).
  </acceptance_criteria>
  <done>Four new files compile under Swift 6 strict concurrency. PlateInventory + extension exposes both computed accessors. JSON round-trip semantics are guaranteed by the accessor implementation — actual end-to-end round-trip is asserted in plan 03-02 SchemaV3MigrationTests + PlateInventoryTests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive default-valued fields on Exercise + RoutineExercise + SetEntry + UserSettings</name>
  <files>
    fitbod/Models/Exercise.swift,
    fitbod/Models/RoutineExercise.swift,
    fitbod/Models/SetEntry.swift,
    fitbod/Models/UserSettings.swift
  </files>
  <read_first>
    @fitbod/Models/Exercise.swift,
    @fitbod/Models/RoutineExercise.swift,
    @fitbod/Models/SetEntry.swift,
    @fitbod/Models/UserSettings.swift,
    @fitbod/Models/Enums/WeightUnit.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - Exercise gains three new optional / default-valued stored fields: `var smallestIncrement: Double? = nil`, `var barWeightOverride: Double? = nil`, `var unitOverrideRaw: String? = nil`.
    - Exercise gains a computed accessor `var unitOverride: WeightUnit?` (get returns `unitOverrideRaw.flatMap(WeightUnit.init(rawValue:))`, set writes `unitOverrideRaw = newValue?.rawValue`). Placed in the existing accessor extension area.
    - RoutineExercise gains one new optional stored field: `var warmupOverrideData: Data? = nil`. Nil semantics = "no override; use default auto-warm-up behavior" per RESEARCH §Pitfall 5.
    - RoutineExercise gains a computed accessor `var warmupOverride: WarmupConfig?` — get returns nil when warmupOverrideData is nil, otherwise decodes via JSONDecoder (decode failure also yields nil per RESEARCH §Pitfall 5); set re-encodes via JSONEncoder, writing `try?` (encode failure writes nil).
    - SetEntry gains one new default-valued stored field: `var wasManualOverride: Bool = false`.
    - UserSettings gains two new default-valued stored fields: `var defaultIncrementKg: Double = 2.5` and `var minCalibrationSets: Int = 10`.
    - All additions are STRICTLY additive — every new property has a literal default value so lightweight migration is valid per RESEARCH §Schema Evolution.
    - No existing field is renamed, retyped, removed, or has its default value changed.
  </behavior>
  <action>
    Edit fitbod/Models/Exercise.swift: locate the existing stored-property block (after the `@Attribute(.unique) public var id` and alongside `mechanicRaw`/`equipmentRaw`/`isCustom`). Append the three new fields exactly as written in <behavior> with the literal defaults. Locate the existing extension that exposes computed enum accessors (the `mechanic`, `equipment`, `force`, `level` getter/setter group) and append the `unitOverride: WeightUnit?` accessor pair mirroring RoutineExercise.intentRaw style per PATTERNS.md "Enum-as-String persistence pattern".

    Edit fitbod/Models/RoutineExercise.swift: append `public var warmupOverrideData: Data? = nil` in the stored-property block. In an extension (or the existing accessor extension), add the computed `warmupOverride: WarmupConfig?` accessor — get does `guard let data = warmupOverrideData else { return nil }` then `try? JSONDecoder().decode(WarmupConfig.self, from: data)`; set writes `warmupOverrideData = try? JSONEncoder().encode(newValue)`.

    Edit fitbod/Models/SetEntry.swift: append `public var wasManualOverride: Bool = false` in the stored-property block, placed near the existing Phase 2 additive `isComplete: Bool = false` field for stylistic grouping.

    Edit fitbod/Models/UserSettings.swift: append the two new fields after the existing `unitsRaw` / `defaultProgressionKindRaw` block — `public var defaultIncrementKg: Double = 2.5` and `public var minCalibrationSets: Int = 10`. PATTERNS.md "UserSettings additive field pattern" is the template; do NOT touch the `default()` factory (new fields default per their declarations).

    Do NOT modify any existing default value, type, or rename anything. Do NOT remove fields. Do NOT yet wire SchemaV3 — Task 3 owns that.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && grep -c 'var smallestIncrement: Double' fitbod/Models/Exercise.swift && grep -c 'var barWeightOverride: Double' fitbod/Models/Exercise.swift && grep -c 'var unitOverrideRaw: String' fitbod/Models/Exercise.swift && grep -c 'var warmupOverrideData: Data' fitbod/Models/RoutineExercise.swift && grep -c 'var wasManualOverride: Bool = false' fitbod/Models/SetEntry.swift && grep -c 'var defaultIncrementKg: Double = 2.5' fitbod/Models/UserSettings.swift && grep -c 'var minCalibrationSets: Int = 10' fitbod/Models/UserSettings.swift && grep -c 'var unitOverride: WeightUnit?' fitbod/Models/Exercise.swift && grep -c 'var warmupOverride: WarmupConfig?' fitbod/Models/RoutineExercise.swift</automated>
  </verify>
  <acceptance_criteria>
    - Each grep above returns 1 (9 expected single-match lines).
    - `find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:'` prints zero lines.
    - `git diff -U0 fitbod/Models/Exercise.swift fitbod/Models/RoutineExercise.swift fitbod/Models/SetEntry.swift fitbod/Models/UserSettings.swift | grep -E '^-[^-]' | grep -vE '^---'` shows zero deletion lines that touch existing field declarations (additive-only enforcement).
  </acceptance_criteria>
  <done>Every Phase 3 additive field is present with the exact default values specified in RESEARCH.md §Additive Fields. Lightweight migration eligibility preserved (all new fields have literal defaults). No existing field mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SchemaV3 + FitbodSchemaMigrationPlan + container wiring</name>
  <files>
    fitbod/Persistence/SchemaV3.swift,
    fitbod/Persistence/FitbodSchemaMigrationPlan.swift,
    fitbod/Persistence/PreviewModelContainer.swift,
    fitbod/fitbodApp.swift
  </files>
  <read_first>
    @fitbod/Persistence/SchemaV2.swift,
    @fitbod/Persistence/FitbodSchemaMigrationPlan.swift,
    @fitbod/Persistence/PreviewModelContainer.swift,
    @fitbod/fitbodApp.swift,
    @.planning/phases/03-smart-prescription-warm-ups/03-PATTERNS.md
  </read_first>
  <behavior>
    - `SchemaV3: VersionedSchema` exists with `static let versionIdentifier = Schema.Version(3, 0, 0)`.
    - `SchemaV3.models == SchemaV2.models + [PlateInventory.self]` (no V2 entities removed; PlateInventory appended).
    - `FitbodSchemaMigrationPlan.schemas == [SchemaV1.self, SchemaV2.self, SchemaV3.self]` (V3 appended at end).
    - `FitbodSchemaMigrationPlan.stages == [migrateV1toV2, migrateV2toV3]` (V2→V3 stage appended).
    - `migrateV2toV3 = MigrationStage.lightweight(fromVersion: SchemaV2.self, toVersion: SchemaV3.self)` — eligible because Task 1 added a default-valued new entity and Task 2 added only default-valued fields (RESEARCH §Schema Evolution cites Apple docs).
    - `fitbodApp.swift` constructs its `ModelContainer` with `Schema(SchemaV3.models)` instead of `Schema(SchemaV2.models)`.
    - `PreviewModelContainer.make()` constructs its in-memory container with `Schema(SchemaV3.models)`.
    - Both still pass `migrationPlan: FitbodSchemaMigrationPlan.self`.
  </behavior>
  <action>
    Create fitbod/Persistence/SchemaV3.swift mirroring fitbod/Persistence/SchemaV2.swift exactly: imports Foundation + SwiftData; `public enum SchemaV3: VersionedSchema` with `public static let versionIdentifier = Schema.Version(3, 0, 0)` and `public static var models: [any PersistentModel.Type]` returning `SchemaV2.models + [PlateInventory.self]`.

    Edit fitbod/Persistence/FitbodSchemaMigrationPlan.swift:
    - Change the `schemas` computed property to return `[SchemaV1.self, SchemaV2.self, SchemaV3.self]`.
    - Change the `stages` computed property to return `[migrateV1toV2, migrateV2toV3]`.
    - Add `public static let migrateV2toV3 = MigrationStage.lightweight(fromVersion: SchemaV2.self, toVersion: SchemaV3.self)` as a sibling declaration of the existing `migrateV1toV2`.
    - Do NOT change `migrateV1toV2`.

    Edit fitbod/Persistence/PreviewModelContainer.swift: change every `Schema(SchemaV2.models)` reference to `Schema(SchemaV3.models)`. Do NOT alter the `isStoredInMemoryOnly: true` config or migrationPlan reference.

    Edit fitbod/fitbodApp.swift: change every `Schema(SchemaV2.models)` reference to `Schema(SchemaV3.models)`. Do NOT alter the migrationPlan reference. Do NOT touch the `.task { ... }` seed-trigger block or any other view wiring.
  </action>
  <verify>
    <automated>cd /Users/chrissaechao/Desktop/fitbod && grep -c 'public enum SchemaV3: VersionedSchema' fitbod/Persistence/SchemaV3.swift && grep -c 'SchemaV2.models + \[PlateInventory.self\]' fitbod/Persistence/SchemaV3.swift && grep -c 'SchemaV3.self' fitbod/Persistence/FitbodSchemaMigrationPlan.swift && grep -c 'migrateV2toV3' fitbod/Persistence/FitbodSchemaMigrationPlan.swift && grep -c 'Schema(SchemaV3.models)' fitbod/fitbodApp.swift && grep -c 'Schema(SchemaV3.models)' fitbod/Persistence/PreviewModelContainer.swift && (grep -c 'Schema(SchemaV2.models)' fitbod/fitbodApp.swift fitbod/Persistence/PreviewModelContainer.swift | grep -v ':0' | wc -l | tr -d ' ')</automated>
  </verify>
  <acceptance_criteria>
    - First six greps each return 1 (or more, in the case of `migrateV2toV3` and `SchemaV3.self` which appear multiple times in the migration plan file — non-zero is acceptable as long as at least 1).
    - The final pipe (counting files where `Schema(SchemaV2.models)` still appears) prints `0` — i.e. neither fitbodApp.swift nor PreviewModelContainer.swift contains any remaining `Schema(SchemaV2.models)` reference.
    - `find fitbod -name '*.swift' -print0 | xargs -0 xcrun swiftc -parse -target arm64-apple-ios18.0 -sdk "$(xcrun --sdk iphoneos --show-sdk-path)" 2>&1 | grep -E '^/.*error:'` prints zero lines.
  </acceptance_criteria>
  <done>SchemaV3 is the live container schema for both production and previews. Migration plan declares the lightweight V2→V3 stage. SchemaV1Tests + SchemaV2MigrationTests from prior phases still pass — Task 4 of plan 03-02 asserts the V3 round-trip explicitly.</done>
</task>

</tasks>

<verification>
- xcodebuild incremental build of the fitbod target succeeds (no compile errors after all 3 tasks). Quick proof: `xcodebuild -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | grep -E 'error:'` returns no lines.
- `grep -RnE 'Schema\(SchemaV2\.models\)' fitbod/ 2>/dev/null` returns no matches in production code (test fixtures may still reference SchemaV2 for legacy migration tests — that is expected, this grep targets `fitbod/` not `fitbodTests/`).
- All existing test suites continue to compile (no migration tests broken by these additions). Plan 03-02 adds new tests that prove behavior; this plan's responsibility ends at "compiles and the container opens".
</verification>

<success_criteria>
- SchemaV3.swift exists with the 3.0.0 version identifier and SchemaV2.models + [PlateInventory.self] composition.
- FitbodSchemaMigrationPlan reports schemas of size 3 and stages of size 2.
- PlateInventory @Model, PlateSpec / WarmupConfig structs, and PlateEquipmentKind enum compile under Swift 6 strict concurrency.
- All four pre-existing @Model entities (Exercise, RoutineExercise, SetEntry, UserSettings) gain their Phase 3 additive fields with literal defaults — no existing field is mutated.
- fitbodApp + PreviewModelContainer use Schema(SchemaV3.models). No straggler Schema(SchemaV2.models) reference remains in fitbod/ production code.
</success_criteria>

<output>
Create `.planning/phases/03-smart-prescription-warm-ups/03-01-SUMMARY.md` when done documenting: the four new files created, the four existing files edited (with exact field-additions list), migration plan changes, and the V3 container wiring flip. Commit per atomic-commit convention: one commit for Task 1 (new types), one for Task 2 (additive fields), one for Task 3 (schema + container wiring).
</output>
