---
phase: 05-fatigue-model-plateau-detection
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - fitbod/Fatigue/FatigueModel.swift
  - fitbod/Fatigue/StimulusWeightSeeder.swift
  - fitbod/Fatigue/MuscleVolumeBar.swift
  - fitbod/Fatigue/FatigueSurfaceView.swift
  - fitbod/App/RootView.swift
  - fitbodTests/Fatigue/FatigueModelWeeklyVolumeTests.swift
  - fitbodTests/Fatigue/FrequencyHitsTests.swift
  - fitbodTests/Fatigue/StimulusWeightSeederTests.swift
  - fitbodUITests/VolumeBarsUITests.swift
autonomous: true
requirements: [VOL-01, VOL-02, VOL-04, VOL-06]

must_haves:
  truths:
    - "User can open the Today tab and see a 'Volume This Week' section with one MuscleVolumeBar row per muscle that has at least 1 stimulus-weighted set this week — the bars render with real session data (NOT mocked)"
    - "Each MuscleVolumeBar shows the muscle display name + '{totalSetsRounded} / {target.mrv}' trailing accessory + a two-tone (direct opacity 1.0 / indirect opacity 0.40) zone-colored fill + MEV / MAV tick marks + D-06 verbatim verb label + '+N vs last week' delta line — VOL-04 closed end-to-end"
    - "FatigueModel.weeklyVolume(muscleSlug:weekStart:in:) is a @MainActor pure function returning WeightedSetTotal with directSets + indirectSets split per D-07; the algorithm filters SetEntry rows by (isComplete && !isWarmup && setTypeRaw ∈ workingKinds && session.startedAt in week window) and traverses Exercise.muscleStimuli in-memory to sum stimulus.weight per role per Pitfall #1 local-let-capture workaround"
    - "FatigueModel.frequencyHits(muscleSlug:weekStart:minContribution:in:) returns the count of sessions whose weighted-set contribution to the muscle meets the threshold (default 2.0 per D-03); a single 0.5 indirect contribution does NOT count as a hit (VOL-06 verbatim semantics)"
    - "FatigueModel.weekOverWeekDelta(muscleSlug:weekStart:in:) computes the +N / -N / no-change copy string per D-08 verbatim ('+{N} vs last week' / '-{N} vs last week' / 'no change vs last week')"
    - "FatigueModel.volumeZone(currentSets:mev:mav:mrv:) delegates to the free volumeZone(...) function defined in plan 01 — single source of truth for the D-05 verbatim boundary semantics"
    - "StimulusWeightSeeder runs on first launch + on every subsequent launch (idempotent); for each of the 51 curated exercise names in StimulusWeightTable.overrides, it locates the matching Exercise by canonicalName, writes the curated (muscleSlug, role, weight) triples onto its ExerciseMuscleStimulus rows WHERE userEditedWeight == false; rows with userEditedWeight == true are skipped (user-edit preservation per VOL-01); seed weights are range-clamped to [0,1] (T-05-01 mitigation)"
    - "RootView's Today tab mounts FatigueSurfaceView inside the existing ScrollView/VStack stack BELOW ResumeWorkoutBanner and (Phase 4) BlockCard; the surface uses @Query<MuscleGroup> + @Query<MuscleVolumeTarget> + @Environment(\\.modelContext) for inline FatigueModel calls (MV-VM-lite per FOUND-06)"
    - "VolumeBarsUITests asserts: launching the app (with a test-seeded ModelContainer containing one Monday strength session of barbell bench press) shows the chest MuscleVolumeBar with verb 'Below MEV — add volume' OR 'Productive range — hold/progress' (depending on seeded volume); the bar row carries an accessibilityLabel containing the muscle name + zone verb"
    - "FatigueModelWeeklyVolumeTests + FrequencyHitsTests + StimulusWeightSeederTests turn GREEN (3 of the 8 RED scaffolds from plan 01)"
  artifacts:
    - path: "fitbod/Fatigue/FatigueModel.swift"
      provides: "public enum FatigueModel namespace with 5 @MainActor static funcs: weeklyVolume / contributionPerSession / frequencyHits / weekOverWeekDelta / volumeZone (delegating to plan 01's free function). Supporting Sendable value types WeightedSetTotal (directSets + indirectSets + totalSets + totalSetsRounded) and WeekOverWeekDelta (currentWeekTotal + previousWeekTotal + delta + deltaCopy)."
      contains: "enum FatigueModel"
    - path: "fitbod/Fatigue/StimulusWeightSeeder.swift"
      provides: "@MainActor public enum StimulusWeightSeeder with seedIfNeeded(in:) static func; row-level idempotency via userEditedWeight guard (NOT UserDefaults — stimulus weights may need re-seeding on app updates); iterates StimulusWeightTable.overrides; clamps weight to [0,1] before write (T-05-01 mitigation); single try? context.save() at end"
      contains: "enum StimulusWeightSeeder"
    - path: "fitbod/Fatigue/MuscleVolumeBar.swift"
      provides: "SwiftUI view that renders one muscle's bar row: HStack with muscle display name + sets-vs-mrv trailing accessory + GeometryReader-based ZStack with track + indirect fill (opacity 0.4) + direct fill (opacity 1.0) + MEV / MAV tick marks + D-06 verbatim verb label + D-08 verbatim delta line; takes (muscle: MuscleGroup, target: MuscleVolumeTarget, total: WeightedSetTotal, delta: WeekOverWeekDelta, isAdvisoryActive: Bool = false) parameters; reduce-motion gated overMRV pulse animation per UI-SPEC §Interaction & Motion"
      contains: "struct MuscleVolumeBar"
    - path: "fitbod/Fatigue/FatigueSurfaceView.swift"
      provides: "SwiftUI ScrollView host with 'Volume This Week' section header + ForEach over MuscleGroup with at least one MuscleVolumeBar per non-empty muscle (filter to muscles with total.totalSetsRounded >= 1 OR target.mev > 0); placeholder for the heatmap section (filled by plan 05). Empty state ('No sets logged this week' + 'Log a workout to see your weekly volume.') when no muscles have non-zero volume."
      contains: "struct FatigueSurfaceView"
    - path: "fitbod/App/RootView.swift"
      provides: "Today tab modification — FatigueSurfaceView mounted inside the existing scrollable VStack below ResumeWorkoutBanner (and below Phase 4 BlockCard if it ships before this plan executes); 1-line StimulusWeightSeeder.seedIfNeeded() call added to RootView.task alongside existing seed orchestration; no other modifications"
      contains: "FatigueSurfaceView"
  key_links:
    - from: "fitbod/Fatigue/FatigueModel.swift"
      to: "fitbod/Models/SetEntry.swift"
      via: "FetchDescriptor<SetEntry> with predicate filtering by isComplete + isWarmup + setTypeRaw + sessionExercise?.session?.startedAt window; in-memory join through Exercise.muscleStimuli per Pitfall #1 local-let workaround"
      pattern: "FetchDescriptor<SetEntry>"
    - from: "fitbod/Fatigue/MuscleVolumeBar.swift"
      to: "fitbod/Fatigue/VolumeZone.swift"
      via: "FatigueModel.volumeZone(currentSets:mev:mav:mrv:) → VolumeZone.verb returns D-06 verbatim copy"
      pattern: "VolumeZone"
    - from: "fitbod/Fatigue/StimulusWeightSeeder.swift"
      to: "fitbod/Fatigue/StimulusWeightTable.swift"
      via: "Iterates overrides dictionary; for each canonical exercise name finds matching Exercise via @Query / FetchDescriptor on canonicalName"
      pattern: "StimulusWeightTable.overrides"
    - from: "fitbod/App/RootView.swift"
      to: "fitbod/Fatigue/FatigueSurfaceView.swift"
      via: "Mount inside TodayView VStack via direct view composition (per FatigueSurfaceView role as Today-tab host per UI-SPEC §Component Inventory)"
      pattern: "FatigueSurfaceView()"
---

<objective>
Ship the first user-visible Phase 5 vertical slice — **Slice A + B combined**: the user opens the Today tab and sees per-muscle weekly volume bars with verb labels and the direct/indirect two-tone fill. After this plan, the user can log a workout and immediately see chest's contribution show up on the chest bar (direct, opacity 1.0) and the bench-press-induced triceps + shoulders contributions show up on those bars (indirect, opacity 0.40). Verb copy is D-06 verbatim, delta line is D-08 verbatim, set count is D-04 (on-demand pure-function aggregation; no snapshot entity).

Slice A delivers VOL-02 (weekly volume math), VOL-04 (bars + verb labels), VOL-06 (frequency hits service available even though no UI surface for it yet — Plan 07's per-muscle detail view consumes it). Slice B delivers VOL-01 — the curated stimulus weights from RESEARCH §6.1 are written onto the seeded ExerciseMuscleStimulus rows by StimulusWeightSeeder on first launch, so the bars actually show the direct/indirect split correctly (barbell row contributes 1.0 to lats vs 0.5 to biceps, NOT a double-1.0 count).

Purpose: this is the BREAD AND BUTTER of Phase 5 — the bar surface is the first thing the user sees when opening the Today tab after this plan. Every later Phase 5 surface (heatmap / plateau / deload advisory / settings editors / weekly recap) composes on top of `FatigueModel`, so getting the math right matters. The two-tone direct/indirect encoding (D-07) is critical for the user to understand "your barbell row is hitting your lats hard and your biceps lightly" without doing the math.

Output: 1 new pure-function service file (`FatigueModel`), 1 new seeder (`StimulusWeightSeeder`), 2 new SwiftUI files (`MuscleVolumeBar`, `FatigueSurfaceView`), 1 modified host (`RootView`), 3 GREEN test suites + 1 GREEN UI test suite. Flips 3 of the 8 RED Wave-0 scaffolds from plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md
@.planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md
@.planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md
@.planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md
@.planning/phases/05-fatigue-model-plateau-detection/05-VALIDATION.md
@.planning/phases/05-fatigue-model-plateau-detection/05-01-PLAN.md

@fitbod/Models/SetEntry.swift
@fitbod/Models/SessionExercise.swift
@fitbod/Models/Session.swift
@fitbod/Models/Exercise.swift
@fitbod/Models/ExerciseMuscleStimulus.swift
@fitbod/Models/MuscleGroup.swift
@fitbod/Models/MuscleVolumeTarget.swift
@fitbod/Models/UserSettings.swift
@fitbod/Models/Enums/SetType.swift
@fitbod/Settings/PlateInventorySeeder.swift
@fitbod/Sessions/PreviousMatchingIntent.swift
@fitbod/Sessions/PinnedNoteCapsule.swift
@fitbod/App/RootView.swift
@fitbod/Fatigue/VolumeZone.swift
@fitbod/Fatigue/StimulusWeightTable.swift
@fitbod/Fatigue/RPVolumeLandmarks.swift
@fitbodTests/Fatigue/FatigueTestFixtures.swift

<interfaces>
<!-- Existing types and contracts the executor needs. Plan 01 ships all Phase 5 primitives; this plan composes against them. -->

From fitbod/Fatigue/VolumeZone.swift (plan 01):
```
public enum VolumeZone: Sendable, Equatable { case belowMEV; case productive; case nearMRV; case overMRV }
public func volumeZone(currentSets: Int, mev: Int, mav: Int, mrv: Int) -> VolumeZone { ... }
extension VolumeZone { public var verb: String { ... } }   // D-06 verbatim
```

From fitbod/Fatigue/StimulusWeightTable.swift (plan 01):
```
public enum StimulusWeightTable {
    public static let overrides: [String: [(muscleSlug: String, role: String, weight: Double)]] = [
        "barbell bench press": [("chest", "primary", 1.0), ("shoulders", "secondary", 0.5), ("triceps", "secondary", 0.5)],
        // ... 51 lifts total
    ]
}
```

From fitbod/Models/SetEntry.swift — relevant fields for the predicate:
```
public var isComplete: Bool       // working sets only when true
public var isWarmup: Bool         // hot-path flag for warm-up exclusion (D-01)
public var setTypeRaw: String     // working / drop / failure / rest_pause for working-kinds set (D-01)
public var weight: Double
public var reps: Int
public var rpe: Double?
public var sessionExercise: SessionExercise?
```

From fitbod/Models/SessionExercise.swift — relevant fields:
```
public var session: Session?
public var exercise: Exercise?
public var intentRaw: String  // #Index'd
```

From fitbod/Models/Exercise.swift — relevant fields:
```
public var canonicalName: String   // #Index'd; lowercased + diacritic-stripped; used as StimulusWeightTable key
public var muscleStimuli: [ExerciseMuscleStimulus]?
```

From fitbod/Models/ExerciseMuscleStimulus.swift (extended by plan 01):
```
public var role: String         // "primary" / "secondary"
public var weight: Double
public var muscle: MuscleGroup?
public var userEditedWeight: Bool   // Phase 5 idempotency flag
```

From fitbod/Models/MuscleVolumeTarget.swift (extended by plan 01):
```
public var muscle: MuscleGroup?
public var mev: Int
public var mav: Int
public var mrv: Int
public var mv: Int
public var userEdited: Bool   // Phase 5 idempotency flag
```

From fitbod/Sessions/PreviousMatchingIntent.swift (Pitfall #1 local-let capture pattern — analog for FetchDescriptor multi-hop predicate):
```
public static func fetchTopWorkingSet(exerciseID: UUID?, intentRaw: String, context: ModelContext) -> PreviousMatchingIntentHit? {
    let targetID = exerciseID    // local-let capture per RESEARCH §10 Pitfall #1
    let targetIntent = intentRaw
    let descriptor = FetchDescriptor<SetEntry>(
        predicate: #Predicate<SetEntry> { entry in
            entry.sessionExercise?.exercise?.id == targetID
            && entry.sessionExercise?.intentRaw == targetIntent
            && entry.isComplete == true
            && entry.isWarmup == false
            // ...
        }
    )
    // ... fetch + in-memory traversal
}
```

From fitbod/Settings/PlateInventorySeeder.swift (analog for @MainActor public enum seeder pattern):
```
@MainActor
public enum PlateInventorySeeder {
    public static let seededKey = "plateInventorySeeded"
    public static func seedIfNeeded(in context: ModelContext, unitSystem: WeightUnit) {
        if UserDefaults.standard.bool(forKey: seededKey) { return }
        // ... idempotency check + inserts + try? save + flag set
    }
}
```
StimulusWeightSeeder DIFFERS: uses row-level userEditedWeight guard (not UserDefaults flag) because stimulus weights may need re-seeding on app updates.

From fitbod/App/RootView.swift — Today tab structure (plan modifies):
```
private struct TodayView: View {
    @Environment(\.modelContext) private var ctx
    @State private var navigationPath = NavigationPath()
    var body: some View {
        NavigationStack(path: $navigationPath) {
            VStack(spacing: 16) {
                ResumeWorkoutBanner(...)
                // PHASE 5: FatigueSurfaceView goes HERE
                Spacer()
            }
            .padding(.horizontal, 32)
            .navigationTitle("Today")
        }
    }
}
```

From fitbod/Sessions/PinnedNoteCapsule.swift (analog for Color asset usage — `Color(.systemYellow).opacity(0.15)` pattern used by amber ring tint).

From fitbodTests/Fatigue/FatigueTestFixtures.swift (plan 01 ships the helper):
```
@MainActor
enum FatigueTestFixtures {
    static func makeContext() throws -> ModelContext   // Schema(SchemaV5.models)
    static func weeklySessions(forExercise:weights:reps:rpe:intent:weekStart:in:) throws -> [Session]
    // edge-case factories (some are fully implemented, some are fatalError stubs awaiting plan 03/04/07)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FatigueModel pure-function service (weeklyVolume + contributionPerSession + frequencyHits + weekOverWeekDelta + volumeZone) + GREEN FatigueModelWeeklyVolumeTests + GREEN FrequencyHitsTests</name>
  <read_first>
    fitbod/Fatigue/VolumeZone.swift,
    fitbod/Models/SetEntry.swift,
    fitbod/Models/SessionExercise.swift,
    fitbod/Models/Session.swift,
    fitbod/Models/Exercise.swift,
    fitbod/Models/ExerciseMuscleStimulus.swift,
    fitbod/Models/Enums/SetType.swift,
    fitbod/Sessions/PreviousMatchingIntent.swift (Pitfall #1 local-let capture analog),
    fitbodTests/Fatigue/FatigueTestFixtures.swift,
    .planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md (D-01 / D-03 / D-04 / D-07 / D-08),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §3.1 (FatigueModel API + algorithms), §10 Pitfall #1 (predicate traversal + local-let capture), §10 Pitfall #3 (filter both isWarmup AND workingKinds), §10 Pitfall #4 (Monday computation),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (FatigueModel section + Pure-function `public enum` service shape)
  </read_first>
  <files>fitbod/Fatigue/FatigueModel.swift, fitbodTests/Fatigue/FatigueModelWeeklyVolumeTests.swift, fitbodTests/Fatigue/FrequencyHitsTests.swift</files>
  <behavior>
    - Test (VOL-02 working set counting): seed an in-memory context with one Session on Monday containing a SessionExercise for "barbell bench press" with three completed working SetEntry rows (isComplete=true, isWarmup=false, setTypeRaw="working"). Assert `FatigueModel.weeklyVolume(muscleSlug: "chest", weekStart: monday, in: ctx).directSets == 3.0` (3 sets × 1.0 primary weight on chest).
    - Test (warmup exclusion per D-01 + Pitfall #3): same fixture but add 2 isWarmup=true SetEntry rows. directSets MUST still be 3.0 (warmups excluded).
    - Test (set-type filtering per D-01 + Pitfall #3): same fixture but add 1 SetEntry with isWarmup=false AND setTypeRaw="warmup" (the rare case where a non-warmup set is mis-categorized). directSets MUST still be 3.0 (the workingKinds contains() filter excludes it).
    - Test (rest-pause cluster = 1 set per D-01 / Pitfall #9): seed 1 SetEntry with setTypeRaw="rest_pause" and clusterSubRepsJoined="8,3,2". Assert directSets == 1.0 (NOT 3.0 — rows are counted, not sub-reps).
    - Test (working-kinds includes drop / failure / rest_pause): seed three completed working sets — one each with setTypeRaw="drop" / "failure" / "rest_pause". Assert directSets == 3.0.
    - Test (D-07 direct/indirect split): for "barbell bench press" with stimulus weights chest 1.0 / shoulders 0.5 / triceps 0.5: seed 3 sets. Assert directSets == 3.0 for chest; indirectSets == 1.5 for shoulders (3 × 0.5); indirectSets == 1.5 for triceps; directSets == 0.0 for triceps and shoulders.
    - Test (week window inclusivity): seed one set on the Sunday BEFORE weekStart (Mon). Assert weeklyVolume for weekStart returns directSets == 0.0 (the set is outside [weekStart, weekStart+7d)).
    - Test (week window upper bound exclusivity): seed one set on the Sunday OF this week (weekStart + 6 days + 23:59). Assert directSets == 1.0 (included). Seed one set on the next Monday (weekStart + 7 days). Assert NOT included.
    - Test (D-04 — no snapshot entity): the function is callable repeatedly with the same args and returns the same result; no side effects (no rows inserted; no caches mutated).
    - Test (WeightedSetTotal.totalSets / totalSetsRounded): for directSets = 3.0 + indirectSets = 1.5 → totalSets == 4.5; totalSetsRounded == 5 (round to nearest).
    - Test (D-08 weekOverWeekDelta — positive): seed 3 sets this week + 1 set last week. Assert `weekOverWeekDelta(...).deltaCopy == "+2 vs last week"`.
    - Test (D-08 weekOverWeekDelta — negative): seed 1 set this week + 3 sets last week. Assert deltaCopy == "-2 vs last week".
    - Test (D-08 weekOverWeekDelta — zero): seed 2 sets each week. Assert deltaCopy == "no change vs last week".
    - Test (D-03 frequency hit — meets threshold): seed two sessions on different days; each has 3 chest sets (so each session contributes 3.0 weighted sets to chest). Assert `FatigueModel.frequencyHits(muscleSlug: "chest", weekStart: monday, minContribution: 2.0, in: ctx) == 2`.
    - Test (D-03 frequency hit — single indirect does NOT count): seed one session with a single triceps-stimulating set (1.0 × 0.5 secondary = 0.5 weighted contribution to triceps). Assert `frequencyHits(muscleSlug: "triceps", weekStart: monday, minContribution: 2.0, in: ctx) == 0`.
    - Test (D-03 frequency hit — multiple indirect can add up): seed one session with FOUR triceps-indirect-stimulating sets (each 0.5 weighted = 2.0 total). Assert frequencyHits == 1 (the session meets the threshold).
    - Test (FatigueModel.volumeZone delegates correctly): assert `FatigueModel.volumeZone(currentSets: 8, mev: 8, mav: 14, mrv: 22) == .productive` (sanity that the delegate forwards to plan 01's free function).
  </behavior>
  <action>
    Create fitbod/Fatigue/FatigueModel.swift mirroring `fitbod/Prescription/Calibration.swift` shape with the differences noted below.
    
    File header comment: "FatigueModel.swift / fitbod / Pure-function namespace for stimulus-weighted weekly volume aggregation per RESEARCH §3.1 + CONTEXT D-01..D-08. / All functions are @MainActor because ModelContext is @MainActor-bound under Swift 6 strict concurrency. / No SwiftData mutations — every function is read-only over SetEntry / SessionExercise / Exercise / ExerciseMuscleStimulus / MuscleGroup. / Supporting Sendable value types (WeightedSetTotal, WeekOverWeekDelta) carry the direct/indirect split and the pre-computed delta copy so views can stay dumb.".
    
    Imports: `import Foundation, import SwiftData`.
    
    Body — `public enum FatigueModel`:
      - `@MainActor public static func weeklyVolume(muscleSlug: String, weekStart: Date, in context: ModelContext) -> WeightedSetTotal` — algorithm per RESEARCH §3.1 + Pitfall #1 local-let workaround:
        1. Compute `weekEnd = weekStart.addingTimeInterval(7 * 86400)`.
        2. Local-let capture: `let workingKinds = ["working", "drop", "failure", "rest_pause"]`; `let captureStart = weekStart; let captureEnd = weekEnd`. (RESEARCH §10 Pitfall #1 requires local-let captures BEFORE the predicate body to dodge SwiftData related-entity-ID compare footguns.)
        3. Build `FetchDescriptor<SetEntry>` with `#Predicate` filtering on `isComplete == true && isWarmup == false && workingKinds.contains(setTypeRaw) && sessionExercise?.session?.startedAt ?? .distantPast >= captureStart && sessionExercise?.session?.startedAt ?? .distantPast < captureEnd`. Defensive nil-coalesce to `.distantPast` per the Pitfall #1 pattern.
        4. Fetch via `let entries = (try? context.fetch(descriptor)) ?? []`.
        5. In-memory join (predicate cannot traverse Exercise.muscleStimuli cleanly per Pitfall #1): for each entry, navigate to `entry.sessionExercise?.exercise?.muscleStimuli ?? []`; for each stimulus where `stimulus.muscle?.slug == muscleSlug`, branch on `stimulus.role` — "primary" → `directSum += stimulus.weight`; "secondary" → `indirectSum += stimulus.weight`; default → skip.
        6. Return `WeightedSetTotal(directSets: directSum, indirectSets: indirectSum)`.
      
      - `@MainActor public static func contributionPerSession(muscleSlug: String, weekStart: Date, in context: ModelContext) -> [UUID: Double]` — algorithm:
        1. Same FetchDescriptor as weeklyVolume.
        2. Group entries by `entry.sessionExercise?.session?.id`. For each (session.id, sets) pair: in-memory sum of stimulus.weight for muscleSlug matches (any role). Return the dictionary keyed on Session UUID.
      
      - `@MainActor public static func frequencyHits(muscleSlug: String, weekStart: Date, minContribution: Double = 2.0, in context: ModelContext) -> Int` — algorithm: call `contributionPerSession(muscleSlug:weekStart:in:)`; return `values.filter { $0 >= minContribution }.count`. D-03 default minContribution = 2.0.
      
      - `@MainActor public static func weekOverWeekDelta(muscleSlug: String, weekStart: Date, in context: ModelContext) -> WeekOverWeekDelta` — algorithm:
        1. Compute `previousWeekStart = weekStart.addingTimeInterval(-7 * 86400)`.
        2. Compute `currentTotal = weeklyVolume(muscleSlug:, weekStart: weekStart, in: ctx).totalSets`.
        3. Compute `previousTotal = weeklyVolume(muscleSlug:, weekStart: previousWeekStart, in: ctx).totalSets`.
        4. Construct WeekOverWeekDelta — see supporting type below for the deltaCopy logic.
      
      - `public static func volumeZone(currentSets: Int, mev: Int, mav: Int, mrv: Int) -> VolumeZone` — delegates to plan 01's free `volumeZone(...)` function (single source of truth; the explicit delegate keeps the API surface complete on FatigueModel so callers don't need to remember the free function).
    
    Supporting value types in the SAME file:
      - `public struct WeightedSetTotal: Sendable, Equatable { public let directSets: Double; public let indirectSets: Double; public init(directSets: Double, indirectSets: Double) { ... }; public var totalSets: Double { directSets + indirectSets }; public var totalSetsRounded: Int { Int(totalSets.rounded()) } }`. NB: `totalSetsRounded` uses banker's rounding (default `.rounded()` mode) per Swift convention; tests assert specific examples that don't fall on the .5 boundary.
      - `public struct WeekOverWeekDelta: Sendable, Equatable { public let currentWeekTotal: Double; public let previousWeekTotal: Double; public init(currentWeekTotal: Double, previousWeekTotal: Double) { ... }; public var delta: Double { currentWeekTotal - previousWeekTotal }; public var deltaCopy: String }`. The deltaCopy computed property implements D-08 verbatim:
        ```
        public var deltaCopy: String {
            let rounded = Int(delta.rounded())
            if rounded > 0 { return "+\(rounded) vs last week" }
            if rounded < 0 { return "\(rounded) vs last week" }   // Int formatting renders "-N" naturally
            return "no change vs last week"
        }
        ```
    
    Create fitbodTests/Fatigue/FatigueModelWeeklyVolumeTests.swift, REPLACING the plan-01 RED scaffold. Annotate `@MainActor @Suite("FatigueModelWeeklyVolume", .serialized)`. Use `FatigueTestFixtures.makeContext()` for setup.
    
    Required @Test functions per the `<behavior>` block:
      - `weeklyVolumeCountsWorkingSetsForBenchPressOnChest`
      - `weeklyVolumeExcludesWarmups`
      - `weeklyVolumeExcludesSetTypeWarmup` (non-warmup row with setTypeRaw="warmup" — both guards in effect per Pitfall #3)
      - `restPauseClusterCountsAsOneSet` (Pitfall #9)
      - `workingKindsIncludesDropFailureRestPause`
      - `directIndirectSplitForBenchPressShoulders` + `directIndirectSplitForBenchPressTriceps` + `directIndirectSplitChestIsAllDirect`
      - `weekWindowExcludesPriorSunday`
      - `weekWindowIncludesCurrentSundayExcludesNextMonday`
      - `weeklyVolumeIsIdempotentAcrossCalls`
      - `totalSetsAndTotalSetsRoundedFromDirectIndirect`
      - `weekOverWeekDeltaPositive` + `weekOverWeekDeltaNegative` + `weekOverWeekDeltaZero`
      - `volumeZoneDelegatesToFreeFunction`
    
    Test helpers (in the suite struct or as a private extension):
      - `func makeBenchPressExercise(in ctx: ModelContext) -> Exercise` — creates an Exercise with canonicalName "barbell bench press", inserts MuscleGroup rows for chest/shoulders/triceps (or fetches if seeded), inserts three ExerciseMuscleStimulus rows with the D-07 weights (chest primary 1.0; shoulders secondary 0.5; triceps secondary 0.5). NB: the helper must insert MuscleGroup rows because in-memory ModelContainer doesn't auto-seed them — write a tiny inline seed for the three muscles needed.
      - `func mondayOfThisWeek() -> Date` — uses Calendar.current with firstWeekday=2 per Pitfall #4.
    
    Create fitbodTests/Fatigue/FrequencyHitsTests.swift, REPLACING the plan-01 RED scaffold. Annotate `@MainActor @Suite("FrequencyHits", .serialized)`. Use the same FatigueTestFixtures.makeContext() pattern.
    
    Required @Test functions:
      - `twoSessionsEachWithFullChestVolumeCountAsTwoHits`
      - `singleIndirectContributionDoesNotCountAsHit` (the D-03 anchoring test)
      - `multipleIndirectAddUpToHit` (four 0.5-weighted sets → 2.0 contribution → 1 hit)
      - `customMinContributionThreshold` (override the default 2.0; assert behavior at 0.5 / 1.0 / 3.0)
      - `sessionsAcrossDifferentWeeksDoNotCount` (a session last Monday is not counted for this week)
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/FatigueModelWeeklyVolume -only-testing fitbodTests/Fatigue/FrequencyHits 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    FatigueModel.swift exists with 5 static funcs + 2 supporting Sendable value types; FatigueModelWeeklyVolumeTests has 15+ GREEN @Test functions covering D-01 / D-04 / D-07 / D-08 + Pitfall #1 / #3 / #9 corners; FrequencyHitsTests has 5 GREEN @Test functions covering D-03 verbatim semantics; both suites use FatigueTestFixtures.makeContext() and Schema(SchemaV5.models); existing Phase 1-4 test suites remain GREEN (no regression).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StimulusWeightSeeder (51 curated lifts; row-level idempotency; range clamp [0,1] per T-05-01) + GREEN StimulusWeightSeederTests + RootView.task wire</name>
  <read_first>
    fitbod/Fatigue/StimulusWeightTable.swift,
    fitbod/Settings/PlateInventorySeeder.swift (analog — @MainActor public enum + seedIfNeeded),
    fitbod/ExerciseLibrary/ExerciseLibraryImporter.swift (analog — the bulk Exercise + ExerciseMuscleStimulus seeder shipped in Phase 1),
    fitbod/Models/Exercise.swift,
    fitbod/Models/ExerciseMuscleStimulus.swift,
    fitbod/Models/MuscleGroup.swift,
    fitbod/App/RootView.swift,
    fitbodTests/Fatigue/FatigueTestFixtures.swift,
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §6.3 + §6.4 (seeder pattern + idempotency flag),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (StimulusWeightSeeder section),
    .planning/phases/05-fatigue-model-plateau-detection/05-VALIDATION.md (Threat T-05-01)
  </read_first>
  <files>fitbod/Fatigue/StimulusWeightSeeder.swift, fitbod/App/RootView.swift, fitbodTests/Fatigue/StimulusWeightSeederTests.swift</files>
  <behavior>
    - Test (51 curated rows written on first launch): build a fresh ModelContext with Exercise rows seeded (use FatigueTestFixtures helper or the Phase 1 importer in test mode); call StimulusWeightSeeder.seedIfNeeded; for each of the 51 keys in StimulusWeightTable.overrides, locate the matching Exercise; assert its muscleStimuli relationship contains rows matching the curated (muscleSlug, role, weight) triples.
    - Test (user-edited row preservation per Pitfall #5 / VOL-01): pre-seed an ExerciseMuscleStimulus row for "barbell bench press" → chest with weight = 0.75 AND userEditedWeight = true. Run seeder. Assert the row's weight is STILL 0.75 (NOT overwritten back to 1.0); userEditedWeight is STILL true.
    - Test (non-curated exercise unchanged): for an exercise NOT in StimulusWeightTable.overrides (e.g., "cable woodchopper"), assert seeder does not modify its existing stimulus rows.
    - Test (idempotent across re-runs): run seeder twice in succession. Assert the second run produces no additional rows (count stable) and no value drift.
    - Test (T-05-01 range clamp): manually corrupt a StimulusWeightTable.overrides value to 1.5 via Swizzling — not possible at compile time; instead, the seeder must defensively clamp every weight to `max(0.0, min(1.0, value))` before write. Verify by writing a unit test that constructs a synthetic seeder call with an out-of-range value in a mock dictionary and asserts the persisted value is clamped to 1.0. Implementation: refactor the seeder's clamp into a small private function `clampedWeight(_ w: Double) -> Double { max(0.0, min(1.0, w)) }` and unit-test it directly via the seeder's per-row write path.
    - Test (D-07 direct/indirect roles preserved): after seeding, FatigueModel.weeklyVolume for a session of barbell bench press shows the expected 1.0 chest / 0.5 shoulders / 0.5 triceps split per RESEARCH §6.1.
  </behavior>
  <action>
    Create fitbod/Fatigue/StimulusWeightSeeder.swift mirroring fitbod/Settings/PlateInventorySeeder.swift's shape with the changes noted below.
    
    File header: "StimulusWeightSeeder.swift / fitbod / Phase 5 — VOL-01 stimulus-weighting seeder. / Idempotent: re-running overwrites curated rows WITHOUT touching user-edited rows. / User-edit preservation tracked via ExerciseMuscleStimulus.userEditedWeight (false → seeder writes; true → seeder skips). / NO UserDefaults flag — stimulus weights may be updated on app updates, so row-level guard is required (per RESEARCH §6.3). / @MainActor because ModelContext is @MainActor-bound.".
    
    Imports: `import Foundation, import SwiftData`.
    
    Body — `@MainActor public enum StimulusWeightSeeder`:
      ```
      @MainActor
      public enum StimulusWeightSeeder {
          public static func seedIfNeeded(in context: ModelContext) {
              for (canonicalName, triples) in StimulusWeightTable.overrides {
                  // 1. Locate the matching Exercise by canonicalName.
                  let target = canonicalName
                  let descriptor = FetchDescriptor<Exercise>(
                      predicate: #Predicate<Exercise> { $0.canonicalName == target }
                  )
                  guard let exercise = (try? context.fetch(descriptor))?.first else { continue }
                  // 2. For each curated (slug, role, weight) triple:
                  for triple in triples {
                      // 2a. Locate the muscle by slug.
                      let slug = triple.muscleSlug
                      let muscleDescriptor = FetchDescriptor<MuscleGroup>(
                          predicate: #Predicate<MuscleGroup> { $0.slug == slug }
                      )
                      guard let muscle = (try? context.fetch(muscleDescriptor))?.first else { continue }
                      // 2b. Find or create the ExerciseMuscleStimulus row.
                      let stims = exercise.muscleStimuli ?? []
                      let stimulusForThisMuscle = stims.first { $0.muscle?.slug == slug }
                      if let existing = stimulusForThisMuscle {
                          // 2c. Skip if user-edited.
                          if existing.userEditedWeight { continue }
                          existing.role = triple.role
                          existing.weight = clampedWeight(triple.weight)
                      } else {
                          let newStim = ExerciseMuscleStimulus()
                          newStim.role = triple.role
                          newStim.weight = clampedWeight(triple.weight)
                          newStim.muscle = muscle
                          newStim.exercise = exercise
                          context.insert(newStim)
                      }
                  }
              }
              try? context.save()
          }
          
          /// T-05-01 mitigation: clamp curated weight to [0, 1] before write.
          private static func clampedWeight(_ w: Double) -> Double {
              max(0.0, min(1.0, w))
          }
      }
      ```
      
      NB: the implementation chooses "find or update" semantics rather than "delete + recreate" to preserve the SwiftData identity of existing rows (which other relationships may reference, e.g., during snapshot tests). The user-edit guard runs on the EXISTING row only — first-time-curated triples that have no existing row simply get inserted.
    
    Modify fitbod/App/RootView.swift to add a 1-line call to `StimulusWeightSeeder.seedIfNeeded(in: ctx)` inside the existing RootView.task block where the Phase 1 ExerciseLibraryImporter is invoked. Placement: AFTER the library importer (must run AFTER Exercise rows are seeded so canonicalName lookups resolve). Wrap in a guard that requires `seedState` to have completed the library import (to avoid race conditions on cold first launch when neither has run yet).
    
    Create fitbodTests/Fatigue/StimulusWeightSeederTests.swift, REPLACING the plan-01 RED scaffold. Annotate `@MainActor @Suite("StimulusWeightSeeder", .serialized)`. Use `FatigueTestFixtures.makeContext()` for setup.
    
    Required @Test functions per the `<behavior>` block:
      - `seederWritesAllFiftyOneCuratedRows`: pre-seed a minimal set of MuscleGroup rows (all 17 slugs) + 51 Exercise rows (one per StimulusWeightTable.overrides key, with the corresponding canonicalName). Call seeder. For each of the 51 keys, assert the matching Exercise's muscleStimuli contains the curated triples with correct weights (use `#expect(stim.weight == triple.weight ± 0.001)`).
      - `userEditedRowsArePreservedAcrossReSeed`: pre-seed bench-press Exercise + chest MuscleGroup + ExerciseMuscleStimulus with weight=0.75 AND userEditedWeight=true. Call seeder. Assert weight is STILL 0.75 and userEditedWeight is STILL true.
      - `nonCuratedExercisesAreUntouched`: pre-seed a "cable woodchopper" Exercise + an existing stimulus row with weight=0.42. Call seeder. Assert weight is STILL 0.42.
      - `idempotentAcrossMultipleRuns`: call seeder twice; assert ExerciseMuscleStimulus row count is stable + all weights are stable.
      - `t0501ClampOutOfRangeWeights`: this is hard to test directly (the table is static-literal); instead, test the private `clampedWeight` directly by exposing it as `internal` for testing OR by writing a test that constructs an ExerciseMuscleStimulus with weight=1.5 manually, calls seeder, and asserts the seeder DID NOT overwrite the manually-set value because userEditedWeight defaulted false BUT the in-memory stimulus row weight is reset to whatever the seeder writes... Practical approach: make `clampedWeight` `internal` (not private), so the test can call `StimulusWeightSeeder.clampedWeight(1.5)` and `#expect == 1.0`, and `clampedWeight(-0.1) == 0.0`. Tag the function with `// internal for testing — T-05-01`. Document this in a code comment.
      - `seederDoesNotInsertDuplicateStimulusRows`: pre-seed bench-press Exercise + 3 ExerciseMuscleStimulus rows for chest/shoulders/triceps already (userEditedWeight=false). Call seeder. Assert stimulus row count for the exercise is STILL exactly 3 (the "find or update" path runs, not "delete + recreate").
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/StimulusWeightSeeder 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    StimulusWeightSeeder.swift exists with seedIfNeeded(in:) and internal clampedWeight helper; RootView.task wires StimulusWeightSeeder.seedIfNeeded after the library importer; StimulusWeightSeederTests has 6 GREEN @Test functions covering 51-row coverage + user-edit preservation + non-curated immutability + idempotency + T-05-01 clamp + no-duplicates; the seeder is called on every app launch (idempotent guard via userEditedWeight); the existing Phase 1 SeedTests remain GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MuscleVolumeBar + FatigueSurfaceView + RootView Today-tab mount + GREEN VolumeBarsUITests</name>
  <read_first>
    fitbod/Fatigue/FatigueModel.swift (built in Task 1),
    fitbod/Fatigue/VolumeZone.swift,
    fitbod/Sessions/PinnedNoteCapsule.swift (analog for inline asset color usage),
    fitbod/Sessions/SetTypeChip.swift (analog for compact accent capsule pattern),
    fitbod/App/RootView.swift,
    fitbod/Sessions/ResumeWorkoutBanner.swift (analog for @Query-driven banner that may or may not render),
    fitbod/Settings/SettingsView.swift (analog for Form rows + sections + LabeledContent),
    .planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md §Component Inventory + §MuscleVolumeBar internal layout + §FatigueSurfaceView layout + §Copywriting Contract verb labels + delta line + set count + §Asset Contract,
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §5 (volume bar UI SwiftUI implementation pattern),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (MuscleVolumeBar + FatigueSurfaceView sections)
  </read_first>
  <files>fitbod/Fatigue/MuscleVolumeBar.swift, fitbod/Fatigue/FatigueSurfaceView.swift, fitbod/App/RootView.swift, fitbodUITests/VolumeBarsUITests.swift</files>
  <behavior>
    - Behavior (UI-SPEC §MuscleVolumeBar internal layout verbatim): the bar renders a top HStack with `Text(muscle.displayName)` in `.body` weight + a trailing `Text("\(total.totalSetsRounded) / \(target.mrv)")` in `.caption.monospacedDigit().foregroundStyle(.secondary)`; then a GeometryReader ZStack with a `Color(.systemGray5)` rounded-rect track + the indirect fill (`Color(zoneAssetName).opacity(0.40)`) + the direct fill (`Color(zoneAssetName)` at full opacity); MEV / MAV tick marks (1pt vertical lines at proportional positions) drawn on top; bar height 16pt; below the bar a `Text(zone.verb)` in `.caption.foregroundStyle(.secondary)` and below that a `Text(delta.deltaCopy)` in `.caption2.foregroundStyle(.tertiary)`.
    - Behavior (D-07 verbatim two-tone): direct fill width is `(total.directSets / Double(target.mrv)) * geo.size.width`; indirect fill width is `(total.totalSets / Double(target.mrv)) * geo.size.width` — the visible indirect strip is the trailing portion because the direct fill overpaints the leading portion.
    - Behavior (UI-SPEC §Color zone palette): zone color is sourced from one of the 4 Asset Catalog entries shipped in plan 01 — `Color("VolumeBelowMEVGray")` / `Color("VolumeProductiveGreen")` / `Color("VolumeNearMRVAmber")` / `Color("VolumeOverMRVRed")`. NEVER use `Color.red` / `Color.green` literals.
    - Behavior (overMRV pulse — reduced-motion gated): when zone == .overMRV AND `@Environment(\.accessibilityReduceMotion)` is false, apply the repeating opacity animation per UI-SPEC §Interaction & Motion code snippet. When reduceMotion is true OR zone is not overMRV, no pulse.
    - Behavior (accessibility per UI-SPEC §Accessibility Contract — `MuscleVolumeBar` row): `accessibilityLabel` = "{muscle.displayName}: {totalSetsRounded} sets this week, {zone.verb}. {delta.deltaCopy}." + `accessibilityHint` = "Tap to see details." + `accessibilityTraits` = `.button`. The whole VStack is the tap target; navigation to `PerMuscleDetailView` is wired in plan 05 (this plan leaves the tap target in place but routes to a no-op closure parameter `onTap: (MuscleGroup) -> Void = { _ in }` — plan 05 wires the real NavigationLink).
    - Behavior (FatigueSurfaceView layout): UI-SPEC §FatigueSurfaceView layout — a ScrollView containing a "Volume This Week" `.headline` section header (UI-SPEC §Copywriting Contract verbatim) + a `LazyVStack(spacing: 8pt)` of `MuscleVolumeBar` rows + a placeholder Spacer where the heatmap will go in plan 05. Empty-state copy verbatim: heading "No sets logged this week" + body "Log a workout to see your weekly volume." (UI-SPEC §Copywriting Contract verbatim).
    - Behavior (FatigueSurfaceView filtering rule): show one MuscleVolumeBar per MuscleGroup where total.totalSetsRounded >= 1 OR (target.mev > 0 AND user has opted to show 'always show all bars'). For Phase 5 MVP: show every muscle that has either non-zero volume this week OR has a non-zero MEV target (i.e., the user expects to train it) — covers the 17 muscle bars with sensible defaults. Empty state only fires when NO muscles meet the filter (no sets logged this week AND no MEV targets exist, which is the cold-launch state before the Phase 5 plan 07 MuscleVolumeTargetSeeder runs OR before any session is logged).
    - Behavior (RootView mount): the existing TodayView VStack stack on the Today tab is extended — `FatigueSurfaceView()` inserted after ResumeWorkoutBanner (and after BlockCard if Phase 4 has shipped) but before the existing `Spacer()`. The mount uses no init parameters (FatigueSurfaceView is @Query-driven).
    - Behavior (VolumeBarsUITests): with a test-seeded ModelContainer containing one Monday strength session of barbell bench press with 3 chest sets, launching the app and tapping the Today tab shows a MuscleVolumeBar row whose accessibilityLabel contains "chest" (case-insensitive) AND contains one of the D-06 verb strings (depending on whether 3 sets falls in belowMEV / productive — should be belowMEV since MEV=8 from RPVolumeLandmarks).
  </behavior>
  <action>
    (1) Create fitbod/Fatigue/MuscleVolumeBar.swift. File header: "MuscleVolumeBar.swift / fitbod / Phase 5 — VOL-04 single muscle bar row. / D-06 verbatim verb copy + D-07 two-tone direct/indirect fill + D-08 verbatim delta line + UI-SPEC §MuscleVolumeBar internal layout verbatim. / Reduce-motion gated overMRV pulse animation. / Asset Catalog zone colors (NOT system literals).". Imports: `import SwiftUI`.
    
    Body — `public struct MuscleVolumeBar: View`:
      - Stored: `public let muscle: MuscleGroup`, `public let target: MuscleVolumeTarget`, `public let total: WeightedSetTotal`, `public let delta: WeekOverWeekDelta`, `public var isAdvisoryActive: Bool = false`, `public var onTap: (MuscleGroup) -> Void = { _ in }`.
      - `@Environment(\.accessibilityReduceMotion) private var reduceMotion`
      - `@State private var overMRVPulse: Bool = false`
      - Computed: `private var zone: VolumeZone { FatigueModel.volumeZone(currentSets: total.totalSetsRounded, mev: target.mev, mav: target.mav, mrv: target.mrv) }`.
      - body: a `VStack(alignment: .leading, spacing: 4)` containing the layout per the UI-SPEC §MuscleVolumeBar internal layout block: top HStack + bar GeometryReader/ZStack + verb Text + delta Text — every element per the layout block in `<behavior>` above.
      - Tap behavior: `.contentShape(Rectangle())` on the VStack + `.onTapGesture { onTap(muscle) }`. Wave 3 (plan 05) will replace `onTap` with a NavigationLink wrapper; for this plan the tap target exists but the default closure is no-op.
      - Accessibility: `.accessibilityElement(children: .combine)` + `.accessibilityLabel("\(muscle.displayName): \(total.totalSetsRounded) sets this week, \(zone.verb). \(delta.deltaCopy).")` + `.accessibilityHint("Tap to see details.")` + `.accessibilityAddTraits(.isButton)`.
      - Pulse animation block (only when zone == .overMRV AND !reduceMotion): `.opacity(overMRVPulse ? 1.0 : 0.7).animation(.easeInOut(duration: 1.2).repeatForever(autoreverses: true), value: overMRVPulse).onAppear { overMRVPulse = (zone == .overMRV && !reduceMotion) }.onChange(of: zone) { _, new in overMRVPulse = (new == .overMRV && !reduceMotion) }`.
      - Advisory ring: when `isAdvisoryActive`, overlay a 2pt RoundedRectangle stroke in `Color("VolumeNearMRVAmber").opacity(0.6)` on the bar track per UI-SPEC §Color "DeloadAdvisory amber accent" subsection. NB: plan 06 surfaces the actual isAdvisoryActive computation; this plan accepts the param but doesn't wire it (defaults to false).
      - Private helper `func zoneColor(_ z: VolumeZone) -> Color { switch z { case .belowMEV: return Color("VolumeBelowMEVGray"); case .productive: return Color("VolumeProductiveGreen"); case .nearMRV: return Color("VolumeNearMRVAmber"); case .overMRV: return Color("VolumeOverMRVRed") } }`.
    
    (2) Create fitbod/Fatigue/FatigueSurfaceView.swift. File header: "FatigueSurfaceView.swift / fitbod / Phase 5 — VOL-04 + VOL-05 host. / Today-tab ScrollView containing the 'Volume This Week' section (per-muscle MuscleVolumeBar) + (plan 05 fills the 'Muscle Heatmap' section). / @Query-driven per FOUND-06 (MV-VM-lite). / Calls FatigueModel.weeklyVolume inline in body — pure-function call is correct per RESEARCH §10 Pitfall #2 (no premature memoization at v1 scale).". Imports: `import SwiftUI, import SwiftData`.
    
    Body — `public struct FatigueSurfaceView: View`:
      - `@Environment(\.modelContext) private var ctx`
      - `@Query private var allMuscles: [MuscleGroup]` (filter to enabled muscles if a `disabled` flag exists; for Phase 5 it doesn't, so just bind all)
      - `@Query private var allTargets: [MuscleVolumeTarget]`
      - `private var weekStart: Date { Date.now.mondayAtMidnight(weekStartsMonday: true) }` (uses a small extension defined in this file or referenced from a utility; see RESEARCH §10 Pitfall #4)
      - Computed `visibleMuscles`: filter allMuscles to those that have either non-zero weekly volume OR a non-nil MuscleVolumeTarget with mev > 0; sort alphabetically by displayName.
      - body: a `ScrollView { VStack(alignment: .leading, spacing: 24) { volumeSection; heatmapPlaceholder } .padding(.horizontal, 16) }`.
      - `volumeSection`:
        ```
        VStack(alignment: .leading, spacing: 12) {
            Text("Volume This Week").font(.headline)
            if visibleMuscles.isEmpty {
                VStack(spacing: 8) {
                    Text("No sets logged this week").font(.headline)
                    Text("Log a workout to see your weekly volume.").font(.body).foregroundStyle(.secondary)
                }
                .padding(.vertical, 32)
                .frame(maxWidth: .infinity)
            } else {
                LazyVStack(spacing: 8) {
                    ForEach(visibleMuscles, id: \.persistentModelID) { muscle in
                        let target = allTargets.first { $0.muscle?.slug == muscle.slug } ?? defaultTarget(for: muscle)
                        let total = FatigueModel.weeklyVolume(muscleSlug: muscle.slug, weekStart: weekStart, in: ctx)
                        let delta = FatigueModel.weekOverWeekDelta(muscleSlug: muscle.slug, weekStart: weekStart, in: ctx)
                        MuscleVolumeBar(muscle: muscle, target: target, total: total, delta: delta)
                    }
                }
            }
        }
        ```
      - `heatmapPlaceholder`: simple section header "Muscle Heatmap" + a `Text("Heatmap renders in plan 05")` placeholder — replaced by plan 05.
      - `defaultTarget(for:)` helper: returns a transient (non-inserted) MuscleVolumeTarget seeded with the RPVolumeLandmarks.landmark(for: muscle.slug) values. Used when the user hasn't yet seeded the Phase 5 MuscleVolumeTargetSeeder (plan 07); the bars still render with sensible defaults so the user isn't blocked.
      - Add a private extension on `Date` for `mondayAtMidnight(weekStartsMonday:calendar:)` per RESEARCH §10 Pitfall #4 verbatim:
        ```
        private extension Date {
            func mondayAtMidnight(weekStartsMonday: Bool, calendar: Calendar = .current) -> Date {
                var cal = calendar
                if weekStartsMonday { cal.firstWeekday = 2 }
                let components = cal.dateComponents([.yearForWeekOfYear, .weekOfYear], from: self)
                return cal.date(from: components) ?? self
            }
        }
        ```
      
    (3) Modify fitbod/App/RootView.swift to mount FatigueSurfaceView on the Today tab. Insertion site: inside the existing `TodayView` inner struct's `body`, in the VStack stack between `ResumeWorkoutBanner(...)` and the existing `Spacer()`. After the modification the VStack should read approximately:
      ```
      VStack(spacing: 16) {
          ResumeWorkoutBanner(...)
          // PHASE 5: FatigueSurfaceView mount
          FatigueSurfaceView()
          Spacer()
      }
      ```
      Preserve every other line of TodayView verbatim — navigation title, padding, NavigationStack wrapper, navigation destinations. NB: Phase 4 plan 04-04a adds a `BlockCard` mount; the relative ordering per UI-SPEC §Phase 4 ↔ Phase 5 ↔ Phase 6 Boundary Reminders is: `DeloadAdvisoryBanner` (plan 06) → `BlockCard` (Phase 4) → `ResumeWorkoutBanner` (Phase 2) → `FatigueSurfaceView` (this plan). If Phase 4's BlockCard is already mounted by execution time, FatigueSurfaceView lands AFTER it. Verify the actual order against the in-tree RootView at execution time.
    
    (4) Create fitbodUITests/VolumeBarsUITests.swift. Annotate `final class VolumeBarsUITests: XCTestCase`. Setup: `continueAfterFailure = false` in setUpWithError. Use XCUIApplication launch with a test-seeded ModelContainer (inject via `app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-volume-bars"` or via launchArguments + a debug branch in fitbodApp.swift that recognizes the flag and runs a known fixture seed before the regular initialization). NB: this pattern requires a small `#if DEBUG` branch in fitbodApp.swift — if that branch doesn't exist yet, create it as part of this task (additive; only kicks in under the test env var).
    
    Required `@MainActor func` test methods:
      - `testChestBarRendersWithVerbForBelowMEV`: launch app with phase5-volume-bars fixture (3 chest sets); tap Today tab if not already there; assert `app.staticTexts["Volume This Week"].exists`; iterate `app.descendants(matching: .any).matching(NSPredicate(format: "label CONTAINS[c] %@", "chest"))` — assert at least one element exists; verify its parent's accessibilityLabel contains a D-06 string ("Below MEV — add volume" for 3 sets vs MEV 8).
      - `testEmptyStateWhenNoSessionsLogged`: launch app with no fixture; tap Today tab; assert `app.staticTexts["No sets logged this week"].exists`.
      - `testVerbCopyVerbatim`: this is hard via UI test (string fragments only); add a sanity check that `app.staticTexts["Productive range — hold/progress"]` or `app.staticTexts["Below MEV — add volume"]` exists when at least one bar renders. Don't enumerate all 4 zones — the unit test VolumeZoneVerbCopyTests already pins each one verbatim.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodUITests/VolumeBarsUITests 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    MuscleVolumeBar.swift exists with the verbatim UI-SPEC layout + D-07 two-tone fill + D-08 delta line + D-06 verb + reduce-motion-gated overMRV pulse + accessibility label; FatigueSurfaceView.swift exists with the volume section + empty state + a heatmap-placeholder section (filled by plan 05); RootView.swift mounts FatigueSurfaceView on the Today tab; VolumeBarsUITests has 3 GREEN UI tests covering bar rendering + empty state + verb-string presence; the user can open the app and see real per-muscle weekly volume bars (the first user-visible Phase 5 deliverable).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local user → SwiftData | Only user input crossing the persistence boundary this plan: none directly. The plan's writes are confined to StimulusWeightSeeder (curated table values clamped to [0,1] per T-05-01 mitigation). The user does not directly enter stimulus weights from any surface in this plan — plan 07's per-exercise editor adds that surface with explicit input validation. |
| Code → Code | All new types live in `fitbod/Fatigue/`; single-target iOS app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `StimulusWeightSeeder` writes `ExerciseMuscleStimulus.weight` per RESEARCH §6 — a stimulus weight outside [0,1] corrupts FatigueModel aggregation downstream | mitigate | `clampedWeight(_:)` private function clamps every weight to `max(0.0, min(1.0, value))` BEFORE write. Unit test `t0501ClampOutOfRangeWeights` pins both bounds. The static-literal StimulusWeightTable.overrides values are all in [0,1] by construction, but the clamp is defense-in-depth against future curation drift. |
| T-05-04 | Race condition | `RootView.task` calls StimulusWeightSeeder.seedIfNeeded — concurrent calls (e.g., if RootView appears twice in quick succession due to a SwiftUI lifecycle quirk) could race on the row-level userEditedWeight guard | mitigate | The seeder is `@MainActor` — Swift 6 strict concurrency serializes its execution. The guard reads + writes happen on the main actor, so race conditions are precluded by the actor isolation contract. No explicit lock needed. |
| T-05-05 | Information Disclosure | `FatigueModel.weeklyVolume` reads SetEntry rows including weight + reps + rpe — this is the user's training data, not externally-sensitive | accept | Single-user local app; the data crosses the function boundary into UI-rendered text. No PII, no externalization. Same disposition as Phase 1-4 read paths. |
| T-05-06 | Denial of Service | `FatigueModel.weeklyVolume` runs 17 times per body invalidation per RESEARCH §15 perf table | mitigate | Pre-computed perf budget: 17 × ~80 rows = ~1360 row traversals < 100ms. RESEARCH §10 Pitfall #2 explicitly advises against premature memoization at v1 scale. If profiling shows regression, the migration path is documented (introduce a `@State`-backed memo or a `WeeklyVolumeSnapshot` entity — neither needed for v1). |
</threat_model>

<verification>
- `FatigueModelWeeklyVolumeTests` + `FrequencyHitsTests` + `StimulusWeightSeederTests` (the 3 RED scaffolds from plan 01) all turn GREEN this plan.
- `VolumeBarsUITests` (NEW UI test in this plan) GREEN: launching the app with a test-seeded session shows MuscleVolumeBar rows on the Today tab; empty state appears when no sessions.
- Phase 1 / 2 / 3 / 4 test suites remain GREEN (no regression in SessionFactory, RestTimerEngine, RPEAutoregStrategy, PreviousMatchingIntent, etc.).
- `swift-format` idempotent on every new file.
- No new third-party SPM dependency introduced.
- All 4 Asset Catalog zone colors from plan 01 are referenced (not `Color.red` or `Color.green` literals).
- RootView.task wiring respects the existing seed orchestration order (library importer first, then StimulusWeightSeeder).
- TodayView VStack stacking order respects UI-SPEC §Phase 4 ↔ Phase 5 boundary reminder (FatigueSurfaceView lands BELOW any existing BlockCard).
- The `mondayAtMidnight` Date extension uses `Calendar.current.firstWeekday = 2` per Pitfall #4 (no raw date math).
</verification>

<success_criteria>
- After this plan, the user opens the Today tab and sees per-muscle weekly volume bars with verb labels — VOL-02 + VOL-04 closed end-to-end (the first user-visible Phase 5 deliverable).
- VOL-01 closed: the 51 curated lifts from RESEARCH §6.1 are seeded onto their ExerciseMuscleStimulus rows on first launch; user-edited rows are preserved across app updates.
- VOL-06 service-side: FatigueModel.frequencyHits is callable and correctly counts sessions meeting the ≥2-set threshold per D-03; the consuming UI is plan 07's per-muscle detail view.
- D-04 honored: no snapshot entity created; FatigueModel reads SetEntry rows on demand per render.
- D-07 honored: bars show direct (1.0 opacity) + indirect (0.40 opacity) two-tone fill correctly; a barbell row session shows lats direct + biceps indirect (NOT double-1.0).
- D-08 honored: every bar shows '+N vs last week' / '-N vs last week' / 'no change vs last week' verbatim.
- 3 RED scaffolds from plan 01 (FatigueModelWeeklyVolume / FrequencyHits / StimulusWeightSeeder) turn GREEN.
- T-05-01 mitigation in place: stimulus weights clamped to [0,1] before persistence write.
- Pitfall #1 (local-let capture), #3 (filter both isWarmup and workingKinds), #4 (Calendar firstWeekday for Monday), #9 (1 SetEntry = 1 set regardless of cluster sub-reps) all observed in the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fatigue-model-plateau-detection/05-02-SUMMARY.md`.
</output>
