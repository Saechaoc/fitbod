---
phase: 05-fatigue-model-plateau-detection
plan: 01
type: execute
wave: 0
depends_on: []
scope_exception: wave_0_infrastructure
files_modified:
  - fitbod/Models/UserSettings.swift
  - fitbod/Models/Exercise.swift
  - fitbod/Models/ExerciseMuscleStimulus.swift
  - fitbod/Models/MuscleVolumeTarget.swift
  - fitbod/Persistence/SchemaV5.swift
  - fitbod/Persistence/FitbodSchemaMigrationPlan.swift
  - fitbod/Prescription/OneRepMaxEstimator.swift
  - fitbod/Fatigue/VolumeZone.swift
  - fitbod/Fatigue/StimulusWeightTable.swift
  - fitbod/Fatigue/RPVolumeLandmarks.swift
  - fitbod/Fatigue/MuscleRegionPaths.swift
  - fitbod/Assets.xcassets/VolumeBelowMEVGray.colorset/Contents.json
  - fitbod/Assets.xcassets/VolumeProductiveGreen.colorset/Contents.json
  - fitbod/Assets.xcassets/VolumeNearMRVAmber.colorset/Contents.json
  - fitbod/Assets.xcassets/VolumeOverMRVRed.colorset/Contents.json
  - fitbodTests/Fatigue/FatigueTestFixtures.swift
  - fitbodTests/Fatigue/FatigueModelWeeklyVolumeTests.swift
  - fitbodTests/Fatigue/VolumeZoneTests.swift
  - fitbodTests/Fatigue/VolumeZoneVerbCopyTests.swift
  - fitbodTests/Fatigue/FrequencyHitsTests.swift
  - fitbodTests/Fatigue/PlateauDetectorTests.swift
  - fitbodTests/Fatigue/SuggestedActionTests.swift
  - fitbodTests/Fatigue/DeloadAdvisorTests.swift
  - fitbodTests/Fatigue/StimulusWeightSeederTests.swift
  - fitbodTests/Fatigue/MuscleVolumeTargetSeederTests.swift
  - fitbodTests/Fatigue/WeeklyRecapTriggerTests.swift
  - fitbodTests/Fatigue/MuscleRegionPathsTests.swift
  - fitbodTests/Fatigue/OneRepMaxEstimatorTests.swift
  - fitbodTests/Persistence/SchemaV5MigrationTests.swift
autonomous: true
requirements: [VOL-01, VOL-03, VOL-04, VOL-05, PROG-06, SET-06]

must_haves:
  truths:
    - "SchemaV5 ships as the additive successor to SchemaV4 (Phase 4 ships SchemaV4 with Block.reviewedAt + RoutineExercise.prescribedWeight; Phase 5 stacks SchemaV5 on top). 8 additive default-valued fields ship: UserSettings.frequencyHitMinSets:Int=2, UserSettings.deloadAdvisoryDismissedWeekStart:Date?=nil, UserSettings.weeklyRecapShownForWeekStart:Date?=nil, UserSettings.plateauTolerance default bump 0.005→0.02, Exercise.plateauWindowOverride:Int?=nil, Exercise.plateauToleranceOverride:Double?=nil, ExerciseMuscleStimulus.userEditedWeight:Bool=false, MuscleVolumeTarget.userEdited:Bool=false. All FOUND-02 safe."
    - "FitbodSchemaMigrationPlan registers SchemaV5 and migrateV4toV5 as MigrationStage.custom with willMigrate that retroactively bumps existing UserSettings.plateauTolerance == 0.005 to 0.02 (per RESEARCH §9.2; lightweight migration is not sufficient because the default change doesn't rewrite existing rows). All other 7 deltas are lightweight-eligible but ride in the same custom stage."
    - "OneRepMaxEstimator is a pure-function enum namespace in fitbod/Prescription/ exposing estimate(weight:Double, reps:Int) -> Double?; Brzycki for reps ≤ 6, Epley for 6 < reps ≤ 10, nil for reps > 10 per REQUIREMENTS PROG-02"
    - "VolumeZone is a Sendable Equatable enum with cases belowMEV / productive / nearMRV / overMRV — verbatim per D-05; free function volumeZone(currentSets:mev:mav:mrv:) -> VolumeZone uses strict < bounds (NOT <=); VolumeZone.verb computed property returns the four D-06 strings character-for-character"
    - "StimulusWeightTable is a public enum with static let overrides: [String: [(muscleSlug:String, role:String, weight:Double)]] containing 51 curated lifts from RESEARCH §6.1 verbatim (keys are canonical exercise names normalized to lowercase + diacritic-stripped to match Exercise.canonicalName already-seeded)"
    - "RPVolumeLandmarks is a public enum with a public struct Landmark (mv/mev/mav/mrv: Int) and static let landmarks: [String: Landmark] for all 17 free-exercise-db muscle slugs with RESEARCH §7.1 values verbatim; landmark(for:) returns a default Landmark(mv:4, mev:6, mav:12, mrv:18) for unknown slugs"
    - "MuscleRegionPaths is a public enum with two static lets — front: [String: Path] and back: [String: Path] — both containing all 17 muscle slugs. Wave 0 ships STUB Path data (simple `Rectangle` paths at approximate body-region positions per UI-SPEC §BodySilhouetteView wave strategy); Wave 3 (Plan 05) refines to anatomically-correct bezier curves. Tests pin 'registry contains all 17 slugs' regardless of geometry"
    - "Asset Catalog ships 4 new colorsets with Phase 5 UI-SPEC §Color verbatim hex pairs: VolumeBelowMEVGray (#8E8E93 / #636366), VolumeProductiveGreen (#34C759 / #30D158), VolumeNearMRVAmber (#FF9500 / #FF9F0A), VolumeOverMRVRed (#FF3B30 / #FF453A). Each colorset directory mirrors AccentColor.colorset Contents.json structure (any-appearance + dark-appearance idiom)"
    - "FatigueTestFixtures is a @MainActor enum providing static helpers: makeContext() (in-memory ModelContainer over Schema(SchemaV5.models) + FitbodSchemaMigrationPlan), weeklySessions(forExercise:weights:reps:rpe:weekStart:in:) (deterministic SetEntry-row builder), and per-edge-case factories from RESEARCH §4 (stalledWindow / progressingWindow / threeSessionWindow / fourSessionsOneHighRep / belowMAVWithStall / rpeCreepStall / blockWeek3Stall / bareStall / noBlockStall / exerciseWithOverride / intentSplitHistory). Test fixture pattern mirrors PreviousMatchingIntentTests.makeContext."
    - "Twelve unit test suites scaffold in fitbodTests/Fatigue/ and one in fitbodTests/Persistence/. Wave 0 GREEN suites: VolumeZoneTests + VolumeZoneVerbCopyTests + OneRepMaxEstimatorTests + MuscleRegionPathsTests + SchemaV5MigrationTests (5 GREEN — pure-function or schema-level checks). Wave 0 RED suites: FatigueModelWeeklyVolumeTests + FrequencyHitsTests + PlateauDetectorTests + SuggestedActionTests + DeloadAdvisorTests + StimulusWeightSeederTests + MuscleVolumeTargetSeederTests + WeeklyRecapTriggerTests (8 RED — Issue.record placeholders, flipped by plans 02 through 07)"
  artifacts:
    - path: "fitbod/Persistence/SchemaV5.swift"
      provides: "public enum SchemaV5: VersionedSchema with versionIdentifier Schema.Version(5,0,0) and models = SchemaV4.models (entity catalog unchanged from V4; only field additions on existing entities)"
      contains: "enum SchemaV5: VersionedSchema"
    - path: "fitbod/Persistence/FitbodSchemaMigrationPlan.swift"
      provides: "schemas list extended to [V1, V2, V3, V4, V5]; stages list extended with migrateV4toV5 (MigrationStage.custom with willMigrate plateauTolerance bump per RESEARCH §9.3); existing V1→V2, V2→V3, V3→V4 stages preserved"
      contains: "migrateV4toV5"
    - path: "fitbod/Prescription/OneRepMaxEstimator.swift"
      provides: "public enum OneRepMaxEstimator namespace; static func estimate(weight:Double, reps:Int) -> Double? implementing Brzycki ≤6 / Epley 6-10 / nil >10 per REQUIREMENTS PROG-02; pure value-type math with no SwiftData coupling; implicitly Sendable"
      contains: "enum OneRepMaxEstimator"
    - path: "fitbod/Fatigue/VolumeZone.swift"
      provides: "public enum VolumeZone (cases belowMEV/productive/nearMRV/overMRV; Sendable; Equatable); free public func volumeZone(currentSets:Int, mev:Int, mav:Int, mrv:Int) -> VolumeZone per D-05 verbatim; VolumeZone.verb computed property per D-06 verbatim"
      contains: "enum VolumeZone"
    - path: "fitbod/Fatigue/StimulusWeightTable.swift"
      provides: "public enum StimulusWeightTable with 51-entry static overrides dictionary keyed by canonical exercise name; value type is array of (muscleSlug:String, role:String, weight:Double) tuples; values verbatim from RESEARCH §6.1"
      contains: "enum StimulusWeightTable"
    - path: "fitbod/Fatigue/RPVolumeLandmarks.swift"
      provides: "public enum RPVolumeLandmarks with public struct Landmark (mv:Int, mev:Int, mav:Int, mrv:Int) and static landmarks dictionary for all 17 muscle slugs; values verbatim from RESEARCH §7.1; landmark(for:) returns conservative fallback for unknown slugs"
      contains: "enum RPVolumeLandmarks"
    - path: "fitbod/Fatigue/MuscleRegionPaths.swift"
      provides: "public enum MuscleRegionPaths with static lets front: [String: Path] and back: [String: Path]; Wave 0 stub Rectangle paths at approximate body positions; both registries cover all 17 muscle slugs"
      contains: "enum MuscleRegionPaths"
    - path: "fitbod/Assets.xcassets/VolumeBelowMEVGray.colorset/Contents.json"
      provides: "Light #8E8E93 / Dark #636366 colorset following AccentColor.colorset JSON shape"
      contains: "any-appearance"
    - path: "fitbod/Assets.xcassets/VolumeProductiveGreen.colorset/Contents.json"
      provides: "Light #34C759 / Dark #30D158 colorset"
      contains: "any-appearance"
    - path: "fitbod/Assets.xcassets/VolumeNearMRVAmber.colorset/Contents.json"
      provides: "Light #FF9500 / Dark #FF9F0A colorset"
      contains: "any-appearance"
    - path: "fitbod/Assets.xcassets/VolumeOverMRVRed.colorset/Contents.json"
      provides: "Light #FF3B30 / Dark #FF453A colorset"
      contains: "any-appearance"
    - path: "fitbodTests/Fatigue/FatigueTestFixtures.swift"
      provides: "@MainActor enum FatigueTestFixtures with makeContext() in-memory builder + weeklySessions(...) helper + named edge-case factories matching RESEARCH §4 table; shared by every downstream Fatigue test"
      contains: "enum FatigueTestFixtures"
  key_links:
    - from: "fitbod/Persistence/SchemaV5.swift"
      to: "fitbod/Persistence/SchemaV4.swift"
      via: "Composition — SchemaV5.models == SchemaV4.models; the additive delta is on existing entities (UserSettings + Exercise + ExerciseMuscleStimulus + MuscleVolumeTarget), not the entity catalog"
      pattern: "SchemaV4.models"
    - from: "fitbod/Persistence/FitbodSchemaMigrationPlan.swift"
      to: "fitbod/Models/UserSettings.swift"
      via: "willMigrate closure on migrateV4toV5 fetches singleton UserSettings; bumps plateauTolerance 0.005→0.02 retroactively"
      pattern: "abs(settings.plateauTolerance - 0.005) < 1e-9"
    - from: "fitbod/Fatigue/VolumeZone.swift"
      to: ".planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md"
      via: "D-05 verbatim Swift snippet (case names, strict < bounds) + D-06 verbatim copy"
      pattern: "case belowMEV"
    - from: "fitbod/Fatigue/StimulusWeightTable.swift"
      to: ".planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md"
      via: "§6.1 YAML table — 51 lifts ingested verbatim as Swift dictionary literals"
      pattern: "barbell bench press"
---

<objective>
Lay the Phase 5 Wave-0 foundation: SchemaV5 with 8 additive default-valued fields + the one custom-migration nuance for the `UserSettings.plateauTolerance` default bump (0.005 → 0.02 per D-12), the `OneRepMaxEstimator` extraction (Brzycki ≤6 / Epley 6-10 / nil >10), the verbatim `VolumeZone` enum + free function + verb-copy property (D-05 + D-06), the curated `StimulusWeightTable` and `RPVolumeLandmarks` static-literal tables (RESEARCH §6 + §7 verbatim), the `MuscleRegionPaths` registry with all 17 slugs as stub `Rectangle` paths (anatomical refinement deferred to Wave 3 / plan 05), the 4 new Asset Catalog color entries (UI-SPEC §Asset Contract verbatim hex pairs), and the `FatigueTestFixtures` helper + thirteen test suites (5 GREEN this plan; 8 RED with `Issue.record` placeholders flipped by downstream plans).

**Critical sequencing note:** Phase 4 has authored plans claiming `SchemaV4` (with `Block.reviewedAt` + `RoutineExercise.prescribedWeight`). This plan ships `SchemaV5` as the next additive schema version. As of plan-write time `ls fitbod/Persistence/` returned `[FitbodSchemaMigrationPlan.swift, PreviewModelContainer.swift, SchemaV1.swift, SchemaV2.swift, SchemaV3.swift]` — SchemaV4 has NOT yet shipped (Phase 4 plan 04-01 is still pending). This plan's execution must run AFTER Phase 4 plan 04-01 ships SchemaV4 so that `SchemaV5.models == SchemaV4.models` is a valid composition.

This plan is registered with `scope_exception: wave_0_infrastructure` because it intentionally scaffolds infrastructure for every downstream Phase 5 plan rather than delivering a user-visible vertical slice. The scope exception lets the planner spread Wave-0 scaffolds (8 schema deltas + 7 production files + 4 Asset Catalog entries + 13 test files) across one foundational plan so downstream plans stay focused on one vertical slice each.

Purpose: every downstream Phase 5 plan composes against these primitives. The schema bump unblocks `FatigueModel.weeklyVolume` (plan 02), the per-exercise plateau override fields unblock `PlateauDetector.evaluate` (plan 03), the seeder idempotency flags unblock `StimulusWeightSeeder` + `MuscleVolumeTargetSeeder` (plan 02 / 07), the dismissal date unblocks `DeloadAdvisor.evaluate` (plan 04), the weekly recap guard unblocks the VOL-07 sheet (plan 07). The `OneRepMaxEstimator` extraction (per RESEARCH §16 / §1 — Phase 3's `Calibration.swift` does NOT itself partition Brzycki/Epley; the partitioning is needed by `PlateauDetector` and Phase 6 charts) is the single shared math primitive that both Phase 5 and Phase 6 depend on.

Output: SchemaV5 + custom migration plan, four model file modifications, three new production files in `fitbod/Prescription/` and `fitbod/Fatigue/`, four Asset Catalog colorsets, thirteen new test files (5 GREEN, 8 RED with explicit Issue.record placeholders flipped by downstream plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md
@.planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md
@.planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md
@.planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md
@.planning/phases/05-fatigue-model-plateau-detection/05-VALIDATION.md

@.planning/phases/01-foundation-exercise-library/01-CONTEXT.md
@.planning/phases/04-periodization-blocks/04-01-PLAN.md

@fitbod/Models/UserSettings.swift
@fitbod/Models/Exercise.swift
@fitbod/Models/ExerciseMuscleStimulus.swift
@fitbod/Models/MuscleVolumeTarget.swift
@fitbod/Models/MuscleGroup.swift
@fitbod/Models/Enums/Intent.swift
@fitbod/Persistence/SchemaV1.swift
@fitbod/Persistence/SchemaV2.swift
@fitbod/Persistence/SchemaV3.swift
@fitbod/Persistence/FitbodSchemaMigrationPlan.swift
@fitbod/Prescription/Calibration.swift
@fitbod/Prescription/TuchschererTable.swift
@fitbod/Prescription/RPEAutoregStrategy.swift
@fitbod/Settings/PlateInventorySeeder.swift
@fitbod/ExerciseLibrary/MuscleRegionMap.swift
@fitbod/Sessions/PreviousMatchingIntent.swift
@fitbodTests/SchemaV3MigrationTests.swift
@fitbodTests/RPEAutoregStrategyTests.swift
@fitbodTests/PreviousMatchingIntentTests.swift

<interfaces>
<!-- Existing types and contracts the executor needs. Extracted from the codebase + RESEARCH §1. -->
<!-- Do NOT explore the codebase for these — they are listed authoritatively below. -->

From fitbod/Models/UserSettings.swift (current shape — plan adds 3 fields + bumps plateauTolerance default):
```
@Model
public final class UserSettings {
    @Attribute(.unique) public var id: UUID = UUID()
    public var unitsRaw: String = "lb"
    public var defaultProgressionKindRaw: String = "double"
    public var warmupSchemeRaw: String = "standard"
    public var customWarmupPercents: [Double]? = nil
    public var plateauWindowSessions: Int = 4
    public var plateauTolerance: Double = 0.005   // BUMPED to 0.02 in this plan
    public var deloadAlertEnabled: Bool = true
    public var weekStartsMonday: Bool = true
    public var defaultIncrementKg: Double = 2.5
    public var minCalibrationSets: Int = 10
    public init() {}
    public static func `default`() -> UserSettings { ... }
}
```

From fitbod/Models/Exercise.swift — current shape ALREADY contains `smallestIncrement: Double?` and `barWeightOverride: Double?` Phase 3 fields. Plan adds two new Optional fields per RESEARCH §9.1.

From fitbod/Models/ExerciseMuscleStimulus.swift (current shape — plan adds 1 Bool flag field):
```
@Model
public final class ExerciseMuscleStimulus {
    @Attribute(.unique) public var id: UUID = UUID()
    public var role: String = "primary"
    public var weight: Double = 1.0
    public var exercise: Exercise? = nil
    public var muscle: MuscleGroup? = nil
    public init() {}
}
```

From fitbod/Models/MuscleVolumeTarget.swift (current shape — plan adds 1 Bool flag field):
```
@Model
public final class MuscleVolumeTarget {
    @Attribute(.unique) public var id: UUID = UUID()
    public var muscle: MuscleGroup? = nil
    public var mev: Int = 8
    public var mav: Int = 14
    public var mrv: Int = 22
    public var mv: Int = 6
    public var notes: String? = nil
    public init() {}
}
```

From fitbod/Persistence/FitbodSchemaMigrationPlan.swift (extending shape; Phase 4 plan 04-01 adds V3→V4 lightweight stage before this plan runs):
```
public enum FitbodSchemaMigrationPlan: SchemaMigrationPlan {
    public static var schemas: [any VersionedSchema.Type] {
        [SchemaV1.self, SchemaV2.self, SchemaV3.self, SchemaV4.self]   // V5 appended by this plan
    }
    public static var stages: [MigrationStage] {
        [migrateV1toV2, migrateV2toV3, migrateV3toV4]                  // migrateV4toV5 appended by this plan
    }
    public static let migrateV1toV2 = MigrationStage.lightweight(...)
    public static let migrateV2toV3 = MigrationStage.lightweight(...)
    public static let migrateV3toV4 = MigrationStage.lightweight(...)   // shipped by Phase 4 plan 04-01
}
```

From fitbod/Prescription/Calibration.swift (analog for pure-function enum namespace + file header conventions):
```
// File header always includes: "No SwiftData coupling. No @MainActor. Implicitly Sendable."
public enum Calibration {
    public static func predict(history: [HistoryPoint], targetReps: Int, targetRPE: Double, now: Date = Date()) -> Double? {
        guard !history.isEmpty else { return nil }
        // pure-function math
    }
}
public struct HistoryPoint: Sendable, Equatable { ... }
```

From fitbod/Prescription/TuchschererTable.swift (analog for static-literal-table enum namespace):
```
public enum TuchschererTable {
    public static let percentFor: [Int: [Double: Double]] = [
        1: [10.0: 1.000, 9.5: 0.978, ...],
        ...
    ]
    public static func percent(reps: Int, rpe: Double) -> Double? { ... }
}
```

From fitbod/ExerciseLibrary/MuscleRegionMap.swift (analog for slug registry — `public enum` with static dictionaries keyed by muscle slug; all 17 free-exercise-db muscle slugs):
```
public enum MuscleRegionMap {
    public static let allSlugs: [String] = [
        "abdominals", "abductors", "adductors", "biceps", "calves", "chest",
        "forearms", "glutes", "hamstrings", "lats", "lower back", "middle back",
        "neck", "quadriceps", "shoulders", "traps", "triceps"
    ]
    public static func region(for slug: String) -> MuscleRegion { ... }
}
```

From fitbod/Prescription/RPEAutoregStrategy.swift — Brzycki/Epley partitioning is currently INSIDE this file's helper functions; Wave 0 extracts the partitioning to OneRepMaxEstimator.swift WITHOUT changing RPEAutoregStrategy's prescribe output (per RESEARCH §1 / §16 / Assumption A4). DO NOT change RPEAutoregStrategy's public surface — only refactor the internal call site to use OneRepMaxEstimator.estimate.

From fitbod/Settings/PlateInventorySeeder.swift (analog for `@MainActor public enum` seeder pattern — referenced by downstream plans 02 and 07 which will write StimulusWeightSeeder + MuscleVolumeTargetSeeder against this pattern).

From fitbodTests/SchemaV3MigrationTests.swift (analog for migration test fixture — makeContext() helper, suite header, plateauTolerance bump assertions; copy structure verbatim, swap V3→V5).

From fitbodTests/RPEAutoregStrategyTests.swift (analog for Swift Testing suite without ModelContext — pure-function tests, no @MainActor, no .serialized).

From fitbodTests/PreviousMatchingIntentTests.swift (analog for ModelContext-based tests — @MainActor + @Suite + .serialized + makeContext() helper).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SchemaV5 + 8 additive fields + custom willMigrate plateauTolerance bump + Asset Catalog colors + GREEN SchemaV5MigrationTests + GREEN OneRepMaxEstimatorTests + extracted OneRepMaxEstimator</name>
  <read_first>
    fitbod/Models/UserSettings.swift,
    fitbod/Models/Exercise.swift,
    fitbod/Models/ExerciseMuscleStimulus.swift,
    fitbod/Models/MuscleVolumeTarget.swift,
    fitbod/Persistence/SchemaV3.swift,
    fitbod/Persistence/FitbodSchemaMigrationPlan.swift,
    fitbod/Prescription/Calibration.swift,
    fitbod/Prescription/RPEAutoregStrategy.swift,
    fitbod/Assets.xcassets/AccentColor.colorset/Contents.json,
    fitbodTests/SchemaV3MigrationTests.swift,
    .planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md (D-03, D-12, D-17, Claude's Discretion fields),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §9 (Schema Migration), §16 (Don't Hand-Roll — OneRepMaxEstimator extraction),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (SchemaV4 → translate to SchemaV5 per sequencing note in objective; OneRepMaxEstimator pattern),
    .planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md §Asset Contract (verbatim hex pairs)
  </read_first>
  <files>fitbod/Models/UserSettings.swift, fitbod/Models/Exercise.swift, fitbod/Models/ExerciseMuscleStimulus.swift, fitbod/Models/MuscleVolumeTarget.swift, fitbod/Persistence/SchemaV5.swift, fitbod/Persistence/FitbodSchemaMigrationPlan.swift, fitbod/Prescription/OneRepMaxEstimator.swift, fitbod/Prescription/RPEAutoregStrategy.swift, fitbod/Assets.xcassets/VolumeBelowMEVGray.colorset/Contents.json, fitbod/Assets.xcassets/VolumeProductiveGreen.colorset/Contents.json, fitbod/Assets.xcassets/VolumeNearMRVAmber.colorset/Contents.json, fitbod/Assets.xcassets/VolumeOverMRVRed.colorset/Contents.json, fitbodTests/Persistence/SchemaV5MigrationTests.swift, fitbodTests/Fatigue/OneRepMaxEstimatorTests.swift</files>
  <behavior>
    - Test: `UserSettings` instance freshly inserted into a Schema(SchemaV5.models) in-memory container has `frequencyHitMinSets == 2`, `deloadAdvisoryDismissedWeekStart == nil`, `weeklyRecapShownForWeekStart == nil`, `plateauTolerance == 0.02` (new default per D-12).
    - Test: `Exercise` instance freshly inserted into a Schema(SchemaV5.models) in-memory container has `plateauWindowOverride == nil` AND `plateauToleranceOverride == nil`.
    - Test: `ExerciseMuscleStimulus` instance freshly inserted has `userEditedWeight == false`.
    - Test: `MuscleVolumeTarget` instance freshly inserted has `userEdited == false`.
    - Test: `FitbodSchemaMigrationPlan.schemas.map { String(describing: $0) } == ["SchemaV1", "SchemaV2", "SchemaV3", "SchemaV4", "SchemaV5"]`.
    - Test: `FitbodSchemaMigrationPlan.stages.count == 4` (V1→V2, V2→V3, V3→V4, V4→V5).
    - Test: `migrateV4toV5` is a `MigrationStage.custom` (not `.lightweight`) — pin the custom-migration shape so the willMigrate closure cannot be quietly converted.
    - Test (the plateauTolerance bump): seed a UserSettings into a Schema(SchemaV4.models) container, set `plateauTolerance = 0.005`, save, then re-open the container against Schema(SchemaV5.models) with the migration plan. Re-fetch the UserSettings — `plateauTolerance` is now `0.02` ± 1e-9.
    - Test (the no-bump path): seed a UserSettings into a Schema(SchemaV4.models) container, set `plateauTolerance = 0.015` (user-edited from default), save, migrate to V5. Re-fetch — `plateauTolerance` is still `0.015` (user value preserved).
    - Test: `OneRepMaxEstimator.estimate(weight: 100, reps: 5)` returns `100 * (36.0 / 32.0) == 112.5` (Brzycki).
    - Test: `OneRepMaxEstimator.estimate(weight: 100, reps: 8)` returns `100 * (1 + 8/30.0) ≈ 126.67` (Epley).
    - Test: `OneRepMaxEstimator.estimate(weight: 100, reps: 6)` uses Brzycki (the boundary — reps ≤ 6 per PROG-02 / D-10 / RESEARCH §1).
    - Test: `OneRepMaxEstimator.estimate(weight: 100, reps: 11)` returns nil (suppression per PROG-02).
    - Test: `OneRepMaxEstimator.estimate(weight: 100, reps: 0)` returns nil (defensive guard against zero reps).
    - Test (regression): the existing `RPEAutoregStrategyTests` suite remains GREEN after the internal refactor that uses `OneRepMaxEstimator.estimate` (no public-surface change to `RPEAutoregStrategy`).
  </behavior>
  <action>
    PRE-EXECUTION GUARD (sequencing per objective): before creating SchemaV5.swift, run `find fitbod/Persistence -name "SchemaV4.swift"`. SchemaV4.swift MUST EXIST on disk (shipped by Phase 4 plan 04-01) for this plan to run. If SchemaV4 is missing, STOP and surface a blocker: "Phase 5 Wave 0 depends on Phase 4 plan 04-01 (SchemaV4). Execute Phase 4 plan 04-01 first." Do NOT downgrade to SchemaV4 — Phase 4 owns that version per its own scope-exception plan.
    
    (1) Modify fitbod/Models/UserSettings.swift: append three new persistent properties immediately after `public var minCalibrationSets: Int = 10` — `public var frequencyHitMinSets: Int = 2` (D-03 frequency-hit threshold), `public var deloadAdvisoryDismissedWeekStart: Date? = nil` (D-17 dismissal state), `public var weeklyRecapShownForWeekStart: Date? = nil` (VOL-07 trigger guard). Change `public var plateauTolerance: Double = 0.005` to `public var plateauTolerance: Double = 0.02` per D-12 (existing rows are handled by the willMigrate closure below; the @Model default change only affects newly-inserted UserSettings rows). Add a Phase-5-tagged section comment above the new fields ("// Phase 5 — D-03 / D-17 / VOL-07 additive default-valued fields"). Preserve all existing fields and the `default()` factory verbatim.
    
    (2) Modify fitbod/Models/Exercise.swift: add two new optional persistent properties per SET-06 (RESEARCH §9.1 + §11.2). Placement: alongside other prescription-related fields (e.g., near `smallestIncrement` and `barWeightOverride` if present from Phase 3, or at end of class body otherwise). Declarations: `public var plateauWindowOverride: Int? = nil` and `public var plateauToleranceOverride: Double? = nil`. Both nil → fall back to UserSettings defaults per SET-06. Section comment: "// Phase 5 — SET-06 per-exercise plateau threshold overrides".
    
    (3) Modify fitbod/Models/ExerciseMuscleStimulus.swift: append `public var userEditedWeight: Bool = false` after `public var weight: Double = 1.0`. Section comment: "// Phase 5 — idempotency flag: StimulusWeightSeeder skips rows where this is true (VOL-01)".
    
    (4) Modify fitbod/Models/MuscleVolumeTarget.swift: append `public var userEdited: Bool = false` after `public var notes: String? = nil`. Section comment: "// Phase 5 — idempotency flag: MuscleVolumeTargetSeeder skips rows where this is true (VOL-03 / SET-05)".
    
    (5) Create fitbod/Persistence/SchemaV5.swift mirroring SchemaV3.swift / SchemaV4.swift verbatim: `public enum SchemaV5: VersionedSchema { public static let versionIdentifier = Schema.Version(5, 0, 0); public static var models: [any PersistentModel.Type] { SchemaV4.models } }`. File header comment: "SchemaV5 — Phase 5 additive delta. 8 new fields: UserSettings.frequencyHitMinSets / .deloadAdvisoryDismissedWeekStart / .weeklyRecapShownForWeekStart + plateauTolerance default bump 0.005→0.02 (handled by V4→V5 custom willMigrate); Exercise.plateauWindowOverride / .plateauToleranceOverride; ExerciseMuscleStimulus.userEditedWeight; MuscleVolumeTarget.userEdited. Entity catalog unchanged from V4. Lightweight-eligible for additive fields; one custom willMigrate covers the plateauTolerance default-bump nuance per RESEARCH §9.2."
    
    (6) Modify fitbod/Persistence/FitbodSchemaMigrationPlan.swift: extend `schemas` to `[SchemaV1.self, SchemaV2.self, SchemaV3.self, SchemaV4.self, SchemaV5.self]`; extend `stages` to `[migrateV1toV2, migrateV2toV3, migrateV3toV4, migrateV4toV5]`; declare the new stage as `MigrationStage.custom(fromVersion: SchemaV4.self, toVersion: SchemaV5.self, willMigrate: { context in ... }, didMigrate: nil)`. willMigrate closure body: fetch the singleton via `FetchDescriptor<UserSettings>()`, take first, if `abs(settings.plateauTolerance - 0.005) < 1e-9` set `settings.plateauTolerance = 0.02` and call `try? context.save()`. (User-edited values like 0.015 fail the float-equality check and are preserved.) Preserve all existing stages.
    
    (7) Create fitbod/Prescription/OneRepMaxEstimator.swift per the verbatim pattern from PATTERNS.md (analog: Calibration.swift file header). File header: "OneRepMaxEstimator.swift / fitbod / Brzycki formula (reps ≤ 6): e1RM = weight × (36 / (37 - reps)) / Epley formula (reps 7–10): e1RM = weight × (1 + reps / 30) / Suppressed (reps > 10): returns nil per REQUIREMENTS PROG-02 / No SwiftData coupling. No @MainActor. Implicitly Sendable.". Body: `public enum OneRepMaxEstimator { public static func estimate(weight: Double, reps: Int) -> Double? { guard reps > 0 else { return nil }; if reps <= 6 { return weight * (36.0 / Double(37 - reps)) }; if reps <= 10 { return weight * (1.0 + Double(reps) / 30.0) }; return nil } }`. Imports: `import Foundation`.
    
    (8) Modify fitbod/Prescription/RPEAutoregStrategy.swift to use `OneRepMaxEstimator.estimate(weight:reps:)` at any internal site where it currently inlines Brzycki/Epley math. DO NOT change RPEAutoregStrategy's public `prescribe(...)` signature or output. If RPEAutoregStrategy does not currently inline this math (it operates only on pre-computed HistoryPoint.e1RM values from RESEARCH §1 Assumption A4), simply add a `// Phase 5 — extraction of OneRepMaxEstimator handled in the call site that builds HistoryPoint arrays from raw weight×reps in plans 02 and beyond` comment near the top of the file and leave the prescribe surface untouched. The point of this step is to ensure the extraction is wired and the existing RPEAutoregStrategyTests pass without modification.
    
    (9) Create the 4 Asset Catalog colorsets, each a directory under `fitbod/Assets.xcassets/` with a single `Contents.json` mirroring the AccentColor.colorset shape (`info.author = "xcode"`, `info.version = 1`, `colors` array with one entry per appearance — `any-appearance` + `dark` appearance). Light/dark hex values per UI-SPEC §Asset Contract verbatim: VolumeBelowMEVGray (light `#8E8E93` / dark `#636366`), VolumeProductiveGreen (light `#34C759` / dark `#30D158`), VolumeNearMRVAmber (light `#FF9500` / dark `#FF9F0A`), VolumeOverMRVRed (light `#FF3B30` / dark `#FF453A`). Use the existing `AccentColor.colorset/Contents.json` format as the JSON-shape template (sRGB color-space, hex components encoded as "0xNN" rgb strings).
    
    (10) Create fitbodTests/Persistence/SchemaV5MigrationTests.swift as `@MainActor @Suite("SchemaV5Migration", .serialized)`. makeContext() helper: open a ModelContainer over Schema(SchemaV5.models) with FitbodSchemaMigrationPlan. Required @Test functions (all GREEN this plan):
      - `migrationPlanRegistersV5`: assert `FitbodSchemaMigrationPlan.schemas.last == SchemaV5.self` via String(describing:).
      - `migrationPlanHasFourStages`: assert `FitbodSchemaMigrationPlan.stages.count == 4`.
      - `migrateV4toV5IsCustomNotLightweight`: pin the custom shape via Mirror reflection of `FitbodSchemaMigrationPlan.migrateV4toV5` (per PATTERNS analog — Mirror.children.first(where: { $0.label == "kind" })?.value contains `custom` not `lightweight`); a fallback acceptable check is the static-property name itself.
      - `schemaV5ModelsCountMatchesV4`: assert `SchemaV5.models.count == SchemaV4.models.count` (no new entity types).
      - `userSettingsPhase5FieldsRoundTrip`: insert UserSettings.default(), save, refetch — assert `frequencyHitMinSets == 2` AND `deloadAdvisoryDismissedWeekStart == nil` AND `weeklyRecapShownForWeekStart == nil` AND `plateauTolerance == 0.02`.
      - `exercisePhase5FieldsRoundTrip`: insert minimal Exercise, save, refetch — assert `plateauWindowOverride == nil` AND `plateauToleranceOverride == nil`.
      - `exerciseMuscleStimulusUserEditedRoundTrip`: insert minimal ExerciseMuscleStimulus, save, refetch — assert `userEditedWeight == false`.
      - `muscleVolumeTargetUserEditedRoundTrip`: insert minimal MuscleVolumeTarget, save, refetch — assert `userEdited == false`.
      - `plateauToleranceBumpFromV4DefaultMigratesToV5Default`: open a Schema(SchemaV4.models) container, insert UserSettings with plateauTolerance = 0.005, save, close. Re-open against Schema(SchemaV5.models) with FitbodSchemaMigrationPlan. Refetch — assert `plateauTolerance ≈ 0.02 ± 1e-9`.
      - `plateauTolerancePreservesUserEditedValue`: same setup, but insert UserSettings with plateauTolerance = 0.015. After migration, refetch — assert `plateauTolerance ≈ 0.015 ± 1e-9` (user value preserved).
    
    (11) Create fitbodTests/Fatigue/OneRepMaxEstimatorTests.swift as `@Suite("OneRepMaxEstimator")` (no @MainActor — pure function). Required @Test functions (all GREEN this plan):
      - `brzyckiAtFiveReps`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 5) == 112.5 ± 0.001)` (100 × 36/32).
      - `brzyckiAtOneRep`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 1) == 100.0 ± 0.001)` (100 × 36/36).
      - `brzyckiAtSixRepBoundary`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 6) == 116.129 ± 0.01)` (100 × 36/31; pins the ≤ 6 boundary per PROG-02).
      - `epleyAtSevenReps`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 7) ≈ 123.333 ± 0.01)` (100 × (1 + 7/30)).
      - `epleyAtTenRepBoundary`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 10) ≈ 133.333 ± 0.01)` (pins the ≤ 10 boundary).
      - `suppressedAtElevenReps`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 11) == nil)` (per PROG-02).
      - `suppressedAtZeroReps`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: 0) == nil)`.
      - `suppressedAtNegativeReps`: `#expect(OneRepMaxEstimator.estimate(weight: 100, reps: -3) == nil)`.
    
    Hard prerequisite: Phase 4 plan 04-01 must ship SchemaV4 before this plan runs. The PRE-EXECUTION GUARD enforces this. If the guard fires, surface the blocker via Issue.record in a stub `SchemaV5MigrationTests.placeholder` test and stop executing the rest of this task — the executor reports the blocker to the user.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Persistence/SchemaV5Migration -only-testing fitbodTests/Fatigue/OneRepMaxEstimator -only-testing fitbodTests/RPEAutoregStrategy 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    SchemaV5.swift exists and compiles; UserSettings has 3 new fields + plateauTolerance default bumped to 0.02; Exercise has 2 new optional fields; ExerciseMuscleStimulus has userEditedWeight; MuscleVolumeTarget has userEdited; FitbodSchemaMigrationPlan lists 5 schemas + 4 stages + custom migrateV4toV5; OneRepMaxEstimator.swift exists with the 3 formula branches; RPEAutoregStrategy compiles unchanged (regression suite GREEN); 4 Asset Catalog colorsets exist with verbatim UI-SPEC hex pairs; SchemaV5MigrationTests has 10 GREEN @Test functions (including the willMigrate bump + preserve path); OneRepMaxEstimatorTests has 8 GREEN @Test functions covering Brzycki/Epley/suppression boundaries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VolumeZone enum + free function + verb-copy property + StimulusWeightTable (51 lifts) + RPVolumeLandmarks (17 muscles) + MuscleRegionPaths stub registry + GREEN VolumeZoneTests + GREEN VolumeZoneVerbCopyTests + GREEN MuscleRegionPathsTests</name>
  <read_first>
    fitbod/Prescription/TuchschererTable.swift (analog — static-literal table enum + lookup func),
    fitbod/ExerciseLibrary/MuscleRegionMap.swift (analog — slug registry pattern with all 17 free-exercise-db slugs),
    fitbod/Models/Enums/Intent.swift,
    .planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md (D-05 + D-06 verbatim),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §6.1 (51-lift YAML table), §7.1 (17-muscle landmarks table), §8 (body silhouette pattern),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (VolumeZone + StimulusWeightTable + RPVolumeLandmarks + MuscleRegionPaths sections),
    .planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md §Copywriting Contract — Volume zone verb labels (D-06 verbatim copy)
  </read_first>
  <files>fitbod/Fatigue/VolumeZone.swift, fitbod/Fatigue/StimulusWeightTable.swift, fitbod/Fatigue/RPVolumeLandmarks.swift, fitbod/Fatigue/MuscleRegionPaths.swift, fitbodTests/Fatigue/VolumeZoneTests.swift, fitbodTests/Fatigue/VolumeZoneVerbCopyTests.swift, fitbodTests/Fatigue/MuscleRegionPathsTests.swift</files>
  <behavior>
    - Test: `volumeZone(currentSets: 5, mev: 8, mav: 14, mrv: 22) == .belowMEV` (currentSets < mev).
    - Test: `volumeZone(currentSets: 8, mev: 8, mav: 14, mrv: 22) == .productive` (the MEV boundary itself per D-05 strict `<`: 8 < mev is false; 8 < mav is true).
    - Test: `volumeZone(currentSets: 13, mev: 8, mav: 14, mrv: 22) == .productive`.
    - Test: `volumeZone(currentSets: 14, mev: 8, mav: 14, mrv: 22) == .nearMRV` (MAV boundary per D-05: 14 < mav is false; 14 < mrv is true).
    - Test: `volumeZone(currentSets: 21, mev: 8, mav: 14, mrv: 22) == .nearMRV`.
    - Test: `volumeZone(currentSets: 22, mev: 8, mav: 14, mrv: 22) == .overMRV` (MRV boundary: 22 < mrv is false).
    - Test: `volumeZone(currentSets: 30, mev: 8, mav: 14, mrv: 22) == .overMRV` (well above MRV).
    - Test: `volumeZone(currentSets: 0, mev: 8, mav: 14, mrv: 22) == .belowMEV` (zero-sets edge case).
    - Test (D-06 verbatim copy — character-for-character): `VolumeZone.belowMEV.verb == "Below MEV — add volume"` (no trailing period per UI-SPEC asymmetry note); `.productive.verb == "Productive range — hold/progress"` (no trailing period); `.nearMRV.verb == "Near MRV — deload soon if performance or recovery drops."` (trailing period); `.overMRV.verb == "Over MRV — deload recommended."` (trailing period). Use exact `==` string comparison.
    - Test: `StimulusWeightTable.overrides["barbell bench press"]` returns a non-nil array containing tuples `(muscle: "chest", role: "primary", weight: 1.0)`, `(muscle: "shoulders", role: "secondary", weight: 0.5)`, `(muscle: "triceps", role: "secondary", weight: 0.5)`.
    - Test: `StimulusWeightTable.overrides["conventional deadlift"]` returns triples for lower back / glutes / hamstrings (all primary, weight 1.0).
    - Test: `StimulusWeightTable.overrides.count == 51` (RESEARCH §6.1 total — pins the curated-lift count so accidental deletion fails the test).
    - Test: every key in `StimulusWeightTable.overrides` is lowercased and contains no diacritics (matches Exercise.canonicalName normalization).
    - Test: `RPVolumeLandmarks.landmarks["chest"]?.mev == 8` AND `.mav == 16` AND `.mrv == 22` AND `.mv == 6` (RESEARCH §7.1 verbatim).
    - Test: `RPVolumeLandmarks.landmarks` has all 17 muscle slugs from MuscleRegionMap.allSlugs.
    - Test: `RPVolumeLandmarks.landmark(for: "unknown-slug")` returns the conservative fallback `Landmark(mv: 4, mev: 6, mav: 12, mrv: 18)`.
    - Test: `MuscleRegionPaths.front` contains keys for chest, shoulders, biceps, forearms, abdominals, quadriceps, calves (front-visible muscles per RESEARCH §8.2).
    - Test: `MuscleRegionPaths.back` contains keys for traps, lats, "middle back", "lower back", shoulders, triceps, glutes, hamstrings, calves.
    - Test: Combined set of `MuscleRegionPaths.front.keys ∪ MuscleRegionPaths.back.keys` contains all 17 muscle slugs from MuscleRegionMap.allSlugs (registry coverage).
    - Test: every Path value in `MuscleRegionPaths.front` and `.back` is non-empty (Wave 0 stub `Rectangle` paths are non-empty; pins that the registry doesn't accidentally ship empty Paths).
  </behavior>
  <action>
    (1) Create fitbod/Fatigue/ directory if it does not yet exist (this is the first Phase 5 file in that directory).
    
    (2) Create fitbod/Fatigue/VolumeZone.swift. File header: "VolumeZone.swift / fitbod / D-05 verbatim per CONTEXT.md — case names + strict-< boundary function are normative. D-06 verbatim verb copy per CONTEXT.md / UI-SPEC §Copywriting Contract. / No SwiftData coupling. No @MainActor. Implicitly Sendable." Imports: `import Foundation`. Body:
      - `public enum VolumeZone: Sendable, Equatable { case belowMEV; case productive; case nearMRV; case overMRV }`.
      - `public func volumeZone(currentSets: Int, mev: Int, mav: Int, mrv: Int) -> VolumeZone { if currentSets < mev { return .belowMEV }; if currentSets < mav { return .productive }; if currentSets < mrv { return .nearMRV }; return .overMRV }` — strict `<` per D-05; do NOT switch to `<=` anywhere.
      - `extension VolumeZone { public var verb: String { switch self { case .belowMEV: return "Below MEV — add volume"; case .productive: return "Productive range — hold/progress"; case .nearMRV: return "Near MRV — deload soon if performance or recovery drops."; case .overMRV: return "Over MRV — deload recommended." } } }` — D-06 verbatim. Note the asymmetric punctuation (first two no trailing period, last two trailing period — intentional per UI-SPEC).
    
    (3) Create fitbod/Fatigue/StimulusWeightTable.swift. File header: "StimulusWeightTable.swift / fitbod / Curated stimulus-weighting overrides for the top 51 compound lifts per RESEARCH §6.1. / Keys are canonical exercise names (lowercased, diacritic-stripped) matching Exercise.canonicalName. / Values: [(muscleSlug, role, weight)] triples where weight ∈ [0,1] (T-05-01 clamp enforced by editor, not by this table). / Non-curated exercises fall back to 1.0/0.5 defaults already shipped by Phase 1's ExerciseMuscleStimulus seeder. / No SwiftData coupling. No @MainActor. Implicitly Sendable.". Imports: `import Foundation`. Body: `public enum StimulusWeightTable { public static let overrides: [String: [(muscleSlug: String, role: String, weight: Double)]] = [ ... ] }`. The dictionary literal must contain ALL 51 entries from RESEARCH §6.1 verbatim — every lift name and every (muscleSlug, role, weight) tuple. Use canonical lowercase + space-separated keys (e.g., "barbell bench press", NOT "barbell_bench_press"; RESEARCH §6.1 uses snake_case YAML keys — convert to space-separated lowercase Strings to match Exercise.canonicalName normalization). Section comments grouping by category (HORIZONTAL PUSH / VERTICAL PUSH / HORIZONTAL PULL / VERTICAL PULL / SQUAT / HINGE / SHOULDER ISOLATION / ARM ISOLATION / ABS / CORE / CALVES) preserved as Swift comments.
    
    (4) Create fitbod/Fatigue/RPVolumeLandmarks.swift. File header: "RPVolumeLandmarks.swift / fitbod / RP-anchored MEV/MAV/MRV per muscle per RESEARCH §7.1. / Sourced from arvo.guru/tools/volume-calculator aggregation of RP Strength's per-muscle hypertrophy guides. / Cited HIGH-confidence for 13/17 muscles; [ASSUMED] for neck / abductors / adductors / abdominals MRV. / All 17 free-exercise-db muscle slugs covered. / No SwiftData coupling. No @MainActor. Implicitly Sendable.". Imports: `import Foundation`. Body:
      - `public enum RPVolumeLandmarks { public struct Landmark: Sendable, Equatable { public let mv: Int; public let mev: Int; public let mav: Int; public let mrv: Int; public init(mv: Int, mev: Int, mav: Int, mrv: Int) { ... } } }`.
      - `public static let landmarks: [String: Landmark] = [ ... ]` — all 17 muscle slugs from RESEARCH §7.1 with their Landmark values verbatim. Slug keys: chest, lats, "middle back", traps, "lower back", shoulders, biceps, triceps, quadriceps, hamstrings, glutes, calves, abdominals, forearms, neck, abductors, adductors (verify count == 17).
      - `public static func landmark(for slug: String) -> Landmark { landmarks[slug.lowercased()] ?? Landmark(mv: 4, mev: 6, mav: 12, mrv: 18) }`.
    
    (5) Create fitbod/Fatigue/MuscleRegionPaths.swift. File header: "MuscleRegionPaths.swift / fitbod / Per-muscle silhouette Path registry per RESEARCH §8.2. / Paths normalized to a 0...1 unit square; caller renders into a GeometryReader to scale. / Wave 0 ships STUB Rectangle paths at approximate body-region positions; Wave 3 (plan 05) refines to anatomically-correct bezier curves. / Tests in fitbodTests/Fatigue/MuscleRegionPathsTests pin the slug registry coverage regardless of geometry. / No SwiftData coupling. Implicitly Sendable via SwiftUI.Path which is a value type." Imports: `import SwiftUI`. Body:
      - `public enum MuscleRegionPaths { public static let front: [String: Path] = [...]; public static let back: [String: Path] = [...] }`.
      - Front dictionary keys: "chest", "shoulders", "biceps", "forearms", "abdominals", "quadriceps", "calves", "neck", "abductors", "adductors" (front-visible muscles; the chest+abs+quads dominant view).
      - Back dictionary keys: "traps", "lats", "middle back", "lower back", "shoulders", "triceps", "glutes", "hamstrings", "calves" (back-visible). NB: "shoulders" and "calves" appear in BOTH front and back — that's intentional per RESEARCH §8 (front delts visible front; rear delts visible back; calves visible both sides). Tapping either path navigates to the same per-muscle detail view (single-bucket slug per shoulders aggregation decision).
      - Wave 0 stub Path values: each is `Path(CGRect(x: ax, y: ay, width: aw, height: ah))` with hand-picked approximate body-region positions normalized to 0...1. Example layout per RESEARCH §8.2 silhouette aspect ratio 0.4 (tall narrow figure occupies the center; horizontal axis 0 = body left edge, 1 = body right edge; vertical axis 0 = head top, 1 = feet bottom):
        - front chest: x=0.25, y=0.20, w=0.50, h=0.10
        - front abdominals: x=0.30, y=0.30, w=0.40, h=0.15
        - front quadriceps: x=0.25, y=0.55, w=0.50, h=0.20
        - front calves: x=0.27, y=0.78, w=0.46, h=0.10
        - front shoulders: x=0.10, y=0.18, w=0.80, h=0.06 (spans both delts)
        - front biceps: x=0.10, y=0.26, w=0.18, h=0.10 (left arm) — Wave 3 will split into per-arm paths or unify; Wave 0 uses one rectangle spanning both arm regions
        - back lats: x=0.20, y=0.25, w=0.60, h=0.20
        - back middle back: x=0.30, y=0.22, w=0.40, h=0.10
        - back lower back: x=0.30, y=0.40, w=0.40, h=0.10
        - back glutes: x=0.27, y=0.48, w=0.46, h=0.12
        - back hamstrings: x=0.25, y=0.60, w=0.50, h=0.18
        - back triceps: x=0.05, y=0.26, w=0.18, h=0.12
        - back traps: x=0.32, y=0.12, w=0.36, h=0.08
        - back calves: same as front calves position (it's still calves either side)
        - back shoulders: x=0.10, y=0.16, w=0.80, h=0.06 (rear delts span)
      - Use Swift dictionary literal syntax; private helper funcs are not required at this scope (Wave 3 will introduce private bezier-builder helpers). Keep paths inline.
    
    (6) Create fitbodTests/Fatigue/VolumeZoneTests.swift as `@Suite("VolumeZone")` (no @MainActor — pure function). Required @Test functions (GREEN this plan):
      - All 8 boundary tests from the `<behavior>` block above. Use named parameters in the function calls (`volumeZone(currentSets: 8, mev: 8, mav: 14, mrv: 22)`).
      - One additional parameterized test `boundaryTransitions` that asserts the strict `<` semantics across all 4 transitions (currentSets = mev-1, mev, mev+1, mav-1, mav, mav+1, mrv-1, mrv, mrv+1) for the chest target (8/14/22).
    
    (7) Create fitbodTests/Fatigue/VolumeZoneVerbCopyTests.swift as `@Suite("VolumeZoneVerbCopy")` (no @MainActor). Required @Test functions (GREEN this plan):
      - `belowMEVVerbVerbatim`: `#expect(VolumeZone.belowMEV.verb == "Below MEV — add volume")`.
      - `productiveVerbVerbatim`: `#expect(VolumeZone.productive.verb == "Productive range — hold/progress")`.
      - `nearMRVVerbVerbatim`: `#expect(VolumeZone.nearMRV.verb == "Near MRV — deload soon if performance or recovery drops.")`.
      - `overMRVVerbVerbatim`: `#expect(VolumeZone.overMRV.verb == "Over MRV — deload recommended.")`.
      - `verbPunctuationAsymmetry`: assert `.belowMEV.verb` and `.productive.verb` do NOT end with `"."`; assert `.nearMRV.verb` and `.overMRV.verb` DO end with `"."`. Pins the intentional D-06 asymmetry.
    
    (8) Create fitbodTests/Fatigue/MuscleRegionPathsTests.swift as `@Suite("MuscleRegionPaths")` (no @MainActor — SwiftUI.Path is value-type and safe to construct in any context). Required @Test functions (GREEN this plan):
      - `frontContainsExpectedSlugs`: `#expect(MuscleRegionPaths.front.keys.contains("chest"))` etc. for all front-visible slugs (chest / shoulders / biceps / forearms / abdominals / quadriceps / calves / neck / abductors / adductors).
      - `backContainsExpectedSlugs`: similar for back-visible (traps / lats / middle back / lower back / shoulders / triceps / glutes / hamstrings / calves).
      - `combinedRegistryCoversAllSeventeenMuscleSlugs`: assert `Set(MuscleRegionPaths.front.keys).union(MuscleRegionPaths.back.keys) == Set(MuscleRegionMap.allSlugs)`. This is the canonical coverage test — if Wave 3 adds anatomical refinement and changes the path structure, this test stays GREEN as long as every slug is registered.
      - `noEmptyPaths`: iterate front and back; assert every Path has at least one boundingRect operation (use `path.boundingRect != .null` or build a Bezier description). Easiest assertion: `#expect(!MuscleRegionPaths.front["chest"]!.cgPath.isEmpty)`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/VolumeZone -only-testing fitbodTests/Fatigue/VolumeZoneVerbCopy -only-testing fitbodTests/Fatigue/MuscleRegionPaths 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    VolumeZone.swift exists with the verbatim D-05 enum + free function + D-06 verbatim verb copy; StimulusWeightTable.swift exists with 51 curated lifts (RESEARCH §6.1 verbatim) as a public dictionary; RPVolumeLandmarks.swift exists with the 17-slug landmark table + Landmark struct + landmark(for:) fallback (RESEARCH §7.1 verbatim); MuscleRegionPaths.swift exists with all 17 slugs covered across front + back as stub Rectangle paths normalized to 0...1; VolumeZoneTests has 9 GREEN tests; VolumeZoneVerbCopyTests has 5 GREEN tests pinning D-06 character-for-character; MuscleRegionPathsTests has 4 GREEN tests pinning registry coverage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FatigueTestFixtures helper + 8 RED test suite scaffolds with Issue.record placeholders (flipped GREEN by downstream Phase 5 plans)</name>
  <read_first>
    fitbod/Sessions/PreviousMatchingIntent.swift,
    fitbod/Persistence/PreviewModelContainer.swift,
    fitbodTests/PreviousMatchingIntentTests.swift,
    fitbodTests/SessionFactoryTests.swift,
    .planning/phases/05-fatigue-model-plateau-detection/05-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §4 (per-edge-case fixture names) + §14 (Phase Requirements → Test Map),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (FatigueTestFixtures pattern)
  </read_first>
  <files>fitbodTests/Fatigue/FatigueTestFixtures.swift, fitbodTests/Fatigue/FatigueModelWeeklyVolumeTests.swift, fitbodTests/Fatigue/FrequencyHitsTests.swift, fitbodTests/Fatigue/PlateauDetectorTests.swift, fitbodTests/Fatigue/SuggestedActionTests.swift, fitbodTests/Fatigue/DeloadAdvisorTests.swift, fitbodTests/Fatigue/StimulusWeightSeederTests.swift, fitbodTests/Fatigue/MuscleVolumeTargetSeederTests.swift, fitbodTests/Fatigue/WeeklyRecapTriggerTests.swift</files>
  <behavior>
    - The FatigueTestFixtures.makeContext() helper returns a working ModelContext over Schema(SchemaV5.models) + FitbodSchemaMigrationPlan that downstream tests can call.
    - The 8 RED scaffolds compile (no syntax errors); each contains at least one @Test function calling `Issue.record(...)` with a non-empty placeholder string naming the downstream plan that will flip the suite GREEN.
    - Running `xcodebuild test -only-testing fitbodTests/Fatigue/FatigueTestFixtures` is N/A — fixtures are a helper module, not a test suite (no @Suite annotation).
    - Each RED suite, when run individually, reports a failure (Issue.record is the failure path) without crashing the test runner.
  </behavior>
  <action>
    (1) Create fitbodTests/Fatigue/FatigueTestFixtures.swift mirroring fitbodTests/TestSupport/InMemoryContainer.swift's structural pattern. Annotate the enum `@MainActor` (the helper builds SwiftData entities). File header: "FatigueTestFixtures.swift / fitbodTests / Shared in-memory fixture factory for every Fatigue test suite. / makeContext(): Schema(SchemaV5.models) + FitbodSchemaMigrationPlan over an in-memory ModelContainer (allowsSave: true). / weeklySessions(...): deterministic SetEntry-row builder anchored to a synthetic weekStart. / Named edge-case factories per RESEARCH §4 — every PlateauDetector / DeloadAdvisor edge case has a corresponding factory func.". Imports: `import Foundation, import SwiftData, @testable import fitbod`.
      
      Body:
      ```
      @MainActor
      enum FatigueTestFixtures {
          static func makeContext() throws -> ModelContext { ... }   // Schema(SchemaV5.models) + plan
          
          /// Build a deterministic in-memory week of working sets for one exercise at one intent.
          /// Used as the building block for every detector/advisor test.
          static func weeklySessions(
              forExercise exercise: Exercise,
              weights: [Double],        // one per session — length == sessions to create
              reps: [Int],              // one per session
              rpe: [Double?] = [],      // one per session; nil entries mean "no RPE logged"
              intent: Intent = .strength,
              weekStart: Date,
              in context: ModelContext
          ) throws -> [Session]
          
          // RESEARCH §4 named factories (each returns a fully-configured (ctx, exercise, fixture state) tuple):
          static func stalledWindow(in context: ModelContext) throws -> (Exercise, [Session])
          static func progressingWindow(in context: ModelContext) throws -> (Exercise, [Session])
          static func threeSessionWindow(in context: ModelContext) throws -> (Exercise, [Session])
          static func fourSessionsOneHighRep(in context: ModelContext) throws -> (Exercise, [Session])
          static func belowMAVWithStall(in context: ModelContext) throws -> (Exercise, [Session], MuscleVolumeTarget)
          static func rpeCreepStall(in context: ModelContext) throws -> (Exercise, [Session])
          static func blockWeek3Stall(in context: ModelContext) throws -> (Exercise, [Session], Block)
          static func bareStall(in context: ModelContext) throws -> (Exercise, [Session])
          static func noBlockStall(in context: ModelContext) throws -> (Exercise, [Session])
          static func exerciseWithOverride(in context: ModelContext) throws -> Exercise
          static func intentSplitHistory(in context: ModelContext) throws -> (Exercise, [Session])
      }
      ```
      
      For each factory, the body need NOT be fully implemented this plan — but the SIGNATURES must exist so downstream plans can wire against them. Provide a body comment block in each factory: `// Wave 0 scaffold — implementation lives in plan <NN> when the consumer suite turns GREEN. For now, throw fatalError("FatigueTestFixtures.<name> — not yet implemented; see plan <NN>")` so that any downstream test that prematurely calls this factory crashes loudly rather than silently passes. The `makeContext()` and `weeklySessions(...)` helpers ARE fully implemented this plan because every later plan needs them.
      
      Detailed implementation of the two fully-implemented helpers:
      
      `makeContext()`: identical structure to PreviousMatchingIntentTests.makeContext() except swap `Schema(SchemaV2.models)` for `Schema(SchemaV5.models)`. Return a fresh ModelContext.
      
      `weeklySessions(forExercise:weights:reps:rpe:intent:weekStart:in:)`: validate `weights.count == reps.count` AND (rpe is empty OR rpe.count == weights.count); for each index i, create a Session with `startedAt = weekStart.addingTimeInterval(Double(i * 86400))` and `completedAt = nil` (use nil for active fixtures; tests that need completed sessions can set this directly). Create a SessionExercise on that session with `exercise: exercise` and `intentRaw: intent.rawValue` and `orderIndex: 0`. Create one SetEntry with `weight: weights[i]`, `reps: reps[i]`, `rpe: rpe.isEmpty ? nil : rpe[i]`, `isWarmup: false`, `isComplete: true`, `setTypeRaw: "working"`. Save the context once at the end. Return the array of created Session rows for caller inspection.
    
    (2) Create the 8 RED test scaffolds. Each file is structured as:
    ```
    import Foundation
    import Testing
    @testable import fitbod
    
    @Suite("<SuiteName>")
    struct <SuiteName>Tests {
        @Test("Wave 0 scaffold — flipped GREEN by plan <NN>")
        func placeholder() {
            Issue.record("Wave 0 RED scaffold for <SuiteName>; flipped GREEN by Phase 5 plan <NN>. See VALIDATION.md row <ID>.")
        }
    }
    ```
    
    Specific scaffolds and their flipping plans (per VALIDATION.md Wave 0 Requirements + Per-Task Verification Map):
      - `FatigueModelWeeklyVolumeTests.swift` — flipped GREEN by **plan 02** (Slice A: FatigueModel pure functions). Annotate `@Suite("FatigueModelWeeklyVolume")`. Issue.record string: "Wave 0 RED scaffold for FatigueModelWeeklyVolumeTests; flipped GREEN by Phase 5 plan 02 (Slice A — FatigueModel.weeklyVolume)."
      - `FrequencyHitsTests.swift` — flipped GREEN by **plan 02** (FatigueModel.frequencyHits). Annotate `@Suite("FrequencyHits")`. Issue.record string: "Wave 0 RED scaffold for FrequencyHitsTests; flipped GREEN by Phase 5 plan 02 (Slice A — FatigueModel.frequencyHits)."
      - `StimulusWeightSeederTests.swift` — flipped GREEN by **plan 02** (Slice B — stimulus seeder runs on first launch). Annotate `@Suite("StimulusWeightSeeder")`. Issue.record string: "Wave 0 RED scaffold for StimulusWeightSeederTests; flipped GREEN by Phase 5 plan 02 (Slice B — StimulusWeightSeeder integration). Threat T-05-01: stimulus weight value range-clamp [0,1] enforced by seeder."
      - `PlateauDetectorTests.swift` — flipped GREEN by **plan 03** (Slice D — PlateauDetector). Annotate `@Suite("PlateauDetector")`. Issue.record string: "Wave 0 RED scaffold for PlateauDetectorTests; flipped GREEN by Phase 5 plan 03 (Slice D — PlateauDetector.evaluate per D-10..D-12)."
      - `SuggestedActionTests.swift` — flipped GREEN by **plan 03** (Slice D — suggestedAction heuristic). Annotate `@Suite("SuggestedAction")`. Issue.record string: "Wave 0 RED scaffold for SuggestedActionTests; flipped GREEN by Phase 5 plan 03 (Slice D — PlateauDetector.suggestedAction per D-13 4-branch decision tree)."
      - `DeloadAdvisorTests.swift` — flipped GREEN by **plan 04** (DeloadAdvisor service). Annotate `@Suite("DeloadAdvisor")`. Issue.record string: "Wave 0 RED scaffold for DeloadAdvisorTests; flipped GREEN by Phase 5 plan 04 (DeloadAdvisor service — multi-signal OR per D-14..D-17). Threat T-05-03: deload-canonicality (advisory only, never mutation; BLOCK-08 + Pitfall #11)."
      - `MuscleVolumeTargetSeederTests.swift` — flipped GREEN by **plan 07** (Slice F — Settings editors + seeders + recap). Annotate `@Suite("MuscleVolumeTargetSeeder")`. Issue.record string: "Wave 0 RED scaffold for MuscleVolumeTargetSeederTests; flipped GREEN by Phase 5 plan 07 (Slice F — RPVolumeLandmarks seeder integration). Threat T-05-02: MEV<MAV<MRV monotonic invariant + clamp [1,30] enforced by editor."
      - `WeeklyRecapTriggerTests.swift` — flipped GREEN by **plan 07** (VOL-07 weekly recap trigger). Annotate `@Suite("WeeklyRecapTrigger")`. Issue.record string: "Wave 0 RED scaffold for WeeklyRecapTriggerTests; flipped GREEN by Phase 5 plan 07 (Slice F — VOL-07 trigger logic + sheet)."
    
    Each scaffold imports `Foundation`, `Testing`, and `@testable import fitbod`. No @MainActor needed at the suite level (the placeholder doesn't touch SwiftData). When downstream plans flip a suite, they replace the placeholder @Test with the real test set and add `@MainActor + .serialized` as needed.
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/FatigueModelWeeklyVolume -only-testing fitbodTests/Fatigue/FrequencyHits -only-testing fitbodTests/Fatigue/PlateauDetector -only-testing fitbodTests/Fatigue/SuggestedAction -only-testing fitbodTests/Fatigue/DeloadAdvisor -only-testing fitbodTests/Fatigue/StimulusWeightSeeder -only-testing fitbodTests/Fatigue/MuscleVolumeTargetSeeder -only-testing fitbodTests/Fatigue/WeeklyRecapTrigger 2>&amp;1 | grep -E 'Test Suite.*RUN|FAIL|failed|error' | head -30</automated>
  </verify>
  <done>
    FatigueTestFixtures.swift exists with @MainActor enum FatigueTestFixtures; makeContext() and weeklySessions(...) helpers are fully implemented and callable; 11 named edge-case factories have signatures + fatalError stub bodies; the 8 RED scaffolds compile and report Issue.record failures (expected — they flip GREEN in downstream plans 02 / 03 / 04 / 07); full xcodebuild test exit code may be non-zero (expected — 8 RED suites) but every Phase 1 / 2 / 3 / 4 test suite remains GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local user → SwiftData | Single-user offline app (PROJECT.md). User input crossing the persistence boundary in Phase 5: stimulus weight (clamped [0,1] in plan 02 editor), MEV/MAV/MRV integers (clamped + monotonic in plan 07 editor), per-exercise plateau overrides (range-bounded steppers in plan 06/07), dismissal date (only set programmatically — no direct user entry). ASVS L1 surface area remains minimal. |
| Code → Code | All Phase 5 types live in `fitbod/Fatigue/` (services + UI) and `fitbod/Prescription/` (math primitive) — single-target iOS app, no XPC, no inter-process trust hop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `ExerciseMuscleStimulus.userEditedWeight` flag added in SchemaV5 — a malicious actor with physical-device access could flip the flag to suppress re-seeding | accept | Single-user local app; physical-device access defeats every persistence layer including iOS data-protection. SwiftData is the project standard. No additional crypto justified per ASVS L1. Same disposition as Phase 4's T-04-01-01. |
| T-05-01-02 | Tampering | `UserSettings.deloadAdvisoryDismissedWeekStart` field could be manipulated to suppress the advisory indefinitely | accept | Same rationale. Worst case: user accidentally dismisses for a month; the dismissal date is per-week (Pitfall #7 isDate(toGranularity:.day) comparison), so a "stuck" dismissal naturally clears on the next Monday. |
| T-05-01-03 | Denial of Service | Malformed SchemaV4→V5 custom migration could brick the database on first launch if the willMigrate closure throws | mitigate | The willMigrate closure uses `try?` to swallow save errors (RESEARCH §9.3 reference impl); the schema delta is still applied even if the plateauTolerance bump fails. The single point of failure is the FetchDescriptor<UserSettings>() fetch itself, which is well-tested in Phase 1's PreviewModelContainer setup. SchemaV5MigrationTests asserts the bump round-trip on an in-memory container with both the bump-eligible path (0.005) and the preserve-eligible path (0.015). |
| T-05-01-04 | Elevation of Privilege | `OneRepMaxEstimator` is consumed by `PlateauDetector` (plan 03) and `DeloadAdvisor` (plan 04) — a bug here propagates to false stall flags + false advisories | mitigate | OneRepMaxEstimatorTests pins the formula boundaries (Brzycki at 1/5/6 reps; Epley at 7/10 reps; nil at 0/11/-3 reps) with verbatim arithmetic. The estimator is a 6-line pure function; the test surface dwarfs the implementation. Regression in RPEAutoregStrategy (which internally uses the same primitive) is also asserted by the existing Phase 3 test suite. |
| T-05-01-05 | Information Disclosure | Wave 0 stub `MuscleRegionPaths` exposes approximate body-region positions — no PII, no health data, no sensitive content | accept | Path coordinates are anatomical UI metadata, not user data. Not in scope for any privacy threat model. |
</threat_model>

<verification>
- Full `xcodebuild test` shows the 5 Wave-0 GREEN suites (SchemaV5MigrationTests / OneRepMaxEstimatorTests / VolumeZoneTests / VolumeZoneVerbCopyTests / MuscleRegionPathsTests).
- The 8 RED scaffolds compile and report Issue.record placeholders that name the downstream-flipping plan; they are visible in the test report as failures, not silently absent.
- No regression in Phase 1 / Phase 2 / Phase 3 / Phase 4 test suites (SchemaV1Tests, SchemaV2MigrationTests, SchemaV3MigrationTests, SchemaV4MigrationTests, all existing fitbodTests pass).
- `swift-format` (or `xcrun swift-format`) idempotent on every new file.
- No new third-party SPM dependency introduced (PROJECT.md constraint).
- UserSettings retains every existing field + adds 3 new fields + plateauTolerance default bumped from 0.005 to 0.02.
- Exercise retains every existing field + adds plateauWindowOverride and plateauToleranceOverride.
- ExerciseMuscleStimulus retains every existing field + adds userEditedWeight.
- MuscleVolumeTarget retains every existing field + adds userEdited.
- Asset Catalog contains 4 new colorsets with verbatim UI-SPEC hex pairs.
- `fitbod/Fatigue/` directory exists with VolumeZone.swift, StimulusWeightTable.swift, RPVolumeLandmarks.swift, MuscleRegionPaths.swift.
- `fitbod/Prescription/` directory gains OneRepMaxEstimator.swift; RPEAutoregStrategy.swift's public surface unchanged.
</verification>

<success_criteria>
- SchemaV5 + custom V4→V5 migration ship; the plateauTolerance default bump (0.005→0.02) executes exactly once on existing user rows; user-edited values (e.g., 0.015) are preserved.
- `OneRepMaxEstimator.estimate(weight:reps:)` correctly partitions Brzycki ≤6 / Epley 6-10 / nil >10 per REQUIREMENTS PROG-02; all formula constants are verbatim per RESEARCH §1.
- `VolumeZone` is the verbatim D-05 enum with strict `<` boundaries; `VolumeZone.verb` returns the verbatim D-06 strings character-for-character (including the intentional punctuation asymmetry).
- `StimulusWeightTable.overrides` contains 51 curated lifts verbatim per RESEARCH §6.1.
- `RPVolumeLandmarks.landmarks` covers all 17 muscle slugs verbatim per RESEARCH §7.1; `landmark(for:)` provides a conservative fallback for unknown slugs.
- `MuscleRegionPaths.front` + `.back` combined cover all 17 muscle slugs as stub Rectangle paths (Wave 3 / plan 05 refines geometry).
- 4 Asset Catalog colorsets exist with UI-SPEC §Asset Contract verbatim hex pairs (any-appearance + dark-appearance idiom).
- `FatigueTestFixtures.makeContext()` and `weeklySessions(...)` are callable from any downstream Fatigue test suite; the 11 named edge-case factories have signatures + fatalError stubs.
- All 13 Wave-0 test files exist on disk; 5 GREEN (SchemaV5Migration / OneRepMaxEstimator / VolumeZone / VolumeZoneVerbCopy / MuscleRegionPaths); 8 RED with Issue.record placeholders flipped by downstream plans 02 / 03 / 04 / 07.
- Phase 4's `FatigueAdvisory` protocol (shipped by Phase 4 plan 04-01 with `StubFatigueAdvisory`) is untouched this plan — Phase 5 plan 04 will replace the stub binding.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fatigue-model-plateau-detection/05-01-SUMMARY.md`.
</output>
