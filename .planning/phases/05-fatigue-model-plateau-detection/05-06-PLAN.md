---
phase: 05-fatigue-model-plateau-detection
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - fitbod/Settings/MuscleVolumeTargetEditor.swift
  - fitbod/Fatigue/MuscleVolumeTargetSeeder.swift
  - fitbod/Settings/SettingsView.swift
  - fitbod/Fatigue/WeeklyRecapSheet.swift
  - fitbod/App/RootView.swift
  - fitbodTests/Fatigue/MuscleVolumeTargetSeederTests.swift
  - fitbodTests/Fatigue/WeeklyRecapTriggerTests.swift
  - fitbodUITests/MuscleVolumeTargetEditorUITests.swift
  - fitbodUITests/WeeklyRecapUITests.swift
autonomous: true
requirements: [VOL-06, VOL-07, SET-05, SET-06]

must_haves:
  truths:
    - "User opens Settings → 'Volume Targets' and sees a sectioned list per muscle (17 muscles per RESEARCH §11.1; each section shows '{muscle.displayName}' header + trailing 'mev / mav / mrv' preview row) with disclosure into the inline MuscleVolumeTargetStepper (plan 05 ships the Stepper); navigation title 'Volume Targets'; 'Reset All to RP Defaults' destructive button at the bottom with .alert confirmation per UI-SPEC §Copywriting Contract verbatim copy — closes SET-05 settings entry point"
    - "SettingsView has a new 'Volume Targets' Section with NavigationLink to MuscleVolumeTargetEditor + a Stepper for UserSettings.frequencyHitMinSets per CONTEXT D-03 + Claude's Discretion (the frequency-hit threshold is editable from Settings; this also surfaces VOL-06 in Settings, complementing the Settings-side D-03 editor)"
    - "MuscleVolumeTargetSeeder runs on first launch + on every subsequent launch (idempotent); for each of the 17 muscle slugs, finds or creates a MuscleVolumeTarget row; if userEdited == false (default), writes RPVolumeLandmarks.landmark(for: slug) values (mv/mev/mav/mrv); if userEdited == true (user has saved a custom target via the SET-05 editor), skips the row (preservation); single try? context.save() at end"
    - "WeeklyRecapSheet auto-presents on first app open of new Monday per VOL-07; gated by UserSettings.weeklyRecapShownForWeekStart (Date? from plan 01 SchemaV5); presentation guard logic lives in RootView's TodayView .task; skip presentation entirely when zero sessions logged the prior week (still mark seen so the sheet doesn't show retroactively); sections per CONTEXT Claude's Discretion + UI-SPEC §Copywriting Contract: Sessions / Muscles Hit / Under-Trained / Progress"
    - "WeeklyRecapSheet's 'Progress' section displays e1RM movement per top-5 exercises (sorted DESC by working-set count last week) via OneRepMaxEstimator.estimate (plan 01) — top-set e1RM last week vs same exercise's top-set e1RM the week before; signed delta in kg or lb depending on settings.unitsRaw"
    - "MuscleVolumeTargetSeederTests turns GREEN (the RED scaffold from plan 01 is replaced) — 5+ @Test functions cover: seeder writes 17 rows on empty context; user-edited rows preserved across re-run (userEdited == true → no overwrite); non-user-edited rows updated to current canonical RPVolumeLandmarks values; idempotent across multiple runs (row count + values stable); T-05-02 monotonic invariant enforced at the editor (defense-in-depth: the seeder itself only writes RP defaults which are pre-validated as monotonic)"
    - "WeeklyRecapTriggerTests turns GREEN (the RED scaffold from plan 01 is replaced) — 4+ @Test functions cover: first launch of new Monday triggers presentation when ≥1 session logged last week; re-launch within same week does NOT re-fire (weeklyRecapShownForWeekStart guard works); zero sessions logged last week → trigger returns 'skip but mark seen' (no fire, but settings.weeklyRecapShownForWeekStart is set); Pitfall #7 Calendar.isDate comparison handles DST/timezone correctly"
    - "MuscleVolumeTargetEditorUITests UI test turns GREEN — navigates to Settings → Volume Targets → Chest section → expands inline Stepper → edits MEV from 8 to 10 → asserts the persisted value round-trips via SwiftData (back out + re-enter, value remains 10); 'Reset All to RP Defaults' destructive global button fires the .alert confirmation and on confirm resets all 17 rows back to RP landmark values"
    - "WeeklyRecapUITests UI test turns GREEN — launches the app with a test-seeded ModelContainer that includes (a) ≥1 logged session last week + (b) UserSettings.weeklyRecapShownForWeekStart == nil → asserts the WeeklyRecapSheet auto-presents on Today tab .task; tap Done dismisses; re-launch (still same week) → asserts sheet does NOT re-present; relaunch with seed-modified state to simulate next-Monday + advisory dismissed → asserts re-presentation"
  artifacts:
    - path: "fitbod/Settings/MuscleVolumeTargetEditor.swift"
      provides: "SwiftUI struct MuscleVolumeTargetEditor: View — Settings screen reachable from SettingsView → 'Volume Targets' NavigationLink; sectioned List per muscle with disclosure into inline MuscleVolumeTargetStepper (plan 05 ships the Stepper); section header is muscle.displayName + trailing preview 'mev / mav / mrv' per UI-SPEC §Copywriting Contract; navigation title 'Volume Targets'; global 'Reset All to RP Defaults' destructive button + .alert confirmation per UI-SPEC §Copywriting Contract verbatim copy"
      contains: "struct MuscleVolumeTargetEditor"
    - path: "fitbod/Fatigue/MuscleVolumeTargetSeeder.swift"
      provides: "@MainActor public enum MuscleVolumeTargetSeeder with seedIfNeeded(in:) static func; iterates MuscleRegionMap.allSlugs (or @Query<MuscleGroup>); finds or creates MuscleVolumeTarget per slug; row-level idempotency via userEdited guard; writes RPVolumeLandmarks.landmark(for: slug) values when userEdited == false; single try? context.save() at end"
      contains: "enum MuscleVolumeTargetSeeder"
    - path: "fitbod/Settings/SettingsView.swift"
      provides: "Modified — adds new 'Volume Targets' Section with NavigationLink to MuscleVolumeTargetEditor + a Stepper for UserSettings.frequencyHitMinSets (D-03 + VOL-06 surface); inserted above the existing 'About' placeholder section per UI-SPEC §Component Inventory Modified views table"
      contains: "Volume Targets"
    - path: "fitbod/Fatigue/WeeklyRecapSheet.swift"
      provides: "SwiftUI struct WeeklyRecapSheet: View — NavigationStack containing 4 Sections per CONTEXT Claude's Discretion + UI-SPEC §Copywriting Contract verbatim: (1) Sessions ('Last week you logged {N} sessions across {M} routines.'), (2) Muscles Hit (list of muscles with frequencyHits ≥ minSets last week), (3) Under-Trained (list of muscles with totalSets < mev last week, foreground VolumeBelowMEVGray), (4) Progress (top 5 exercises by working-set count last week with their e1RM delta vs week prior); .presentationDetents([.large]); .toolbar Done button"
      contains: "struct WeeklyRecapSheet"
    - path: "fitbod/App/RootView.swift"
      provides: "Modified — Today tab gets WeeklyRecapSheet trigger logic via .task / .onAppear that compares Date.now.mondayAtMidnight to settings.weeklyRecapShownForWeekStart per Pitfall #7 (Calendar.isDate equalTo: toGranularity: .day); presents the sheet when (a) thisMonday != weeklyRecapShownForWeekStart AND (b) ≥1 session logged in the prior week; marks weeklyRecapShownForWeekStart = thisMonday regardless of whether the sheet is presented (handles zero-sessions short-circuit per RESEARCH §10 Pitfall #8); preserves all existing DeloadAdvisoryBanner + FatigueSurfaceView mount + NavigationPath wiring from plans 02/04/05"
      contains: "weeklyRecapShownForWeekStart"
  key_links:
    - from: "fitbod/Settings/MuscleVolumeTargetEditor.swift"
      to: "fitbod/Fatigue/MuscleVolumeTargetStepper.swift"
      via: "Reuses plan 05's MuscleVolumeTargetStepper component inline within each muscle's expanded section; the editor is a thin host that adds the sectioned list + global reset button on top of the shared Stepper"
      pattern: "MuscleVolumeTargetStepper(target:"
    - from: "fitbod/Fatigue/MuscleVolumeTargetSeeder.swift"
      to: "fitbod/Fatigue/RPVolumeLandmarks.swift"
      via: "Seeder iterates RPVolumeLandmarks.landmarks (or MuscleRegionMap.allSlugs) and writes the mv/mev/mav/mrv values per muscle"
      pattern: "RPVolumeLandmarks.landmark"
    - from: "fitbod/App/RootView.swift"
      to: "fitbod/Models/UserSettings.swift"
      via: "WeeklyRecapTrigger reads + writes settings.weeklyRecapShownForWeekStart (Date? field shipped by plan 01)"
      pattern: "weeklyRecapShownForWeekStart"
    - from: "fitbod/Fatigue/WeeklyRecapSheet.swift"
      to: "fitbod/Prescription/OneRepMaxEstimator.swift"
      via: "Progress section computes per-exercise top-set e1RM last week vs week prior via OneRepMaxEstimator.estimate (plan 01 ships)"
      pattern: "OneRepMaxEstimator.estimate"
    - from: "fitbod/Fatigue/WeeklyRecapSheet.swift"
      to: "fitbod/Fatigue/FatigueModel.swift"
      via: "Muscles Hit + Under-Trained sections read FatigueModel.weeklyVolume + frequencyHits across all 17 muscles for last week"
      pattern: "FatigueModel.weeklyVolume"
---

<objective>
Ship the **Slice F vertical**: a serious lifter opens Settings → "Volume Targets" → "Chest" → expands the inline Stepper → edits MEV from 8 to 10 → backs out → the chest bar on the Today tab now uses 10 as the MEV threshold for the zone-recompute (the user's MEV target is editable from Settings as the primary entry point per CONTEXT Claude's Discretion). Separately, the first time the user opens the app on a new Monday with ≥1 session logged last week, the WeeklyRecapSheet auto-presents (.large detent) showing 4 sections (Sessions / Muscles Hit / Under-Trained / Progress per CONTEXT Claude's Discretion) — VOL-07 closure.

Purpose: this is the LAST Phase 5 vertical slice — the Settings entry point for SET-05 (MuscleVolumeTargetEditor) finalizes the editor reach (plan 05's inline editor in PerMuscleDetailView is one entry point; this plan adds the Settings entry point per Claude's Discretion "Both surfaces edit the same row; Settings is the primary entry point"). The MuscleVolumeTargetSeeder closes plan 01's last RED scaffold for seeder idempotency. The WeeklyRecapSheet auto-presents the per-Monday Mon morning recap that summarizes last week's training — VOL-07 closure.

Output: 1 new Settings view (`MuscleVolumeTargetEditor`), 1 new seeder (`MuscleVolumeTargetSeeder`), 1 modified Settings view (`SettingsView` — adds Volume Targets section + frequencyHitMinSets Stepper), 1 new sheet (`WeeklyRecapSheet`), 1 modified host (`RootView` — adds WeeklyRecapSheet trigger logic), 2 GREEN unit test suites (MuscleVolumeTargetSeeder + WeeklyRecapTrigger; replacing the final 2 of the 8 RED scaffolds from plan 01), 2 GREEN UI test suites (MuscleVolumeTargetEditor + WeeklyRecap).

End-to-end user-visible deliverables (two distinct flows):
1. **SET-05 Settings entry point**: User opens Settings → 'Volume Targets' → Chest section → expands Adjust Targets → MEV stepper from 8 to 10 → backs out → the bar's zone-recompute reflects the new MEV immediately on the Today tab (round-trip via SwiftData).
2. **VOL-07 Weekly Recap**: First time user opens the app on a new Monday with ≥1 session logged last week, the WeeklyRecapSheet auto-presents on the Today tab with 4 sections — Sessions count, Muscles Hit (with frequency counts), Under-Trained (with sets-vs-MEV deltas), Progress (top 5 exercises with e1RM movement). The sheet does NOT re-fire on subsequent opens within the same week.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@.planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md
@.planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md
@.planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md
@.planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md
@.planning/phases/05-fatigue-model-plateau-detection/05-VALIDATION.md
@.planning/phases/05-fatigue-model-plateau-detection/05-01-PLAN.md
@.planning/phases/05-fatigue-model-plateau-detection/05-02-PLAN.md
@.planning/phases/05-fatigue-model-plateau-detection/05-04-PLAN.md
@.planning/phases/05-fatigue-model-plateau-detection/05-05-PLAN.md

@fitbod/Fatigue/FatigueModel.swift
@fitbod/Fatigue/MuscleVolumeTargetStepper.swift
@fitbod/Fatigue/PerMuscleDetailView.swift
@fitbod/Fatigue/RPVolumeLandmarks.swift
@fitbod/Fatigue/VolumeZone.swift
@fitbod/Fatigue/FatigueSurfaceView.swift
@fitbod/Prescription/OneRepMaxEstimator.swift
@fitbod/Models/MuscleGroup.swift
@fitbod/Models/MuscleVolumeTarget.swift
@fitbod/Models/UserSettings.swift
@fitbod/Models/Exercise.swift
@fitbod/Models/Session.swift
@fitbod/Models/SessionExercise.swift
@fitbod/Models/SetEntry.swift
@fitbod/Settings/SettingsView.swift
@fitbod/Settings/PlateInventorySeeder.swift
@fitbod/ExerciseLibrary/MuscleRegionMap.swift
@fitbod/App/RootView.swift
@fitbod/Sessions/PinnedNoteSheet.swift
@fitbodTests/Fatigue/FatigueTestFixtures.swift

<interfaces>
<!-- Existing types and contracts the executor needs. Plans 01 + 02 + 04 + 05 ship these. -->

From fitbod/Fatigue/MuscleVolumeTargetStepper.swift (plan 05):
```
public struct MuscleVolumeTargetStepper: View {
    @Bindable public var target: MuscleVolumeTarget
    public init(target: MuscleVolumeTarget)
    // 4 Steppers (MV/MEV/MAV/MRV) + monotonic validation + Reset to RP defaults button
}
```

From fitbod/Fatigue/RPVolumeLandmarks.swift (plan 01):
```
public enum RPVolumeLandmarks {
    public struct Landmark: Sendable, Equatable { public let mv, mev, mav, mrv: Int }
    public static let landmarks: [String: Landmark] = [...]
    public static func landmark(for slug: String) -> Landmark
}
```

From fitbod/Models/MuscleVolumeTarget.swift (plan 01 extends):
```
public final class MuscleVolumeTarget {
    public var muscle: MuscleGroup?
    public var mev: Int = 8
    public var mav: Int = 14
    public var mrv: Int = 22
    public var mv: Int = 6
    public var userEdited: Bool = false   // Phase 5 plan 01 added
}
```

From fitbod/Models/UserSettings.swift (plan 01 extends):
```
public var weeklyRecapShownForWeekStart: Date? = nil  // VOL-07 trigger guard; plan 01 added
public var frequencyHitMinSets: Int = 2               // D-03 + VOL-06; plan 01 added
public var weekStartsMonday: Bool = true              // already existed
public var unitsRaw: String = "lb"                    // already existed; used by WeeklyRecapSheet Progress
```

From fitbod/Settings/PlateInventorySeeder.swift (analog for @MainActor public enum seeder):
```
@MainActor
public enum PlateInventorySeeder {
    public static let seededKey = "plateInventorySeeded"
    public static func seedIfNeeded(in context: ModelContext, unitSystem: WeightUnit) {
        if UserDefaults.standard.bool(forKey: seededKey) { return }
        let descriptor = FetchDescriptor<PlateInventory>()
        let existingCount = (try? context.fetchCount(descriptor)) ?? 0
        if existingCount >= threshold { ...; return }
        for kind in PlateEquipmentKind.allCases { ... }
        try? context.save()
        UserDefaults.standard.set(true, forKey: seededKey)
    }
}
```
MuscleVolumeTargetSeeder differs: uses row-level userEdited guard (NOT UserDefaults flag — RP landmarks may be updated on app updates, so row-level is required).

From fitbod/Settings/SettingsView.swift — current shape this plan extends:
```
public struct SettingsView: View {
    @Query private var settingsList: [UserSettings]
    public var body: some View {
        NavigationStack {
            Form {
                if let settings = settingsList.first {
                    unitsSection(settings: settings)
                    smartProgressionSection(settings: settings)
                    // PHASE 5 INSERTION POINT: Volume Targets section here
                    aboutSectionPlaceholder
                } else {
                    Section { Text("Settings unavailable...") }
                }
            }
            .navigationTitle("Settings")
        }
    }
}
```

From fitbod/Sessions/PinnedNoteSheet.swift (analog for sheet + toolbar Done button):
```
struct PinnedNoteSheet: View {
    @Environment(\.dismiss) private var dismiss
    var body: some View {
        NavigationStack {
            // content
            .toolbar { ToolbarItem(placement: .confirmationAction) { Button("Done") { dismiss() } } }
        }
    }
}
```

From fitbod/App/RootView.swift — TodayView shape after plans 02 + 04 + 05:
```
private struct TodayView: View {
    @Environment(\.modelContext) private var ctx
    @State private var navigationPath = NavigationPath()
    @State private var currentAdvisory: DeloadAdvisory? = nil    // plan 04
    @State private var presentingSignalSheet: Bool = false       // plan 04
    @State private var presentingWeeklyRecap: Bool = false       // PLAN 06 ADDS
    @Query private var settingsList: [UserSettings]
    
    var body: some View {
        NavigationStack(path: $navigationPath) {
            VStack(spacing: 16) {
                ResumeWorkoutBanner(...)
                FatigueSurfaceView(navigationPath: $navigationPath, isAdvisoryActive: currentAdvisory != nil)  // plan 05 added the path
                Spacer()
            }
            .safeAreaInset(edge: .top) { /* DeloadAdvisoryBanner */ }    // plan 04
            .sheet(isPresented: $presentingSignalSheet) { /* DeloadSignalDetailSheet */ }   // plan 04
            // PHASE 5 PLAN 06 INSERTION POINT: WeeklyRecapSheet sheet + .task trigger
            .task { 
                await refreshAdvisory()                           // plan 04
                await checkWeeklyRecap()                          // PLAN 06 ADDS
            }
            // ...
        }
    }
}
```

From fitbodTests/Fatigue/FatigueTestFixtures.swift (plan 01 helper; plan 06 fills MuscleVolumeTargetSeeder + WeeklyRecapTrigger-specific factories):
```
@MainActor
enum FatigueTestFixtures {
    static func makeContext() throws -> ModelContext
    // plan 06 fills:
    static func emptyMuscleVolumeTargets(ctx: ModelContext) throws    // seeded library + 17 MuscleGroups + zero MuscleVolumeTarget rows
    static func someUserEditedTargets(ctx: ModelContext) throws       // 17 muscles; chest's userEdited=true with custom MEV=10
    static func sessionsLastWeek(ctx: ModelContext, count: Int) throws // N completed Sessions in prior calendar week
    static func noSessionsLastWeek(ctx: ModelContext) throws          // empty SwiftData for prior week
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MuscleVolumeTargetSeeder + MuscleVolumeTargetEditor + SettingsView Volume Targets section + GREEN MuscleVolumeTargetSeederTests + GREEN MuscleVolumeTargetEditorUITests</name>
  <read_first>
    fitbod/Settings/PlateInventorySeeder.swift (analog — @MainActor public enum + idempotency guard; key difference: row-level userEdited guard NOT UserDefaults flag),
    fitbod/Fatigue/RPVolumeLandmarks.swift (plan 01 ships; provides the seed values),
    fitbod/Fatigue/MuscleVolumeTargetStepper.swift (plan 05 ships; reused inline by MuscleVolumeTargetEditor),
    fitbod/ExerciseLibrary/MuscleRegionMap.swift (analog for all-17-slug iteration),
    fitbod/Models/MuscleGroup.swift,
    fitbod/Models/MuscleVolumeTarget.swift (plan 01 added userEdited),
    fitbod/Models/UserSettings.swift (frequencyHitMinSets Stepper + Volume Targets NavigationLink),
    fitbod/Settings/SettingsView.swift (the file this task modifies — read the existing Form + Section + NavigationLink pattern from the smartProgressionSection helper),
    fitbodUITests/fitbodUITests.swift (analog for XCUIApplication launch + tab navigation),
    fitbodTests/Fatigue/FatigueTestFixtures.swift (plan 01 helper; plan 06 adds emptyMuscleVolumeTargets + someUserEditedTargets factories),
    .planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md (Claude's Discretion: MuscleVolumeTargetEditor surface — Settings → 'Volume Targets' section; primary entry point + inline from per-muscle detail (plan 05 ships the inline); D-03 frequency-hit threshold editable from Settings),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §7.3 (MuscleVolumeTargetSeeder pattern + userEdited idempotency guard), §11.1 (SET-05 editor design — sectioned list per muscle + global Reset All),
    .planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md §Component Inventory MuscleVolumeTargetEditor entry + §Copywriting Contract MuscleVolumeTargetEditor section verbatim copy (Navigation title 'Volume Targets', section header per muscle, row trailing preview 'mev / mav / mrv', global reset button, .alert confirmation copy, footer per section),
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (MuscleVolumeTargetEditor + MuscleVolumeTargetSeeder + SettingsView modification sections)
  </read_first>
  <files>fitbod/Fatigue/MuscleVolumeTargetSeeder.swift, fitbod/Settings/MuscleVolumeTargetEditor.swift, fitbod/Settings/SettingsView.swift, fitbodTests/Fatigue/MuscleVolumeTargetSeederTests.swift, fitbodTests/Fatigue/FatigueTestFixtures.swift, fitbodUITests/MuscleVolumeTargetEditorUITests.swift</files>
  <behavior>
    MuscleVolumeTargetSeeder.seedIfNeeded behavior tests:
    - Test (17 rows written on empty context): seed emptyMuscleVolumeTargets fixture (17 MuscleGroup rows present, zero MuscleVolumeTarget rows). Call seeder. Assert 17 MuscleVolumeTarget rows exist after; for each slug, assert mv/mev/mav/mrv match RPVolumeLandmarks.landmark(for: slug).
    - Test (user-edited row preserved): seed 1 chest MuscleVolumeTarget with userEdited=true, mev=10 (custom). Call seeder. Assert the chest row STILL has mev=10 and userEdited=true (NOT overwritten with RP default mev=8).
    - Test (non-edited rows updated to current RP values): seed 1 chest MuscleVolumeTarget with userEdited=false, mev=99 (somehow stale). Call seeder. Assert the chest row is now mev=8 (RP default per RESEARCH §7.1 chest).
    - Test (idempotent across runs): call seeder twice in succession. Assert MuscleVolumeTarget row count is stable (17 rows) + all values stable.
    - Test (defense-in-depth — RP values are monotonic): for each of the 17 RP landmarks, assert mv ≤ mev < mav < mrv (RPVolumeLandmarks data is pre-validated; the seeder cannot introduce a non-monotonic row by mistake).
    
    MuscleVolumeTargetEditor + SettingsView Volume Targets section + UI tests:
    - Behavior (UI-SPEC §Component Inventory MuscleVolumeTargetEditor): sectioned List per muscle (17 muscles); each muscle section has a header "{muscle.displayName}" + trailing preview "mev / mav / mrv" (e.g., "8 / 16 / 22") + disclosure into the inline MuscleVolumeTargetStepper (plan 05's component); section footer "MEV / MAV / MRV — weekly working sets. Seeded from RP volume landmarks." (`.caption .secondaryLabel`).
    - Behavior (UI-SPEC §Copywriting Contract MuscleVolumeTargetEditor verbatim): navigation title "Volume Targets"; global "Reset All to RP Defaults" destructive button at the bottom; .alert confirmation with title "Reset all volume targets?" + body "Your custom values will be replaced with RP defaults for all 17 muscles." + buttons "Reset All" (destructive) / "Cancel" — verbatim copy.
    - Behavior (UI-SPEC §Copywriting Contract destructive global button): on .alert "Reset All" tap, iterate all MuscleVolumeTarget rows and write RPVolumeLandmarks.landmark values + set userEdited=false; try? ctx.save().
    - Behavior (UI-SPEC §Component Inventory SettingsView modification): SettingsView adds a new Section labeled "Volume Targets" with a NavigationLink to MuscleVolumeTargetEditor + a "17 muscles" trailing preview; insert above the existing aboutSectionPlaceholder; ALSO add a Stepper for `settings.frequencyHitMinSets` per CONTEXT D-03 + Claude's Discretion (the planner places this here — VOL-06 surfaces in Settings as well as in the per-muscle detail's Frequency This Week section).
    - Behavior (frequencyHitMinSets Stepper bounds): range 1...5 step 1 (D-03 default is 2; sane bounds for "frequency hit" threshold per RESEARCH §11 conventions — anything above 5 is implausible at a v1 personal-app scale).
    - Behavior (UI test — navigation + Stepper round-trip): launch app → tap Settings tab → assert "Volume Targets" Section header exists → tap NavigationLink → assert MuscleVolumeTargetEditor navigation title "Volume Targets" → find "Chest" section → tap to expand (DisclosureGroup or NavigationLink — see action) → assert 4 MuscleVolumeTargetStepper Steppers appear → tap MEV stepper "+" 2 times (8 → 10) → back out + re-enter → assert MEV is still 10.
    - Behavior (UI test — Reset All global button): from MuscleVolumeTargetEditor, scroll to the global "Reset All to RP Defaults" button at the bottom; tap → assert .alert appears with title "Reset all volume targets?" → tap "Reset All" → assert MEV reverts to 8 (RP default for chest).
  </behavior>
  <action>
    (1) Create fitbod/Fatigue/MuscleVolumeTargetSeeder.swift. File header: "MuscleVolumeTargetSeeder.swift / fitbod / Phase 5 VOL-03 + SET-05 — RP MEV/MAV/MRV seeder per RESEARCH §7.3. / Idempotent: re-running overwrites rows where userEdited == false; user-edited rows skipped. / NO UserDefaults flag — RP landmarks may be updated on app updates, so row-level guard is required. / @MainActor because ModelContext is @MainActor-bound.". Imports: `import Foundation, import SwiftData`. Body:
    
    ```
    @MainActor
    public enum MuscleVolumeTargetSeeder {
        public static func seedIfNeeded(in context: ModelContext) {
            // 1. Fetch all MuscleGroup rows (seeded by Phase 1 importer).
            let muscleDescriptor = FetchDescriptor<MuscleGroup>()
            let muscles = (try? context.fetch(muscleDescriptor)) ?? []
            
            for muscle in muscles {
                let slug = muscle.slug
                let landmark = RPVolumeLandmarks.landmark(for: slug)
                
                // 2. Find or create MuscleVolumeTarget for this muscle.
                let targetDescriptor = FetchDescriptor<MuscleVolumeTarget>(
                    predicate: #Predicate<MuscleVolumeTarget> { $0.muscle?.slug == slug }
                )
                let existing = (try? context.fetch(targetDescriptor))?.first
                
                if let target = existing {
                    // 3a. Skip if user-edited.
                    if target.userEdited { continue }
                    // 3b. Otherwise overwrite with current RP defaults.
                    target.mv = landmark.mv
                    target.mev = landmark.mev
                    target.mav = landmark.mav
                    target.mrv = landmark.mrv
                } else {
                    // 4. Create new row with RP defaults.
                    let newTarget = MuscleVolumeTarget()
                    newTarget.muscle = muscle
                    newTarget.mv = landmark.mv
                    newTarget.mev = landmark.mev
                    newTarget.mav = landmark.mav
                    newTarget.mrv = landmark.mrv
                    newTarget.userEdited = false
                    context.insert(newTarget)
                }
            }
            
            try? context.save()
        }
    }
    ```
    
    (2) Modify fitbod/App/RootView.swift to call MuscleVolumeTargetSeeder.seedIfNeeded(in:) inside the existing seed orchestration block (alongside the Phase 1 ExerciseLibraryImporter + plan 02's StimulusWeightSeeder). Placement: AFTER the library importer + StimulusWeightSeeder runs (must run AFTER MuscleGroup rows are seeded so the per-slug lookup resolves).
    
    Insertion site: in the RootView.task block where seedState progression happens. Pragmatic: simply add the call near the end of the seed sequence:
    
    ```
    // PHASE 5 PLAN 06 — MuscleVolumeTargetSeeder runs after library importer + StimulusWeightSeeder
    MuscleVolumeTargetSeeder.seedIfNeeded(in: ctx)
    ```
    
    (3) Create fitbod/Settings/MuscleVolumeTargetEditor.swift. File header: "MuscleVolumeTargetEditor.swift / fitbod / Phase 5 SET-05 Settings entry point — sectioned List per muscle with disclosure into MuscleVolumeTargetStepper. / Global 'Reset All to RP Defaults' destructive button with .alert confirmation. / UI-SPEC §Copywriting Contract verbatim copy.". Imports: `import SwiftUI, import SwiftData`. Body:
    
    ```
    public struct MuscleVolumeTargetEditor: View {
        @Environment(\.modelContext) private var ctx
        @Query(sort: \MuscleGroup.displayName) private var allMuscles: [MuscleGroup]
        @Query private var allTargets: [MuscleVolumeTarget]
        @State private var expandedSlug: String? = nil    // which muscle's editor is expanded
        @State private var presentingResetAllAlert: Bool = false
        
        public init() {}
        
        public var body: some View {
            List {
                if allMuscles.isEmpty {
                    Section {
                        VStack(alignment: .leading, spacing: 8) {
                            Text("No muscles found").font(.headline)
                            Text("Muscles are seeded on first launch. Restart the app if this persists.")
                                .font(.body)
                                .foregroundStyle(.secondary)
                        }
                    }
                } else {
                    ForEach(allMuscles, id: \.persistentModelID) { muscle in
                        muscleSection(muscle: muscle)
                    }
                    
                    Section {
                        Button("Reset All to RP Defaults", role: .destructive) {
                            presentingResetAllAlert = true
                        }
                        .frame(maxWidth: .infinity, alignment: .center)
                    }
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Volume Targets")
            .navigationBarTitleDisplayMode(.large)
            .alert("Reset all volume targets?", isPresented: $presentingResetAllAlert) {
                Button("Cancel", role: .cancel) { }
                Button("Reset All", role: .destructive) {
                    resetAllToRPDefaults()
                }
            } message: {
                Text("Your custom values will be replaced with RP defaults for all 17 muscles.")
            }
        }
        
        @ViewBuilder
        private func muscleSection(muscle: MuscleGroup) -> some View {
            let target = allTargets.first { $0.muscle?.slug == muscle.slug }
            let isExpanded = expandedSlug == muscle.slug
            
            Section {
                Button {
                    expandedSlug = isExpanded ? nil : muscle.slug
                } label: {
                    HStack {
                        Text(muscle.displayName).font(.body)
                        Spacer()
                        if let t = target {
                            Text("\(t.mev) / \(t.mav) / \(t.mrv)")
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                        Image(systemName: isExpanded ? "chevron.up" : "chevron.down")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
                .buttonStyle(.plain)
                
                if isExpanded, let target = target {
                    MuscleVolumeTargetStepper(target: target)
                        .padding(.vertical, 8)
                }
            } footer: {
                if isExpanded {
                    Text("MEV / MAV / MRV — weekly working sets. Seeded from RP volume landmarks.")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            }
        }
        
        @MainActor
        private func resetAllToRPDefaults() {
            for muscle in allMuscles {
                let slug = muscle.slug
                let landmark = RPVolumeLandmarks.landmark(for: slug)
                if let target = allTargets.first(where: { $0.muscle?.slug == slug }) {
                    target.mv = landmark.mv
                    target.mev = landmark.mev
                    target.mav = landmark.mav
                    target.mrv = landmark.mrv
                    target.userEdited = false
                }
            }
            try? ctx.save()
        }
    }
    ```
    
    (4) Modify fitbod/Settings/SettingsView.swift to add the new 'Volume Targets' Section between smartProgressionSection and aboutSectionPlaceholder. Add the frequencyHitMinSets Stepper to the same section (or a sub-row within it):
    
    ```
    // Insert before aboutSectionPlaceholder:
    if let settings = settingsList.first {
        volumeTargetsSection(settings: settings)
    }
    ```
    
    Add the helper method:
    
    ```
    @ViewBuilder
    private func volumeTargetsSection(settings: UserSettings) -> some View {
        @Bindable var s = settings
        
        Section {
            NavigationLink {
                MuscleVolumeTargetEditor()
            } label: {
                HStack {
                    Text("Volume Targets")
                    Spacer()
                    Text("17 muscles")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            }
            
            Stepper(value: $s.frequencyHitMinSets, in: 1...5, step: 1) {
                LabeledContent("Frequency hit threshold") {
                    Text("\(s.frequencyHitMinSets) sets")
                        .foregroundStyle(.secondary)
                }
            }
            .accessibilityLabel("Frequency hit threshold")
            .accessibilityValue("\(s.frequencyHitMinSets) sets")
        } header: {
            Text("Volume Targets")
        } footer: {
            Text("Frequency hit threshold: a session counts as a 'hit' for a muscle when the stimulus-weighted set contribution is at least this many sets.")
                .font(.caption)
                .foregroundStyle(.secondary)
        }
    }
    ```
    
    Preserve every other line of SettingsView verbatim — unitsSection, smartProgressionSection, aboutSectionPlaceholder, navigation title.
    
    (5) Create fitbodTests/Fatigue/MuscleVolumeTargetSeederTests.swift, REPLACING the plan-01 RED scaffold. Annotate `@MainActor @Suite("MuscleVolumeTargetSeeder", .serialized)`. Use `FatigueTestFixtures.makeContext()`.
    
    Required @Test functions:
      - `seedsSeventeenRowsOnEmptyContext`: seed emptyMuscleVolumeTargets fixture (17 MuscleGroup rows + zero MuscleVolumeTargets). Call seeder. Assert MuscleVolumeTarget row count == 17. For each slug, assert mv/mev/mav/mrv match RPVolumeLandmarks.landmark(for: slug).
      - `preservesUserEditedRowAcrossReSeed`: seed chest MuscleVolumeTarget with userEdited=true + mev=10. Call seeder. Assert mev still 10 + userEdited still true.
      - `overwritesNonEditedRowsWithCurrentRPDefaults`: seed chest MuscleVolumeTarget with userEdited=false + mev=99 (stale). Call seeder. Assert mev now 8 (RP default).
      - `idempotentAcrossMultipleRuns`: call seeder twice. Assert row count stable (17) + all values stable.
      - `allRPLandmarksAreMonotonic`: iterate RPVolumeLandmarks.landmarks; for each Landmark, assert `mv <= mev < mav < mrv`. (Defense-in-depth — the seeder cannot introduce non-monotonic rows because RP data is pre-validated.)
    
    (6) Fill in the FatigueTestFixtures factories needed by this task:
      - `emptyMuscleVolumeTargets(ctx:)`: insert 17 MuscleGroup rows (matching MuscleRegionMap.allSlugs) with displayName capitalized; zero MuscleVolumeTarget rows.
      - `someUserEditedTargets(ctx:)`: same 17 MuscleGroups + 1 MuscleVolumeTarget for chest with userEdited=true + mev=10 (custom).
    
    (7) Create fitbodUITests/MuscleVolumeTargetEditorUITests.swift. Use launchEnvironment["FITBOD_TEST_SEED"] = "phase5-volume-targets" — a new seed flag that the app's #if DEBUG branch (already added by plan 02/04 for other seeds) handles by NOT seeding the MuscleVolumeTargetSeeder (so the test starts with the seeder ready to run) OR ensures the 17 MuscleGroup rows are seeded.
    
    Required tests:
    ```
    final class MuscleVolumeTargetEditorUITests: XCTestCase {
        override func setUpWithError() throws { continueAfterFailure = false }
        
        @MainActor
        func testNavigateToVolumeTargetsAndEditChestMEVRoundTrip() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-volume-targets"
            app.launch()
            
            // Settings → Volume Targets.
            app.tabBars.buttons["Settings"].tap()
            
            // Assert Volume Targets section header exists.
            let volumeTargetsHeader = app.staticTexts["Volume Targets"]
            XCTAssertTrue(volumeTargetsHeader.waitForExistence(timeout: 3))
            
            // Tap the NavigationLink row (the one with "17 muscles" preview).
            let row = app.cells.containing(.staticText, identifier: "Volume Targets").firstMatch
            row.tap()
            
            // Assert navigation title.
            XCTAssertTrue(app.navigationBars["Volume Targets"].waitForExistence(timeout: 2))
            
            // Tap Chest section to expand.
            let chestRow = app.buttons.matching(NSPredicate(format: "label CONTAINS[c] 'Chest'")).firstMatch
            XCTAssertTrue(chestRow.waitForExistence(timeout: 2))
            chestRow.tap()
            
            // Assert MEV stepper appears.
            let mevStepper = app.steppers.matching(NSPredicate(format: "label CONTAINS 'Minimum effective volume'")).firstMatch
            XCTAssertTrue(mevStepper.waitForExistence(timeout: 2))
            
            // Tap MEV + 2 times (8 → 10).
            mevStepper.buttons["Increment"].tap()
            mevStepper.buttons["Increment"].tap()
            
            // Back out + re-enter.
            app.navigationBars.buttons.element(boundBy: 0).tap()
            row.tap()
            chestRow.tap()
            
            // Assert MEV is now 10.
            XCTAssertTrue(app.staticTexts.matching(NSPredicate(format: "label CONTAINS '10 sets'")).firstMatch.waitForExistence(timeout: 2))
        }
        
        @MainActor
        func testResetAllToRPDefaultsRestoresEditedRow() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-volume-targets"
            app.launch()

            // 1) Navigate Settings → Volume Targets → Chest, edit MEV from 8 to 10 (same path as the round-trip test).
            app.tabBars.buttons["Settings"].tap()
            app.cells.staticTexts["Volume Targets"].tap()
            app.cells.staticTexts["Chest"].tap()
            let mevStepper = app.steppers["MEV stepper"]
            XCTAssertTrue(mevStepper.waitForExistence(timeout: 2))
            mevStepper.buttons["Increment"].tap()
            mevStepper.buttons["Increment"].tap()
            XCTAssertEqual(app.staticTexts["MEV value"].label, "10")

            // 2) Back to the Volume Targets list, scroll to bottom, tap the global reset button.
            app.navigationBars.buttons.element(boundBy: 0).tap()  // back to Volume Targets
            let resetAllButton = app.buttons["Reset All to RP Defaults"]
            // Scroll until visible (the button lives in the list footer).
            while !resetAllButton.isHittable {
                app.swipeUp()
            }
            resetAllButton.tap()

            // 3) Assert the destructive confirmation alert appears with the expected title.
            let alert = app.alerts["Reset all volume targets?"]
            XCTAssertTrue(alert.waitForExistence(timeout: 2))

            // 4) Tap the destructive "Reset All" button on the alert.
            alert.buttons["Reset All"].tap()

            // 5) Re-enter Chest and assert MEV reverts to the RP default (8).
            app.cells.staticTexts["Chest"].tap()
            XCTAssertTrue(app.staticTexts["MEV value"].waitForExistence(timeout: 2))
            XCTAssertEqual(app.staticTexts["MEV value"].label, "8")
        }
        
        @MainActor
        func testFrequencyHitThresholdStepperEdit() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-volume-targets"
            app.launch()
            
            app.tabBars.buttons["Settings"].tap()
            
            // Find frequency-hit-threshold Stepper.
            let threshStepper = app.steppers["Frequency hit threshold"]
            XCTAssertTrue(threshStepper.waitForExistence(timeout: 3))
            
            // Tap + once (2 → 3).
            threshStepper.buttons["Increment"].tap()
            
            // Assert label updates.
            XCTAssertTrue(app.staticTexts["3 sets"].exists)
        }
    }
    ```
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/MuscleVolumeTargetSeeder -only-testing fitbodUITests/MuscleVolumeTargetEditorUITests 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    MuscleVolumeTargetSeeder.swift exists with seedIfNeeded(in:) writing 17 RP-defaulted rows on empty context + preserving userEdited=true rows + overwriting userEdited=false rows; RootView wires seeder call in seed orchestration block; MuscleVolumeTargetEditor.swift exists with sectioned List per muscle + DisclosureGroup-style expansion into MuscleVolumeTargetStepper + global "Reset All to RP Defaults" .alert + verbatim UI-SPEC copy; SettingsView adds 'Volume Targets' Section with NavigationLink + frequencyHitMinSets Stepper (range 1...5); MuscleVolumeTargetSeederTests has 5 GREEN @Test functions; MuscleVolumeTargetEditorUITests has 3 GREEN UI tests; FatigueTestFixtures gets emptyMuscleVolumeTargets + someUserEditedTargets factories. SET-05 closed both inline (plan 05) AND from Settings (this plan).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeeklyRecapSheet + WeeklyRecapTrigger guard logic in RootView + GREEN WeeklyRecapTriggerTests + GREEN WeeklyRecapUITests</name>
  <read_first>
    fitbod/Sessions/PinnedNoteSheet.swift (analog for sheet with toolbar Done button — WeeklyRecapSheet uses similar shape),
    fitbod/Fatigue/FatigueModel.swift (need weeklyVolume + frequencyHits for the Muscles Hit + Under-Trained sections),
    fitbod/Prescription/OneRepMaxEstimator.swift (need estimate for the Progress section's e1RM delta computation),
    fitbod/Models/Session.swift,
    fitbod/Models/SessionExercise.swift,
    fitbod/Models/SetEntry.swift,
    fitbod/Models/Exercise.swift,
    fitbod/Models/UserSettings.swift (weeklyRecapShownForWeekStart + weekStartsMonday + unitsRaw for the Progress display),
    fitbod/App/RootView.swift (the file this task modifies — read the existing TodayView structure including plan 04's DeloadAdvisoryBanner via .safeAreaInset + plan 05's FatigueSurfaceView mount + the existing .task block from plan 04 that needs to be extended for checkWeeklyRecap),
    fitbod/Fatigue/PerMuscleDetailView.swift (analog for inline FatigueModel call sites + computing contributing exercises — WeeklyRecapSheet's Progress section uses similar in-memory aggregation),
    fitbodTests/Fatigue/FatigueTestFixtures.swift (plan 06 adds sessionsLastWeek + noSessionsLastWeek factories for WeeklyRecapTrigger tests),
    fitbodUITests/fitbodUITests.swift (analog for XCUIApplication launch + sheet detection),
    .planning/phases/05-fatigue-model-plateau-detection/05-CONTEXT.md (D-02 week boundary; Claude's Discretion VOL-07 surface form — full-screen sheet on first app open of new calendar week; 4 sections),
    .planning/phases/05-fatigue-model-plateau-detection/05-RESEARCH.md §12.1 (trigger logic verbatim — RootView.task + skip when zero sessions logged last week), §12.2 (sheet content — 4 sections per Claude's Discretion), §12.3 (sheet presentation — .presentationDetents([.large])), §10 Pitfall #7 (Calendar.isDate equalTo: toGranularity: .day for dismissal compare), §10 Pitfall #8 (re-firing prevention via UserSettings.weeklyRecapShownForWeekStart),
    .planning/phases/05-fatigue-model-plateau-detection/05-UI-SPEC.md §Copywriting Contract WeeklyRecapSheet section verbatim copy for all 4 sections (Sessions / Muscles Hit / Under-Trained / Progress) + §Component Inventory WeeklyRecapSheet entry,
    .planning/phases/05-fatigue-model-plateau-detection/05-PATTERNS.md (WeeklyRecapSheet + RootView modification sections)
  </read_first>
  <files>fitbod/Fatigue/WeeklyRecapSheet.swift, fitbod/App/RootView.swift, fitbodTests/Fatigue/WeeklyRecapTriggerTests.swift, fitbodTests/Fatigue/FatigueTestFixtures.swift, fitbodUITests/WeeklyRecapUITests.swift</files>
  <behavior>
    WeeklyRecapTrigger behavior tests (unit-level for the guard logic without the UI):
    - Test (D-02 + VOL-07 trigger on first launch of new Monday with prior-week sessions): seed sessionsLastWeek(count: 3) (3 completed Sessions in the prior calendar week); UserSettings.weeklyRecapShownForWeekStart == nil. Invoke the trigger guard function (extracted as a testable static func, e.g., `WeeklyRecapTrigger.shouldPresent(weekStart:lastShown:hasSessionsLastWeek:) -> Bool`). Assert `true` (presentation should fire).
    - Test (Pitfall #8 re-firing prevention): same fixture but settings.weeklyRecapShownForWeekStart = thisMonday. Assert `shouldPresent(...) == false` (already shown this week).
    - Test (Pitfall #8 zero sessions last week → skip but mark seen): seed noSessionsLastWeek; UserSettings.weeklyRecapShownForWeekStart == nil. The trigger function should return `false` for "should present" but a separate `shouldMarkSeen(...)` flag should return true (so the sheet doesn't show retroactively next week). Test the combined logic via a higher-level `WeeklyRecapTrigger.evaluate(...)` returning a struct `{ shouldPresent: Bool; shouldMarkSeen: Bool }`.
    - Test (Pitfall #7 DST/timezone date compare): construct a scenario where lastShown is "2026-05-04 23:59:50 UTC" and thisMonday is "2026-05-05 00:00:10 UTC" — they are different Date instances but Calendar.current.isDate(equalTo: toGranularity: .day) treats them as same day in some timezones. Assert the comparison correctly identifies the SAME or DIFFERENT week based on the day-granularity Calendar comparison. (This test pins Pitfall #7 mitigation.)
    
    WeeklyRecapSheet behavior tests (UI-side):
    - Behavior (UI-SPEC §Copywriting Contract WeeklyRecapSheet verbatim): NavigationStack containing 4 Sections per Claude's Discretion:
      1. "Sessions": "Last week you logged {N} sessions across {M} routines." (handle singular/plural — 1 session vs N sessions; 1 routine vs M routines).
      2. "Muscles Hit": list of muscle rows where frequencyHits ≥ minSets last week; row format "{muscle.displayName}" leading + "{N} sessions" trailing.
      3. "Under-Trained": list of muscle rows where weeklyVolume.totalSetsRounded < target.mev last week; row format "{muscle.displayName}" leading + "below MEV — {N} / {mev} sets" trailing (foreground Color("VolumeBelowMEVGray")).
      4. "Progress": top 5 exercises by working-set count last week (sorted DESC by total sets), each with their e1RM delta vs the week prior; row format "{exercise.canonicalName}" leading + "{sign}{Δ e1RM} {unit}" trailing (positive in .systemGreen, negative in .systemRed, zero in .secondaryLabel); e1RM delta computed via OneRepMaxEstimator.estimate on the top-set of each exercise in each week, then subtracting.
    - Behavior (UI-SPEC §Component Inventory WeeklyRecapSheet entry): .presentationDetents([.large]); navigation title "Weekly Recap"; .toolbar Done button dismisses.
    - Behavior (empty states per UI-SPEC §Copywriting Contract Phase 5 Additions):
      - Muscles Hit empty: "No muscles met the frequency threshold last week." (`.body .secondaryLabel`)
      - Under-Trained empty: "All trained muscles reached MEV last week." (`.body .secondaryLabel`)
      - Progress empty: "Not enough data to compute e1RM movement." (`.body .secondaryLabel`)
      - Progress footnote: "Top 5 exercises by working-set count last week. e1RM vs same exercise week prior." (`.caption .secondaryLabel`)
    
    WeeklyRecapUITests:
    - Behavior (auto-presents on first app open of new Monday with prior-week sessions): launch with phase5-weekly-recap seed (which sets up: ≥1 logged session in prior calendar week + UserSettings.weeklyRecapShownForWeekStart == nil + the simulated "today is a Monday" via Date injection if possible, OR rely on real Date.now if the seed happens to land near a Monday — a robust simulation requires the test seed to write a UserSettings with weeklyRecapShownForWeekStart set to a 2-week-old Monday, so the comparison fires). Assert WeeklyRecapSheet appears (`app.navigationBars["Weekly Recap"].waitForExistence(timeout: 5)`).
    - Behavior (Done dismisses): tap Done. Assert sheet dismissed.
    - Behavior (does NOT re-fire same week): terminate + relaunch. Assert WeeklyRecapSheet does NOT auto-present (the previous Done tap should have marked seen via the trigger guard).
  </behavior>
  <action>
    (1) Create fitbod/Fatigue/WeeklyRecapSheet.swift. File header: "WeeklyRecapSheet.swift / fitbod / Phase 5 VOL-07 — full-screen sheet auto-presented on first app open of new calendar week. / 4 sections per CONTEXT Claude's Discretion + UI-SPEC §Copywriting Contract verbatim copy. / Trigger guard logic lives in RootView's TodayView .task.". Imports: `import SwiftUI, import SwiftData`. Body:
    
    ```
    public struct WeeklyRecapSheet: View {
        public let weekStart: Date    // the START of the new week (this Monday); content shows the PRIOR week
        
        @Environment(\.modelContext) private var ctx
        @Environment(\.dismiss) private var dismiss
        @Query private var settingsList: [UserSettings]
        @Query private var allMuscles: [MuscleGroup]
        @Query private var allTargets: [MuscleVolumeTarget]
        
        public init(weekStart: Date) {
            self.weekStart = weekStart
        }
        
        private var priorWeekStart: Date {
            weekStart.addingTimeInterval(-7 * 86400)
        }
        
        private var settings: UserSettings? {
            settingsList.first
        }
        
        public var body: some View {
            NavigationStack {
                List {
                    sessionsSection
                    musclesHitSection
                    underTrainedSection
                    progressSection
                }
                .listStyle(.insetGrouped)
                .navigationTitle("Weekly Recap")
                .navigationBarTitleDisplayMode(.large)
                .toolbar {
                    ToolbarItem(placement: .confirmationAction) {
                        Button("Done") { dismiss() }
                    }
                }
            }
            .presentationDetents([.large])
        }
        
        @ViewBuilder
        private var sessionsSection: some View {
            let count = lastWeekSessionsCount()
            let routineCount = lastWeekRoutinesCount()
            let sessionWord = count == 1 ? "session" : "sessions"
            let routineWord = routineCount == 1 ? "routine" : "routines"
            
            Section("Sessions") {
                Text("Last week you logged \(count) \(sessionWord).")
                    .font(.body)
                if routineCount > 1 {
                    Text("Across \(routineCount) \(routineWord).")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            }
        }
        
        @ViewBuilder
        private var musclesHitSection: some View {
            let minSets = settings?.frequencyHitMinSets ?? 2
            let hits = musclesHitLastWeek(minSets: minSets)
            
            Section("Muscles Hit") {
                if hits.isEmpty {
                    Text("No muscles met the frequency threshold last week.")
                        .font(.body)
                        .foregroundStyle(.secondary)
                } else {
                    ForEach(hits, id: \.slug) { row in
                        HStack {
                            Text(row.displayName)
                            Spacer()
                            Text("\(row.count) \(row.count == 1 ? "session" : "sessions")")
                                .font(.caption)
                                .foregroundStyle(.secondary)
                        }
                    }
                }
            }
        }
        
        @ViewBuilder
        private var underTrainedSection: some View {
            let underTrained = musclesUnderTrainedLastWeek()
            
            Section("Under-Trained") {
                if underTrained.isEmpty {
                    Text("All trained muscles reached MEV last week.")
                        .font(.body)
                        .foregroundStyle(.secondary)
                } else {
                    ForEach(underTrained, id: \.slug) { row in
                        HStack {
                            Text(row.displayName)
                            Spacer()
                            Text("below MEV — \(row.totalSets) / \(row.mev) sets")
                                .font(.caption)
                                .foregroundStyle(Color("VolumeBelowMEVGray"))
                        }
                    }
                }
            }
        }
        
        @ViewBuilder
        private var progressSection: some View {
            let progress = topExercisesProgress()
            
            Section {
                if progress.isEmpty {
                    Text("Not enough data to compute e1RM movement.")
                        .font(.body)
                        .foregroundStyle(.secondary)
                } else {
                    ForEach(progress, id: \.exerciseID) { row in
                        HStack {
                            Text(row.exerciseName.capitalized)
                            Spacer()
                            Text(row.deltaCopy)
                                .font(.caption.monospacedDigit())
                                .foregroundStyle(row.deltaColor)
                        }
                    }
                }
            } header: {
                Text("Progress")
            } footer: {
                Text("Top 5 exercises by working-set count last week. e1RM vs same exercise week prior.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
        }
        
        // MARK: - Data computations (@MainActor; all read-only)
        
        @MainActor
        private func lastWeekSessionsCount() -> Int {
            let start = priorWeekStart
            let end = weekStart
            let descriptor = FetchDescriptor<Session>(
                predicate: #Predicate<Session> {
                    $0.startedAt >= start && $0.startedAt < end && $0.completedAt != nil
                }
            )
            return (try? ctx.fetchCount(descriptor)) ?? 0
        }
        
        @MainActor
        private func lastWeekRoutinesCount() -> Int {
            // Approximate: distinct routineSnapshotName count.
            let start = priorWeekStart
            let end = weekStart
            let descriptor = FetchDescriptor<Session>(
                predicate: #Predicate<Session> {
                    $0.startedAt >= start && $0.startedAt < end && $0.completedAt != nil
                }
            )
            let sessions = (try? ctx.fetch(descriptor)) ?? []
            return Set(sessions.compactMap { $0.routineSnapshotName }).count
        }
        
        @MainActor
        private func musclesHitLastWeek(minSets: Int) -> [MuscleRow] {
            allMuscles.compactMap { muscle in
                let count = FatigueModel.frequencyHits(
                    muscleSlug: muscle.slug,
                    weekStart: priorWeekStart,
                    minContribution: Double(minSets),
                    in: ctx
                )
                guard count > 0 else { return nil }
                return MuscleRow(slug: muscle.slug, displayName: muscle.displayName, count: count)
            }.sorted { $0.count > $1.count }
        }
        
        @MainActor
        private func musclesUnderTrainedLastWeek() -> [UnderTrainedRow] {
            allMuscles.compactMap { muscle in
                let target = allTargets.first { $0.muscle?.slug == muscle.slug }
                guard let target = target else { return nil }
                let total = FatigueModel.weeklyVolume(
                    muscleSlug: muscle.slug,
                    weekStart: priorWeekStart,
                    in: ctx
                )
                let rounded = total.totalSetsRounded
                guard rounded > 0 && rounded < target.mev else { return nil }
                return UnderTrainedRow(
                    slug: muscle.slug,
                    displayName: muscle.displayName,
                    totalSets: rounded,
                    mev: target.mev
                )
            }
        }
        
        @MainActor
        private func topExercisesProgress() -> [ProgressRow] {
            // 1. Fetch all working SetEntries in prior week.
            let start = priorWeekStart
            let end = weekStart
            let priorStart = priorWeekStart.addingTimeInterval(-7 * 86400)
            let workingKinds = ["working", "drop", "failure", "rest_pause"]
            
            let lastWeekDescriptor = FetchDescriptor<SetEntry>(
                predicate: #Predicate<SetEntry> { entry in
                    entry.isComplete == true
                    && entry.isWarmup == false
                    && workingKinds.contains(entry.setTypeRaw)
                    && entry.sessionExercise?.session?.startedAt ?? .distantPast >= start
                    && entry.sessionExercise?.session?.startedAt ?? .distantPast < end
                }
            )
            let lastWeekEntries = (try? ctx.fetch(lastWeekDescriptor)) ?? []
            
            // 2. Group by exercise.id; count working sets; pick top 5.
            var setCountByExercise: [UUID: (exercise: Exercise, count: Int)] = [:]
            for entry in lastWeekEntries {
                guard let exercise = entry.sessionExercise?.exercise else { continue }
                var existing = setCountByExercise[exercise.id] ?? (exercise: exercise, count: 0)
                existing.count += 1
                setCountByExercise[exercise.id] = existing
            }
            let top5 = setCountByExercise.values.sorted { $0.count > $1.count }.prefix(5)
            
            // 3. For each top exercise, compute last-week top-set e1RM vs prior-week top-set e1RM.
            return top5.compactMap { entry in
                let lastWeekE1RM = topSetE1RM(
                    exerciseID: entry.exercise.id,
                    weekStart: start,
                    weekEnd: end
                )
                let priorWeekE1RM = topSetE1RM(
                    exerciseID: entry.exercise.id,
                    weekStart: priorStart,
                    weekEnd: start
                )
                guard let lw = lastWeekE1RM, let pw = priorWeekE1RM else {
                    return ProgressRow(
                        exerciseID: entry.exercise.id,
                        exerciseName: entry.exercise.canonicalName,
                        deltaCopy: "—",
                        deltaColor: .secondary
                    )
                }
                let delta = lw - pw
                return ProgressRow(
                    exerciseID: entry.exercise.id,
                    exerciseName: entry.exercise.canonicalName,
                    deltaCopy: formatDelta(delta),
                    deltaColor: delta > 0.1 ? Color(.systemGreen) : (delta < -0.1 ? Color(.systemRed) : .secondary)
                )
            }
        }
        
        @MainActor
        private func topSetE1RM(exerciseID: UUID, weekStart: Date, weekEnd: Date) -> Double? {
            let workingKinds = ["working", "drop", "failure", "rest_pause"]
            let targetID = exerciseID
            let descriptor = FetchDescriptor<SetEntry>(
                predicate: #Predicate<SetEntry> { entry in
                    entry.isComplete == true
                    && entry.isWarmup == false
                    && workingKinds.contains(entry.setTypeRaw)
                    && entry.sessionExercise?.exercise?.id == targetID
                    && entry.sessionExercise?.session?.startedAt ?? .distantPast >= weekStart
                    && entry.sessionExercise?.session?.startedAt ?? .distantPast < weekEnd
                }
            )
            let entries = (try? ctx.fetch(descriptor)) ?? []
            let e1RMs = entries.compactMap { OneRepMaxEstimator.estimate(weight: $0.weight, reps: $0.reps) }
            return e1RMs.max()
        }
        
        private func formatDelta(_ delta: Double) -> String {
            let unit = settings?.unitsRaw == "kg" ? "kg" : "lb"
            let sign = delta >= 0 ? "+" : ""
            return "\(sign)\(String(format: "%.1f", delta)) \(unit) e1RM"
        }
        
        // MARK: - Row types
        
        private struct MuscleRow {
            let slug: String
            let displayName: String
            let count: Int
        }
        
        private struct UnderTrainedRow {
            let slug: String
            let displayName: String
            let totalSets: Int
            let mev: Int
        }
        
        private struct ProgressRow {
            let exerciseID: UUID
            let exerciseName: String
            let deltaCopy: String
            let deltaColor: Color
        }
    }
    ```
    
    (2) Create a small testable WeeklyRecapTrigger helper. Place at the top of WeeklyRecapSheet.swift OR in a separate file:
    
    ```
    public enum WeeklyRecapTrigger {
        public struct Decision: Sendable, Equatable {
            public let shouldPresent: Bool
            public let shouldMarkSeen: Bool
        }
        
        /// Pure-function trigger logic per RESEARCH §12.1 + Pitfalls #7/#8.
        /// Caller (RootView) injects all dependencies — testable without UI.
        public static func evaluate(
            thisMonday: Date,
            lastShown: Date?,
            hasSessionsLastWeek: Bool,
            calendar: Calendar = .current
        ) -> Decision {
            // Pitfall #7: compare via Calendar.isDate(..., equalTo:, toGranularity: .day)
            if let lastShown = lastShown,
               calendar.isDate(lastShown, equalTo: thisMonday, toGranularity: .day) {
                return Decision(shouldPresent: false, shouldMarkSeen: false)  // already shown this week
            }
            // Pitfall #8: skip presentation when no sessions logged last week, but still mark seen
            // so the recap doesn't show retroactively at some future point.
            if !hasSessionsLastWeek {
                return Decision(shouldPresent: false, shouldMarkSeen: true)
            }
            return Decision(shouldPresent: true, shouldMarkSeen: true)
        }
    }
    ```
    
    (3) Modify fitbod/App/RootView.swift TodayView to wire the WeeklyRecapSheet trigger:
    
    ```
    @State private var presentingWeeklyRecap: Bool = false   // PLAN 06 ADDS
    
    var body: some View {
        // ... existing NavigationStack ...
        // After plan 04's .sheet(isPresented: $presentingSignalSheet) { ... }, add:
        .sheet(isPresented: $presentingWeeklyRecap) {
            let weekStart = Date.now.mondayAtMidnight(weekStartsMonday: settingsList.first?.weekStartsMonday ?? true)
            WeeklyRecapSheet(weekStart: weekStart)
        }
        .task {
            await refreshAdvisory()                  // plan 04
            await checkWeeklyRecap()                 // PLAN 06 ADDS
        }
        // ... existing .onAppear ...
    }
    
    @MainActor
    private func checkWeeklyRecap() async {
        guard let settings = settingsList.first else { return }
        let thisMonday = Date.now.mondayAtMidnight(weekStartsMonday: settings.weekStartsMonday)
        
        // Check if any sessions logged last week.
        let priorWeekStart = thisMonday.addingTimeInterval(-7 * 86400)
        let hasSessions = lastWeekSessionsCount(start: priorWeekStart, end: thisMonday) > 0
        
        let decision = WeeklyRecapTrigger.evaluate(
            thisMonday: thisMonday,
            lastShown: settings.weeklyRecapShownForWeekStart,
            hasSessionsLastWeek: hasSessions
        )
        
        if decision.shouldMarkSeen {
            settings.weeklyRecapShownForWeekStart = thisMonday
            try? ctx.save()
        }
        if decision.shouldPresent {
            presentingWeeklyRecap = true
        }
    }
    
    @MainActor
    private func lastWeekSessionsCount(start: Date, end: Date) -> Int {
        let descriptor = FetchDescriptor<Session>(
            predicate: #Predicate<Session> {
                $0.startedAt >= start && $0.startedAt < end && $0.completedAt != nil
            }
        )
        return (try? ctx.fetchCount(descriptor)) ?? 0
    }
    ```
    
    Preserve every other line of TodayView verbatim — all plan 02/04/05 wiring (FatigueSurfaceView mount, DeloadAdvisoryBanner safeAreaInset, DeloadSignalDetailSheet wiring, navigation path).
    
    (4) Create fitbodTests/Fatigue/WeeklyRecapTriggerTests.swift, REPLACING the plan-01 RED scaffold. Annotate `@Suite("WeeklyRecapTrigger")` (no @MainActor — pure-function tests).
    
    Required @Test functions:
      - `firstLaunchOfNewMondayWithSessionsFiresPresentation`:
        ```
        let thisMonday = Date(timeIntervalSince1970: 1_716_336_000)  // arbitrary Monday
        let decision = WeeklyRecapTrigger.evaluate(thisMonday: thisMonday, lastShown: nil, hasSessionsLastWeek: true)
        #expect(decision.shouldPresent == true)
        #expect(decision.shouldMarkSeen == true)
        ```
      - `reLaunchSameWeekSuppresses`:
        ```
        let thisMonday = Date(timeIntervalSince1970: 1_716_336_000)
        let decision = WeeklyRecapTrigger.evaluate(thisMonday: thisMonday, lastShown: thisMonday, hasSessionsLastWeek: true)
        #expect(decision.shouldPresent == false)
        ```
      - `zeroSessionsLastWeekMarksSeenWithoutPresenting`:
        ```
        let thisMonday = Date(timeIntervalSince1970: 1_716_336_000)
        let decision = WeeklyRecapTrigger.evaluate(thisMonday: thisMonday, lastShown: nil, hasSessionsLastWeek: false)
        #expect(decision.shouldPresent == false)
        #expect(decision.shouldMarkSeen == true)
        ```
      - `nextMondayAfterLastShownRefires`:
        ```
        let lastMonday = Date(timeIntervalSince1970: 1_716_336_000)
        let nextMonday = lastMonday.addingTimeInterval(7 * 86400)
        let decision = WeeklyRecapTrigger.evaluate(thisMonday: nextMonday, lastShown: lastMonday, hasSessionsLastWeek: true)
        #expect(decision.shouldPresent == true)
        ```
      - `pitfall7CalendarIsDateHandlesSameDayDifferentTime`:
        ```
        // lastShown at 00:00:01 of thisMonday; thisMonday at 12:30 — same day.
        let cal = Calendar(identifier: .iso8601)
        let baseMonday = cal.date(from: DateComponents(year: 2026, month: 5, day: 18))!
        let lastShownEarly = baseMonday.addingTimeInterval(1)
        let thisMondayLater = baseMonday.addingTimeInterval(45000)  // ~12:30 same day
        let decision = WeeklyRecapTrigger.evaluate(thisMonday: thisMondayLater, lastShown: lastShownEarly, hasSessionsLastWeek: true, calendar: cal)
        #expect(decision.shouldPresent == false)   // same day per .day granularity → suppressed
        ```
    
    (5) Fill in FatigueTestFixtures for the UI-side test fixtures:
      - `sessionsLastWeek(ctx:count:)`: insert N completed Sessions in the prior calendar week with completedAt non-nil + at least one SessionExercise + one SetEntry per session (so the lastWeekSessionsCount predicate matches).
      - `noSessionsLastWeek(ctx:)`: empty SwiftData for prior week — no Sessions in the [-14d, -7d] range.
    
    (6) Create fitbodUITests/WeeklyRecapUITests.swift. Use `FITBOD_TEST_SEED=phase5-weekly-recap` — a new seed flag that the app's #if DEBUG branch handles by inserting: (a) Phase 1 library + 17 MuscleGroup rows; (b) 3 completed Sessions in the prior calendar week (using priorWeekStart relative to Date.now); (c) UserSettings with weeklyRecapShownForWeekStart = a date 2 weeks ago (so the trigger fires).
    
    Required tests:
    ```
    final class WeeklyRecapUITests: XCTestCase {
        override func setUpWithError() throws { continueAfterFailure = false }
        
        @MainActor
        func testWeeklyRecapAutoPresentsOnFirstLaunchOfNewMonday() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-weekly-recap"
            app.launch()
            
            // Today tab should be active by default.
            // Assert WeeklyRecapSheet auto-presents.
            XCTAssertTrue(app.navigationBars["Weekly Recap"].waitForExistence(timeout: 5))
            
            // Assert 4 section headers exist.
            XCTAssertTrue(app.staticTexts["Sessions"].exists)
            XCTAssertTrue(app.staticTexts["Muscles Hit"].exists)
            XCTAssertTrue(app.staticTexts["Under-Trained"].exists)
            XCTAssertTrue(app.staticTexts["Progress"].exists)
        }
        
        @MainActor
        func testDoneDismissesSheet() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-weekly-recap"
            app.launch()
            
            XCTAssertTrue(app.navigationBars["Weekly Recap"].waitForExistence(timeout: 5))
            
            app.buttons["Done"].tap()
            
            // Assert sheet dismissed.
            XCTAssertFalse(app.navigationBars["Weekly Recap"].waitForExistence(timeout: 2))
        }
        
        @MainActor
        func testDoesNotReFireWithinSameWeek() throws {
            let app = XCUIApplication()
            app.launchArguments.append("--ui-testing")
            app.launchEnvironment["FITBOD_TEST_SEED"] = "phase5-weekly-recap"
            app.launch()
            
            XCTAssertTrue(app.navigationBars["Weekly Recap"].waitForExistence(timeout: 5))
            app.buttons["Done"].tap()
            
            // Terminate + relaunch.
            app.terminate()
            app.launch()
            
            // Assert sheet does NOT auto-present (weeklyRecapShownForWeekStart guard works).
            XCTAssertFalse(app.navigationBars["Weekly Recap"].waitForExistence(timeout: 3))
        }
    }
    ```
  </action>
  <verify>
    <automated>xcodebuild test -scheme fitbod -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing fitbodTests/Fatigue/WeeklyRecapTrigger -only-testing fitbodUITests/WeeklyRecapUITests 2>&amp;1 | grep -E 'Test Suite.*passed|FAIL|error'</automated>
  </verify>
  <done>
    WeeklyRecapSheet.swift exists with NavigationStack + 4 Sections (Sessions / Muscles Hit / Under-Trained / Progress per CONTEXT Claude's Discretion + UI-SPEC verbatim copy) + .presentationDetents([.large]) + .toolbar Done + per-section empty states; WeeklyRecapTrigger.evaluate pure function in the same file (or a sibling file) implements the trigger guard logic per Pitfalls #7/#8; RootView.swift wires checkWeeklyRecap() in the existing .task block + .sheet(isPresented: $presentingWeeklyRecap) presents WeeklyRecapSheet; WeeklyRecapTriggerTests has 5 GREEN @Test functions covering D-02 first-launch-fire + Pitfall #8 re-fire prevention + zero-sessions skip + next-Monday refire + Pitfall #7 same-day Calendar.isDate; WeeklyRecapUITests has 3 GREEN UI tests covering auto-present + Done dismiss + no-refire-same-week; FatigueTestFixtures gets sessionsLastWeek + noSessionsLastWeek factories. VOL-07 closed end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local user → SwiftData | Critical user-input crossings: (a) MuscleVolumeTargetEditor's per-muscle Stepper edits (plan 05's MuscleVolumeTargetStepper component handles validation — range [1, 30] + monotonic invariant); (b) Settings' frequencyHitMinSets Stepper (range 1...5 step 1); (c) MuscleVolumeTargetEditor's "Reset All to RP Defaults" destructive button (with .alert confirmation per UI-SPEC). |
| Code → Code | MuscleVolumeTargetSeeder reads MuscleGroup + writes MuscleVolumeTarget; WeeklyRecapSheet reads Session / SessionExercise / SetEntry / Exercise / MuscleGroup / MuscleVolumeTarget / UserSettings + computes via FatigueModel + OneRepMaxEstimator; RootView writes settings.weeklyRecapShownForWeekStart via @Bindable. No actor crossings beyond @MainActor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | MuscleVolumeTargetEditor + MuscleVolumeTargetStepper (plan 05 component reused) write mv/mev/mav/mrv — out-of-range or non-monotonic values | mitigate | Plan 05's MuscleVolumeTargetStepper enforces range [1, 30] per Stepper bounds + displays inline monotonic violation copy when MV > MEV OR MEV ≥ MAV OR MAV ≥ MRV. The seeder writes only RP defaults (pre-validated monotonic per the `allRPLandmarksAreMonotonic` test). The "Reset All to RP Defaults" path also writes only RP defaults. No path can introduce non-monotonic rows; the only risk is the user manually creating one via the Stepper, which the editor surfaces as a visible error. |
| T-05-04 | Race condition | RootView's .task simultaneously calls refreshAdvisory (plan 04) AND checkWeeklyRecap (plan 06) — both write to settings (deloadAdvisoryDismissedWeekStart vs weeklyRecapShownForWeekStart) | mitigate | Both functions run on @MainActor (the .task block is @MainActor by default in SwiftUI); Swift 6 strict concurrency serializes their execution. The writes are to different fields on the same UserSettings instance — SwiftData @Bindable handles serialized updates correctly. No race condition. |
| T-05-WR-01 | Information Disclosure | WeeklyRecapSheet enumerates the user's training data across last week (session counts, muscle frequency hits, e1RM deltas per exercise) | accept | Single-user local app; the data crosses the function boundary into UI-rendered text. No PII, no externalization. Same disposition as plans 03/04/05. |
| T-05-WR-02 | Denial of Service | WeeklyRecapSheet computes 4 sections per render: lastWeekSessionsCount + musclesHitLastWeek (17 FatigueModel.frequencyHits calls) + musclesUnderTrainedLastWeek (17 FatigueModel.weeklyVolume calls) + topExercisesProgress (fetch + group + 5 × 2 = 10 OneRepMaxEstimator calls) | mitigate | RESEARCH §15 budget: WeeklyRecapSheet load ~100ms — fits the 100ms perceptual threshold. The sheet's body re-runs on @Query invalidation but typically only renders once per presentation. No memoization needed for v1. |
| T-05-MS-01 | Tampering | MuscleVolumeTargetSeeder writes MuscleVolumeTarget rows — a malformed seed (e.g., writing non-monotonic mv > mev) would corrupt the editor's UX | mitigate | The seeder reads from RPVolumeLandmarks.landmarks (static dictionary literal, pre-validated as monotonic at compile time per the `allRPLandmarksAreMonotonic` test). The seeder cannot construct non-monotonic rows by mistake. Defense-in-depth: the test pins the invariant. |
| T-05-WT-01 | Tampering | WeeklyRecapTrigger.evaluate uses Calendar.isDate(toGranularity: .day) — if the Calendar.current instance has a non-Gregorian configuration on the user's device, the same-day comparison could behave unexpectedly | accept | Calendar.current honors the user's locale + timezone — this is the correct behavior for a personal app. RESEARCH §10 Pitfall #7 documents this approach as the recommended pattern. The tests pin a fixed Calendar via the `calendar` parameter (default Calendar.current; tests pass Calendar(identifier: .iso8601) for determinism). |
</threat_model>

<verification>
- `MuscleVolumeTargetSeederTests` + `WeeklyRecapTriggerTests` (the last 2 of the 8 RED scaffolds from plan 01) both turn GREEN this plan.
- `MuscleVolumeTargetEditorUITests` + `WeeklyRecapUITests` (NEW in this plan) GREEN.
- Phase 1 / 2 / 3 / 4 + plans 01/02/03/04/05 test suites remain GREEN (no regression).
- `swift-format` idempotent on every new file.
- No new third-party SPM dependency introduced.
- MuscleVolumeTargetSeeder uses row-level userEdited guard (NOT UserDefaults flag) per RESEARCH §7.3.
- MuscleVolumeTargetEditor reuses plan 05's MuscleVolumeTargetStepper component inline — single source of truth for the 4-Stepper editor UX.
- SettingsView additions per UI-SPEC §Component Inventory Modified views table: 'Volume Targets' Section with NavigationLink + 'Frequency hit threshold' Stepper.
- WeeklyRecapTrigger.evaluate is a pure function — testable without UI; tests cover Pitfall #7 (Calendar.isDate equalTo: toGranularity: .day) + Pitfall #8 (zero sessions skip but mark seen + re-fire prevention).
- WeeklyRecapSheet uses .presentationDetents([.large]) + .toolbar Done — matches UI-SPEC §Component Inventory.
- All 4 WeeklyRecapSheet sections render verbatim per UI-SPEC §Copywriting Contract — Sessions / Muscles Hit / Under-Trained / Progress.
- Empty states for each section render verbatim per UI-SPEC.
- Progress section uses OneRepMaxEstimator.estimate (plan 01 shipped) for per-exercise e1RM delta.
- RootView's checkWeeklyRecap respects Pitfall #8: marks settings.weeklyRecapShownForWeekStart = thisMonday EVEN WHEN zero sessions logged last week (so the sheet doesn't show retroactively).
- T-05-02 (monotonic invariant) mitigation in place: MuscleVolumeTargetStepper (plan 05) enforces; seeder writes only RP defaults (pre-validated monotonic).
- T-05-04 (dismiss race + refresh race) mitigated by @MainActor isolation.
</verification>

<success_criteria>
- After this plan, a user opens Settings → 'Volume Targets' and can edit any of the 17 muscle MEV/MAV/MRV/MV values via the per-muscle Stepper expansion — SET-05 closed via both inline (plan 05 PerMuscleDetailView) AND Settings entry points per CONTEXT Claude's Discretion "Both surfaces edit the same row; Settings is the primary entry point".
- The user can also adjust the frequencyHitMinSets threshold (D-03) from Settings — VOL-06 surfaced in Settings (complementing the per-muscle detail's Frequency This Week section from plan 05).
- 'Reset All to RP Defaults' destructive global button resets all 17 muscles' rows back to RP landmark values with .alert confirmation per UI-SPEC §Copywriting Contract verbatim copy.
- First app open of new Monday with ≥1 session logged last week auto-presents the WeeklyRecapSheet with 4 sections per CONTEXT Claude's Discretion + UI-SPEC §Copywriting Contract.
- Sheet does NOT re-fire on subsequent opens within the same week (Pitfall #8 mitigation via weeklyRecapShownForWeekStart guard).
- Zero sessions logged last week → sheet does NOT fire, but weeklyRecapShownForWeekStart is still marked (so it doesn't show retroactively).
- Next Monday refires correctly per Calendar.isDate(toGranularity: .day) compare.
- VOL-07 closed end-to-end (auto-presented weekly recap with all 4 sections).
- VOL-06 closed end-to-end (Settings entry point for the threshold; per-muscle detail Frequency This Week section consumes the threshold).
- MuscleVolumeTargetSeeder closes the seeder idempotency + user-edit preservation contract for VOL-03 / SET-05.
- 2 of the 8 RED scaffolds from plan 01 (MuscleVolumeTargetSeederTests + WeeklyRecapTriggerTests) turn GREEN — running total of GREEN scaffolds after plan 06: ALL 8/8 RED scaffolds now GREEN (plan 01's 5 originally-GREEN + plan 02's 3 GREEN + plan 03's 2 GREEN + plan 04's 1 GREEN + plan 06's 2 GREEN = 13 total Wave 0 suites, all GREEN).
- T-05-02 monotonic invariant + clamp mitigated via plan 05's MuscleVolumeTargetStepper (reused) + seeder writes only pre-validated RP defaults.
- T-05-04 race condition mitigated by @MainActor isolation of both refreshAdvisory (plan 04) + checkWeeklyRecap (this plan).
- Pitfall #7 (Calendar.isDate equalTo: toGranularity: .day) observed in WeeklyRecapTrigger.evaluate AND in plan 04's DeloadAdvisor dismissal compare — single source of truth for the date-comparison pattern.
- Pitfall #8 (re-fire prevention + zero-sessions short-circuit) observed in WeeklyRecapTrigger.evaluate's two-flag Decision (shouldPresent + shouldMarkSeen).
</success_criteria>

<output>
After completion, create `.planning/phases/05-fatigue-model-plateau-detection/05-06-SUMMARY.md`.
</output>
</content>
</invoke>